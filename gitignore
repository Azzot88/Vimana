# --- Локальные рабочие файлы Claude (не публиковать на GitHub) ---
CLAUDE.md
.claude/

# --- PRD-файлы: оставить только локальными? см. примечание ниже ---
# ВНИМАНИЕ: если раскомментировать строки ниже, git вообще перестанет
# отслеживать PRD-файлы — а значит, по ним НЕ будет diff и отката.
# Для варианта «локально, с историей правок, но не на GitHub» используй
# приватный репозиторий или вообще не добавляй remote — а не .gitignore.
#
# /prd/
# *.prd.md

# --- Системное ---
.DS_Store
Thumbs.db
*.log
.env
.env.*

# --- Зависимости / сборка (если в проекте есть код) ---
node_modules/
__pycache__/
*.pyc
.venv/
venv/
dist/
build/
