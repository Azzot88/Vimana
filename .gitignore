# Claude
CLAUDE.md
.claude/
graphify-out/
skills/

# Разбор чужой платформы + замер её экономики по блокчейну. Ссылки на файл
# в PRD есть, сам файл — только локально.
PRD/hodlBTCescrow.md

# Environment
.env
.env.*
!.env.example

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
dist/
build/
.mypy_cache/
.pytest_cache/

# Node
node_modules/
dist/
build/
.vite/

# OS
.DS_Store
Thumbs.db
*.log

# Docker volumes (if mapped locally)
postgres_data/

# Scan / load artefacts — snapshots of one moment, they age badly in a repo
.zap/zap-report.*
load/results/*.json

# T_SEC.6 — MaxMind GeoLite2 City, mounted into celery-worker read-only.
# Out of git for three separate reasons, any one of which is enough: MaxMind's
# licence is not ours to redistribute, the file is 63 MB, and it refreshes on
# MaxMind's schedule rather than on our releases.
data/geoip/
