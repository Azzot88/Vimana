# Vimana — Sacred Logistics · CHANGELOG.md

> FILL ──────────────────────────────
> Назначение: короткие логи значимых изменений проекта.
> Когда обновлять: при любом значимом изменении кода, моделей, инфраструктуры или PRD.
> Что НЕ дублировать: подробную логику (TECHSTATE), стек (ENVIRONMENT), задачи (TASKS).
> Формат записи (новые сверху): `YYYY-MM-DD · <уровень> · что изменилось · затронутые файлы`
>   уровень ∈ { PRD, MODEL, INFRA, FEATURE, FIX, DECISION, MILESTONE }
> ───────────────────────────────────

---

## Записи

- **2026-07-17 · MILESTONE** · 🎉 **T3.1 MVP закрыт — УБА (Уровень Бизнес-Активности)**. `app/core/uba.py` — чистые функции формулы: `F_norm = min((F/3)/8, 1.0)` (месячная скорость closed deals как carrier за 90 дней), `Q_norm = min(log₁₀(Q+1)/log₁₀(51), 1.0)` (Q считается только по сделкам с **обоими** DealVault-фото handoff+receipt через два `EXISTS` sub-queries на Attachment×AttachmentKind), `V_norm = min(log₁₀(V+1)/log₁₀(50001), 1.0)` (SUM Order.declared_value), `D_factor = 1.0 + 0.5×min(D/5000, 1.0)` (**D=0 пока Collateral не появится в T5.x**, factor = нейтральный 1.0, не штраф), `V_verify_norm = _VERIFY_FACTOR[User.highest_verification_level]/1.30` (null→1.00 / auto→1.05 / peer→1.15 / kyc→1.30 — расширение из T2.1). УБА = round(F×Q×V×D×Vrf × 1000), clamp [0, 1000]. Level slugs (стабильные для i18n): `newbie/verified/reliable/trusted/elite` с порогами 0/50/200/450/750. `recompute_and_persist(db, user_id)` через sync-сессию для Celery. `app/tasks/uba.py::recompute_all_uba` — Celery beat раз в час, обходит всех user-ов с активностью за 90 дней (distinct carrier_id из Deal). Beat schedule зарегистрирован в `worker.py::beat_schedule`. 2 endpoints: `GET /api/me/uba` + `GET /api/users/{id}/uba` → `{uba, level, components:{f_count,q_count,v_sum,d_peak,verify_level}}` (компоненты пересчитываются on-demand + кеш `User.business_activity_level` обновляется). Frontend: `api/uba.ts` типы + fetch, `UBASection` в профиле (score X/1000, level chip с градацией цветов navy→cyan→amber, 4 component tile'а F/Q/V/D), заменяет прежний `profile.level/levelPhase/levelNote` плейсхолдер. i18n `profile.uba.*` (title/hint/levels/components) в 6 языках. 10 backend-тестов: zero → 0, saturation → ≈1000, verify factor scaling monotonic, D мультипликатор (не штраф), F монтжлы, Q log shape, level thresholds точно на границах, level slugs list stable, `/me/uba` для fresh user → 0/newbie, `/users/{unknown}/uba` → 404, e2e confirmed deal без фото → F/V увеличиваются но Q=0 → УБА=0 (proves Q-gate). Версия `0.03.1` · `backend/app/{core/uba,tasks/uba,api/uba,worker,schemas/user,main}.py`, `backend/tests/test_uba.py`, `frontend/src/{api/uba,components/UBASection,pages/ProfilePage,version}.ts`, `frontend/src/i18n/locales/*.json`.

- **2026-07-17 · MILESTONE** · 🎉 **T2.3 MVP закрыт — Threshold 2-of-3 e2e encryption для DealVault → Фаза 2 полностью закрыта**. Backend (миграция 0016): `deal_vault_messages` получает `is_e2e BOOLEAN DEFAULT false`, `wrapped_shares JSONB`, `read_packages JSONB` (nullable). `app/core/threshold.py` — NIP-04 (raw-x ECDH через `PublicKey.multiply()` + AES-256-CBC PKCS7): `nip04_encrypt`/`nip04_decrypt`, `E2EPayload` валидатор blob'а с обязательными полями (ciphertext, nonce, wrapped_shares{sender,carrier,arbiter}, read_packages{sender,carrier}), `get_arbiter_user_id()` из env `ARBITER_USER_ID`. Property `msg.text` для `is_e2e=true` **всегда возвращает None** — сервер не выдаёт plaintext даже при полном доступе к БД. `app/api/threshold.py` — 3 endpoints: `GET /arbiter-info` (503 без env), `POST /dealvault/messages/{id}/reveal-my-share` (returns own NIP-04 envelope, роль sender/carrier auto по deal.sender_id/carrier_id, 403 outsider), `POST /disputes/{deal_id}/arbiter-reveal` (permission `THRESHOLD_ARBITER_REVEAL`, decrypts arbiter's share для всех e2e-messages через custodial arbiter nsec, пишет `DealEvent(event_type=arbiter_opened, payload={kind:"arbiter_share_revealed", count:N})` audit). Расширение RBAC: `THRESHOLD_ARBITER_REVEAL` добавлен в `_ARBITER_PERMS`. `deals.get_deal` теперь возвращает `sender_npub`/`carrier_npub` — клиент использует для encrypt под обе стороны. Frontend: **новые deps** `@noble/ciphers`, `@noble/curves`, `@noble/hashes`, `shamir-secret-sharing`. `lib/threshold.ts` — `encryptE2E(plaintext, sender_npub, carrier_npub, arbiter_npub)` (session_key → AES-256-GCM → Shamir 2/3 → NIP-04 wrap каждой share + session_key в read_packages через `window.nostr.nip04.encrypt`), `decryptE2E(ct, nonce, own_read_pkg, writer_npub)` (nip04.decrypt → session_key → AES-GCM decrypt), `decryptFromShares` (dispute recovery через SSS combine). `api/threshold.ts` + auto-encrypt в `api/dealvault.ts::createMessage` (self-custody + NIP-07 + parties known → e2e path; иначе plaintext). `DealVaultPage` — fetch deal-detail → parties.e2e npubs, inline decrypt e2e messages с fallback "🔒 расшифровываю…" / "🔒 e2e-сообщение — требуется NIP-07 расширение для чтения" для custodial. **Настройка**: ops должен создать arbiter-юзера (role=arbiter) и задать `ARBITER_USER_ID` в env; иначе `/arbiter-info` вернёт 503, но остальные пути (в т. ч. plaintext DealVault) продолжат работать. 9 backend-тестов: arbiter-info OK/503, e2e write stores blob + text hidden, e2e/text mutually exclusive 422, invalid shape 422, reveal-my-share sender/carrier, outsider 403, arbiter-reveal happy path + audit, non-arbiter 403, NIP-04 round-trip sanity. **MVP-компромиссы** (документированы follow-up): backfill старых T1.21 записей — отдельный user-инициированный «re-encrypt my history» flow; Inquiry чат пока не e2e (только DealVault); attachments не chunk-encrypt перед R2 upload; NIP-04 → NIP-44 v2 миграция когда extensions устаканятся; custodial e2e fallback (server-side decrypt через NSEC_ENCRYPTION_KEY) как compromise. Версия `0.02.4` (T2.3 = task-номер 3 меньше T2.4 = 4, поэтому patch не сдвигается — но **Фаза 2 полностью закрыта**) · миграция `0016_threshold_encryption`, `backend/app/{core/threshold,api/threshold,api/dealvault,api/deals,models/deal,schemas/dealvault,schemas/marketplace,core/permissions,main}.py`, `backend/tests/{conftest,test_threshold_encryption}.py`, `frontend/{package.json,src/{lib/threshold,api/threshold,api/dealvault,pages/DealVaultPage}.tsx}`.

- **2026-07-17 · MILESTONE** · 🎉 **T2.2 pt.2 закрыт — NIP-01 event format + NIP-07 client-signing → полный self-custody end-to-end**. Backend (миграция 0015): `deal_vault_messages` + `deal_events` получают `nostr_event_id VARCHAR(64)`, `nostr_created_at BIGINT`, `nostr_pubkey VARCHAR(64)` (nullable — pt.1 записи остаются как есть, маркер = `nostr_event_id IS NULL`). `app/core/keypair.py` — новые `sign_event_id(event_id_hex, nsec)` / `verify_event_id(event_id, sig, npub)` над готовым 32-byte NIP-01 event id (legacy `sign_event`/`verify_event` из pt.1 сохранены для старых записей). `app/core/signing.py` — refactor на NIP-01 event format: `event_id = sha256([0, pubkey, created_at, kind, tags, content])`, kind **4801** vault_message / **4802** deal_event. Tags vault_message: `[["k","vault_message"],["deal",<uuid>]]` (+ `["system","1"]` для системных). Tags deal_event: `[["k","deal_event"],["deal",<uuid>],["e",event_type]]`. Content vault_message = plaintext (client-reproducible без знания server-side шифрования); content deal_event = canonical JSON `{event_type, actor_id, payload}`. **Асимметрия self-custody**: vault_message = strict (self-custody без sig → 422); deal_event = lenient (self-custody → `nostr_sig=None`, actor attribution через `actor_id` сохраняется). ±5 min clock-skew guard для client-provided ts. Frontend: `lib/nostr.ts` — `signVaultMessageViaNip07(dealId, text, isSystem)` через `window.nostr.signEvent()`; `api/dealvault.ts` — при `key_self_custody && hasNip07Extension()` автоматически подписывает и добавляет `nostr_sig` + `nostr_created_at` в POST body. `KeypairSection` — кнопка «Claim self-custody» + модалка с амбер-warn (доступна если `has_encrypted_nsec && nip07`). i18n `profile.keypair.claim*` + `selfCustodyHint`/`nip07Detected` обновлены в 6 языках. 7 backend-тестов: pre-signed accept + wrong sig 422 + missing ts 422 + stale ts (±5 min) 422 + custodial NIP-01 event_id populated + self-custody deal event lenient + keypair-тест обновлён (vault-strict вместо deal-strict). **Follow-up**: (1) добавить NIP-01 event поля в `InquiryMessage` (сейчас inquiry чат без sig); (2) read-time verify endpoint для аудита pubkey snapshot. Версия остаётся `0.02.4` (T2.2 pt.2 меньше T2.4 по номеру задачи) · миграция `0015_nostr_event_format`, `backend/app/{core/{keypair,signing},api/dealvault,schemas/dealvault,models/deal}.py`, `backend/tests/{conftest,test_nostr_keypair,test_nip07_signing}.py`, `frontend/src/{lib/nostr,api/dealvault,components/KeypairSection}.tsx`, `frontend/src/i18n/locales/*.json`.

- **2026-07-17 · MILESTONE** · 🎉 **T2.4 MVP закрыт — Trust Graph (Web-of-Trust)**. Backend (миграция 0014): `TrustEdge(from_user_id, to_user_id, kind ∈ {peer_verified, dealt_with, invited}, weight, source_ref, created_at, revoked_at?)` + UNIQUE(from, to, kind, source_ref) для идемпотентности + 2 partial index на активных рёбрах. `User` расширен 3 денормализованными счётчиками: `verifications_issued_count`, `verifications_received_count`, `dealt_with_count`. `app/core/trust.py` — веса рёбер (peer_verified=1.0, dealt_with=0.5, invited=0.2), `add_edge()` с `on_conflict_do_nothing`, symmetric `add_dealt_with(deal)` (source_ref=`deal:{id}`), symmetric `add_invited(inviter, invitee, token)` (source_ref=`invite:{token}`), `refresh_trust_counts(user)`, `bfs_circles(root, depth[1..6], kind?)` (deque, visited set, revoked excluded), `distance_between(a, b, kind?)`. **Sybil-guard**: `peer_verified` требует closed/confirmed Deal между парой (`SybilGuardError`); `dealt_with`/`invited` — auto-populated без гейта. Auto-populate: `confirm_deal` → `add_dealt_with` + `refresh_trust_counts`, `accept_invite` → `add_invited` + refresh. 2 endpoints: `GET /me/trust-circle?depth=N&kind=?` → `{depth, kind, circles: {"1": [ids], "2": [ids], …}, total_reachable}`; `GET /users/{id}/trust-metrics` → counters + `distance_from_viewer`. 9 backend-тестов: invite/deal symmetric edges, depth validation (0 → 422), kind filter, self-metrics (no distance), sybil-guard block без closed deal, idempotency, revoked exclusion, denormalized count refresh. Frontend: `TrustCirclesSection` в профиле — depth selector 1–6, 3 counter tiles (issued/received/dealt_with), hop list per level. `api/trust.ts` — types + fetch. i18n `trust.*` в 6 языках с plural forms (hopLabel_one/other/few/many для славянских). **MVP-компромиссы** (документированы follow-up): Redis-кэш BFS с TTL 5 мин, UserBadge «You know them through 2 hops» chip на TripCard, hourly Celery task как backup для counter refresh. Версия `0.02.4` · миграция `0014_trust_graph`, `backend/app/{models/trust,core/trust,api/trust,api/deals,api/social,models/user,models/__init__,main}.py`, `backend/tests/{conftest,test_trust_graph}.py`, `frontend/src/{api/trust,components/TrustCirclesSection,pages/ProfilePage}.tsx`, `frontend/src/i18n/locales/*.json`.

- **2026-07-17 · MILESTONE** · 🎉 **T2.1 MVP закрыт — Peer Identity Verification end-to-end**. Backend (миграция 0013): 5 моделей (VerificationLevel enum, VerificationRequest, IdentityContainer, VerificationBadge, SanctionsList) + 7 enum'ов + `User.highest_verification_level` (денормализация). Container encryption: AES-256-GCM, ключ = owner's nsec[:32] (custodial-only в MVP; self-custody → 422 до T2.3 threshold). 9 endpoints: create/respond/submit/request-additional/escalate/self-upload/public listing/deal listing/revoke. Асимметричные ответы: `declined` только для sender-target (badge на профиле), `declined_polite` только для carrier-target (без последствий). Escalate → создаёт Dispute из T1.23 с `identity_fraud`. Revoke: verifier (30-day window для peer), subject (свой auto), arbiter (любой). RBAC расширен: `IDENTITY_REQUEST`, `IDENTITY_SELF_UPLOAD`, `VERIFICATION_REVOKE_OWN` — базовые user; `IDENTITY_CONTAINER_READ`, `VERIFICATION_REVOKE_ANY` — арбитр. 15 backend-тестов (create + 3-варианта respond + submit + escalate + self-upload + public listing без blob + revoke). Frontend: `VerificationSection` в профиле (self-upload + list badges + counts по уровням), `VerificationBadgeChip` (цветной chip auto/peer/kyc с иконкой), кнопки Ask for ID на DealPage (обе стороны), `VerificationRequestModal` + `VerificationRespondModal` (три-кнопочная модалка с разным copy для sender/carrier), amber-баннер «pending for you». i18n `verification.*` в 6 языках (40+ ключей). **MVP-компромиссы** (документированы): OCR = stub (`doc_type`/`doc_country` из body), sanctions match = всегда `clean`, container encryption только custodial. **176 backend-тестов зелёные**. Follow-up: PaddleOCR, OFAC/EU wiring, self-custody upload · миграция `0013_verification`, `backend/app/{models,api,core,schemas}/verification.py`, `backend/app/api/verification.py`, `backend/app/core/{verification,permissions}.py`, `backend/tests/test_verification.py`, `frontend/src/{api/verification,components/Verification*}.tsx`, `frontend/src/pages/{ProfilePage,DealPage}.tsx`, `frontend/src/i18n/locales/*.json`.

- **2026-07-15 · MILESTONE** · 🎉 **T2.2 pt.1 закрыт — Nostr keypair (custodial + management UI)**. Backend (миграция 0012): `User.nsec_encrypted BYTEA`, `nsec_nonce BYTEA`, `key_self_custody bool`, backfill keypair для существующих юзеров. `app/core/keypair.py` — secp256k1 через coincurve, Schnorr sign/verify, AES-256-GCM wrap с **отдельным** `NSEC_ENCRYPTION_KEY` env (не MESSAGE_ENCRYPTION_KEY). `app/core/signing.py` — единый signing helper: server-signs для custodial, raise 422 для self-custody без pre-signed, verify для pre-signed. Интеграция во все места создания DealVaultMessage/DealEvent (dealvault.py, deals.py, admin.py). `app/api/keypair.py` — 4 endpoints: `GET /me/keypair/status`, `POST /export` (password re-auth), `POST /claim` (deletes nsec, self-custody), `POST /import` (nsec or npub-only). Frontend: `KeypairSection` в профиле — npub display + Copy, export с password re-auth + warn, import с radio nsec/npub, NIP-07 detection banner (без функциональности до pt.2). `lib/nostr.ts` — light-touch NIP-07 detection (`hasNip07Extension()`, `getNip07Pubkey()`). 9 backend-тестов. i18n `profile.keypair.*` в 6 языках. **T2.2 pt.2** (open): backend signing refactor на NIP-01 event формат для NIP-07 client-side signing → полный self-custody. Версия `0.02.2` · миграция `0012_nostr_keypair`, `backend/app/{core/{keypair,signing},api/{auth,keypair,dealvault,deals,admin},models/user}`, `backend/tests/test_nostr_keypair.py`, `frontend/src/{api/keypair,lib/nostr,components/KeypairSection}.tsx`, i18n.

- **2026-07-14 · INFRA** · **Nginx custom warming-up page + healthcheck-based startup + build chunk splitting**. Проблема: после reboot сервера ~30 сек nginx отдавал bare `502 Bad Gateway` пока backend/frontend прогревались. Fix: (1) `nginx/_error.html` — красивая карточка ✈️ «Vimana starting…» с `<meta http-equiv="refresh" content="3">` для 502/503/504; `error_page` в конфиге. (2) `proxy_next_upstream error timeout http_502 http_503 http_504` с `proxy_next_upstream_tries 3` — 3 попытки before 502. (3) `docker-compose.dev.yml` healthchecks на backend (`python -c urllib /health`) и frontend (`wget /`), nginx `depends_on: condition: service_healthy`. Frontend build: `"type": "module"` в package.json + `manualChunks` в vite.config.ts разделяет vendor-react/i18n/phone от main. Main bundle 564 → 228 kB, оба warning'а исчезли · `nginx/{_error.html,default.conf}`, `docker-compose.dev.yml`, `frontend/{package.json,vite.config.ts}`.

- **2026-07-14 · MILESTONE** · 🎉 **Хвост Фазы 1 закрыт — T1.25 + T1.26**. **T1.26** Receiving Address: миграция 0011 добавляет 6 приватных полей в `User` (`receiving_country_iso`, `receiving_city`, `receiving_city_geoname_id`, `receiving_street`, `receiving_postal_code`, `receiving_note`). Новая схема `MeOut` extends `UserOut` — только `/api/auth/me` (владельцу) отдаёт `receiving_*`, `UserOut` не изменился (публичный). `PATCH /me` использует `exclude_unset=True` — можно очистить поле передав `null`. Локальный OCR-endpoint `GET /api/cities?q=&country=&limit=` через новый `app/core/cities.py` (переиспользует GeoNames `cities15000.txt` из T1.16 — 34k городов, мультиязычный поиск через alt_names). Рейт-лимитированный `POST /messages/share-address` в DealVault и Inquiry чатах — формирует `is_system=true` message с префиксом `📍 SHARED ADDRESS` через `app/core/address.py`. Frontend: `AddressForm` компонент (Country dropdown из airports API + City autocomplete с debounce 200ms + Street/Postal/Note); `AddressCard` парсит и рендерит адрес в чате с Copy + Open in Maps; кнопки «📍 Share my address» в `InquiryPanel` и `DealVaultPage`. i18n `profile.address.*` + `chat.shareAddress.*` + `chat.addressCard.*` в 6 языках. 10 backend-тестов (privacy, sanitize country ISO, cities search multilang, share flow, no-address 422). **T1.25** NewTripPage full redesign: Bento layout 6 ячеек (Route 2×1, Date, Capacity со слайдером, Categories 2×1, Publish, Preview 3×1); 3 hook-icons в углу заголовка (🎤/📷/⌨️) — feature-flagged VOICE_ENABLED/SCAN_ENABLED false, disabled с tooltip «coming soon» под EXP-03/04. Auto-save draft в localStorage через `useEffect`; `Cmd/Ctrl+Enter` publish shortcut; валидация (past date → error, capacity >15kg → confirm); Nostr checkbox под T3.5. Версия `0.01.26` · миграция `0011_receiving_address`, `backend/app/{core/{address,cities},api/{auth,cities,dealvault,inquiries},main,models/user,schemas/user}`, `backend/tests/test_receiving_address.py`, `frontend/src/{api/{auth,cities,dealvault,inquiry},components/{AddressCard,AddressForm,InquiryPanel},pages/{DealVaultPage,NewTripPage,ProfilePage},i18n/locales/*.json,version.ts}`.
- **2026-07-14 · PRD** · **Receiving Address в профиле + мультиязычный Nostr publish**. **T1.26 Receiving Address** (хвост Фазы 1): User расширяется приватными полями `receiving_country_iso`, `receiving_city`, `receiving_city_geoname_id`, `receiving_street`, `receiving_postal_code`, `receiving_note`. Миграция 0011. Адрес **приватный по умолчанию** — виден только владельцу через `GET /me`, никогда в list-endpoints; shared только через явную кнопку «📍 Share my address» в inquiry/DealVault-чате → уходит как `is_system=true` message с префиксом `📍 SHARED ADDRESS`, frontend рендерит как карточку с Copy + Open in Maps. `GET /api/cities?q=&country=` реиспользует существующий GeoNames dataset из T1.16. i18n `profile.address.*` + `chat.shareAddress.*` в 6 языках. **T3.5 Multilingual publishing strategy** (default approach D): trip publish = один event с оригиналом + `["lang", ISO 639-1]` тег; структурированные данные (IATA, ISO datetime, category enums) — универсальны без перевода; свободный текст переводится client-side через LLM/DeepL с Redis-кэшем. **EXP-06 Translated Nostr Events** (MASTERPLAN §10) — опция публиковать 6 переведённых event'ов для внешних Nostr-клиентов, feature-flag `EXP_TRANSLATED_NOSTR`. **D-TRANSLATION (open)** в TECHSTATE — выбор провайдера перевода (Claude Haiku / DeepL Free / локальный NLLB). USERJOURNEY §5 обновлён — упоминание share-address в этапе получения · TASKS §T1.26 §T3.5, MASTERPLAN §10 EXP-06, TECHSTATE §1 §2 §4, USERJOURNEY §5.
- **2026-07-13 · PRD** · **Verification Levels — 3-tier system + carrier as owner**. T2.1 расширен: `IdentityContainer.owner_role ∈ {sender, carrier, both}` — перевозчик может опционально загрузить документы; `IdentityContainer.storage_mode ∈ {encrypted_blob, zk_snark}` — миграционный путь к T6.4. `PeerVerification` заменена на `VerificationBadge(level ∈ {auto, peer, kyc}, source, verified_by?, in_deal_id?)` — единая модель для всех trust-источников. `VerificationRequest.target_role ∈ {sender, carrier}` — симметричный запрос от любой стороны; **асимметричные последствия**: sender declined = badge/cancel; carrier `declined_polite` = **без последствий**, чат получает нейтральное system-message «В целях собственной безопасности перевозчик решил не раскрывать своё имя. Отправитель вправе отменить сделку без штрафа.» `User.highest_verification_level` денормализован. `Trip.carrier_verification_level` для chip'а на карточке + фильтр `min_level`. Новый **V_verify_factor** в формуле УБА (T3.1): множитель [1.0…1.3] по highest badge level (`auto → 1.05`, `peer → 1.15`, `kyc → 1.3`), с нормализацией чтобы диапазон УБА остался [0…1000]. T6.4 переформулирован как ZK-native migration path — тот же API IdentityContainer, другой backend хранения. Новые METRICS: auto/peer-verified share, carrier-verified trips share, carrier polite-decline rate. USERJOURNEY §3a переписан под симметричный flow с polite refusal · TASKS §T2.1 §T6.4, IMPLEMENTATIONPLAN §2.1 §3.1, MASTERPLAN §6, METRICS §2 §4, TECHSTATE §4, USERJOURNEY §3a.
- **2026-07-13 · PRD** · **Полный аудит PRD** — sync после трёх волн изменений. Пакет A (P0 conflicts): IMPLEMENTATIONPLAN §5.1 переписан под Peer verification, §7 расширен regulatory KYC, USERJOURNEY §5.3 — убрано «фото документов перевозчика» (перевозчик отвечает депозитом), ENVIRONMENT §4 `black_box_messages` → `deal_vault_messages`, ENVIRONMENT §7/§8 порядок разделов восстановлен. Пакет B (P1 stale): MASTERPLAN §1/§6 разделяют peer/regulatory KYC; METRICS §2 «Verified-carrier share» → 4 новые: peer-verified sender share + trust-graph density + verification acceptance + regulatory-KYC-verified share; ENVIRONMENT §3 добавлены actual prod-env vars (MESSAGE_ENCRYPTION_KEY, CORS_ORIGINS, RATE_LIMIT_ENABLED, Telegram/Twilio/Admin); TECHSTATE §1 Status обновлён на 24 закрытые задачи, §3 полный backfill (44 pointers), §4 модели дополнены (Category, TripInquiry, Dispute, WaitlistEntry, IdentityContainer, TrustEdge, KycRecord, CorridorRestriction). Пакет C (P2): DESIGNGUIDELINES §5 добавлены peer-verification badge, trust circles, Nostr layer badge, ModeSwitcher, Bento skill; USERJOURNEY §5 расширен этапами 3a (peer verification), 5a (dispute updated), 6 (mode switch); PROJECT §10 Integrity Check разделён на peer/regulatory KYC. CHANGELOG расширен уровнем MILESTONE · все PRD-файлы + README.
- **2026-07-12 · DECISION** · D-NOSTR-RELAY зафиксирован: Vimana Nostr Relay = **strfry** (C++, LMDB), event kind trip = **NIP-99 30402** (Classified Listing), federation = whitelist friendly relays (publish only, subscribe отложен). D-NOSTR-FEDERATION (open) — конкретный whitelist уточняется в Фазе 3.5 · TECHSTATE §2.
- **2026-07-12 · PRD** · **Nostr-slope стратегия** — Vimana сознательно движется к Nostr-native слою постепенно. MASTERPLAN §1 архитектурный обзор → добавлен блок «📡 Vimana Nostr Relay». §7 → новый подраздел «Nostr-slope» + **Фаза 3.5** между 3 и 4 (T3.5). §8 Non-goals V1 → Vimana Nostr Relay = Фаза 3.5. §10 EXP → **EXP-03 Voice Trip Input** (Whisper + LLM), **EXP-04 Ticket Scan** (BCBP barcode + Vision LLM fallback), **EXP-05 Agent Interface** (MCP + Nostr subscribe + REST API keys). TASKS → **новая ЭТАП 3.5 T3.5** (strfry + publish + whitelist federation) + **T1.25 NewTripPage redesign** с hook-points для EXP · MASTERPLAN, TASKS, TECHSTATE, `frontend/src/version.ts` → 0.01.24.
- **2026-07-11 · PRD** · **Пересборка вокруг Peer verification + RBAC**. T2.1 переписан из «KYC-провайдер» в «Peer Identity Verification (P2P KYC)» — перевозчик просит документы отправителя, три варианта ответа (later/decline/upload), multi-doc по принципу US DMV, escalation в арбитра при подозрении на фальшивку. Локальный OCR (PaddleOCR/tesseract) + публичные санкционные списки (OFAC SDN + EU consolidated). Encrypted `IdentityContainer` (ключ = owner's Nostr nsec). Новая **T2.4 Trust Graph (Web-of-Trust)** — транзитивный граф `peer_verified`/`dealt_with`/`invited`, sybil-guard через closed deal. **T4.1 Classical KYC/AML** — переезд классического regulatory KYC в Фазу 4 (перед карточными платежами), старый T4.1 Payments переименован в T4.2. **T6.4 ZK-Proof of Verification** — Circom/halo2 circuit для приватности без раскрытия. MASTERPLAN §8 Non-goals обновлён · MASTERPLAN, TASKS, ENVIRONMENT.
- **2026-07-11 · MILESTONE** · 🎉 **T1.24 закрыта — Dual role + RBAC**. Часть 1: **RBAC permissions** (`app/core/permissions.py`) — `Permission` enum (8 permissions), `Role` enum, `ROLE_PERMISSIONS` map, `perms_of()`, `require_perm()` FastAPI factory. Миграция 0010 заменяет `is_superuser`+`is_arbiter` boolean на единую колонку `User.role`. Часть 2: **Dual role** — `can_carry`/`can_send`/`active_mode`, миграция 0009 удаляет `is_carrier`. Frontend: `ModeSwitcher` в Navbar (текст противоположного режима), разный визуал Dashboard (cyan+✈️ carrier / amber+📦 sender), i18n `mode.*` в 6 языках. **140 backend-тестов** зелёные (было 112), чистый `npm run build`. Версия `0.01.24` · `backend/app/{core/permissions,api/admin,api/deps,models/user}`, миграции 0009/0010, `frontend/src/{lib/permissions,components/ModeSwitcher,stores/auth}` + 3 admin-страницы + Navbar + Dashboard.
- **2026-07-10 · FEATURE** · T1.23: **User Zero + Arbiter role + Dispute model**. `nyxter@dealvault.club` промоутится в superuser при старте (idempotent через `ensure_user_zero()`). Новая модель `Dispute(id, deal_id UNIQUE, opened_by, arbiter_id?, reason, status, verdict?)`. Endpoints: `POST /deals/{id}/dispute`, `GET /admin/disputes`, `POST /disputes/{id}/claim`, `GET /admin/deals/{id}/vault` (**invite-only доступ** — арбитр читает переписку только по claimed dispute), `POST /disputes/{id}/resolve`, `GET/POST /admin/users`. Каждое открытие переписки арбитром пишет `DealEvent(arbiter_opened)` + system-message в чат — обе стороны видят красный баннер «⚖️ Арбитр открыл переписку». Frontend: кнопка «Open dispute» на DealPage, `/admin/disputes`, `/admin/users`, `/admin/deals/:id/vault`. 9 новых тестов · `backend/app/{api/admin,core/superuser,models/{user,deal}}`, миграция `0006_arbiter_dispute`, `frontend/src/pages/Admin*.tsx`.
- **2026-07-09 · FEATURE** · T1.22: **Inquiry chat panel + TripInquiry model**. Чат «до сделки» — sender открывает опубликованный рейс, справа появляется панель для общения с carrier'ом. Модель `TripInquiry(trip_id, sender_id, carrier_id, deal_id?)` UNIQUE(trip_id, sender_id) + `InquiryMessage` (encrypted at-rest как в T1.21). Endpoints: `POST /trips/{id}/inquiry` (idempotent), `GET /inquiries`, `GET/POST /inquiries/{id}/messages`. При `POST /deals/match` — `inquiry.deal_id` линкуется автоматически. Frontend `InquiryPanel.tsx` — правая панель 380px на десктопе, full-screen drawer на мобилке с backdrop-blur. Cyan-пузыри свои / ivory чужие. 8 тестов · `backend/app/{api/inquiries,schemas/inquiry,models/marketplace}`, миграция `0008_inquiry`, `frontend/src/{components/InquiryPanel,api/inquiry}`.
- **2026-07-08 · FEATURE** · T1.21: **At-rest AES-256-GCM шифрование DealVault-сообщений**. Переходный вариант перед T2.3 threshold-encryption. `MESSAGE_ENCRYPTION_KEY` env (32 байта base64, fail-loud если пусто). Колонки `text_ciphertext BYTEA` + `text_nonce BYTEA` заменяют `text` TEXT в `deal_vault_messages`. `DealVaultMessage.text` — Python `@property`: getter расшифровывает, setter шифрует. Существующий код `DealVaultMessage(text=...)` работает как раньше через SQLAlchemy `setattr` → триггер setter. Миграция `0007_encrypt_messages` — идемпотентная, backfill существующего plaintext, drop `text`. 7 тестов (roundtrip, разные nonce, missing key → RuntimeError, DB хранит bytes) · `backend/app/{core/crypto,models/deal}`, миграция 0007, `.env.example`.
- **2026-07-08 · MILESTONE** · ✅ **T1.20 закрыта — Cloudflare R2 в prod**. Bucket `vimana` подключён (endpoint `https://1742351dcc4d64320934d6659abdef6f.r2.cloudflarestorage.com`). Presigned URL работает, `<img>` рендерится. Добавлен `GET /health/storage` для мониторинга. Frontend `ImageLightbox` — full-screen preview с Esc/click-outside/× close. Впервые фото реально грузятся в UI · `backend/app/{core/storage,main}`, `frontend/src/{api/dealvault,pages/DealVaultPage,components/ImageLightbox}`.
- **2026-07-07 · PRD** · Добавлены задачи T1.23 (User Zero `nyxter@dealvault.club` + роль `arbiter` + модель `Dispute` с invite-only доступом к DealVault — арбитр читает переписку **только по claimed Dispute**, каждое открытие пишется в `DealEvent.arbiter_opened` + system-message в чате; User Zero видит всех и назначает арбитров) и T1.24 (dual role: `can_carry`+`can_send`+`active_mode`; `is_carrier` удаляется миграцией `0006`; явный `ModeSwitcher` в Navbar с текстом **противоположного** режима — «Switch to Deliver»/«Switch to Send», без указания текущего; разный визуальный дизайн Sender/Carrier dashboards) · TASKS §T1.23 T1.24.
- **2026-07-07 · MILESTONE** · ✅ **T1.19 закрыта — pre-production hardening**. 5 блоков за один заход: (1) race conditions устранены Postgres UPSERT + UNIQUE constraint + `IntegrityError → 409`; (2) upload — 10MB лимит, streaming SHA-256, MIME whitelist по kind; (3) `slowapi` rate-limit (login 5/мин, waitlist 3/мин), CORS whitelist из env, Telegram webhook secret через `X-Telegram-Bot-Api-Secret-Token`; (4) global exception handler + `X-Request-ID` middleware + `jsonable_encoder` для Pydantic errors, silent-catch в `core/telegram.py` и `core/whatsapp.py` заменены на `logger.exception`; (5) cursor pagination `{items, next_cursor}` для `/deals`, `/trips`, `/dealvault`, `/waitlist` + frontend `api/pagination.ts::Page<T>` + обновлены страницы. **103 backend-теста + 8 frontend** зелёные. Версия `0.01.19` · `backend/app/{core/{pagination,logging_setup,rate_limit},api/*,models/social,tests/*}`, миграция `0005_hardening_race_conditions`, `frontend/src/api/{pagination,trips,deals,dealvault}.ts`, страницы, `.env.example`, `nginx/default.conf`.
- **2026-07-07 · PRD** · Добавлены задачи T1.20–T1.22 (V1.1 pre-scaling) и T2.3 (Фаза 2). T1.20 — R2/S3 setup для DealVault-аттачей (сейчас в graceful-degradation режиме, фото не грузятся). T1.21 — переходное at-rest AES-256-GCM шифрование сообщений (защита от утечки БД, серверный ключ). T1.22 — right-side inquiry chat panel (`TripInquiry` модель + `InquiryMessage`, чат до сделки, миграция в DealVault при match). T2.3 — **threshold 2-of-3 encryption** (истинный e2e через Shamir Secret Sharing + Nostr ECIES): расшифровать могут любые 2 из 3 участников `{sender, carrier, arbiter}`, один не может. Заменяет at-rest из T1.21 в V2. Зависит от T2.2 (Nostr keypair) · TASKS §T1.20–T1.22, §T2.3.
- **2026-07-06 · MILESTONE** · 🎉 **ФИНИШ V1**. T1.8 закрыта: Vimana в продакшн-доступе на `https://vimana.dealvault.club`. Let's Encrypt сертификат, HTTPS-редирект, cron автообновления. Smoke test пройден. Фаза 1 (Ядро доверия) завершена: 18 задач + waitlist + 2 тест-сьюта. Дальнейшие шаги перед открытием для реальных пользователей — T1.19 (pre-production hardening).
- **2026-07-06 · INFRA** · T1.8: SSL/HTTPS в nginx. Two-server-block (:80 ACME challenge + redirect → :443 TLS 1.2/1.3), `X-Forwarded-Proto https` во все upstream'ы, `client_max_body_size 15M`. `/etc/letsencrypt` + `/var/www/certbot` смонтированы в nginx read-only. Порт 443 открыт. `.env.example` дополнен `CORS_ORIGINS` · `nginx/default.conf`, `docker-compose.dev.yml`, `.env.example`.
- **2026-07-06 · PRD** · Добавлена T1.19 (Pre-production hardening) — 5 групп критичных проблем из code-review 2026-07-06: race conditions на upsert'ах (Category/Invite/Waitlist), upload-attachment безопасность (limit/MIME/streaming), CORS whitelist + rate limiting + admin timing-safe compare + Telegram webhook secret, cursor pagination для длинных списков, global exception handler + JWT rotation с refresh-токеном · TASKS §T1.19.
- **2026-07-05 · FEATURE** · T1.18: waitlist + landing. Публичный `LandingPage.tsx` (bento-дизайн, 486 строк, i18n `landing.*` в 6 языках). Роутинг: `/` = landing, `/dashboard` = приложение (Navbar/логотип → `/dashboard`, аутентифицированный пользователь на `/` редиректится). Backend: модель `WaitlistEntry(email unique, name, source, created_at)`, миграция `0004_waitlist`, `POST /api/waitlist` (email нормализуется, 409 при дубликате, 422 при невалиде) + Telegram-уведомление админам через `ADMIN_TELEGRAM_CHAT_IDS`. `GET /api/waitlist` защищён header `X-Admin-Token` (env `ADMIN_API_TOKEN`). Фронт: убран fake-success в catch, показывается реальная ошибка. 7 новых тестов (78 всего) · `backend/app/models/waitlist.py`, `alembic/versions/0004_waitlist.py`, `app/api/waitlist.py`, `frontend/src/pages/LandingPage.tsx`, `App.tsx`, `Navbar.tsx`, `i18n/locales/*.json`, `.env.example`.
- **2026-07-05 · INFRA** · Nginx с динамическим DNS-resolver (`resolver 127.0.0.11 valid=10s`) + переменные `$backend_upstream`/`$frontend_upstream` в `proxy_pass` — больше не падает при смене IP frontend/backend после ребилда · `nginx/default.conf`.
- **2026-07-05 · INFRA** · Vite `server.allowedHosts` разрешает `vimana.dealvault.club` + wildcard `.dealvault.club` — иначе dev-сервер блокирует запросы через домен (защита от DNS-rebinding) · `frontend/vite.config.ts`.
- **2026-07-04 · FEATURE** · T_TEST.2: frontend smoke-тесты. `vitest` + `@testing-library/react` + jsdom. `vite.config.ts` расширен блоком `test`. Helper `renderWithProviders` c `MemoryRouter` + `I18nextProvider`. Setup mock `matchMedia`, cleanup localStorage. 7 кейсов: LoginPage/RegisterPage/LanguageSwitcher/StatusBadge. Скрипты `test` и `test:run` · `frontend/package.json`, `vite.config.ts`, `src/test/*`.
- **2026-07-04 · FEATURE** · T1.17: расширяемые категории. Enum `OrderCategory` заменён на модель `Category(name_key, is_default, usage_count)`, дефолты: `document/medicine/electronics/gift/animal/other`. Миграция `0003_category_freeform`. `GET /api/categories?q=` (default first, then by usage). При match с новой категорией — автосоздание записи + инкремент. Frontend `CategorySelect` — autocomplete + allow-new через Enter. `TripsPage`: одиночный выбор; `NewTripPage`: chip-мультивыбор. 5 новых backend-тестов. i18n `categories.*` в 6 языках. Версия `0.01.17` · `backend/{app/{models,api},alembic/versions/0003}`, `frontend/src/{api,components}/CategorySelect.tsx`, `TripsPage.tsx`, `NewTripPage.tsx`.
- **2026-07-04 · FEATURE** · T1.16 (расширение): мультиязычный поиск городов через GeoNames `cities15000.txt` (34k городов с alternate names — кириллица, китайский, арабский, ...). Airport обогащён `alt_names` (все переводы) + `population`. `search()` и `search_cities()` матчат по всем языкам. Сортировка аэропортов внутри города по OpenFlights id — старые хабы (DXB) первыми. Пример: печатаю «Москва» → нахожу Moscow → MOW/DME/SVO/VKO · `backend/app/data/cities15000.txt`, `core/airports.py`, +4 теста.
- **2026-07-04 · FEATURE** · T1.16: cascade выбор аэропорта. Backend `Airport.country_iso` через `pycountry` + alias-map для нестандартных имён OpenFlights (Russia, South Korea и др.). 3 новых endpoints (`/countries`, `/cities`, `/by-city`). Frontend `AirportCascadeModal` — 3 шага, страны локализованы через `Intl.DisplayNames`, поиск на UI-языке + английском. Интегрирован в `AirportSelect` через ссылку «Not working?». 5 новых backend-тестов. Версия `0.01.16` · `backend/app/{core,api}/airports.py`, `requirements.txt`, `frontend/src/components/AirportCascadeModal.tsx`, `AirportSelect.tsx`, i18n `airports.*` в 6 локалях.
- **2026-07-04 · PRD** · Добавлены T1.16 и T1.17: T1.16 — cascade выбор аэропорта (страна → город → аэропорт) когда геолокация не работает; названия стран локализованы через `Intl.DisplayNames`; поиск на языке UI + английском. T1.17 — категории груза становятся расширяемым справочником: дефолтные `document/medicine/electronics/gift/animal/other`; пользователи могут вводить свои категории, они попадают в общую базу и показываются в autocomplete другим · TASKS §T1.16 T1.17.
- **2026-07-04 · FEATURE** · T1.15: UX-полишинг. Универсальный хук `usePersistedState(key, initial)` — форма Login/Register/TripsPage filter восстанавливается из localStorage (пароль НЕ сохраняется). Backend register нормализует email в lowercase; login case-insensitive + trim whitespace для email. Кликабельный логотип в Navbar (Link → `/`, сделан в T1.12). 3 новых backend-теста (`test_register_normalizes_email_lowercase`, `test_login_is_case_insensitive_for_email`, `test_login_trims_whitespace`). Версия `0.01.15` · `backend/app/api/auth.py`, `backend/tests/test_auth.py`, `frontend/src/hooks/usePersistedState.ts`, `pages/{Login,Register,Trips}Page.tsx`.
- **2026-07-04 · FEATURE** · T1.14: раздел «Инвайты» в ЛК. Backend `GET /api/invites/mine` (`token`, `created_at`, `expires_at`, `status: pending|accepted|expired`, `accepted_by_display_name`). TTL увеличен 7д→14д. Frontend — секция в `ProfilePage` между «Контактами» и «Уведомлениями»: кнопка создания, список со статусом (цветной бейдж), обратный отсчёт формата «Xд Yч» (только дни + часы, без минут), кнопка Copy link для активных. 4 новых backend-теста. i18n-ключи `profile.invite*` в 6 локалях. Версия `0.01.14` · `backend/app/api/social.py`, `schemas/social.py`, `frontend/src/pages/ProfilePage.tsx`, `api/social.ts`, `i18n/locales/*.json`.
- **2026-07-04 · FEATURE** · T1.13: локализация — 6 языков (EN / UA / RU / PL / FR / ES). `uk.json` → `ua.json`, добавлен `ru.json` (переведены все ключи). Миграция localStorage `uk → ua` при инициализации. `LanguageSwitcher` переработан из строки кнопок в компактный dropdown с endonym-названиями (English / Українська / Русский / Polski / Français / Español), без флагов. Версия `0.01.13` · `frontend/src/i18n/`, `LanguageSwitcher.tsx`.
- **2026-07-04 · FEATURE** · T1.12: мобильная версия. `BottomNav` (4 иконки, `md:hidden`), десктопные ссылки Navbar скрыты на мобильном, логотип «Vimana» кликабельный (Link → `/`). Формы: `min-h-[2.75rem]` (44px+) тач-зоны, grid `1 sm:2` для origin/destination, поля на всю ширину. `DealVaultPage` — адаптивная высота чата, form-controls стакаются на мобильном. `DealPage` boarding pass — header/actions вертикальные на мобильном, ID `break-all`. Версия `0.01.12` · `frontend/src/components/{BottomNav,Layout,Navbar}.tsx`, все страницы.
- **2026-07-04 · FEATURE** · T1.11: телефон убран из RegisterPage; в ProfilePage — редактируемое поле с `CountryCodeSelect` (флаг + dial code через `libphonenumber-js/min`, локализованные названия стран через нативный `Intl.DisplayNames` — 0 зависимостей на переводы). Парсинг сохранённого phone через `parsePhoneNumberFromString`. Версия `0.01.11` · `frontend/src/components/CountryCodeSelect.tsx`, `pages/{Register,Profile}Page.tsx`, `package.json`, i18n-ключи `profile.phone{SelectCountry,SearchCountry}` (5 языков).
- **2026-07-04 · FEATURE** · T1.10: база аэропортов + геолокация. `backend/app/data/airports.dat` (7698 записей OpenFlights, 1.1MB) в репо → офлайн-старт; скрипт `scripts/update_airports.py` для обновления. `GET /api/airports?q=` (ранжированный поиск) + `GET /api/airports/nearest?lat=&lon=` (Haversine, D11). Фронт: `AirportSelect` — autocomplete + кнопка геолокации + дропдаун 3+прокрутка; заменил свободный ввод origin/destination в NewTripPage и TripsPage. 7 backend-тестов зелёные. Версия `0.01.10` · `backend/app/{core,api}/airports.py`, `backend/scripts/update_airports.py`, `frontend/src/{api,components}/AirportSelect.tsx`, i18n-ключ `trips.useGeolocation` (5 языков).
- **2026-07-04 · FEATURE** · T_TEST.1: backend тест-сьют — pytest + pytest-asyncio, автосоздание `vimana_test` через psycopg2, идемпотентные сид-фикстуры (`seed_carrier`, `seed_sender`, `seed_trip`, `seed_deal`) `scope="session"`, autouse-мок Celery `notify_deal_status.delay`. Тесты покрывают auth / trips / deals / dealvault / social — 30+ кейсов · `backend/tests/conftest.py`, `test_{auth,trips,deals,dealvault,social}.py`, `backend/pytest.ini`, `backend/requirements.txt`, `.env.example`.
- **2026-07-04 · PRD** · T_TEST.1 (backend-сьют) перенесён вверх — сразу после T1.9, до T1.10. Стал фундаментом: каждая последующая задача T1.x обязана добавлять свои тесты и прогонять весь сьют перед закрытием. Правило зафиксировано в TASKS.md (шапка), CLAUDE.md (новый раздел) · TASKS §T_TEST.1, CLAUDE.md.
- **2026-07-04 · PRD** · Добавлены задачи Фазы 1: T1.13 (локализация UK→UA, +RU — итого 6 языков), T1.14 (раздел «Инвайты» в ЛК с трекингом статусов и обратным отсчётом), T1.15 (UX-полишинг: персистентность форм в localStorage, case-insensitive логин + no-autocapitalize, кликабельный логотип) · TASKS §T1.13–T1.15, IMPLEMENTATIONPLAN §1.4.
- **2026-07-04 · FIX** · VITE_API_URL захардкоженный на старый IP ломал логин после смены EC2 IP; переменная убрана — фронт шлёт запросы на текущий origin через nginx-прокси; `.env.example` документирован · `.env.example`, `frontend/src/api/client.ts` (fallback `?? ''`).
- **2026-07-04 · INFRA** · `.dockerignore` для backend/frontend (убирает `node_modules`, `__pycache__` из build context); ротация Docker-логов через YAML anchor `x-logging` (10MB × 3 файла на сервис); EBS root расширен 6.7→30GB gp3; cron еженедельной чистки `docker system prune` · `frontend/.dockerignore`, `backend/.dockerignore`, `docker-compose.dev.yml`.
- **2026-07-04 · FEATURE** · T1.9: i18n (react-i18next) — 5 языков (EN/UK/PL/FR/ES), namespace nav/auth/common/dashboard/trips/deals/profile; LanguageSwitcher в Navbar; выбор в localStorage; даты через `i18n.language` · `frontend/src/i18n/`, `frontend/src/components/LanguageSwitcher.tsx`, все страницы.
- **2026-07-04 · FEATURE** · T1.7: Celery уведомления — email (smtplib), Telegram (Bot API), WhatsApp (Twilio); beat schedule для проверки дедлайнов; тумблеры в профиле; линкинг Telegram через `/start {token}` · `backend/app/worker.py`, `backend/app/tasks/notifications.py`, `backend/app/core/{email,telegram,whatsapp}.py`, `backend/app/api/telegram.py`, миграция `0002_notification_fields`.
- **2026-06-28 · PRD** · T1.10 уточнён: OpenFlights в память, Haversine (D11), кнопка геолокации, дропдаун 3+прокрутка; добавлен T1.12 (мобильная версия); D11 зафиксирован в Decision Log · TASKS §T1.10 T1.12, IMPLEMENTATIONPLAN §1.4, TECHSTATE §1 §2.
- **2026-06-28 · PRD** · Добавлены задачи Фазы 1: T1.9 (i18n, 5 языков), T1.10 (база аэропортов + autocomplete), T1.11 (телефон в профиль); T1.7 расширен — Telegram + WhatsApp каналы уведомлений · TASKS §T1.7–T1.11, IMPLEMENTATIONPLAN §1.4–1.5, TECHSTATE §1.
- **2026-06-28 · FIX** · Auth: login URL `/api/auth/token` → `/api/auth/login`; me URL `/api/me` → `/api/auth/me`; OAuth2 form → JSON body; токен сохраняется в localStorage до вызова me() · `frontend/src/api/auth.ts`, `frontend/src/pages/LoginPage.tsx`, `frontend/src/pages/RegisterPage.tsx`, `backend/app/api/auth.py`.
- **2026-06-28 · FIX** · bcrypt: заменена `passlib[bcrypt]` на прямой `bcrypt==4.2.0` (Python 3.12 совместимость) · `backend/requirements.txt`, `backend/app/core/security.py`.
- **2026-06-28 · FEATURE** · T1.6: Frontend SPA — React 18 + Vite + TypeScript + TailwindCSS; дизайн-система (navy/amber/ivory, IBM Plex Mono); 11 страниц; Zustand auth store; DealDetailOut на бэкенде · `frontend/src/`, `backend/app/api/deals.py`, `backend/app/schemas/marketplace.py`.
- **2026-06-28 · FEATURE** · T1.5: DealVault API — чат, загрузка файлов (multipart), SHA-256 хеш, presigned URL, иммутабельность на уровне API; R2 graceful degradation без R2_ENDPOINT · `backend/app/api/dealvault.py`, `backend/app/core/storage.py`.
- **2026-06-28 · FEATURE** · T1.4: Trips API (POST/GET с фильтрами), Deals API (match→accept→event→confirm→closed), DealEvent append-only · `backend/app/api/trips.py`, `backend/app/api/deals.py`.
- **2026-06-28 · FEATURE** · T1.3: JWT auth (register/login/me), InviteLink (TTL 7д, одноразовый), двусторонняя Connection, GET /api/me/connections · `backend/app/api/auth.py`, `backend/app/api/social.py`, `backend/app/core/security.py`.
- **2026-06-28 · MODEL** · T1.2: доменные модели — User, InviteLink, Connection, Trip, Order, Deal, DealEvent, DealVaultMessage, Attachment; миграция 0001_initial_models; nostr_sig/ipfs_cid/nostr_pubkey как nullable-заглушки Фазы 2/6 · `backend/app/models/`, `backend/alembic/versions/0001_initial_models.py`.
- **2026-06-28 · INFRA** · T1.1: FastAPI скелет + async SQLAlchemy + Alembic env · `backend/app/main.py`, `backend/app/core/`, `backend/alembic/`.
- **2026-06-27 · INFRA** · T0.1 выполнен: git init (ветка Azzot_main), структура `backend/` + `frontend/`, `docker-compose.dev.yml` (postgres:16 + redis:7), `.env.example`, `.gitignore` · затронуты: backend/Dockerfile, backend/requirements.txt, frontend/Dockerfile, docker-compose.dev.yml, .env.example, TECHSTATE §1.
- **2026-06-27 · PRD** · УБА-формула зафиксирована: `round(F_norm × Q_norm × V_norm × D_factor × 1000)` [0–1000]; 5 уровней (Новичок→Элита); D — бонус-множитель [1.0–1.5], не штраф; Q считает только сделки с двумя DealVault-фото · затронуты: IMPLEMENTATIONPLAN §6 §3.1, TASKS T3.1, METRICS §4, TECHSTATE §4 Фаза 3.
- **2026-06-27 · DECISION** · D10 зафиксирован: Nostr-совместимость = Вариант A (кастодиальный keypair → self-custody path) + Вариант D (NIP-07 browser extension override); детали механики в TECHSTATE §2b · затронуты: TECHSTATE, IMPLEMENTATIONPLAN §5, TASKS T2.2.
- **2026-06-27 · PRD** · Реструктуризация фаз: 4 → 6 (крипто-эскроу выведен в Фазу 5, добавлены Фазы 2/3/4 — Identity+Keys, UBA+Arbitration, Card payments); добавлен Социальный граф (Invite, Connections) в Фазу 1; BlackBox переименован в **DealVault** во всех файлах; добавлены варианты Nostr-совместимости (D10 в TECHSTATE) · затронуты: MASTERPLAN, IMPLEMENTATIONPLAN, TASKS, TECHSTATE, все PRD-файлы.
- **2026-06-27 · PRD** · Проект переименован PeerFlew → Vimana — Sacred Logistics; добавлены слоганы (Wings of Trust / Peer-to-Air / People Are the Network); заполнена Карта файлов в CLAUDE.md · затронуты: все PRD-файлы, CLAUDE.md.
- **2026-06-?? · PRD** · Инициализирована PRD-система Vimana — Sacred Logistics (PROJECT + 5 PRD-узлов + 5 артефактов) · затронуты: все файлы системы.
- **2026-06-?? · DECISION** · Зафиксированы решения D1–D5 (стек, не-кастодиальный BTC-эскроу 2-of-3, Уровень Бизнес-Активности вместо репутации, фазы-функциональные-блоки, «Чёрный ящик») · TECHSTATE.md.

*(Новые записи добавлять сверху. Дату проставлять фактическую.)*
