# Vimana — Sacred Logistics · TECHSTATE_ARCHIVE_01.md

> АРХИВ ──────────────────────────────
> Активный файл: [../TECHSTATE.md](../TECHSTATE.md)
> Содержимое: полные строки **§1 Status**, **§2 Decision Log** и **§3 Обнаруженная логика** — в оригинальном виде.
> В активном файле остались имя механики, статус и файлы реализации; сюда перенесено объяснение «почему так».
>
> ⚠️ **Правило обращения — обязательное, игнорировать нельзя (PROJECT.md §6.4).** Файл не читается в начале сессии.
> Если задача трогает, переписывает или опирается на описанную здесь логику — исполнитель **обязан остановиться и
> запросить у владельца разрешение** прочитать конкретную строку, назвав якорь (например `#L067`).
> ───────────────────────────────────

---


## §1 Status — полные строки

<a id="S09"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Уведомления (email + Telegram + WhatsApp) | 1 | 🟨 **код готов, доставку даёт только почта** (T1.7; уточнено 2026-08-06). Email — живой с 2026-07-30 (`D-MAIL-VIA-MAILU`). **Telegram и WhatsApp не доставили ни одного сообщения за всё время:** на проде не заданы `TELEGRAM_BOT_TOKEN` / `TELEGRAM_BOT_USERNAME`, аккаунта Twilio нет. `send_telegram` выходит на первой строке при пустом токене (`core/telegram.py:11`), `send_whatsapp` — при пустом SID (`core/whatsapp.py:9`), оба молча. Мёртвыми оказались и **пять админских путей**, все идущие через Telegram: сканер загрузок не отвечает и заражённый файл найден (T3.8), ZAP нашёл High (T_TEST.7), новая заявка waitlist (T1.18), уведомления по сделкам (T1.7). Острее всех второй — по решению владельца 2026-08-02 заражённый файл не блокируется автоматически, значит сообщение и есть весь механизм. Фолбэк `logger.warning` спасает только при пустом `ADMIN_TELEGRAM_CHAT_IDS`; заданные chat_id при пустом токене не оставляют даже строки в логе. Включение и живая проверка — T3.27. |

<a id="S10"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Интернационализация (i18n, 6 языков EN/UA/RU/PL/FR/ES) | 1 | ✅ готово (T1.9, T1.13). **Письма — T_UX.9 ✅ MVP** (2026-08-08): восемь писем на шести локалях, HTML+текст, `users.locale`/`waitlist.locale`, предпросмотр без отправки, почтовая консоль в админке с двумя раздельными контурами (pt.2). Сьют 1335 passed. Открыто: селектора языка писем в профиле нет (`UserUpdate.locale` принимается API) |

<a id="S15"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Waitlist + публичный Landing | 1 | ✅ готово (T1.18). **Письма — T_UX.8 ✅ MVP** (2026-08-08): подтверждение заявителю + уведомление владельцу на `USER_ZERO_EMAIL`, отметка `confirmation_sent_at` только по факту доставки. Разовая рассылка выполнена 22:17 UTC — `sent: 3, failed: 0`, все строки помечены. **pt.2** (2026-08-08): `GET /api/waitlist` переведён с общего заголовка `ADMIN_API_TOKEN` на `require_perm(Permission.WAITLIST_READ)` — это было единственное место в проекте, где авторизация шла мимо ролевой модели. Открыто: UI списка заявок нет |

<a id="S18"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Relay по TLS (`wss://<домен>/relay`) | 3.5 | ✅ MVP (T_SEC.4, 2026-08-01). Проксирование через nginx с апгрейдом соединения, порт 7777 больше не публикуется, `realIpHeader` включён. Снимает техническую невозможность Integrity Check Фазы 3.5 — но сама публикация по-прежнему выключена (`NOSTR_PUBLISH_ENABLED=false`, включение — `T3.20`). |

<a id="S19"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Форма доступа к данным (индексы, N+1, блокирующий I/O) | сквозная | ✅ MVP (T_PERF.1, 2026-08-01). pt.1 — миграция `0034` с 10 индексами под названные запросы, `verify_content` и BFS Trust Graph не зависят от объёма данных по числу запросов, фильтры рейсов на точном сравнении. pt.2 — boto3-клиент кэширован, R2-загрузка и декод изображений уведены с event loop, публикация в релеи параллельная. pt.3 — индекс GeoNames не живёт в памяти после загрузки, регистр предвычислен, счётчик публикаций атомарен, slowapi на Redis. |

<a id="S20"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Периметр контейнеров и запуск (публикация портов, пароль Redis, uvicorn без `--reload`, пин образа relay) | сквозная | ✅ готово (T_SEC.3, 2026-08-01). Наружу только nginx и relay; `db`/`redis`/`backend` на `127.0.0.1`. Попутно удалён неиспользуемый `weasyprint`, снявший потолок `Pillow<11`. |

<a id="S21"></a>

| Блок | Фаза | Статус |
|---|---|---|
| IDOR-матрица из роутера + ZAP baseline | сквозная | ✅ MVP (T_TEST.7, 2026-08-03, прогнано: полный сьют **1141 passed**; ZAP по проду **0 High, 3 Medium, 4 Low**, Acceptance выполнен, все три Medium разобраны в TASKS). pt.2: `tests/test_idor_matrix.py` обходит `app.routes`, сверяет 49 путей с параметрами против таблицы ожиданий **в обе стороны** — новый эндпоинт без записи валит тест фактом появления. Три вердикта: `DENIED` / `PUBLIC` (причина рядом) / `CAPABILITY` (неизвестный токен → 404). Попутно найден и исправлен порядок проверок в `reveal-my-share` (§3). pt.1: `.zap/baseline.sh` + `rules.tsv` + `app/cli/zap_report.py`, порог «любой High — провал» в коде, не в TSV; алерт через существующий админский путь Telegram. **Ни один прогон ещё не состоялся** — ни pytest, ни сканер. |

<a id="S22"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Нагрузка (k6) | сквозная | ✅ разобрано (T_TEST.6, 2026-08-11): пять сценариев, пятнадцать прогонов, две машины. **Код быстрый и масштабируется линейно** — пол 4–7 мс медианы, на 4 ядрах rps идёт 1:1 с конкуренцией до 200 VU. Единственная точка сериализации — `pg_advisory_xact_lock` — держит **одну сделку** на 20 записях/с, восемь сделок дают втрое больше и упираются в CPU. **Порог `p95<500` был требованием к железу:** на 2 ядрах выполняется до ~50 читателей и на реальной смеси с 15-кратным запасом, рушится на синтетических ста (за ёмкостью инстанса), на 4 ядрах выполняется везде до 200. Ошибок нет нигде. Три сценария из четырёх были мертвы четыре недели (звали удалённый `/auth/register`) — оживлены сеялкой `app/cli/load_seed.py`. Журнал условий и чисел — `load/RESULTS.md`. **Оговорка ко всем числам:** база была почти пустой (40 рейсов); посев 100 000 и повтор — блокирующее условие Альфы |

<a id="S23"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Деплой без простоя | сквозная | 🟨 дешёвая часть готова (T_OPS.1, 2026-08-11): `/ready` отдельно от `/health`, пауза `DRAIN_SECONDS` по SIGTERM, `--timeout-graceful-shutdown 20` + `stop_grace_period 40s`. **Дыру не закрывает и не может:** измерено тремя способами — деплой на одном инстансе это 8–13 секунд, когда отвечать некому, и 6–7 % отказов под нагрузкой, причём все шлюзовые. Закрывает только вторая реплика; записано **блокирующим условием выхода в Альфу** |

<a id="S24"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Доступность (axe-core) | сквозная | ✅ **9 из 9 страниц, 0 нарушений** (T_TEST.8, закрыто 2026-08-23). Покрытие доведено с пяти страниц до девяти; шесть авторизованных не проверялись вовсе с 2026-08-10, потому что весь браузерный e2e был мёртв (`registerUser` ждёт форму, удалённую в T3.28). Обойдено `signInFixed` — тесту нужна сессия, а не регистрация; пароль постоянному аккаунту ставит сам продукт через step-up с кодом на почту. Две настоящие находки: безымянные `input[type=date]` и `input[type=range]`. **У правила `label` есть запасная проверка на непустой `placeholder`** — поэтому три подписи полей аэропортов не работали, а axe молчал; плейсхолдер именем не является, он исчезает при первом символе. Ниже — история контраста и структурных находок. 🟨 структурные находки закрыты, контраст выведен из-под теста решением владельца (T_TEST.8, 2026-08-03). Первый прогон: 5/5 страниц падают. `select-name` (безымянный `<select>`) исправлен, попутно связаны шесть подписей в `AddressFormFields` через `useId`. `list`/`listitem` закрыты пропом `as` у `Reveal` (картинка не изменилась). `color-contrast`: замена шкалы на сплошные токены была сделана в 59 файлах и **откачена по требованию владельца** — вместе с низким контрастом уходила градация вёрстки. Правило и следующее из него `link-in-text-block` отключены с записанной причиной, признание вынесено в DESIGNGUIDELINES §6: по контрасту текста продукт WCAG AA не выполняет. Открыто: `button-name ×3` и `label ×1` — ждут селекторов из детального вывода. |

<a id="S25"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Мутационное тестирование (mutmut) | сквозная | ✅ **87,5 % на `permissions.py`, 78,3 % на `threshold.py`** при пороге 60 % (T_TEST.10, 2026-08-23, досрочно). Одна настоящая дыра, серьёзная: инверсия `viewer.role == "superuser"` в `visible_to` пережила **весь сьют, 1746 тестов**, делая публичным всё, что аккаунт спрятал. В `threshold.py` 112 выживших из 144 сидели в непроверяемых ветках отказа — все тесты подавали только корректный ввод. Доказано числом, что **round-trip слеп к симметричным мутациям**. Инструмент **не приписывает ни одному тесту код, исполняемый на импорте**, поэтому выдумывает выживших (но не прячет их) — перепроверка `backend/scripts/verify_survivors.py`. Оснастка: `backend/setup.cfg`. Подробности и разбор остатка — TASKS `T_TEST.10` |

<a id="S27"></a>

| Блок | Фаза | Статус |
|---|---|---|
| At-rest AES-256-GCM шифрование DealVault-сообщений | 1 | ✅ готово (T1.21, переходно к T2.3) |

<a id="S29"></a>

| Блок | Фаза | Статус |
|---|---|---|
| User Zero + Arbiter role + Dispute model (базовая механика) | 1→3 | ✅ готово (T1.23; полная Фаза 3 — позже) |

<a id="S33"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Peer Identity Verification (P2P KYC) | 2 | ✅ MVP (T2.1: backend + frontend, custodial only. OCR/OFAC — stub, self-custody 422 до T2.3. pt.3 закрыт: `VerificationRequestOut` типизирован enum'ами + list-endpoint покрыт тестами) |

<a id="S34"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Trust Graph (Web-of-Trust) | 2 | ✅ MVP (T2.4: TrustEdge + auto dealt_with/invited + BFS endpoints + denormalized counts + UI. Follow-up: Redis-кэш, UserBadge на TripCard) |

<a id="S35"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Keypair + Nostr-совместимость (D10: A+D) | 2 | ✅ MVP (pt.1 custodial + UI; pt.2 NIP-01 event format + NIP-07 signing + Claim self-custody end-to-end) |

<a id="S36"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Threshold 2-of-3 encryption (замена at-rest из T1.21) | 2 | ✅ MVP (T2.3: NIP-44 v2 wrap of Shamir shares (переведено с NIP-04 в `T_KEYS.1`, 2026-08-02) + read_packages для нормального read; `/arbiter-reveal` с audit-event; DealVault e2e-путь, Inquiry — follow-up) |

<a id="S37"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Уровень Бизнес-Активности (УБА) | 3 | ✅ MVP (T3.1: формула F/Q/V/D/Vrf в `core.uba`, Celery beat hourly recompute, `/me/uba` + `/users/{id}/uba` endpoints, `UBASection` в профиле. D=0 до T5.x Collateral) |

<a id="S38"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Оператор-арбитр и споры + Access Grants | 3 | ✅ MVP (T3.2: OperatorAccessGrant + auto-create от opener + explicit grant/revoke + vault-read gate на active grant. UBA-chip на TripCard. Роль/консоль/Dispute — из T1.23/T1.24. Escrow → T5.x) |

<a id="S39"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Recipient role в DealVault | 3 | ✅ MVP (T3.3: DealParticipant модель + invite/join/revoke/list endpoints + невидимый custodial keypair per recipient + server-mediated decrypt-for-me для custodial callers. Threshold 2-of-3 не тронут — recipient орто) |

<a id="S40"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Vimana Nostr Relay (strfry) + Federation | 3.5 | ✅ MVP (T3.5 pt.1 + pt.2: publish bridge + toggle + strfry контейнер + badge + NIP-07 self-custody publish + WoT-gate через writePolicy plugin + metrics endpoint + superuser republish. Follow-up: D-TRANSLATION мультиязычный перевод описаний — pt.3) |

<a id="S41"></a>

| Блок | Фаза | Статус |
|---|---|---|
| DealVault Protocol: полнота цепи (msgs/files/seal) + контент-валидация + identity-пересечение | 3.6 | ✅ MVP: T3.7 (цепь messages/files/seal) + T3.8 (контент-валидация) + T3.9 (identity-пересечение: копия в сделке + identity_ref, тройной doc_hash). Остался только T3.10 (преза + лендинг, v1 → сравнение с альтернативой) |

<a id="S42"></a>

| Блок | Фаза | Статус |
|---|---|---|
| DealVault: презентация + лендинг | 3.6 | 🟨 в работе (T3.10: выставлены `frontend/public/dealvault-v5-{corporate,rebel}.html` + `vimana-cta.js`. Открыты: преза, выбор версии, **расхождение по тону** — v5 заявляет tamper-proof вместо tamper-evident, см. TASKS T3.10) |

<a id="S43"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Лестница владения личностью (Identity Vault) | 3.7 | 🟨 решение принято, кода нет. `D-KEY-TIERS` (2026-08-01) заменил бинарную модель четырьмя ступенями: только аккаунт → личность у платформы → копия у пользователя (`.dvlt` под парольной фразой, запечатывается в браузере) → копия у платформы удалена. Задачи: `T3.21` (формат + скачивание), `T3.22` (удаление копии), `T3.23` (импорт своей личности), `T3.24` (ридер без платформы). Переписаны `T3.16`/`T3.17`; отменено решение №6, смягчено №4. |

<a id="S44"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Идентичность и вход (email-подтверждение, npub, Passkeys, step-up, recovery) | 3.7 | 🟨 в работе. **T3.11 ✅ MVP** (email-only регистрация + код подтверждения; телефон убран из auth, `password_hash` nullable, миграция `0028`). **T3.12 ✅ MVP** (служебный ключ ≠ личность; `establish` с доказательством владения, `declare-lost`, снятие `claim`/`import`/`export`, перешифровка обоих сейфов с самопроверкой, публикация под платформенным ключом с фильтром; миграции `0029`/`0030`). **T3.13 ✅ MVP** (вход и регистрация по Nostr-ключу; три purpose вместо URL-привязки NIP-98; аккаунт без пароля и без email). **T3.14 ✅ MVP** (passkeys: несколько устройств на одну личность, guard последнего аутентификатора, исключение для синхронизируемых passkey'ев в sign-count; миграция `0031`; env `WEBAUTHN_RP_ID`/`ORIGIN` **обязаны** совпадать с доменом). **T3.15 ✅ MVP** (step-up re-auth: одноразовый грант в Redis со скоупом операции, fail-closed, три пруфа по числу способов входа, `X-Step-Up-Token` в заголовке; `declare-lost` и отвязка passkey переведены на него — беспарольный аккаунт больше не заперт). **T_KEYS.1 ✅ решение принято** (`D-KEY-TIERS`, 2026-08-01): бинарная модель владения заменена лестницей из четырёх ступеней, средний уровень — копия ключа и у платформы, и у пользователя (файл `.dvlt` под парольной фразой). **T3.16 ✅ MVP** (recovery-коды: код принят как четвёртое доказательство step-up, scoped-токен не является сессией по построению, SHA-256 вместо bcrypt с обоснованием, восстановление режимом на экране входа; миграция `0035`). T3.17 ⬜ переписана под лестницу (экран последствий уехал с создания личности на удаление копии с платформы). **T3.18 ✅ MVP** (публичная страница личности по npub, читается без входа; видимость `full/minimal/hidden` одной функцией `visible_to`, скрытое отвечает 404, а не 403; миграция `0038`). **T3.19 ✅ MVP** (архив завершённых личностей: окно 15 дней от `key_lost_at`, разовая модалка + постоянный баннер, закрытие витрины навсегда при сохранении всех записей, агрегаты только со знаменателями; миграция `0039`). **T3.20 ✅ MVP** (якоря цепи включены 2026-08-01 и проверены живым тиком: голова опубликована на четырёх сторонних релеях; API отдаёт принявшие релеи, сторонние отдельно и число незаякоренных записей). **T3.21–T3.24 ✅ MVP** (формат `.dvlt` и скачивание, удаление копии платформы, импорт своей личности, автономный ридер). **Почта заработала 2026-07-30** (см. `D-MAIL-VIA-MAILU`): порт 465 вместо 587, обязательные `Date`/`Message-ID`; регистрация → Gmail «Входящие», `dmarc=pass`, оценка rspamd 0.00/15. Не сделано в T3.14: conditional mediation (autofill в поле email). Открыто из T3.12: NOT NULL на `nostr_pubkey` отдельной миграцией; правило «длинных хопов» ждёт структурных origin/destination. **T3.15 добит 2026-07-30**: смена почты через `pending_email`, смена/установка пароля с завершением остальных сессий, секция «Почта и пароль» в профиле; миграции `0032`/`0033`. Открыто из T3.15: добавление passkey/Nostr как способа входа (`ADD_AUTH_METHOD` заведён, эндпоинтов нет). **Почта закрыта полностью 2026-07-30**: custom MAIL FROM (`bounce.dealvault.club`) подтверждён, SPF и DKIM оба выровнены с `From`. |

<a id="S45"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Правовой и информационный контур (условия, политика, «о нас», принятие с версией) | 4 | ⬜ не начато (T4.0, заведено 2026-08-12). **Идёт первым в этапе:** процессор не откроет счёт без опубликованных документов, то есть T4.2 без него невозможен физически. Проверено фактом: **кук в продукте нет вовсе, аналитики ни одной** — только `localStorage` под токен и язык, а он освобождён от согласия как строго необходимый. Значит сегодня нужно **раскрытие, а не баннер согласия**; механика согласия закладывается на вырост и включается в день появления первого трекера |

<a id="S46"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Regulatory KYC/AML (только KYC-провайдер + person-level SDN) | 4 | ⬜ не начато (T4.1). Corridor-периметр не блокируем — информируем через RouteNote (T_UX.2, D-COMPLIANCE-STANCE). |

<a id="S47"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Route notes + platform disclaimers | 3 | ✅ MVP (T_UX.2: backend + admin CRUD + PlatformNoticeBanner + UI slots + DealVault pinned system-msg на match + direct headline/body text вместо i18n_key). Multi-lang translations — pt.5 (когда появится curation workflow). |

<a id="S48"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Multiple receiving addresses + Edit profile + avatars + landing on logo | 3 | ✅ MVP (T_UX.4: `ReceivingAddress` таблица c partial-unique index на default + CRUD + share-address address_id + `users.avatar_key` + POST/DELETE /me/avatar через тот же R2 бакет + `<AddressesSection>` inline edit + `<EditProfileModal>` + `<ShareAddressModal>` picker в чатах + two-col Bento на /profile + Navbar лого → landing). Legacy `User.receiving_*` колонки остаются fallback'ом на contract-фазу; удаление отдельной миграцией. |

<a id="S49"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Agentic MCP server | 3 → 7 | ✅ MVP кода (T_AGENT.1 pt.1 + pt.2: 4 tools — `list_trips`, `search_trips`, `get_trip_details`, `get_mcp_metrics`; `MCP_API_KEY`, rate-limit sliding window). **Но не развёрнут:** транспорт stdio, сервис удалён из compose 2026-07-30 — как демон он получал закрытый stdin и перезапускался по кругу 11 дней. Выбор между сетевым транспортом и отказом от контейнера — **T7.1 (Фаза 7)**. До него `D-AGENTIC-INTERFACE` и код расходятся сознательно. |

<a id="S50"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Контакты: `user_contacts` + один код-механизм на все каналы | 3.8 | 🟨 **T3.25 ✅ код готов** (2026-08-09): таблицы `user_contacts` (частичный UNIQUE только по подтверждённым) и `verification_challenges`, `core/contacts.py` (E.164 через `phonenumbers`), `core/contact_verification.py` (машина кодов), миграция `0045` с бэкфилом. **`T3.26` закрыт 2026-08-09** — `core/channels.py` с одной сигнатурой `deliver` на все транспорты; ~~не начат~~. T3.27 добавил сюда `open_exchange`/`mint_into` и nullable `code_hash`: Telegram единственный канал, где код нельзя выпустить заранее |

<a id="S51"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Вход и регистрация одним полем (код-first) | 3.8 | ✅ MVP (T3.28, 2026-08-10, сьют 1495 passed). `POST /auth/otp/{request,verify}` — 202 всегда, кроме cooldown; один конструктор `core/accounts.create_user`; парольная регистрация удалена вместе с эндпоинтом; `contact_code` как четвёртый step-up-пруф, иначе аккаунт из кода заперт. ~~**Открыто:** правило «доказательство контроля забирает контакт»…~~ → **снято 2026-08-10** (`D-CHANNELS-EMAIL-AND-CHAT`): опасность возникала из-за переиздания телефонных номеров, а телефон перестал быть входом. Лимиты на запрос кода — T3.29 |

<a id="S52"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Telegram как канал кода и способ входа | 3.8 | ✅ MVP (T3.27, 2026-08-11). Ссылка `t.me/<bot>?start=<nonce>` → `/start` чеканит код → `otp/verify` по nonce. Аккаунт разрешается из chat_id: существующий контакт, усыновление legacy-колонки или новый аккаунт без адреса вовсе. Миграция `0048`: `code_hash` nullable + `resolved_value`/`resolved_label` |

<a id="S53"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Лимиты на запрос кода | 3.8 | ✅ MVP (T3.29, 2026-08-11). `core/code_limits.py`: потолок на почтовый ящик (5/час, не обходится сменой адреса источника) и на число **разных** ящиков с одного источника (10/час). Врезано в `otp/request`, `contact/request-code`, `password/forgot` — три формы, один бюджет. Redis недоступен → запрос проходит (та же сделка, что у slowapi) |

<a id="S54"></a>

| Блок | Фаза | Статус |
|---|---|---|
| SMS и WhatsApp как каналы кода | 3.8 | ⬜ SMS ❌ не делаем (T3.30). WhatsApp (T3.31) — **отложен до регистрации**: заявка в Meta подана 2026-08-11, кода нет и не пишется до ответа. Возвращаемся после верификации бизнеса и одобрения authentication-шаблона |

<a id="S55"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Матрица оповещений «событие × канал» + локализация | 3.8 | ✅ MVP (T3.32 + T3.33, 2026-08-11). Язык писем следует за языком интерфейса: переключатель в шапке пишет `users.locale` вошедшему, `UserUpdate.locale` валидируется по шести локалям с сужением `ru-RU`→`ru`. `users.notification_prefs` JSONB (миграция `0047`, бэкфил из трёх булевых — иначе миграция молча включила бы всё обратно тем, кто выключал), реестр классов `core/notification_prefs.py`, `_notify_user` спрашивает матрицу. Класс «безопасность» неотключаем — теперь свойство класса, а не привычка семи функций. Показываются только классы, у которых есть отправитель (`deal`, `deadline`, `security`), и каналы, включённые в `channels.enabled` — WhatsApp появится колонкой в день T3.31 |

<a id="S56"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Письмо о входе с нового устройства | 3.8 | ✅ MVP (T_SEC.6, 2026-08-11). `user_sign_ins` (миграция `0046`), отпечаток = хеш(браузер + ОС + сеть /24), письмо на шести локалях. Врезано во все пути входа: пароль, код, passkey, Nostr — с письмом; recovery-код и оба сценария создания аккаунта — молча (у первого уже есть своё письмо, у вторых нечему быть незнакомым). Геолокация — MaxMind локально и **опционально**: без `GEOIP_DB_PATH` письмо уходит без строки о месте. Хранение 90 дней, чистит `purge_old_sign_ins` |

<a id="S57"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Протокол сделки в чате: типизация карточек | 3.9 | ✅ MVP (T3.34, 2026-08-16). `DealVaultMessage` получил конверт (`card_kind`, `card_payload`, `card_state`, `requires_ack_by`, `acked_by/at`, `supersedes`), миграция `0050` с бэкфиллом старых `📍 SHARED ADDRESS`. Каталог из 37 типов объявлен целиком, реализован один. Сьют 1685 passed |

<a id="S58"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Договор о сделке (`terms.*`) | 3.9 | ✅ MVP (T3.35, 2026-08-16). Две точки входа — поля рейса и чат по рейсу — сходятся в одну карточку; нормализация к $/кг и $/км на сервере; `terms.agreed` штампует версию параметров. Миграция `0051` дала `Trip` первые ценовые поля. Открыто: фронта нет, `Deal.status → accepted` пока достижим и через legacy `POST /deals/{id}/accept` |

<a id="S59"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Параметры бизнес-логики в админке | 3.9 | ✅ MVP (T3.40, 2026-08-16). `PlatformParameter` версионируемый, с областью `global`/коридор, миграция `0049`. Реестр из 16 ключей с дефолтами в коде, поэтому пустая таблица = работающая платформа. Отдельное право `PARAMS_MANAGE`, не в наборе арбитра. Открыто: запись версии в сделку — только вместе с `handoff.confirmed` из T3.37 |

<a id="S60"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Карточки сделки: логистика, владение, расчёты, исключения | 3.9 | ✅ MVP (T3.36–T3.39, 2026-08-16). Четыре группы — **один механизм**: каталог декларативный, единственный `POST /deals/{id}/cards` читает объявление. Фиксация сумм и версии параметров при `handoff.confirmed`; `handoff.declared` после вылета отклоняется и просит переподтвердить условия. Фронт зеркалит каталог (`lib/cardForms.ts`). Открыто: `deal.sealed` и `dispute.*` на старых путях |

<a id="S61"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Единицы, форматы и правила перевозки | — | ✅ MVP (T_UX.14 + T_UX.15, 2026-08-16). `users.unit_weight` / `date_format` / `carriage_rules`, `trips.carriage_rules`, миграция `0052`. Хук `usePrefs` — единственный источник формата; `lib/format.ts` только отображает, хранение остаётся метрическим и в ISO |

<a id="S62"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Навигация по режиму, страница перевозчика, панель | — | ✅ MVP (T_UX.18 + T_UX.19, 2026-08-17). Меню и панель зависят от активного режима; `/carriers/:id`; `/deals` → `/history`; споры одним пунктом с секцией арбитра; `POST /trips/{id}/cancel` |

<a id="S63"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Публичные страницы по аудиториям + панель по адресу режима | — | 🟨 в работе (T_UX.23, 2026-08-25). `/` с акцентом на перевозчика, `/carrier`, `/send`, `/business`. `/carrier` и `/send` — один адрес, два экрана: гостю лендинг, вошедшему панель; `/dashboard` стал редиректом по режиму. Общая оболочка `LandingShell` + скелет `AudienceLanding`, различаются только тексты (`audience.*` ×6 локалей). Пререндер и nginx расширены с одного адреса до четырёх. Ждёт прод-сборки и прогона e2e |

<a id="S64"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Профиль: витрина и операционка | — | ✅ MVP (T_UX.21 + T_UX.22, 2026-08-23, сьют 1806 passed). «Уровень активности» свёрнут в «Аккаунт» (УБА + верификация на витрине, адреса и список рейсов оттуда убраны); заведён раздел «Мои правила» второй строкой — правила перевозки, правила взаимодействия, почтовые адреса, платёжные инструкции. Миграция `0053` дала `users.interaction_rules` и `users.payment_instructions`, оба owner-only. **Каталог платёжных методов отложен до фаз платежей** — сегодня это текст, и подписи не обещают большего (§9.1) |

<a id="S65"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Профиль: боковая навигация по разделам | — | ✅ MVP (T_UX.20, 2026-08-23). **Восемь** разделов вместо одной ленты из 24 блоков: аккаунт (витрина с УБА и верификацией), мои правила, круги доверия, доступ и данные, история и споры, регион и форматы, оповещения, администрирование по роли. Разделы — вложенные маршруты под `/profile`; `/profile/keys` и `/profile/notifications` сохранили адреса, `/profile/activity` ретайрен редиректом. Единый способ правки и описание под каждым заголовком — `DESIGNGUIDELINES §9b` |

<a id="S66"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Карточка создания рейса: переработка | 3.10 | ⬜ ждёт данных владельца (T3.41, заведено 2026-08-23). Форма выросла историей правок с `T1.25`; состав недостающих полей владелец задаёт после разбора переписок — там видно, что стороны переспрашивают в чате руками. Хук-точки `EXP-03`/`EXP-04` обязаны пережить переработку |

<a id="S67"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Роль как предложение: принятие и журнал | 3.10 | ⬜ не начато (T3.42, заведено 2026-08-23). Сегодня `promote-arbiter` меняет `users.role` и коммитит: без согласия, без записи кто и когда, снятие так же бесследно. Для арбитра это особенно плохо — доступ к сейфу по `OperatorAccessGrant` пишется в цепь, а происхождение самой роли не пишется никуда |

<a id="S75"></a>

| Блок | Фаза | Статус |
|---|---|---|
| DealVault на Pubky: распределённые сейфы, эпохи ключа, гардианы | 8 | ⬜ не начато (T8.1–T8.10, заведено 2026-08-23). **Первая фаза после выхода Альфа 1** — не входит в набор, закрытие которого даёт релиз. Спецификация — `DealVault_architecture.md`, порядок — `IMPLEMENTATIONPLAN §9.6`. Снимает терминальность `key_lost_at`: восстановление не возвращает ключ, а переводит личность на новую эпоху по threshold-решению гардианов. Раздел «Социальное восстановление» в «Доступе и данных» приходит с T8.6/T8.7, до этого не рисуется |

<a id="S76"></a>

| Блок | Фаза | Статус |
|---|---|---|
| Агентский доступ по сети (транспорт MCP) | 7 | ⬜ не начато (T7.1). Фаза заведена 2026-07-30 после того, как выяснилось, что stdio-сервер нельзя держать сервисом. |


## §2 Decision Log — полные строки

<a id="D-D3"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D3 | Вместо репутации — Уровень Бизнес-Активности (частота × оценочная стоимость × размер залога) | Метрика делом, устойчивее к накрутке отзывами | 2026-06 |

<a id="D-D8__open_"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D8 (open) | Портативность: формат экспортируемого пакета — IPFS DAG + Nostr events | DealVault как Nostr event JSON → пин в IPFS → CID = верификационный хеш; реализация в Фазе 6 | TBD |

<a id="D-D11"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D11 | **Геолокация аэропортов: Haversine в Python.** Датасет OpenFlights (~7 000 аэропортов) загружается в память при старте бэкенда. Расстояние до ближайших считается формулой Haversine без внешних API и без PostGIS | Датасет мал (< 1 МБ); Haversine даёт ответ за микросекунды; Redis GEO и PostGIS избыточны | 2026-06-28 |

<a id="D-D10"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D10 | **Nostr-совместимость: Вариант A + D.** Платформа генерирует secp256k1-keypair при регистрации и хранит зашифрованно. Пользователь «забирает» nsec в любой момент → платформа удаляет свою копию. Если обнаружен NIP-07 браузерный extension (Alby, nos2x) — Vimana использует его для подписи вместо custodial ключа | Масс-маркет онбординг без барьеров + поддержка существующей Nostr-идентичности для продвинутых пользователей | 2026-06-27 |

<a id="D-D-NOSTR-RELAY"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-NOSTR-RELAY | **Vimana Nostr Relay = strfry** (C++ от hoytech, LMDB storage). Дeploy как отдельный контейнер `nostr-relay` в docker-compose. Event kind для trip = **NIP-99 30402** (Classified Listing, replaceable per `d`-tag) — совместим с существующими Nostr-клиентами. Federation = **whitelist friendly relays** (publish only, subscribe отложен до Фазы 4+). Auth NIP-42, rate-limit 30 events/hour per pubkey, WoT-gate из T2.4 | strfry — production-ready, лёгкий (~50 MB idle), нативная поддержка нужных NIP-ов; NIP-99 = стандартный marketplace-формат (уже понят damus/amethyst/coracle); publish-only федерация — предсказуемее subscribe с точки зрения spam/moderation | 2026-07-12 |

<a id="D-D-NOSTR-FEDERATION"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-NOSTR-FEDERATION | **Стартовый whitelist** friendly relays: `wss://relay.damus.io`, `wss://nostr.wine`, `wss://relay.nostr.band`. Env `NOSTR_FRIENDLY_RELAYS=<comma-separated>`. Ревизия каждые 3–6 месяцев по популярности и uptime. Пороги для замены: >10% publish failures в течение недели, или полная недоступность >48 ч. | Три relay покрывают три разных клиентских экосистемы (damus/iOS, coracle/web, nostr.band/search-first). Достаточно redundancy без избытка network chatter. | 2026-07-18 |

<a id="D-D-TRANSLATION__open_"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-TRANSLATION (open) | Провайдер on-the-fly перевода Nostr-описаний для мультиязычного UI | Варианты: **Claude Haiku** (~$0.0002/call, стабильно), **DeepL Free API** (500k символов/мес, бесплатно), **локальный NLLB** (0 стоимость, но 3+ GB модель). Кэш в Redis TTL 30 дней по `(event_id, target_lang)`. Уточнить при подходе к T3.5 | TBD (Фаза 3.5) |

<a id="D-D-DOCS-EXPOSURE"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-DOCS-EXPOSURE | ✅ **Реализовано T_SEC.1.** Prod: `EXPOSE_DOCS=false` → `/docs`, `/redoc`, `/openapi.json` возвращают 404 (FastAPI фабрика конструируется с `docs_url=None` при флаге false). Nginx security headers: HSTS 1y, X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy минимальный, CSP baseline. Probe-deny: `.env`, `.git`, `.svn`, `.php/aspx/jsp/cgi`, `wp-admin/wp-login/wp-content/xmlrpc.php`, `.ht*` → 404. Rate-limit zone `auth_zone` (10r/min IP) на /auth/login+/auth/register с burst=5 nodelay. `server_tokens off`. | Swagger UI = живая карта API + модели данных = разведка для атакующего. Полное отключение проще auth-протокола за ним, dev не страдает. | 2026-07-17 → 2026-07-18 |

<a id="D-D-STATIC-FRONTEND"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-STATIC-FRONTEND | ✅ **Реализовано T_SEC.2 (2026-07-19).** Frontend больше не запускает `npm run dev` в prod: multi-stage Dockerfile (`node:22-alpine` builds → `nginx:1.27-alpine` serves `dist/`). Внутренний nginx контейнера обрабатывает SPA fallback + immutable cache. CSP `script-src 'self'` (без `unsafe-inline`/`unsafe-eval`). Побочный эффект: React StrictMode double-effect в prod физически невозможен, backfilled invite idempotency остаётся как defense-in-depth. | Vite dev-server = HMR websocket, source код в контейнере, `eval` для module bootstrap, StrictMode double-fire ловил реальные race conditions в проде. Static bundle решает всё разом + сжимает CSP. | 2026-07-19 |

<a id="D-D-BENTO-BREAKPOINT"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-BENTO-BREAKPOINT | ✅ **Реализовано T_UX.1.** Bento 2 колонки на desktop/tablet, 1 колонка на phone **даже landscape**. Хук `useBentoLayout()` в `frontend/src/hooks/` + `<BentoGrid>` контейнер в `components/`. Правила: phone = `width < 768 OR (height < 500 AND any-pointer: coarse)`, tablet = 768-1023, desktop = 1024+. 5 vitest тестов, iPhone 14 Pro Max landscape (932×430 coarse) → phone. Миграция существующих мест — follow-up. | iPhone Pro Max landscape = 932px попадает в `md:` (768+), Tailwind width-only даёт 2 колонки — противоречит UX-намерению (пользователь развернул для большего размера, не для больше колонок). | 2026-07-17 → 2026-07-18 |

<a id="D-D-AGENTIC-INTERFACE"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-AGENTIC-INTERFACE | Два канала параллельно: (A) Nostr publish trip-events (kind 30402) в наш strfry — читается любым Nostr-агентом. (B) MCP-server (`mcp` SDK от Anthropic) как отдельный docker-контейнер — публикует tools `list_trips`, `get_trip_details`, `search_trips` для Claude Desktop / Claude Code / других MCP-совместимых клиентов. | Nostr = долгосрочная децентрализованная позиция (совпадает с Nostr-slope). MCP = быстрый вход для AI-агентов сегодня. REST + OpenAPI избыточно — дублирует и добавляет attack surface. Реализация — T_AGENT.1. | 2026-07-17 |

<a id="D-D-COMPLIANCE-STANCE"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-COMPLIANCE-STANCE | **Платформа не блокирует направления.** Corridor-периметр как жёсткий фильтр (idea из ранних версий T4.1) удалён. Вместо: `RouteNote` показывает статус коридора (standard / attention / complex / restricted) с i18n-текстом; sender/carrier сами решают. `PlatformNotice` — глобальные плашки. Person-level sanctions (OFAC/EU SDN) остаются в T2.1 (peer verification container) и T4.1 (KYC-провайдер) — это про людей в санкционных списках, не про направления. ToS явно дисклеймит: «Vimana = инфраструктура, не курьер, не цензор; пользователь несёт риск за свой выбор». Реализация — cross-cutting `T_UX.2`. | Блокировка коридоров смещает Vimana в юр. позицию посредника-цензора → потеря децентрализованной идентичности + противоречит Nostr-slope. Информирование сохраняет свободу пользователя + разгружает платформу от роли арбитра geopolitics. Legal exposure ограничивается ясным ToS + аудит-логом RouteNote impressions. | 2026-07-18 |

<a id="D-D-DVLT-PROTOCOL"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-DVLT-PROTOCOL | **DealVault = Verifiable Vault Protocol, Фаза 3.6 (T3.7–T3.10).** (1) Цепь T3.6 расширяется на сообщения/файлы/identity-события через payload обычных `DealEvent` — preimage-формат не меняется. (2) Хешируются **хранимые байты** (`sha256(ciphertext+nonce)`) — верификация без расшифровки, E2E сохраняется; следствие: ротация `MESSAGE_ENCRYPTION_KEY` только envelope-схемой (перешифровка ключа, не данных). (3) Закрытие сделки запечатывает vault (`sealed` событие + запрет append). (4) Identity-пересечение: канонический документ в `IdentityContainer`, **полная копия в сделке** (Attachment `identity_doc`) + `identity_ref` в цепи по общему `doc_hash` — решение владельца, vault самодостаточен. (5) Публикация якорей остаётся выключенной; расширяемость — колонка `deal_chain_anchors.backend` (`nostr`\|`ipfs`\|`ots`); для proof-of-time целевой второй бэкенд — OpenTimestamps (IPFS = content-addressing, не timestamp). (6) ⚠️ **частично исполнено, запись обновлена 2026-08-02.** Изначально: «Query API, .dvlt+Reader — отложены осознанно». С тех пор `T3.21` сделал формат `.dvlt` и запечатывание Identity Vault, `T3.24` — автономный читатель. Отложенными остались: **выгрузка сделки** в `.dvlt` (формат общий, механики экспорта нет) и **Query API**. Пункт (5) тоже сдвинулся: публикация якорей включена `T3.20` 2026-08-01, работает на четырёх сторонних релеях; расширяемость через `deal_chain_anchors.backend` в силе, OpenTimestamps остаётся целевым вторым бэкендом. | Концепция «DealVault Concept v0.1» (владелец, 2026-07-25): vault = переносимый иммутабельный криптографически проверяемый артефакт; полнота истории — обязательна, публикация — расширение. Копия документа в сделке выбрана владельцем вместо hash-only-референса ради самодостаточности vault'а. | 2026-07-25 |

<a id="D-D-SEAL-SEMANTICS"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-SEAL-SEMANTICS | **Seal замораживает контент, не audit-trail (T3.7).** `Deal.sealed_at` ставится при `confirm` (после события `sealed`); guard в `append_deal_event` (под advisory lock) отклоняет всё, кроме: (1) `dispute_opened` — спор после `closed` возможен и **распечатывает** vault (`sealed_at=NULL`); закрывающий вердикт арбитра запечатывает снова (второе `sealed`-событие); (2) `arbiter_opened` — audit-события проходят через seal, иначе пришлось бы либо терять аудит доступа арбитра к sealed vault, либо блокировать чтение. System-message в чат при чтении sealed vault не пишется (контент заморожен). Цепь фиксирует и seal, и unseal — исключения ничего не скрывают. | Проблемы обнаруживаются после подтверждения получения (продуктовая реальность); в коде нет запрета спора по closed-сделке. Мгновенный неснимаемый seal сломал бы арбитраж — evidence нельзя было бы приложить. | 2026-07-25 |

<a id="D-D-REVOCATION-IS-BEST-EFFORT"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-REVOCATION-IS-BEST-EFFORT | **Отзыв JWT — best-effort, а не гарантия (2026-07-29).** `core/token_blacklist.is_blacklisted` работает **fail-soft**: при недоступности Redis возвращает «не отозван» и пишет warning. Прямое следствие, которое надо называть вслух: **пока Redis лежит, logout не отзывает токены — украденный работает до истечения своих 30 дней.** Выбор в пользу доступности: fail-closed означал бы, что падение Redis выкидывает всех пользователей разом, а на платформе есть сделки в пути. **Открыто к T3.15:** для чувствительных операций (экспорт nsec, отвязка устройства, declare-lost) fail-soft неуместен — «Redis недоступен, поэтому считаем токен живым» здесь звучит неверно. Разумная середина: обычные запросы fail-soft, step-up fail-closed. Принять осознанно в T3.15, а не унаследовать. Сопутствующее: компромисс был **невидим** — из-за loop-бага blacklist под тестами вообще не работал, а тесты про logout проходили (починено 2026-07-27). | Доступность важнее для потока обычных запросов: массовая блокировка входа наносит больше вреда, чем окно, в котором отозванный токен ещё живёт. Но применять то же правило к операциям, которые нельзя отменить, — значит распространять компромисс туда, где его никто не принимал. | 2026-07-29 |

<a id="D-D-EVIDENCE-DECAYS"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-EVIDENCE-DECAYS | **Все доказательства устаревают; свежее сильнее старого (2026-07-27).** Верификация вчера — сильная, месяц назад — нормальная, пять лет — под вопросом. Следствия: (1) ни одно доказательство не показывается без своей даты — голый бейдж «проверен» утверждает больше, чем факт; (2) возраст входит множителем в УБА рядом с `V_verify_factor`; (3) рёбра Trust Graph взвешиваются по свежести в BFS; (4) шкала затухания фиксируется в PRD, а не расползается по коду. Данные в основном уже есть (`VerificationBadge.verified_at`/`expires_at`, `TrustEdge.created_at`, `DealEvent.timestamp`) — не хватало не полей, а их использования. **Шкала зафиксирована 2026-08-02** (`T_TRUST.1`), пять точек прямо из формулировки владельца: **≤ 7 дней → 1.00** (неделя — это ещё «вчера», поэтому кривая начинается плоской), **30 дней → 0.95**, **год → 0.70**, **5 лет → 0.40**, дальше — пол. Между точками линейно: числа здесь — продуктовое суждение о том, как читается дата, а не измеренный распад чего-либо, и экспонента выдавала бы это суждение за физику. **Пол 0.40, никогда не ноль:** проверка, которая была, — была; затухание в ноль утверждало бы, что встречи не происходило, и делало бы старый бейдж неотличимым от поддельного. Стареет вес, а не факт. В УБА множитель гасит **бонус** (`1.0 + (factor − 1.0) × freshness`), а не человека: пятилетнее доказательство делает обычным, но никогда не хуже того, кто не проверялся вовсе — иначе верификация становилась бы обязательством, которого выгодно избегать. Недатированное доказательство получает пол, а не полную силу: возраст неизвестен, и считать неизвестное сегодняшним значит дать наибольший вес тому, о чём знаем меньше всего. Единственный источник шкалы в коде — `core/freshness.py`. Реализация — `T_TRUST.1`. | Доверие в физической логистике опирается на недавнее поведение, а не на факт когда-то пройденной проверки. Бинарное «проверен/не проверен» позволяет годами предъявлять давно неактуальное доказательство как текущее. | 2026-07-27 |

<a id="D-D-RETIRED-IS-ARCHIVE"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-RETIRED-IS-ARCHIVE | **Завершённая личность — исторический документ, а не мёртвый аккаунт (2026-07-27).** Утрата ключа отнимает способность действовать и не трогает достоверность прошлых действий — у нас эти две вещи впервые независимы. Отсюда: аккаунт не удаляется, а меняет жанр; UI переключается в режим чтения архива, а не деградирует в урезанный профиль; поручительства завершённой личности **остаются в силе** (подпись была сделана живым ключом и проверяется до сих пор). Слово **Archive**, не Cemetery. Окно 15 дней от `key_lost_at`: **бездействие → экспонат становится видимым**, выбор «Нет» закрывает страницу навсегда. Асимметрия намеренная — ошибочное «Нет» ведёт к приватности, ошибочное бездействие к публичности, но с окном на исправление. **Ничего не удаляется ни в одном случае**: закрывается витрина (страница, `display_name`, агрегаты), остаются цепь, подписи и события сделок — они наполовину принадлежат контрагенту (ENVIRONMENT §8.2). Слово «кремация» в UI не использовать: обещает уничтожение, которого нет. **Архив вплетён в сеть, а не стоит отдельным зданием:** к завершённой личности приходят прогулкой по рёбрам `peer_verified` от живых участников; `dealt_with` и `invited` публично не обходятся (клиентская база и социальный граф). Скрытая личность остаётся в графе обезличенным узлом, и её поручительства продолжают считаться — скрыть можно себя, но не свой вклад в чужую репутацию. Экспонаты архива **несвязанные** — преемственности личностей нет по решению №4, «династии» были бы его отменой. Реализация — `T3.18`/`T3.19`. | Продукт строится на том, что информация не исчезает. Логическое завершение жизненного цикла — не смерть записи, а переход из режима «участник сети» в режим «историческая запись». Это продолжение философии DealVault, а не дополнительная функция. | 2026-07-27 |

<a id="D-D-KEY-IS-IDENTITY____частично_отменено__"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-KEY-IS-IDENTITY ⚠️ частично отменено `D-KEY-TIERS` (2026-08-01) | **⚠️ Читать вместе с `D-KEY-TIERS`.** Пункт (2) ниже — «переход всегда даёт новый ключ» (решение №6) — **отменён**: суверенность определяется отсутствием копии у платформы, а не происхождением ключа, и `import`/`export` возвращены с обязательным доказательством владения. Пункт (4) — «смена или потеря ключа = новая личность» (решение №4) — **смягчён**: утрата требует двух потерь сразу (нет копии у платформы и нет файла с фразой). Остальное — служебный ключ как механика шифрования, envelope-перешифровка, отказ от сущности DID — в силе. **Служебный ключ ≠ личность; личность создаётся при переходе и всегда новым ключом (Фаза 3.7, T3.11–T3.17).** (1) Механика T2.2 сохраняется: keypair выдаётся при регистрации, платформа хранит nsec, им шифруются контейнеры и подписываются записи — но это **служебный ключ сейфа**, а не личность. Пользователю он не показывается, наружу не публикуется. Отдельной колонки не нужно: `key_self_custody = false` означает «служебный», `true` — «личность». (2) `POST /me/keypair/claim` **удаляется**: он повышал служебный ключ до личности удалением серверной копии nsec, но ключ, приватная часть которого всю жизнь лежала у платформы, суверенным быть не может — отсутствие копии недоказуемо. Переход (`POST /me/identity/establish`) всегда даёт **новый** ключ: сгенерированный в браузере (`@noble/curves`, сервер nsec не видит) либо принесённый из NIP-07. Для сервера ветки неразличимы — он требует только доказательство владения заявленным npub. `import` и `export` тоже удаляются: первый принимал голый npub без проверок (присвоение чужой личности), второй выдавал служебный ключ, который пользователю не принадлежит. (3) Рейсы до перехода публикуются **только** под отдельным `PLATFORM_PUBLISH_NSEC` и **только отобранные фильтром «интересных»** (редкие направления, длинные хопы). Служебным ключом не публикуем: событие в сторонних relay'ях живёт вечно, а служебный npub при переходе уничтожается — остались бы события ничьей личности. (4) Смена и потеря ключа = **новая личность**: история, УБА и trust не переезжают; `key_lost_at` — терминальное состояние (read-only, публичный чип, обратного перехода нет). Recovery-код возвращает доступ, но никогда ключ. (5) Перешифровка сейфов при переходе — **envelope-схемой** (session-ключи переупаковываются NIP-44 на новый npub, байты контента не меняются) → цепь T3.6 и `verify_content` целы; `IdentityContainer` перешифровывается целиком, его `doc_hash` считается по plaintext и переживает смену шифра. (6) Сущность DID **не вводится** — `nostr_pubkey` и есть идентификатор. | Философия проекта ближе к Nostr и Bitcoin: «ключ и есть личность» (решение владельца, 2026-07-26). Вариант «ключа нет до перехода» отвергнут как дорогой: он требовал переписать шифрование `IdentityContainer`, ветку безключевого участника в `dealvault.py`, поведение threshold и весь механизм `D-RECIPIENT-ROLE` — риск на работающем коде ради семантики, которую даёт переименование. Отказ от «неизменной личности при смене ключа» принят сознательно как цена self-sovereign identity: восстановление личности силами платформы означало бы, что источник идентичности — платформа, а не пользователь. | 2026-07-26 |

<a id="D-D-KEY-TIERS"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-KEY-TIERS | **Владение личностью — лестница из четырёх ступеней, а не булев флаг (решение владельца 2026-08-01).** Модель описывается вокруг **личности**, а не вокруг того, где лежит ключ; тогда «где копия» становится настройкой, а не судьбой. Ступени: **(0) только аккаунт** — пользовательского ключа нет, подписывает серверный ключ платформы; обычный пользователь проходит её мгновенно, реальным состоянием она остаётся для **приглашённых получателей**, которые не регистрировались. **(1) Nostr-личность есть** — пользовательский ключ создан при регистрации, хранится у платформы, она подписывает и расшифровывает от имени пользователя; копии у пользователя нет. **(2) Копия у вас** — пользователь скачал **Identity Vault** (`.dvlt`): ключ запечатан парольной фразой по NIP-49 **в браузере**, фраза на сервер не уходит никогда; ключ есть и у платформы, и у пользователя, платформа по-прежнему может всё. **(3) Только ваш** — копия на платформе удалена; подпись уходит в браузер (NIP-07 либо ключ из файла), платформа хранит сделки, но открыть их не может. Движение вверх повышает независимость, движение вниз возвращает удобство: загруженный Identity Vault восстанавливает копию на платформе. **Быстрый путь:** свою личность можно привязать сразу после регистрации — ключ из NIP-07 мы не видим вовсе (сразу ступень 3), вставленный `nsec`/`ncryptsec` распечатывается в браузере, и пользователь **явно** выбирает, оставлять ли копию платформе (по умолчанию не оставлять). **Отменяет решение №6** (`D-KEY-IS-IDENTITY`): переход к самовладению больше не требует нового ключа — суверенность определяется не происхождением ключа (недоказуемым в обе стороны), а проверяемым фактом «копии у платформы нет». **Смягчает решение №4**: личность утрачивается только при **двух** потерях сразу — нет копии у платформы и нет файла с фразой; `key_lost_at` остаётся терминальным состоянием, но становится редким. Следствия для реализации: булев `key_self_custody` недостаточен — состояние равно набору существующих копий ключа; `import`/`export` возвращаются, но `import` обязан требовать доказательство владения (`core/identity_proof.py`); перешифровка сейфов нужна только при импорте **чужого** ключа, в основном сценарии ключ не меняется. Парольная фраза — **не пароль от аккаунта**: она нужна только для работы с файлом, появляется в момент первого скачивания и необязательна до него. Платформа её не хранит и не проверяет; забытая фраза не теряет ничего, пока жива копия на платформе — файл перескачивается с новой. **Identity Vault — второй тип внутри уже заведённого `.dvlt`** (`D-DVLT-PROTOCOL`), не второй формат: один файл может нести личность и её сделки, один ридер открывает оба. Термин «эскроу» не использовать — он обещает выдачу третьей стороной по условию, чего здесь нет. | Прежняя модель была бинарной: либо служебный ключ целиком у платформы, либо полный self-custody без страховки, и между ними обрыв. Отсюда одновременно избыточность (десять recovery-кодов возвращали только доступ — меньшую задачу большим механизмом) и пробел (после перехода резервной копии не существовало вовсе: «скачайте файл, второй раз не покажем»). Лестница закрывает обе: удобство остаётся на нижних ступенях, независимость достижима без потери истории, а каждое утверждение в UI («пока копия у нас — мы можем подписывать; удалите её — не сможем») проверяемо, в отличие от «мы не имеем доступа». | 2026-08-01 |

<a id="D-D-MAIL-VIA-MAILU"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-MAIL-VIA-MAILU | **Исходящая почта — только через собственный Mailu (2026-07-29, доведено до работы 2026-07-30).** Приложение подключается к `mail.dealvault.club:465` (implicit TLS) под ящиком `vimana@dealvault.club`; прямое подключение к SES из backend'а отвергнуто. **Порт 465, а не 587:** в этой установке Mailu nginx внутри `front` слушает 25/465/993/995, но не 587 — Docker порт публикует, обслуживать некому, снаружи это выглядит как `Connection refused` при живом `LISTEN 0.0.0.0:587` в `ss` (заглушка `docker-proxy`). Чинить 587 не стали: по RFC 8314 implicit TLS предпочтителен, так что 465 — не обходной путь. SES остаётся дальше по цепочке — Mailu релеит через него, там же Easy DKIM для `dealvault.club` (проверено 2026-07-29: SPF PASS, DKIM PASS с доменом `dealvault.club`). Следствия: (1) отдельный `SMTP_FROM` не нужен — логин и есть видимый отправитель, `From = SMTP_USER` корректен; правка, вводившая `SMTP_FROM` / `SMTP_FROM_NAME` / `SMTP_REPLY_TO`, откачена как решение под прямой SES. (2) Доставка кодов подтверждения завязана на доступность почтового инстанса: пока Mailu лежит, письма не уходят — принято осознанно, подтверждение почты ничего не гейтит (T3.11). (3) Нужен сетевой доступ с инстанса Vimana на порт 465 почтового сервера — отдельный инстанс, не localhost. (4) Ответы приходят на живой ящик, а не в никуда, потому что отправитель — настоящий mailbox в Mailu. (5) **DMARC опирается на оба механизма (с 2026-07-30).** Изначально SES подставлял свой обратный адрес, SPF проходил для `amazonses.com` и с `From` не совпадал — всё держалось на DKIM. Настроен custom MAIL FROM `bounce.dealvault.club` (MX на `feedback-smtp.us-east-1.amazonses.com` + SPF-TXT), `Behavior on MX failure` = использовать домен по умолчанию, чтобы сбой DNS не рубил отправку. Теперь `Return-Path` на своём домене и SPF выровнен. Совпадение нестрогое, поддомен засчитывается — в DMARC-записи нет `aspf=s`, и добавлять его нельзя, иначе выравнивание сломается. (6) `Message-ID` наш до SES и переписывается им на `@email.amazonses.com`; сохранить свой можно только отправкой напрямую (порт 25 наружу, Elastic IP, PTR, своя репутация) — обмен признан невыгодным. | Решение владельца: не заводить в приложении прямых интеграций с сервисами пересылки. Один набор учётных данных и одна точка исходящей политики вместо двух; отправка в обход собственного сервера размывает контроль над тем, что уходит от имени домена, и разводит репутацию по двум путям. Цена — общая судьба с почтовым инстансом — принята. | 2026-07-29 |

<a id="D-D-VAULT-NIP44"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-VAULT-NIP44 | **Конверты сейфа — NIP-44 v2, NIP-04 снят целиком (2026-08-02, `T_KEYS.1`).** NIP-04 помечен deprecated в самой спецификации Nostr, и по той причине, которая здесь и важна: AES-256-CBC с сырой x-координатой ECDH и **без MAC**. Сохранённый конверт можно было изменить, и читатель не имел способа это заметить — неправильный примитив под сессионными ключами в хранилище, вся идея которого «изменение обнаружимо». NIP-44 v2 закрывает ровно это: HKDF поверх ECDH вместо сырой координаты, ChaCha20 вместо AES-CBC, HMAC-SHA256 над `nonce\|\|ct` (аутентификация, которой не было), паддинг по расписанию — длина сообщения перестаёт выдавать его содержание, а «да» и «нет, и вот почему» перестают различаться по размеру шифротекста. **Миграция единовременная, ветки совместимости нет.** Замер на проде 2026-08-02: ноль E2E-сообщений, ноль контейнеров личности, следовательно ноль существующих конвертов — перешифровывать нечего. Окно закрылось бы с первым реальным сейфом: часть ключей платформе недоступна по построению (верхняя ступень `D-KEY-TIERS`), и единовременный вариант стал бы невозможен не по трудоёмкости, а в принципе. **Фронт по-прежнему делегирует расширению**, только `window.nostr.nip44` вместо `nip04`: приватный ключ пользователя не попадает на страницу, и своя реализация втащила бы его в наш код. Расширениям без `nip44` — явный отказ, а не откат: молча записать неаутентифицированный конверт из-за старого браузера значило бы вернуть ровно ту дыру, которую задача закрывает. | Хранилище, которое обещает обнаружимость изменений, не может держать ключи в формате без аутентификации. Отдельный аргумент за срочность: окно дешёвой замены существует только пока нет живых данных, и оно закрывается само, без предупреждения. | 2026-08-02 |

<a id="D-D-SHAMIR-DEP"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-SHAMIR-DEP | **`shamir-secret-sharing@0.0.3` остаётся, выбор наконец записан (2026-08-02, `T_KEYS.1`).** Библиотека реализует разделение секрета над GF(256) и держит на себе схему 2-of-3 (`T2.3`), а решение о ней нигде не фиксировалось: ни кто сопровождает, ни почему не своя реализация. Записываем сейчас: **своя не пишется** — ручной Шамир над конечным полем это ровно тот код, который выглядит правильным и течёт (постоянное время, корректность обратных элементов, обработка вырожденных долей), а property-тесты roundtrip (`test_props_threshold.py`) проверяют математику и ничего не говорят о побочных каналах. Версия зафиксирована точно, **но признаём честно: это про читаемость, а не про безопасность** — записанное в постановке опасение «диапазонный пин» для `0.0.x` неверно, npm-каретка при нулевой мажорной и минорной означает ровно одну версию (`^0.0.3` := `0.0.3`), так что диапазона здесь никогда и не было. Настоящий пробел был не в пине, а в отсутствии записи. Что остаётся риском: pre-1.0 и один сопровождающий, то есть цепочка поставки, а не алгоритм; при смене версии — читать диф, а не доверять номеру. | Примитив, на котором держится доступ арбитра к сейфу, не может быть выбран «по умолчанию из npm» без записанной причины. При этом подмена его собственной реализацией — обмен проверяемого стороннего кода на непроверенный свой в самом чувствительном месте. | 2026-08-02 |

<a id="D-D-RECIPIENT-ROLE"></a>

| # | Решение | Причина | Дата |
|---|---|---|---|
| D-RECIPIENT-ROLE | Recipient — приглашённый sender'ом участник чата с собственным **невидимым custodial keypair** (генерится при регистрации через `generate_keypair` + `encrypt_nsec`, UI не показывает). Threshold 2-of-3 {sender, carrier, arbiter} **не расширяется**; расширяется только `read_packages` map — `recipient_<uuid>` NIP-44-конверт с session_key. Server-mediated decrypt endpoint (`/decrypt-for-me`) даёт plaintext recipient'у через custodial nsec — сервер видит plaintext на мс, не хранит. Sender/carrier с self-custody продолжают расшифровывать client-side через NIP-07 (для них E2E полный). | Разделять sender's nsec с recipient'ом ломает изоляцию: recipient получит доступ ко ВСЕМ прошлым чатам sender'а + impersonation в подписях + невозможность revoke. Отдельный custodial keypair — чистая изоляция per-deal, revocable через `DealParticipant.revoked_at`. Server-mediated read — честный документированный компромисс для сути роли (recipient = "читатель по приглашению", уже доверяет платформе). | 2026-07-18 |


## §3 Обнаруженная логика — полные строки

<a id="L001"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Один адрес — два экрана, и почему это не таб (T_UX.23, 2026-08-25).** `pages/ModeHomePage.tsx` решает по `authState`: гостю — `CarrierLandingPage` / `SenderLandingPage`, вошедшему — `<Layout><DashboardPage/></Layout>`. Отсюда проп `children` у `Layout`: лендинг несёт собственные шапку и подвал, поэтому оболочка приложения не может быть родительским маршрутом. **Направление зависимости выбрано осознанно: адрес следует за режимом.** Несовпадение даёт редирект на адрес, отвечающий `active_mode`, а не переключение режима — иначе любая внешняя ссылка писала бы в поле, от которого зависит вся панель и обе навигации. Единственная точка смены режима — `ModeSwitcher`, и он же переносит адрес; без переноса `ModeHomePage` вернул бы пользователя обратно, и кнопка выглядела бы сломанной. Проверяется и `can_carry`, а не только режим: комбинация «режим carrier при снятом флаге» достижима через админку. Пререндер: `entry-ssr` экспортирует `PRERENDER_PATHS`, сборщик читает список оттуда — страницу нельзя добавить в роутер и забыть в сборке; `nginx.conf` отдаёт каждый адрес точным `location =`, поэтому `/carrier` пререндерен, а `/carriers/<id>` продолжает падать в SPA-правило. Файлы: `pages/ModeHomePage.tsx`, `components/landing/{LandingShell,AudienceLanding,FlowStrip}.tsx`, `pages/{Carrier,Sender,Business}LandingPage.tsx`, `entry-ssr.tsx`, `scripts/prerender.mjs`, `nginx.conf` |

<a id="L002"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Один способ правки на все параметры профиля (T_UX.22, 2026-08-23).** `components/StandingNoteSection.tsx` — значение на экране, «Добавить» при пустом и «Изменить» при заполненном, редактор по нажатию, «Отменить» / «Сохранить». Принимает **список** полей, потому что две настройки бывают одной карточкой для читателя и двумя колонками в схеме. `descKey` объявлен **обязательным**, и это несущая деталь, а не строгость типов: правило `DESIGNGUIDELINES §9b` требует строку описания под каждым заголовком, а необязательное поле остаётся незаполненным тихо. Образец, к которому всё приведено, — `AddressesSection`. Файлы: `components/StandingNoteSection.tsx`, `components/DisplayPrefsSection.tsx`, `components/{Connections,Invites}Section.tsx`, `pages/Profile{Rules,Trust,History}Page.tsx` |

<a id="L003"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Стоячие заметки перевозчика: три текста, два поведения (T_UX.21, 2026-08-23).** `users.carriage_rules` **копируется** в `trips.carriage_rules` при публикации (T_UX.15) — правило, изменённое в марте, не должно переписывать то, о чём договорились в феврале. `users.interaction_rules` и `users.payment_instructions` (миграция `0053`) **не копируются никуда**: они описывают человека, а не отправление, и текущее время ответа обязано читаться как текущее. Отсюда же разная видимость: все три owner-only через `MeOut`, и попадание любого в `UserOut` опубликовало бы платёжные данные на странице личности — против этого стоит отдельный schema-тест, потому что ошибка выглядела бы правильно на всех экранах. Каталог платёжных методов сознательно не заводился: платформа денег не проводит (Фазы 4 и 5 не начаты), и таблица под несуществующий механизм — это схема, которую потом нельзя не переделать. Файлы: `models/user.py`, `schemas/user.py`, `alembic/versions/0053_*.py`, `tests/test_carrier_notes.py`, `components/StandingNoteSection.tsx`, `pages/ProfileRulesPage.tsx` |

<a id="L004"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Разделы профиля — вложенные маршруты, и это условие, а не стиль (T_UX.20, 2026-08-23).** Оболочка `pages/ProfileLayout.tsx` рендерит шапку, панель и `<Outlet/>`; разделы объявлены массивом `SECTIONS` там же, роль фильтрует пункт администрирования, `data-testid` живут на ссылках панели. Вкладками это сделать нельзя: `/profile/keys` (T_UX.6) — цель баннера «осталось 2 кода», письма об использованном коде и ридера, `/profile/notifications` (T3.32) — цель писем «изменить, что вам приходит». Оба адреса сохранены как вложенные пути, а не переизобретены. **Панель выбирается `useBentoLayout()`, а не классом `md:`** — по той же причине, ради которой хук написан (T_UX.1): телефон в landscape шириной 932px попадает в `md:` и получил бы боковую панель на экране, который развернули ради большего размера. Побочный эффект важнее повода: в DOM один `<nav>`, а не два скрытых по CSS, то есть нет ни дубля лендмарка, ни `data-testid`, совпадающего дважды. Заголовок `h1` — название раздела, «Профиль» над ним надстрочником: карточки во всех разделах начинаются с `h2`, и обратный порядок дал бы два уровня заголовка над одним и тем же. Файлы: `pages/ProfileLayout.tsx`, `pages/Profile{,Activity,Trust,Prefs,Admin,Keys}Page.tsx`, `pages/NotificationsPage.tsx`, `components/{Connections,Invites,PublicPage}Section.tsx`, маршруты в `App.tsx`, ключи `profile.nav.*` в шести локалях |

<a id="L005"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Живость и готовность — разные вопросы, и одним эндпоинтом их не задать (T_OPS.1, 2026-08-11).** `/health` спрашивает «жив ли процесс» — супервизор перезапускает, когда нет. `/ready` спрашивает «давать ли ему трафик» — балансировщик выводит из ротации, **ничего не перезапуская**. Процесс, который выключается, находится ровно в том состоянии, которое одним эндпоинтом невыразимо: жив и трафика получать не должен. По SIGTERM `core/readiness` переводит `/ready` в 503 и держит паузу `DRAIN_SECONDS`, продолжая обслуживать, и только потом передаёт сигнал штатному обработчику uvicorn — цепочкой, а не заменой, потому что переписать выключение значило бы владеть куском uvicorn, который меняется между версиями. `stop_grace_period` в compose обязан превышать паузу плюс `--timeout-graceful-shutdown`, иначе SIGKILL приходит посреди корректного завершения. **Сегодня маршрутизацию это не меняет** — бэкенд один и проверок здоровья перед ним нет; польза пока только от паузы, а `/ready` это половина zero-downtime, которая должна существовать до появления балансировщика. Ничего облачно-специфичного: HTTP-эндпоинт и переменная окружения читаются nginx, HAProxy, Traefik и любым управляемым балансировщиком | `backend/app/core/readiness.py`, `backend/app/main.py`, `docker-compose.dev.yml`, `nginx/default.conf`, `backend/tests/test_readiness.py` |

<a id="L006"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Telegram — единственный канал, который нельзя попросить первым (T3.27, 2026-08-11).** Бот не может написать тому, кто ему не писал, поэтому обмен идёт задом наперёд: сначала ссылка с nonce, потом `/start`, и только тогда появляется код. Отсюда три следствия в схеме, а не в логике. `verification_challenges.code_hash` стал **nullable** — строка обязана существовать с момента выдачи ссылки, иначе webhook не отличит наш nonce от строки, которую придумал посторонний; но кода в ней ещё нет, а хеш неотправленного — значение, которое выглядит кодом и им не является. `resolved_value` хранит chat_id, узнанный на `/start` и прочитанный на `otp/verify`: сайт этот идентификатор не видит никогда. `resolved_label` хранит имя из Telegram, потому что заглушкой имени у почтового аккаунта служит локальная часть адреса, а у chat_id такой части нет. Окно кода отсчитывается **от отправки**, а не от выдачи ссылки: открывший ссылку через десять минут должен получить полный срок на то, что ему только что вручили | `backend/app/api/telegram.py`, `backend/app/api/auth.py`, `backend/app/core/contact_verification.py`, `backend/app/models/contact.py`, `backend/alembic/versions/0048_telegram_login_exchange.py` |

<a id="L007"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Подключение Telegram из профиля стало способом входа (T3.27, 2026-08-11).** Расширение, названное вслух: контакт получает `is_login=True`, и владелец чата может войти им, даже если подключал канал ради оповещений. Альтернатива хуже — вход не узнавал бы такой чат и выдавал бы человеку **второй аккаунт вместо своего**, что для маркетплейса с репутацией означает потерю истории. Правило: один чат — один аккаунт, и строка в БД говорит то, что есть, вместо `is_login=false` при разрешении имени, которое его игнорирует | `backend/app/api/telegram.py`, `backend/app/api/auth.py` |

<a id="L008"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Пустой ключ в настройках — это дефолт, а не «выключено» (T3.32, 2026-08-11).** `notification_prefs` хранится **частично**: в JSONB лежит только то, что человек трогал. Полностью материализованный словарь пришлось бы мигрировать при каждом новом классе события, и миграция была бы вынуждена угадывать, чего хотел каждый существующий аккаунт. При чтении дырка = дефолт класса, поэтому новый класс приезжает включённым в разумное значение вообще без миграции. Наружу при этом уходит **полная** матрица (`resolved` в `me_out_with_avatar`): экран, которому пришлось бы знать дефолты, чтобы дорисовать наполовину пустой объект, — это вторая копия правил, написанная на TypeScript и свободно расходящаяся с первой. Обратная сторона того же решения — сама миграция `0047` частичной быть не может: она **выписывает** старые три булевых по классам, потому что записать всем `{}` значило бы тихо включить уведомления обратно каждому, кто их выключал | `backend/app/core/notification_prefs.py`, `backend/app/core/avatar_url.py`, `backend/alembic/versions/0047_notification_prefs.py` |

<a id="L009"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Список того, что показывать, живёт на сервере в одном экземпляре (T3.32, 2026-08-11).** Строки матрицы — только классы с `emitted=True`, то есть те, которые в коде кто-то действительно отправляет; `vault`, `trust` и `dispute` объявлены, но не показываются, пока у них нет отправителя. Колонки — **все три канала всегда** (решение владельца 2026-08-11): прежнее правило прятало канал до `channels.enabled`, и это оставляло человека в неведении, существует ли WhatsApp здесь вообще. Тревога «колонка для трубы, которой нет» снимается иначе — `connected_channels(user)` возвращает те же три атрибута, которые `_notify_user` проверяет перед отправкой, колонка без адреса подписана «не подключён», а её чекбоксы сняты и заблокированы. **Сохранённое `true` без адреса — это желание, а не доставка, и галочка не должна утверждать обратное.** Оба списка приезжают во фронт в ответе `/me` вместе со значениями, и `NotificationMatrix.tsx` не содержит собственного перечня — он рисует то, что пришло. Правило одной фразой: **переключатель для сообщения, которое никогда не приходит, — это обещание, которого продукт не держит**, и держать такой список в двух местах значит однажды его нарушить | `backend/app/core/notification_prefs.py`, `frontend/src/components/NotificationMatrix.tsx`, `frontend/src/components/ChannelsSection.tsx` |

<a id="L010"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **«Безопасность нельзя выключить» стало свойством класса (T3.32, 2026-08-11).** До этого правило действовало де-факто: семь функций (`send_verification_code`, `send_recovery_code_used`, `send_password_reset`, `send_password_changed`, `send_new_device`, `send_platform_copy_deleted`, `send_archive_window_opened`) намеренно не смотрели на `notify_email` — но нигде не было записано, и восьмая унаследовала бы это только по внимательности автора. Теперь `wants()` возвращает `True` для `mandatory`-класса раньше, чем заглянет в хранилище, а `sanitize()` молча выбрасывает попытку записать туда значение. Причина не в аккуратности: с `T3.28` почтовый ящик в одиночку открывает аккаунт, и «в аккаунт вошли по коду восстановления» — не подписка, а единственный сигнал, по которому владелец узнаёт, что аккаунт больше не только его | `backend/app/core/notification_prefs.py`, `backend/app/tasks/notifications.py` |

<a id="L011"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **`X-Forwarded-For` читался слева, то есть со стороны вызывающего (T_SEC.6, 2026-08-11).** nginx ставит `$proxy_add_x_forwarded_for` — он **дописывает** свой `$remote_addr` справа к тому, что прислал клиент. Значит левый элемент это строка вызывающего, а не адрес. `_key_func` брал именно его: запрос с `X-Forwarded-For: <случайное>` попадал в собственную корзину, и **каждый лимит в продукте отменялся одним заголовком** — пять кодов в час, десять попыток Nostr в минуту. Спасало только то, что зоны `limit_req` в nginx ключуются на `$binary_remote_addr` и обмануть их так нельзя. Правый элемент — единственный, который не мог написать вызывающий. Отдельно названо в `core/client_ip.py`: если перед nginx появится CDN, правило станет неверным в другую сторону (правым будет edge провайдера), и это правка одной строки — но найти её надо до того, как счётчики снова начнут врать | `backend/app/core/client_ip.py`, `backend/app/core/rate_limit.py`, `backend/tests/test_sign_ins.py` |

<a id="L012"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Отпечаток устройства должен быть грубее, чем данные, из которых он сделан (T_SEC.6, 2026-08-11).** Хеш берётся от «семейство браузера + семейство ОС + сеть /24», а не от `User-Agent` и адреса. Причина в обе стороны: сырой агент меняется при каждом обновлении браузера, и владелец получал бы письмо после каждого апдейта Chrome; полный адрес меняется, когда телефон переходит между вышками, и человеку писали бы про него самого. Цена названа вслух, а не спрятана: сосед по /24 с тем же браузером и ОС письма не поднимет. Обратная сделка — письмо раз в неделю — не покупает ничего, потому что заканчивается папкой «прочитанные не глядя». Из тех же соображений в БД лежит только сеть и хеш, срок хранения 90 дней | `backend/app/core/sign_ins.py`, `backend/app/models/sign_in.py`, `backend/app/tasks/cleanup.py` |

<a id="L013"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Одно событие — одно письмо (T_SEC.6, 2026-08-11).** Пути входа делятся на два вида: те, где письмо о новом устройстве единственный сигнал (пароль, код, passkey, Nostr), и те, где о событии уже сказано иначе. `recovery/consume` шлёт своё письмо «использован код восстановления» в ту же секунду; второе письмо рядом с ним — ровно та ловушка №1, ради которой задача и оговаривала «не писать про каждый вход». Поэтому устройство там **запоминается без письма**: это же гасит дубль на следующем обычном входе с той машины. Создание аккаунта (кодом, passkey, Nostr) — тот же случай по другой причине: первому устройству не с чем быть несхожим. `sign_ins.record` и `sign_ins.announce` — это и есть два вида, разнесённые в две функции, чтобы выбор был виден в месте вызова | `backend/app/core/sign_ins.py`, `backend/app/api/auth.py`, `backend/app/api/passkey.py`, `backend/app/api/nostr_auth.py` |

<a id="L014"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Считать запросы и считать адресатов — разные вещи (T3.29, 2026-08-11).** Всё, что было (slowapi, зоны nginx, 60-секундный cooldown в `contact_verification`), считает **запросы**. Десять запросов в час — нормальный бюджет человека, промахнувшегося адресом; те же десять по десяти чужим ящикам — наша форма в роли рассыльщика, и ни один существующий лимит этого не видит. Отсюда два счётчика: по ящику (не обходится сменой источника) и по числу **разных** ящиков с одного источника. Второй намеренно свободный: мобильные операторы держат за одним адресом целый город (CGNAT), и жёсткий порог отрезал бы тот самый коридор, ради которого продукт существует. Пороги живут в коде рядом с объяснением цифры, а не в `.env`: значение, которое можно поменять молча, меняют молча | `backend/app/core/code_limits.py`, `backend/app/api/auth.py`, `backend/tests/test_code_limits.py` |

<a id="L015"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Контраст приглушённого текста — осознанное отступление от AA (T_TEST.8, 2026-08-03, решение владельца после отката).** Шкала `text-navy/{20…60}` лежит под 4.5:1 на светлых подложках (2.57 / 3.43 / 1.52), `text-cyan` как текст — 2.43. Замена на сплошные токены уровня AA была сделана (59 файлов) и **откачена в тот же день**: вместе с низким контрастом ушла градация, на которой держится вёрстка — три степени приглушения схлопывались в одну, и лендинг переставал выглядеть спроектированным. Вывод, который стоит помнить дольше конкретных хексов: **тест нашёл настоящее расхождение с обещанием, но чем платить за обещание — решает не тест.** `color-contrast` и `link-in-text-block` отключены в `axe-rules.json` с записанной причиной, признание вынесено в DESIGNGUIDELINES §6. Остальная часть a11y-сьюта включена и обязательна: имена контролов, подписи полей, структура списков — там нет вкуса, там баги | `frontend/e2e/axe-rules.json`, `PRD/DESIGNGUIDELINES.md` §6 |

<a id="L016"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Деплой мог быть верным и невидимым (2026-08-04).** Внутренний nginx фронта отдавал ассеты как `immutable` на год, а на HTML не слал `Cache-Control` **вообще** — браузеры в этом случае кэшируют эвристически по `Last-Modified`. Документ несёт хеши ассетов, поэтому его устаревшая копия прикалывает браузер к **предыдущей** сборке целиком: на сервере уже новая версия, на экране старая, и выглядит это как «правка не приехала». Обнаружено ровно так — палитра была откачена и выкачена, а владелец видел прежнее. `no-cache` на обе HTML-точки входа; это не «не хранить», а «хранить и переспрашивать по ETag», неизменённая страница по-прежнему отвечает 304 | `frontend/nginx.conf` |

<a id="L017"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Референс жил вне репозитория, поэтому пропажу было не с чем сверить (T_UX.7, восстановлено 2026-08-04, принято владельцем 2026-08-06).** Владелец заметил, что посадочный талон на лендинге обеднел. История показала, что **из приложения ничего не удаляли**: вырубка, дуга с летящим самолётом и штрихкод существовали только в отдельном HTML (`~/Downloads/Output/peerflew-offer/peerflew-investor-offer.html`, 9 июня, **вне git**), а React-лендинг их не имел никогда — до T_UX.7 стоял бенто-вариант из `landing.html`. В записи pt.2 названы два сознательных непереноса (счётчик участников, «tamper-proof»), про детали талона решения нет нигде. То есть это не выбор, а пропуск, и незаметен он был ровно потому, что источник лежит там, куда никто не смотрит при ревью диффа. Общее правило: **если правка делается «по референсу», референс должен лежать в репозитории** — иначе следующее сравнение снова случится через два месяца и по памяти | `frontend/src/pages/LandingPage.tsx`, `~/Downloads/Output/peerflew-offer/` (вне репо) |

<a id="L018"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Компонент, объявленный внутри компонента, монтируется заново на каждый ререндер (2026-08-04).** `FlightArc` держит `useEffect` с `requestAnimationFrame`; будь он объявлен внутри `LandingPage`, как соседний `Label`, его тип менялся бы при каждом рендере, React размонтировал бы и монтировал его заново, и самолёт стартовал бы с начала дуги при каждом открытии модалки вейтлиста. Для `Label` это безвредно и потому не бросается в глаза — цена появляется ровно тогда, когда у вложенного компонента заводится состояние или эффект | `frontend/src/pages/LandingPage.tsx` |

<a id="L019"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Анимация не решает, что за элемент в документе (T_TEST.8, 2026-08-03).** `<Reveal>` рендерил `<div>` между `<ol>` и `<li>` на лендинге: список переставал объявлять длину, а каждый пункт — быть пунктом (7 нарушений `listitem`, 2 `list`). Появился проп `as`; классы не тронуты, картинка идентична. Обёртка ради движения — украшение, и структура документа не должна быть её побочным эффектом | `frontend/src/components/Reveal.tsx`, `frontend/src/pages/LandingPage.tsx` |

<a id="L020"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Рукописный тип фронта — утверждение о чужом коде, и его не проверяет никто (T_TEST.12, 2026-08-23).** `Connection` объявлял `{ id, display_name, is_carrier, connected_at }` против настоящего `ConnectionOut` = `{ id, connected_user_id, connected_user, created_at }`. Совпадал один `id`. `conn.display_name[0]` — индексация `undefined`, она бросает, и `/profile` уходил в ErrorBoundary **целиком, у любого аккаунта хотя бы с одним контактом**. `?.` стоял после индексации: защищал результат, а не объект, — почему `""[0]` безопасно, а `undefined[0]` нет, и почему строка выглядела аккуратной. TypeScript тут бессилен по устройству. Отдельно поучительно, что на `is_carrier` висел комментарий «backend now returns `connected_user.active_mode`»: расхождение **заметили один раз, на одном поле**, и тип оставили описывать старую форму — частичная правка задокументировала дрейф вместо того, чтобы его устранить | `frontend/src/api/social.ts`, `frontend/src/pages/ProfilePage.tsx` |

<a id="L021"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Одноразовый тестовый аккаунт не может найти баг накопленного состояния (T_TEST.12, 2026-08-23).** Падение `/profile` выше ждало первого контакта — а аккаунт, живущий одну минуту, контактов не имеет по определению, и пустой список рисует заглушку, не доходя до строки. Это не «тесты были слабые»: конструкция **структурно** исключала целый класс дефектов, и e2e выглядел зелёным ровно потому, что каждый раз приходил на чистое место. У живого пользователя состояние копится всегда. Обратная сторона — порядок спек стал значимым: `golden-path` меняет режим аккаунта, `invite-flow` заводит связь, passkey добавляет ключи, и всё это видит следующий тест. Отсюда `clearPasskeys` перед passkey-тестами и требование «первый аккаунт остаётся обычным пользователем» из-за `admin-guard` | `frontend/e2e/helpers.ts`, `frontend/e2e/README.md` |

<a id="L022"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Тесты быстрее людей, и это не повод двигать лимит (T_TEST.12, 2026-08-23).** Два passkey-теста подряд тратят около одиннадцати запросов к `/api/auth/passkey/` за семь секунд; `passkey_zone` в nginx даёт 30 r/m со всплеском 10, человек делает четыре за минуту. Ждёт тест, а не ослабевает зона: лимит стоит против перебора учёток. Прежняя версия укладывалась в бюджет не потому, что была бережной, а потому что тратила меньше запросов. Диагностический признак на будущее: 429 пришёл **HTML-страницей от nginx**, а не JSON от `slowapi` — по формату ответа сразу видно, какой из двух слоёв отказал | `nginx/default.conf`, `frontend/e2e/specs/passkey.spec.ts` |

<a id="L023"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Тест, молчащий о статусе ответа, врёт о причине (T_TEST.12, 2026-08-23).** `passkey` падал как «expected 1, received 0» — и ровно так же выглядел бы упавший экран, потому что локатор внутри отсутствующего контейнера тоже даёт ноль. Соседний тест в том же файле статус проверял, этот нет. Одна строка `if (!resp.ok()) throw` превратила «регистрация не сработала, ищите сами» в `HTTP 429 — nginx`. Общее правило: если тест ждёт ответ через `waitForResponse`, он обязан этот ответ и проверить, иначе следующий ассерт описывает последствие, а не причину | `frontend/e2e/specs/passkey.spec.ts` |

<a id="L024"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Незамоканный запрос в jsdom-тесте — шум, который учит не смотреть в stderr (T_TEST.12, 2026-08-23).** `LoginPage` зовёт `contactChannels('')` на монтировании; `LoginPage.test.tsx` рендерил страницу шесть раз без единого мока, и каждый рендер выпускал живой XHR в никуда — шесть `AggregateError` за прогон. Тесты были зелёными, потому что в компоненте стоит `.catch`: они всё это время проверяли поведение страницы **при недоступном сервере** и не знали об этом. Мок возвращает пустой список — то же, что давал упавший запрос, поэтому смысл ассертов не сдвинулся; `['telegram']` дорисовал бы кнопку и переписал бы то, на что смотрят четыре теста. Тот же механизм, по которому браузерный e2e простоял мёртвым двенадцать дней: «сломано» и «всегда так шумит» выглядят одинаково | `frontend/src/test/LoginPage.test.tsx` |

<a id="L025"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Round-trip слеп к симметричным мутациям (T_TEST.10, 2026-08-23).** Шифрование проверялось только сходимостью «зашифровали → расшифровали». Мутация, сдвигающая границу между ChaCha-ключом и MAC-ключом, сдвигает её **одинаково на обеих сторонах**: сообщение возвращается, тест зелёный, а payload перестаёт читаться любым чужим NIP-44 клиентом. Сходимость проверяет, что реализация согласна сама с собой, — а совместимость это утверждение о согласии с чужой. Лечится оракулом: тест пересчитывает алгоритм по спецификации вместо сравнения функции с собой. `_nip44_message_keys` — 10 выживших → 2. Остаток (срез x-координаты в `nip04_shared_x`) не убивается ничем, кроме официальных векторов → `T_TEST.13` | `backend/tests/test_threshold_encryption.py`, `backend/app/core/threshold.py` |

<a id="L026"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Эшелонированная защита делает внешнюю проверку ненаблюдаемой (T_TEST.10, 2026-08-23).** Двенадцать выживших в `nip44_decrypt` — это подмены порога длины, `validate=False` у base64 и маркера версии. Все они выживают потому, что испорченный payload **всё равно не проходит MAC**: ответ остаётся 422, меняется только формулировка. Это не слабость сьюта, а свойство архитектуры — пока цела внутренняя проверка, внешняя не влияет на наблюдаемое поведение. Убить такого мутанта можно только тестом на текст сообщения, то есть ухудшив сьют ради метрики. **Следствие для чтения отчётов: низкий kill-rate на защищённом коде — это не диагноз** | `backend/app/core/threshold.py` |

<a id="L027"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Сьют полагался на то, что прогон и процесс — одно и то же (T_TEST.10, 2026-08-23).** Допущение ничего не стоило, пока каждый запуск был новым процессом. mutmut гоняет pytest много раз в одном интерпретаторе — и три места вылезли подряд, ещё до первого мутанта. Строки в общей таблице параметров, которые никто не убирает и которые **дозревают по календарю** (прошлая версия въезжает в окно запроса через четыре дня). Блок IP-адресов лимитера, выбранный один раз при импорте, — второй прогон стартует с потраченным бюджетом. И `os.environ`, испорченный `pop`'ом: **`pop` — это не возврат**, переменная из продового `.env` контейнера после него исчезает навсегда. Общее правило: **всё, что обязано отличаться между прогонами, не может вычисляться при импорте** — включая значения по умолчанию в сигнатурах функций | `backend/tests/conftest.py`, `test_platform_params.py`, `test_code_limits.py`, `test_telegram_login.py` |

<a id="L028"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **mutmut не приписывает ни одному тесту код, исполняемый на импорте (T_TEST.10, 2026-08-23).** Отбор тестов идёт по статистике покрытия, которую собирают **во время тестов**. Фабрика зависимостей `require_perm` вызывается FastAPI при построении маршрутов — то есть до первого теста, — поэтому получила пустой набор, и три её мутанта отчитались выжившими. Руками их убивают 15 тестов. Тот же класс: декораторы, код уровня модуля, всё вычисляемое при импорте. Ошибка **односторонняя — выдумывает выживших, но не прячет их**, поэтому прогон это нижняя оценка kill-rate. Отдельно: **нумерация мутантов между прогонами не сквозная**, читать выживших только по дифам `mutmut show` | `backend/scripts/verify_survivors.py`, `backend/setup.cfg` |

<a id="L029"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **У правила `label` в axe есть запасная проверка на непустой `placeholder` (T_TEST.8, 2026-08-23).** Три подписи полей на доске рейсов не были связаны с контролами вовсе, но axe ругнулся только на дату: у полей аэропортов стоят `placeholder="DXB"` и `"JFK"`, и правило зачло их за имя. Плейсхолдер именем не является — он исчезает при первом введённом символе, ровно когда подсказка нужнее всего. **Следствие: зелёный axe не означает, что подписи связаны**; проверять глазами наличие `htmlFor`, а не только вывод сканера | `frontend/src/pages/TripsPage.tsx`, `frontend/src/components/AirportSelect.tsx` |

<a id="L030"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Тесту нужна сессия, а не регистрация (T_TEST.8, 2026-08-23).** Блокер «браузерный e2e починить нечем, упирается в решение владельца» простоял двенадцать дней и был неверно поставленным вопросом: он исходил из того, что тест обязан **завести** аккаунт, и упирался в размен «безопасность против проверяемости». Аккаунт можно завести один раз и жить с ним — `signInFixed` логинится по паролю из `e2e/.env.local`, `E2E_KEEP_EMAILS` выводит аккаунт из-под ночной чистки. Пароль ставит **сам продукт**, через step-up с кодом на почту: `contact_code` заведён в T3.28 ровно для аккаунтов, созданных кодом, — то есть ответ лежал внутри того же изменения, которое и сломало `registerUser` | `frontend/e2e/helpers.ts`, `backend/app/tasks/cleanup.py`, `backend/app/core/step_up.py` |

<a id="L031"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Проверка прав идёт раньше проверки формы (T_TEST.7 pt.2, 2026-08-03).** `reveal-my-share` сначала смотрел, e2e ли сообщение, и только потом — участник ли зовущий. Посторонний с чужим `message_id` получал 400 «Message is not e2e-encrypted»: подтверждение существования строки и её тип, выданные человеку без прав на неё. Ключей не утекало, но эндпоинт **описывал чужие записи**. Порядок переставлен. Цена нулевая — проверке участия нужны ровно те же две загрузки из БД. Почему дожило до матрицы: существующий негативный тест писал e2e-сообщение и получал корректный 403, то есть проверял правильный случай, а не тот, что ломался | `backend/app/api/threshold.py`, `backend/tests/test_idor_matrix.py` |

<a id="L032"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Таблица ожиданий сверяется с роутером, а не пишется руками (T_TEST.7 pt.2, 2026-08-03).** Матрица IDOR обходит `app.routes`, отбирает пути с `{`, и сравнивает множество с таблицей **в обе стороны**: незаписанный эндпоинт — дыра, ради которой файл существует; запись о несуществующем — способ для матрицы тихо перестать описывать приложение. Тела запросов лежат в самой таблице, потому что без них FastAPI отвечает 422 **до** обработчика, и отсутствующая проверка прав прошла бы тест, ни разу не выполнившись. Вердикта три, а не один: `PUBLIC` и `CAPABILITY` — это решения, а не отсутствие решения, и каждое несёт написанную причину | `backend/tests/test_idor_matrix.py` |

<a id="L033"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Порог сканера живёт в коде, а не в конфиге сканера (T_TEST.7 pt.1, 2026-08-03).** ZAP умеет валить прогон сам — через действия в `rules.tsv` и код возврата. Не используем: порог, размазанный между TSV, флагом `-I` и `if` в shell, нельзя прочитать целиком. `zap-baseline.py` запускается с `-I`, решение принимает `app/cli/zap_report.py`: любой High — провал, Medium считаются и печатаются. Там же единственное неочевидное умолчание — **нечитаемый `riskcode` считается High**: отчёт, который не разобрался, не доказывает отсутствия находок, а обратное умолчание даёт молчаливый зелёный прогон | `.zap/baseline.sh`, `.zap/rules.tsv`, `backend/app/cli/zap_report.py`, `backend/tests/test_zap_report.py` |

<a id="L034"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Одна система токенов, потому что четырёх не бывает (T_UX.7, 2026-08-02).** Бренд существовал в четырёх несовпадающих экземплярах: токены приложения, собственный CSS лендинга на 576 строк со своей палитрой, и два статических v5 (у одного засечный дисплей при бессечном тексте — первый по частоте признак ИИ-вёрстки). Человек, пришедший с лендинга в продукт, попадал в другой продукт. Сведено к `tailwind.config.js`: цвет, два радиуса по роли (`card` для контейнеров, `field` для контролов — а не четыре значения вразнобой), тени с примесью navy вместо чистого чёрного, именованный `z-index`. `danger` стал токеном вместо восьми оттенков сырого красного в 24 местах: у сырого хекса нет приложенного правила, и следующий человек не знает, каким должен быть следующий красный. Фокус-кольцо одно на весь продукт через `:where(...):focus-visible` с нулевой специфичностью — было 48 фокус-стилей на 73 перехода, то есть большая часть реагирующего на мышь была невидима клавиатуре, а чинить по компонентам значило тронуть шестьдесят файлов и пропустить шестьдесят первый | `frontend/tailwind.config.js`, `frontend/src/index.css`, 52 файла в `src/` |

<a id="L035"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Порядок наложения сведён частично и намеренно (T_UX.7 pt.3, 2026-08-02).** Модалки и подложка переведены на именованную шкалу; выпадающие списки с `z-10`/`z-20` оставлены как есть. Причина конкретная: сейчас список внутри формы стоит выше липкой шапки, а шкала подняла бы шапку до `nav: 100` и список ушёл бы под неё. Стек-контекст проверяется только глазами, и слепая массовая замена порядка наложения — типовой способ сломать выпадающий список так, что тесты этого не увидят. Записано как незакрытое, а не выдано за сделанное | `frontend/src/components/{AirportSelect,CategorySelect,CountryCodeSelect,LanguageSwitcher}.tsx` |

<a id="L036"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Язык интерфейса не должен попадать в цепь (T_UX.7 pt.3, 2026-08-02).** `DealPage` слал `addEvent(dealId, 'handoff', 'Груз передан перевозчику')`; `note` уходит в `payload` события и **хешируется в `entry_hash`**, то есть локаль отправителя становилась частью доказательства, которое читает контрагент на другом языке. Строка снята: `event_type: 'handoff'` несёт ровно тот же смысл, и каждая сторона рендерит его у себя. Общее правило: в цепь кладётся то, что произошло, а не то, как это подписано в чьём-то браузере | `frontend/src/pages/DealPage.tsx`, `backend/app/api/deals.py` |

<a id="L037"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Путь-параметр проверяется до базы, а не после (T_KEYS.1, 2026-08-02).** `GET /api/identities/{npub}` отдавал `npub` в запрос как есть. NUL-байт в пути доходил до asyncpg, который отвергает его на уровне протокола (`invalid byte sequence for encoding "UTF8"`), и **публичный** URL отвечал 500 вместо ответа. Нашёл контрактный фаззер (`test_contract_fuzz.py`), не ревью и не ручная проверка — ровно то, ради чего он заведён. Форма ключа известна точно (64 hex), всё остальное строкой совпасть не может, поэтому проверка стоит перед запросом. Код ответа — **404, а не 422**: эндпоинт на всё, о чём не говорит, отвечает «такой личности нет», и отдельный код для «неправильный формат» был бы второй формой ответа на публичном URL без выигрыша | `backend/app/api/trust.py`, `backend/tests/test_trust_graph.py` |

<a id="L038"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Дата обязательна на уровне типа, а не соглашения (T_TRUST.1, 2026-08-02).** У `VerificationBadgeChip` проп `at` **required**. Правило «никогда „проверен“ без „когда“» держится тем, что вопрос задаёт компилятор: сделай проп необязательным — и забыть дату станет путём наименьшего сопротивления, а голый бейдж вернётся по одному месту за раз. `null` принимается и рисуется как «дата неизвестна» — читается хуже даты, и правильно, потому что это хуже. Тем же приёмом закрыта публичная страница личности: она больше не рисует уровень своей вёрсткой, а зовёт общий чип | `frontend/src/components/VerificationBadgeChip.tsx`, `frontend/src/pages/IdentityPage.tsx`, `frontend/src/components/VerificationSection.tsx`, `frontend/src/test/VerificationBadgeChip.test.tsx` |

<a id="L039"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Затухание гасит бонус, а не человека (T_TRUST.1, 2026-08-02).** В УБА множитель свежести применяется к превышению над единицей: `1.0 + (factor − 1.0) × freshness`. Наивный вариант — умножить сам `V_verify_factor` — превратил бы пятилетний KYC в `1.30 × 0.4 = 0.52`, то есть старое доказательство стало бы **штрафом** относительно того, кто не проверялся вовсе, и дало бы прямую выгоду не верифицироваться. Пол шкалы 0.40, не ноль: проверка, которая была, — была, стареет её вес, а не факт. Недатированное доказательство получает пол, а не полную силу — иначе случай, о котором мы знаем меньше всего, весил бы больше всех. Шкала живёт в одном файле, `core/freshness.py`, и её пять точек закреплены тестом: сдвиг «года» с 0.70 на 0.50 — другое обещание пользователям, и он обязан падать здесь, а не тихо переранжировать всех на следующем деплое | `backend/app/core/freshness.py`, `backend/app/core/uba.py`, `backend/tests/test_freshness.py`, `backend/tests/test_uba.py` |

<a id="L040"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **`expires_at` писался и не читался (T_TRUST.1, 2026-08-02).** Peer-верификация ставит `expires_at = +365 дней`, и это же делает эскалированный путь — а `refresh_highest_level` и подсчёт активных бейджей смотрели только на `revoked_at`. Бейдж, истёкший два года назад, держал уровень на аккаунте вечно и считался активным. Дата истечения, которую система пишет и затем игнорирует, хуже её отсутствия: это обещание на профиле, за которым ничего не стоит. Сам бейдж из списка не исчезает — он был выдан, и запись об этом не удаляется; меняется только то, что он больше не считается. Остаточный долг: уровень пересчитывается на INSERT/revoke, а не по таймеру, поэтому может оставаться просроченным до следующего такого события | `backend/app/core/verification.py`, `backend/app/api/verification.py`, `backend/tests/test_verification.py` |

<a id="L041"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **`refresh_allowed_pubkeys` запускать только в `celery-worker` (2026-08-01).** Том `nostr_whitelist` смонтирован в `celery-worker` и `nostr-relay`, но **не** в `backend`. Ручной прогон задачи из backend-контейнера отрабатывает успешно и возвращает честный `{'path': '/data/allowed_pubkeys.txt', 'count': N}` — записав файл в собственную ФС контейнера, которую релей никогда не читает. Симптом при этом выглядит как проблема логики: strfry продолжает отвечать `blocked: pubkey not in Vimana WoT` на ключ, который «только что добавили». Проверка результата — не по выводу задачи, а из релея: `exec nostr-relay wc -l /data/allowed_pubkeys.txt` | `docker-compose.dev.yml` (volumes), `backend/app/tasks/nostr_whitelist.py` |

<a id="L042"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Первый живой тик якорения вскрыл три отдельные поломки (T3.20, 2026-08-01).** Все четыре релея вернули `false`, и до этого дня одинаковый `false` означал что угодно: `_publish_one` логировал **только исключения**, а явный отказ по NIP-20 (`["OK", id, false, "<причина>"]`) возвращался молча — то есть «релей отказал и объяснил почему» и «мы до него не дошли» были неразличимы в аудите. После добавления логов диагнозы разошлись мгновенно: **(1)** свой strfry — `error: internal error`, потому что `nostr/write_policy.py` лежал в git с режимом `100644`; strfry запускает плагин процессом, без exec-бита он падает на каждом событии, и WoT-gate не блокировал, а ломался — на всём подряд, а не только на якорях. **(2)** ключ якоря не попадал в whitelist: в T3.5 pt.2 туда добавили ключ публикации платформы, а ключ якоря по решению T3.6 намеренно **другой**, поэтому одно не покрыло другое. **(3)** список сторонних релеев не содержал ни одного, принимающего запись от незнакомого ключа: `nostr.wine` платный и отвечает `restricted: sign up`, `relay.nostr.band` — индексатор, таймаут на публикацию, `relay.damus.io` — HTTP 503 на рукопожатии с этого хоста. Сеть при этом исправна: два релея ответили **по протоколу**, то есть исходящие WSS работают. Вывод, который стоит помнить: якорь стоит ровно столько, сколько сторонних релеев его держат, и список, где отказывают все, — это отсутствие якорения с видом настроенного | `backend/app/core/nostr_publish.py`, `backend/app/tasks/nostr_whitelist.py`, `backend/app/core/chain_anchor.py`, `nostr/write_policy.py` (режим), `.env.example` |

<a id="L043"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Якорь стоит ровно столько, сколько сторонних релеев его держат (T3.20, 2026-08-01).** `GET /deals/{id}/chain` отдаёт `anchor_relays` — **только принявшие**: `relays` хранит ответ каждого опрошенного, и показать отказавший значило бы отправить аудитора искать событие, которого там нет. Отдельно `anchor_third_party_relays` — то же без нашего strfry: мы им владеем, и голова, дошедшая только туда, не доказывает о нас ничего, то есть доказательная масса это именно второй список. Третье поле `unanchored_entries` — сколько записей выше последнего якоря: они покрыты только нашей же проверкой целостности, и без этого числа `ok: true` читалось бы как «всё зафиксировано снаружи». При отсутствии якоря значение равно всей длине цепи, а не нулю: ноль прочитался бы как «непокрытого нет». В `ArchiveRecord` — `last_anchor_at` и `anchored_deals`, из которых собирается единственная сильная фраза, разрешённая продукту: «проверяемо независимо по состоянию на <дата>», и она **не рисуется вовсе**, пока якорей нет | `backend/app/api/{deals,trust}.py`, `frontend/src/components/ArchiveRecordCard.tsx`, `backend/tests/test_deal_chain.py`, `frontend/src/test/ArchiveRecordCard.test.tsx` |

<a id="L044"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Якорение включается тремя значениями, а не одним (T3.20, 2026-08-01).** `NOSTR_PUBLISH_ENABLED=true`, `CHAIN_ANCHOR_NSEC` и хотя бы один сторонний адрес в `NOSTR_FRIENDLY_RELAYS` — при любом одном отсутствующем тик молча возвращает `skipped`, и продукт не имеет права ничего утверждать о независимой проверке. `CHAIN_ANCHOR_NSEC` до сих пор существовал **только в комментарии** `.env.example`: код его читал, а объявления переменной не было, то есть узнать о необходимости задать её было неоткуда. Ключ обязан отличаться от `PLATFORM_PUBLISH_NSEC` и не быть ничьим пользовательским — якорь это утверждение платформы о своих же записях, и подписант, который вдобавок подписывает события сделок, делает неразличимым, кто из двоих говорит. `.env` читают три сервиса (`backend`, `celery-worker`, `celery-beat`), тик исполняет воркер — значит `up -d --force-recreate`, `restart` оставил бы старое значение | `.env.example`, `PRD/ENVIRONMENT.md` §5, `backend/app/core/chain_anchor.py`, `backend/app/worker.py` |

<a id="L045"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Окно архива: решение хранится, срок вычисляется (T3.19, 2026-08-01).** Две колонки — «человеку сказали» (`archive_notice_seen_at`) и «человек ответил» (`archive_choice`), — а дата окончания окна **выводится** из `key_lost_at + 15 дней`. Хранить срок третьей колонкой значило бы завести вторую истину о дате, которая уже есть: модалка, письмо и проверка эндпоинта разошлись бы при первой правке любой из них. Решение намеренно **не выводится из `public_profile`**, хотя итог у них один: «скрыт при живом ключе» — обратимая настройка, «витрина закрыта» — окончательный ответ, и склеив их, мы сделали бы необратимым выбор, который человек делал как обратимый, ни разу его не спросив. Асимметрия реализована буквально: `'show'` перезаписывается сколько угодно, `'hide'` даёт 409 навсегда, и оба возможны только внутри окна — модалка обещает дату, после которой ответ фиксируется, а API, продолжающий принимать правки, сделал бы это обещание ложью. Проверка стоит **в `visible_to`**, а не в эндпоинте страницы: следующий публичный срез унаследует правило, ничего не зная про архив. Прямое следствие — `PATCH /auth/me` с `public_profile` у закрытого архива отвечает 409: настройка, которая записывается, но ни на что не влияет, врёт | `backend/app/core/permissions.py`, `backend/app/api/{keypair,auth,trust}.py`, `backend/app/models/user.py`, миграция `0039_archive_notice`, `backend/tests/test_identity_establish.py` |

<a id="L046"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Архивные агрегаты: только счётное, и всегда со знаменателем (T3.19, 2026-08-01).** `_archive_record` считает двумя запросами и не оценивает ничего. Каждый итог отдаётся вместе с множеством, из которого посчитан (`deals_closed/deals_total`, `routes_measured/routes_closed`, `signatures/chain_entries`) — голое «12 закрыто» приглашает читателя подставить знаменатель самому. **Подписью считается только запись с `nostr_sig`**: запись цепи без неё — реальное событие, но не доказательство. Расстояние названо `straight_line_km` в самом имени поля, чтобы подпись на экране нельзя было написать иначе: `route_distance_km` даёт дугу большого круга, реальный трек длиннее на 3–7%, и меряется маршрут посылки, а не налёт самолёта. Вместимость суммируется **по различным рейсам** — две посылки на одном борту не удваивают борт — и называется заявленной, потому что ничего не взвешивалось. Неизмеримое отсутствует, а не равно нулю. Запрет «verified forever» держится тестом (`forever`, `tamper-proof`, `guaranteed`), а не бдительностью: цепь tamper-evident, и верная формулировка появится с якорями T3.20 | `backend/app/api/trust.py`, `frontend/src/components/ArchiveRecordCard.tsx`, `frontend/src/test/ArchiveRecordCard.test.tsx` |

<a id="L047"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Модалка один раз, баннер всегда (T3.19, 2026-08-01).** Первый вход после утраты ключа — единственный момент, когда объяснение является новостью; постоянный диалог над необратимым состоянием превращается в нытьё и закрывается не читая, то есть проигрывает ровно на тех, кому был нужен. Баннер, наоборот, постоянен: состояние не меняется обратно, и спрятать его значило бы дать аккаунту тихо перестать говорить, что он такое. Признак «показывали» лежит на сервере, а не в localStorage: решение принимается один раз на аккаунт, а не один раз на браузер. **Закрытие диалога не является ответом** — `notice-seen` и `choice` разные эндпоинты, и согласие, собранное с кнопки «закрыть», не согласие. Компонент не делает ни одного запроса на живом ключе: `MeOut.key_lost` отвечает на единственный вопрос, решающий, нужен ли он вообще. Карточка архива одна и та же в профиле и на публичной странице — числа совпадают по построению, а не потому, что две реализации сошлись | `frontend/src/components/{ArchiveNotice,Layout}.tsx`, `frontend/src/pages/{ProfilePage,IdentityPage,DashboardPage}.tsx`, `backend/app/schemas/user.py`, `frontend/src/test/ArchiveNotice.test.tsx` |

<a id="L048"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Тестовая БД не под alembic (T3.19, 2026-08-01).** `vimana_test` строится `Base.metadata.create_all`, поэтому таблицы `alembic_version` в ней нет, и `alembic upgrade head` с подменённым `DATABASE_URL` начинает с 0001 и падает на `relation "users" already exists`. Правило ENVIRONMENT §8 «миграции применяются подменой URL» описывает не то, что происходит. Рабочий приём — идемпотентное зеркало ALTER в conftest (`_ensure_identity_columns` и соседи), куда добавляется каждая новая колонка. `alembic stamp head` тестовой базе **не давать**: он объявит применёнными 39 миграций, ни одна из которых там не выполнялась, и первое же расхождение схемы всплывёт загадочным падением теста вместо честной ошибки миграции | `backend/tests/conftest.py`, `PRD/ENVIRONMENT.md` §8 |

<a id="L049"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Периметр контейнеров (T_SEC.3, 2026-08-01).** Наружу публикуется только nginx (80/443) и relay (7777, профиль `nostr`); `db`, `redis`, `backend` слушают `127.0.0.1`. До этого `backend` висел на `0.0.0.0:8000` **и порт был открыт в security group**: прямое обращение обходило TLS, security-заголовки T_SEC.1, probe-deny и обе зоны `limit_req`, причём ключ slowapi берётся из `X-Forwarded-For` (`core/rate_limit.py`), то есть per-IP лимит снимался подстановкой заголовка. Комментарий в compose утверждал обратное («не опубликован на хосте») — обоснование разошлось с конфигом и жило так до ревизии. Хостовый файрвол здесь не помощник: Docker пишет DNAT-правила **до** ufw. Redis дополнительно закрыт `--requirepass` — в нём гранты step-up, челленджи, blacklist и **брокер Celery**, а запись в брокер это выполнение кода на воркере. Пароль healthcheck'у идёт через `REDISCLI_AUTH`, не аргументом `-a` (аргумент виден в `ps`). Подстановка `${...}` внутри `.env` не работает: `env_file` отдаёт значения буквально, пароль вписывается в `REDIS_URL` руками | `docker-compose.dev.yml`, `.env.example`, `PRD/ENVIRONMENT.md` §5.1 |

<a id="L050"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Восстановление живёт на экране входа (T3.16 pt.2, 2026-08-01).** Отдельная страница `/recover` не заведена намеренно: человек, потерявший устройство, уже стоит на странице логина, и отправлять его искать другой адрес — худшее, что можно сделать в этот момент. Режим переключается там же, поле идентификатора при этом меняет тип с `email` на `text` (может прийти npub, и браузер иначе откажется отправлять форму). Ошибка одна на все случаи — сервер намеренно не различает неверный код и несуществующий аккаунт, и UI не имеет права это различие вернуть. Коды показываются в `SecuritySection` рядом с почтой и паролем, потому что это способы **входа**; ключ — то, чем аккаунт является, — живёт в своей секции (то же разделение, что в T3.15). Предупреждение при `N ≤ 2` стоит внутри блока, а не глобальной плашкой: оно должно быть там, где его чинят одной кнопкой. Текст остатка меняется со ступенью `D-KEY-TIERS` — «вернёт доступ и переписку», пока копия ключа у платформы, и только «доступ» после её удаления | `frontend/src/components/SecuritySection.tsx`, `frontend/src/pages/LoginPage.tsx`, `frontend/src/api/auth.ts`, `frontend/src/i18n/locales/*.json`, `backend/app/tasks/notifications.py` |

<a id="L051"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Recovery-код — это step-up-доказательство, а не вторая дверь (T3.16 pt.1, 2026-08-01).** Потраченный одноразовый код, записанный пользователем на бумагу, отвечает ровно на тот вопрос, который задаёт step-up: «ты всё ещё здесь?». Поэтому `consume` не строит параллельный механизм доступа, а выдаёт **step-up-гранты** на `change_password` и `add_auth_method` вместе со scoped-токеном (`scope: recovery`, 15 минут). Ключевое устройство — **запрет по умолчанию**: `get_current_user` отвергает любой токен с claim'ом `scope`, поэтому новый скоуп не наследует доступ ко всем существующим эндпоинтам по забывчивости; две двери открываются явной зависимостью `get_recovery_or_current_user` (`PUT /me/password`, регистрация passkey). Отвязка passkey намеренно осталась вне: потеря телефона должна позволять **добавить** ключ, а не снять чужой. Хеш кода — SHA-256, не bcrypt: 59 бит энтропии от `secrets` не имеют словаря, а медленный KDF заставил бы проверять догадку против каждого неиспользованного кода по очереди — десять намеренных задержек на попытку, то есть DoS-рычаг на себя; быстрый дайджест ищется равенством одним индексным запросом (`(user_id, code_hash)`). Ответы на неверный код и на несуществующий аккаунт **идентичны** — иначе эндпоинт становится справочной «какие аккаунты есть» | `backend/app/api/{auth,deps,passkey}.py`, `backend/app/core/security.py`, `backend/app/models/user.py`, миграция `0035_recovery_codes`, `nginx/default.conf`, `backend/tests/test_step_up.py` |

<a id="L052"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Работа, сделанная один раз, а не на каждый запрос (T_PERF.1 pt.3, 2026-08-01).** Три случая одной ошибки. (1) Индекс GeoNames (8,4 МБ) строился при импорте `core/airports.py` в модульный глобал, хотя читает его только цикл `_load()` — перенесён внутрь функции и освобождается после загрузки. (2) Нижний регистр вычислялся в `search()` для ~7 000 аэропортов и их альтернативных названий **на каждое нажатие клавиши** — теперь поля `*_lower` заполняются при загрузке; `nearest()` перешёл на `heapq.nsmallest` вместо сортировки всего списка ради пяти элементов; `list_countries`/`list_cities` под `lru_cache`, потому что таблица аэропортов в рантайме не меняется. (3) `bump_publish_metric` делал read-modify-write через ORM при докстринге, обещавшем атомарный `UPDATE`: одновременные publish'и затирали инкременты друг друга. Сложение уехало в БД, а чтение — на колоночный select, иначе identity map при `expire_on_commit=False` вернула бы значения до UPDATE (та же грабля, что в `feedback_orm_cache_after_raw_sql`) | `backend/app/core/{airports,metrics}.py`, `backend/tests/{test_airports,test_nostr_pt2}.py` |

<a id="L053"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Rate-limit слоями: nginx грубо, slowapi точно (T_PERF.1 pt.3).** Счётчики slowapi жили в памяти процесса: обнулялись каждым деплоем и не были общими между процессами. Перенесены в Redis (`storage_uri` из настроек) с `swallow_errors=True` — недоступный Redis пропускает запрос, а не роняет его, что согласуется с `D-REVOCATION-IS-BEST-EFFORT` для обычного трафика (step-up остаётся fail-closed). Разделение ответственности: nginx держит грубые per-IP зоны на границе, slowapi — поэндпоинтные бюджеты (5/час на код подтверждения, 10/мин на вход по ключу), которых в nginx нет. Клиент `limits` синхронный, то есть на лимитированном эндпоинте есть один блокирующий INCR; принято сознательно — это сотые доли миллисекунды по compose-сети, а не сетевая заливка файла, и асинхронный бэкенд потребовал бы новой зависимости (`coredis`) | `backend/app/core/rate_limit.py`, `nginx/default.conf` |

<a id="L054"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Блокирующие вызовы в async-эндпоинтах (T_PERF.1 pt.2, 2026-08-01).** `async def` не делает вызов внутри неблокирующим — это самая дорогая иллюзия в асинхронном коде. Три пути (`api/{avatar,dealvault,verification}.py`) выполняли синхронный PUT в R2 и полный декод Pillow прямо на event loop: пока чьё-то фото уезжало в хранилище, ни один другой запрос на этом воркере не обрабатывался. Уведены в `run_in_threadpool` — без новых зависимостей, `aioboto3` для этого не нужен. Рядом: boto3-клиент строился на каждый вызов (`get_presigned_url` зовётся по разу на вложение), теперь `lru_cache` + `reset_client_cache()` для тестов; клиенты boto3 потокобезопасны, что и требуется, раз вызовы теперь идут из пула. Публикация в релеи — `asyncio.gather` вместо цикла: 4 релея × 5 с таймаута = до 20 с на событие, и один мёртвый релей задерживал всех за ним. **Проверка параллельности сделана барьером, а не таймингом** — фейковые публикации ждут, пока в функцию войдут все, поэтому последовательная реализация детерминированно виснет и падает по `wait_for`, а не «иногда» | `backend/app/core/{storage,nostr_publish}.py`, `backend/app/api/{avatar,dealvault,verification}.py`, `backend/tests/{test_presign_ttl,test_nostr_publish}.py` |

<a id="L055"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Индексы под названные запросы (T_PERF.1 pt.1, 2026-08-01).** До миграции `0034` схема несла семь индексов, ни одного на горячем пути: Postgres не индексирует внешние ключи сам, а `create_all`/автогенерация об этом не напоминают. Правило, по которому выбраны десять: индекс существует под конкретный запрос, и он назван в комментарии рядом. Композитные там, где фильтр по одной колонке, а сортировка по другой — `deal_vault_messages (deal_id, created_at, id)` превращает чтение чата в один упорядоченный range-scan вместо scan + sort. Обратная сторона правила: **не** индексируем то, что уже покрыто левым префиксом unique-констрейнта (`deal_events`, `deal_participants`, `trip_inquiries`, `trust_edges.from_user_id`). Индексы живут в трёх местах одновременно — миграция, `__table_args__` моделей и `_ensure_hot_path_indexes` в conftest; последнее потому, что `create_all` строит индексы только для таблиц, которые создаёт сам | `backend/alembic/versions/0034_hot_path_indexes.py`, `backend/app/models/{deal,marketplace,trust,verification}.py`, `backend/tests/conftest.py` |

<a id="L056"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Число запросов не должно зависеть от объёма данных (T_PERF.1 pt.1).** Два места нарушали это по-разному. `verify_content` делал `db.get` на каждое событие цепи — сделка с 300 сообщениями стоила 300 round-trip'ов на эндпоинте, доступном любому участнику; теперь ссылки собираются одним проходом и грузятся одним запросом на тип. `bfs_circles` ходил в БД на каждый узел обхода; теперь один запрос на уровень (`from_user_id IN (frontier)`), обход тот же. Второе важно как прецедент: записанный в T2.4 follow-up предлагал **Redis-кэш BFS** — то есть спрятать дорогой вопрос вместо того, чтобы сделать его дешёвым. После правки кэшировать нечего. Побочно: нечитаемый id в payload цепи теперь даёт mismatch, а не 500 — сообщать о порче и есть работа этой функции | `backend/app/core/{deal_chain,trust}.py`, `backend/tests/test_vault_completeness.py` |

<a id="L057"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Канонизировать на записи, а не у каждого читателя (T_PERF.1, 2026-08-01).** Переход на точное сравнение в фильтре рейсов вскрыл, что канонической формы у `trips.origin`/`.destination` не было вовсе: `POST /api/trips` сохранял строку как прислали. Пока фильтр был `ilike '%code%'`, это не проявлялось; с равенством рейс, созданный прямым POST'ом как `dxb`, стал бы невидим для любого поиска `DXB`. Вариант «приводить обе стороны в запросе» отвергнут: `upper(origin)` в WHERE отменяет индекс, ради которого всё и делалось. Нормализация ушла в запись (`strip().upper()`), читатели остались простыми. Обнаружено дважды упавшим тестом пагинации, который клал `P-<hex>` в нижнем регистре — тест оказался единственным местом, где значение приходило не из `AirportSelect` | `backend/app/api/trips.py`, `backend/tests/test_trips.py` |

<a id="L058"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Фильтры рейсов: точное сравнение вместо подстроки (T_PERF.1 pt.1).** `Trip.origin.ilike('%code%')` был неверен дважды: ведущий wildcard исключает индекс, а по смыслу `?origin=A` матчил каждый аэропорт с буквой A. В колонках лежат IATA-коды — `AirportSelect` физически не может отдать ничего другого. Запрос нормализуется в верхний регистр (набранный руками `dxb` находит `DXB`), хранимые значения приходят из пикера уже такими. Дата: полуоткрытый UTC-день вместо `cast(depart_at, Date) = :date` — функция на колонке отменяет индекс для всех строк | `backend/app/api/trips.py`, `backend/tests/test_trips.py` |

<a id="L059"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Самодостаточный файл и CSP тянут в разные стороны (T3.24, 2026-08-01).** `vite-plugin-singlefile` встраивает скрипт инлайном — в этом весь смысл одного файла, — а прод-CSP `script-src 'self'` (T_SEC.2) инлайн запрещает. Страница отрисовывается, скрипт молча не выполняется: то же самое, о чём предупреждает T3.10 про лендинги, и я наступил на это, поставив плагин через сообщение после собственного комментария «инлайн запрещён». Решение — **своя политика на `location = /reader.html`**, и обмен в ней выгодный: отдаём `unsafe-inline` на одном пути, забираем `connect-src 'none'`, то есть «ничего никуда не отправляется» становится правилом браузера, а не фразой на странице. Всё остальное закрыто (`default-src 'none'`): ридер читает локальный файл и считает — ему не нужны ни картинки, ни шрифты, ни фреймы, ни формы | `nginx/default.conf`, `frontend/reader.html`, `frontend/vite.config.ts` |

<a id="L060"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Relay не запускался из-за `nofiles` (найдено 2026-08-01).** `nostr/strfry.conf` просил `nofiles = 1000000`, а жёсткий лимит контейнера — 524288; strfry считает это **фатальной** ошибкой и выходит с кодом 1 ещё до открытия сокета, оставляя `Restarting (1)` при внятном сообщении в логе. Значение стояло с момента написания контейнера и молчало, потому что relay ни разу по-настоящему не поднимали — вскрылось при T_SEC.4, когда его впервые включили профилем. Тот же класс, что `mcp-server`, круживший одиннадцать дней: **сервис под профилем не проверяется обычным деплоем, и его неработоспособность ничего не ломает — до дня, когда он понадобится.** Поставлено `0` («не трогать лимит»): число, выбранное как «побольше», превращается в падение, как только потолок ниже желаемого. Диагностика заняла одну команду — `docker compose logs nostr-relay`; предположения про версию образа и `realIpHeader` не подтвердились | `nostr/strfry.conf`, `docker-compose.dev.yml` |

<a id="L061"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Тест, зависящий от календарной даты, обязан ограничивать себя своим прогоном (2026-08-01).** `test_list_trips_filter_by_departure_date` брал фиксированный origin и `now + 11 дней`. На следующий день он упал — и **справедливо**: сид-данные не удаляются (ENVIRONMENT §7), поэтому вчерашние рейсы на 12 августа попали в сегодняшний запрос «день до» (сегодня `now+11` = 13-е). Ни фильтр, ни продукт при этом не сломаны — сломан был тест, проверявший отрицание («ничего не найдено») в общем пространстве данных. Правило: **любой тест, чьё утверждение звучит как «ничего не найдено», должен сам создать себе изолированное пространство** — уникальный origin, префикс, токен. Та же болезнь, что у `test_trips_pagination_next_cursor_walks_all_items`, только проявившаяся не сменой семантики, а сменой суток | `backend/tests/test_trips.py`, `PRD/ENVIRONMENT.md` §7 |

<a id="L062"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Гоча `.env`: дубль ключа перекрывает правку (T_SEC.3, 2026-08-01).** При вводе пароля Redis старая строка `REDIS_URL=redis://redis:6379/0` осталась в файле рядом с новой — контейнер получил версию без пароля. Симптом читался не как «опечатка в `.env`», а как разъехавшийся продукт: падала ровно группа тестов, которым нужен step-up-грант (он живёт в Redis и **fail-closed** — `core/step_up.py`), и весь прогон шёл заметно дольше. Диагностика в одну команду: `docker compose exec backend printenv REDIS_URL` — окружение контейнера, а не то, что кажется написанным в файле. Правило: правя `.env`, менять существующую строку, а не дописывать новую; при странном поведении после смены переменной первым делом смотреть `printenv` внутри контейнера | `.env`, `backend/app/core/{step_up,redis_client}.py` |

<a id="L063"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **`--reload` снят с prod-uvicorn (T_SEC.3).** Watcher держал вторую копию приложения (инстанс 2 ГБ, OOM уже случался) и перезапускал воркеров при изменении любого файла в примонтированном `./backend` — `git pull` во время запроса ронял этот запрос. Следствие для процесса: правка исходников теперь требует пересоздания контейнера, штатный `up -d --build backend` это и делает | `docker-compose.dev.yml`, `PRD/ENVIRONMENT.md` §9 |

<a id="L064"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **`weasyprint` не импортировался ни разу (T_SEC.3).** Числился в `requirements.txt` под «PDF посадочного талона», которого нет, и единственным его следствием был потолок `Pillow<11` — на библиотеку, через которую проходит **каждый** загруженный файл (`core/file_validation.py`: `Image.open` + `verify()` + `load()`). Удалён, `Pillow` поднят `10.4.0 → 12.3.0`. Урок общий: неиспользуемая зависимость не нейтральна — она навязывает ограничения версий соседям. Движок PDF выбирается вместе с самой фичей | `backend/requirements.txt`, `PRD/ENVIRONMENT.md` §1 |

<a id="L065"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Образ relay'я пиновать (T_SEC.3).** `dockurr/strfry:latest` → `1.1.1`: плавающий тег позволял пересборке молча подменить бинарь компонента, который в MASTERPLAN назван точкой внешней децентрализации. Сверено по реестру тегов — `latest` и `1.1.1` запушены в одну секунду 2026-07-21. Отдельно замечено и **не** исправлено: relay отдаётся голым `ws://` на 7777, проксирования `wss://` в `nginx/default.conf` нет, а клиенты Nostr требуют TLS — Integrity Check Фазы 3.5 («виден в damus») этим не выполняется | `docker-compose.dev.yml`, `nginx/default.conf` |

<a id="L066"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Аудит IDOR (2026-07-29)** — проверены все 36 эндпоинтов с path-параметрами. Классического IDOR нет: владение сверяется до действий, вложенные ресурсы скоупятся (`_get_request` → `req.deal_id`, `decrypt-for-me` → `msg.deal_id`), UUID v4 не перебираются, арбитр читает сейф только при заявленном споре + активном `OperatorAccessGrant`. **Но автоматика это не удерживает:** schemathesis проверяет «нет 500-х» (403 и 200 для него равны), ZAP baseline пассивен. Отсутствие IDOR держится на 41 ручной проверке 403 в 18 файлах → IDOR-матрица заведена в `T_TEST.7 pt.2`. Найдено и исправлено: presigned-ссылки на все вложения жили час, включая `identity_doc` (копии паспортов, T3.9) — теперь 5 минут через `presign_ttl_for_kind` | `backend/app/core/storage.py`, `backend/app/api/dealvault.py`, `backend/tests/test_presign_ttl.py` |

<a id="L067"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Расстояние маршрута — `route_distance_km(origin, destination)` в `core/airports.py`: IATA → координаты → haversine (дуга большого круга, погрешность ~0.5% из-за сплюснутости Земли). **`Trip.origin`/`.destination` хранят IATA-коды**, не свободный текст — `AirportSelect` вызывает `onChange(a.iata)`; схема при этом обычный `str`, поэтому прямой POST может положить что угодно → `None`, не исключение. Реальный налёт из внешних сервисов сознательно не берём: +3–7% точности ценой ключа/лимитов/точки отказа, для будущего рейса трека не существует, и это ответ на другой вопрос — мы меряем маршрут доставки, а не мили самолёта. В UI писать **«по прямой»**, никогда «пройдено километров»: расхождение с реальным путём на длинных перелётах 3–7%, но на коротких наземных плечах доходит до 60% (Тбилиси→Ереван: 175 км по прямой, ~280 по трассе) | `backend/app/core/airports.py`, `backend/tests/test_route_distance.py` |

<a id="L068"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Курсорная пагинация теряла строки с одинаковым `created_at`** (найдено 2026-07-30). Сортировка шла по паре `(created_at, id)`, а курсор сравнивался только по времени: `A(T1) B(T2) C(T2) D(T3)` при limit=2 давал страницу `A,B`, после чего `created_at > T2` **пропускал C**. Совпадение времени — норма, а не край: сообщения одного запроса, сид одной транзакцией. Проявилось как нестабильный счётчик сообщений в сейфе (`test_create_message_appends`: +4 вместо +1) и выглядело флейком. Починено сравнением по той же паре: `(created_at, id) > (cursor_ts, cursor_id)`; курсор остаётся непрозрачным UUID, API не менялся, индекс используется как раньше. Тесты параметризованы по размеру страницы 1–6 — при фиксированном лимите баг не воспроизводится | `backend/app/core/pagination.py`, `backend/tests/test_pagination.py` |

<a id="L069"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Смена почты — через `pending_email`, а не заменой на месте (T3.15).** Код уходит на новый адрес, старый остаётся рабочим и подтверждённым до возврата кода. Опечатка стоит повтора, а не канала восстановления; угнанная сессия не может переставить адрес, не читая новый ящик. Адрес **не резервируется**: побеждает тот, кто подтвердил первым, конфликт ловится `IntegrityError` на уникальном индексе `users.email` → 409, аккаунт остаётся при своём. Одна колонка вместо копии всей машинерии кодов — инварианта «в любой момент проверяется ровно один адрес» достаточно | `backend/app/api/auth.py`, `backend/app/core/email_verification.py` (`target_email`), миграция `0032_pending_email`, `backend/tests/test_email_verification.py` |

<a id="L070"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Смена пароля завершает остальные сессии (T3.15).** Чужой токен нельзя отозвать по `jti` — мы его не видели; вместо этого токены несут `iat`, а `users.sessions_valid_from` задаёт отсечку. **Fail-closed по построению**: отсечка в Postgres, куда запрос и так идёт за пользователем, второго хранилища нет (ср. `D-REVOCATION-IS-BEST-EFFORT`). `iat` — дробное число секунд, а не целое: при целых токен, выпущенный в ту же секунду, пережил бы отсечку, и сменный токен было бы не отличить от чужого. Сменный токен минтится **после** записи отсечки и возвращается в ответе — иначе своё же защитное действие выкидывало бы пользователя. NULL по умолчанию: деплой никого не разлогинил | `backend/app/{api/auth,api/deps,core/security}.py`, миграция `0033_sessions_valid_from`, `backend/tests/test_step_up.py` |

<a id="L071"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Healthcheck фронтенда 11 дней врал** (найдено 2026-07-30). Проба ходила на `http://localhost/health`, но внутри контейнера имя резолвится в `::1`, а `listen 80;` без `[::]:80` слушает только IPv4 → `Connection refused`, `FailingStreak: 263`. Диагностику скрывал сам healthcheck: `2>&1` в команде отправлял сообщение wget в `/dev/null`, и `docker inspect` показывал пустой `Output`. Починено адресом `127.0.0.1` и снятием подавления stderr. На сайт не влияло — внешний nginx ходит по имени сервиса, — но ломало `depends_on: service_healthy` | `docker-compose.dev.yml`, `frontend/nginx.conf` |

<a id="L072"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **`mcp-server` перезапускался вечно** (найдено 2026-07-30). `server.py` использует `stdio_server()` — транспорт по stdin/stdout. В контейнере stdin закрыт, `main()` получает EOF и завершается **с кодом 0**, ничего не напечатав; `restart: unless-stopped` поднимает заново. Отсюда `Restarting (0)` при пустом `logs`. Флагом `stdin_open` не лечится: **сервер на stdio нельзя держать демоном** — клиент запускает его сам и общается через пайпы, снаружи до stdin не дотянуться. Контейнер удалён; `profiles: ["mcp"]` не даёт ему подняться при обычном `up`. **Расхождение с `D-AGENTIC-INTERFACE`:** решение обещает MCP-сервер контейнером для внешних клиентов, что с транспортом stdio недостижимо. Выбор между сетевым транспортом и отказом от контейнера вынесен в **`T7.1`** (Фаза 7); до него решение и код расходятся сознательно, и это записано, а не забыто | `mcp-server/server.py`, `docker-compose.dev.yml` |

<a id="L073"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Step-up re-auth (T3.15) — одноразовый грант в Redis, TTL 5 мин, ключ = `user_id` + скоуп операции (`declare_lost` · `unlink_passkey` · `change_email` · `add_auth_method`). **Fail-closed** при недоступности Redis — сознательное расхождение с `token_blacklist` (`D-REVOCATION-IS-BEST-EFFORT`). Забирается `GETDEL`: повтор перехваченного гранта не проходит. Пруф любым способом входа, `available_methods` не предлагает пароль беспарольному аккаунту. **Грант в заголовке `X-Step-Up-Token`** — query-строка оседает в логах, истории и `Referer`, тот же класс утечки, что у presigned-ссылок | `backend/app/core/step_up.py`, `backend/app/api/{keypair,passkey}.py`, `backend/tests/test_step_up.py`, `frontend/src/components/{StepUpDialog,KeypairSection,PasskeySection}.tsx`, `frontend/src/api/stepUp.ts` |

<a id="L074"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Отправка почты (T3.11, доведено 2026-07-30) — режим TLS выбирается **по номеру порта**: 465 → `SMTP_SSL` (шифрование с первого байта), иначе `SMTP` + `STARTTLS`. Отдельной настройки нет намеренно — её бы забыли синхронизировать с портом. TLS-контекст задаётся явно: умолчание `smtplib` меняло поведение между версиями Python, а по каналу идёт пароль ящика; следствие — `SMTP_HOST` обязан быть именем из сертификата, голый IP теперь отвергается. `Date` и `Message-ID` проставляются нами: Python их не ставит, Postfix чужие письма не дописывает, а rspamd берёт за их отсутствие 3.5 балла (`MISSING_MID` 2.50 + `MISSING_DATE` 1.00) — ни одна DNS-запись этого не лечит. `From` несёт имя `Vimana — Sacred Logistics` — константа в модуле, не env: одинакова во всех средах, а длинное тире требует RFC 2047, поэтому собирается через `formataddr`; конверт остаётся голым адресом | `backend/app/core/email.py`, `backend/tests/test_notifications.py` |

<a id="L075"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Passkeys / WebAuthn (T3.14) — `webauthn_credentials` (credential_id UNIQUE **глобально**: вход usernameless, id должен опознавать аккаунт сам). Ceremony-состояние в Redis по одноразовому `ceremony_id`, не по юзеру — на входе юзера ещё нет, а две вкладки затирали бы друг друга; scope в ключе не даёт потратить челлендж логина на регистрацию. **Sign-count: регресс проверяется только когда `stored > 0`** — синхронизируемые passkey'и (iCloud/Google) всегда отдают 0. **Guard последнего входа** считает пароль + свой ключ + credential'ы; email не дверь, утраченный ключ тоже. `WEBAUTHN_RP_ID`/`ORIGIN` обязаны совпадать с доменом — иначе браузер прерывает ceremony **до** сети и в логах пусто | `backend/app/{core/webauthn,api/passkey,models/webauthn}.py`, миграция `0031`, `frontend/src/components/Passkey*.tsx`, `frontend/e2e/specs/passkey.spec.ts` |

<a id="L076"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Тестирование WebAuthn без железа — виртуальный аутентификатор Chrome через CDP (`WebAuthn.addVirtualAuthenticator`), Playwright им управляет. Криптография настоящая: реальная пара ключей, реальная подпись, сервер проверяет обычным кодом. Эмулируется только жест пользователя (`isUserVerified: true`). Единственный способ покрыть успешный путь — юнит-тесты валидный credential не производят | `frontend/e2e/specs/passkey.spec.ts` |

<a id="L077"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Вход и регистрация по Nostr-ключу (T3.13) — `/api/auth/nostr/{challenge,verify,signup}` поверх той же машинерии, что `establish`. Три purpose внутри подписанного события вместо URL-привязки NIP-98: подпись одного потока бесполезна в другом, и проверка не зависит от origin из деплой-конфига. Неизвестный ключ → 404 (не 401), аккаунт молча не создаётся. Signup отдаёт токен — пароля нет, иначе аккаунт недостижим | `backend/app/api/nostr_auth.py`, `backend/app/core/identity_proof.py`, `backend/tests/test_nostr_auth.py`, `frontend/src/components/NostrAuthButton.tsx` |

<a id="L078"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Личность vs служебный ключ (T3.12) — `establish` с NIP-98-подобным доказательством владения (`core/identity_proof.py`, одноразовый челлендж в `core/challenge.py`), `declare-lost` + `require_live_identity`, идемпотентный бэкфилл служебных ключей в lifespan. `claim`/`import`/`export` удалены | `backend/app/api/keypair.py`, `backend/app/core/{identity,identity_proof,challenge,service_keys}.py`, миграция `0029_identity`, `backend/tests/{test_identity_establish,test_service_keys,test_identity_proof_contract}.py` |

<a id="L079"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Перешифровка сейфов при переходе (T3.12) — контейнер: случайный ключ содержимого + NIP-44-конверт на новый npub; vault-конверты: переупаковка session-ключей. Формат конверта `{ct, sender_pubkey}` (строка = legacy, отправитель = автор сообщения) — **без этого переадресовать пакет новому владельцу невозможно**, ECDH требует приватный ключ автора. Читатели: `dealvault`, `threshold`, arbiter-reveal, `DealVaultPage` | `backend/app/core/{verification,identity,threshold}.py`, `backend/app/api/{keypair,dealvault,threshold}.py`, миграция `0030_container_key_envelope`, `frontend/src/lib/threshold.ts` |

<a id="L080"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Публикация рейсов под платформенным ключом (T3.12) — `PLATFORM_PUBLISH_NSEC` (отдельный от `CHAIN_ANCHOR_NSEC`), авторство платформенное без подделки под перевозчика, фильтр «интересных» по редкости коридора, `trips.nostr_published_by_pubkey` для NIP-09 | `backend/app/core/{nostr_publish,publish_filter}.py`, `backend/app/tasks/{nostr_publish,nostr_whitelist}.py`, `backend/tests/test_platform_publish.py` |

<a id="L081"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Клиентская криптография личности (T3.12) — генерация ключа и подпись челленджа в браузере (`@noble/curves`), сервер получает только npub и подпись. Каноническое событие пришпилено к одному литералу с обеих сторон, иначе расхождение выглядит как 401 «неверный ключ» | `frontend/src/lib/identity.ts`, `frontend/src/test/identity.test.ts`, `backend/tests/test_identity_proof_contract.py`, `frontend/e2e/specs/identity-establish.spec.ts` |

<a id="L082"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Email-подтверждение по коду (T3.11) — 6 цифр, bcrypt-хеш в `users.email_verification_code_hash`, TTL 15 мин, cooldown 60 с, 5 попыток. **Исчерпание попыток обнуляет код**, а не только отклоняет догадку. Plaintext существует лишь как аргумент Celery-таска. **Ничего не гейтит** — только баннер в UI | `backend/app/core/email_verification.py`, `backend/app/api/auth.py`, `backend/app/tasks/notifications.py`, миграция `0028_email_verification`, `backend/tests/test_email_verification.py` (20 тестов) |

<a id="L083"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Auth только по email (T3.11) — `phone` удалён из `UserCreate`/логина, остаётся полем профиля; `password_hash` nullable под T3.13/T3.14, логин явно отклоняет NULL | `backend/app/{models,schemas}/user.py`, `backend/app/api/auth.py` |

<a id="L084"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| E2E-обход подтверждения — `E2E_AUTO_VERIFY_EMAIL_DOMAINS` (список доменов через запятую). Пусто на проде; непустое значение → WARNING в lifespan. Гейта нет, поэтому влияет только на то, плодятся ли неиспользуемые коды | `backend/app/core/config.py`, `backend/app/main.py`, `backend/tests/conftest.py` |

<a id="L085"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Гоча тестовой схемы**: `Base.metadata.create_all` создаёт только отсутствующие таблицы и **не делает ALTER** существующих. Любая новая колонка в `users` требует своего `_ensure_*`-хелпера в conftest — зеркала миграции. Иначе сьют падает на несуществующей колонке | `backend/tests/conftest.py` (`_ensure_email_verification_columns` и соседи) |

<a id="L086"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Перешифровка при `establish` — inline и с самопроверкой (T3.12 pt.2b).** Контейнеры переезжают на новый ключ **в транзакции запроса**, а не Celery-таском: таск стартует только после коммита, то есть уже без служебного ключа, и его падение необратимо — перечитать данные больше нечем. Inline любое исключение откатывает всё, пользователь остаётся кастодиальным. Перед уничтожением ключа идёт **доказательство**: ECDH симметричен, поэтому платформа вскрывает только что созданный конверт ключом отправителя, дешифрует блоб до plaintext и сверяет `doc_hash`. Не сошлось → 500 и откат. Цена — латентность по числу контейнеров; при росте объёмов правильный ответ не «унести в очередь», а разнести перешифровку и смену ключа на два шага | `backend/app/api/keypair.py`, `backend/app/core/verification.py` (`rewrap_container_to_identity`, `verify_container_envelope`) |

<a id="L087"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **КРИТИЧНО — Celery-воркер работал с пустым реестром задач.** `celery -A app.worker.celery_app` импортирует только `app/worker.py`; ни `include`, ни `autodiscover_tasks` заданы не были, `app/tasks/__init__.py` пуст. Ни одна задача не была зарегистрирована → воркер отвечал `Received unregistered task` на всё, beat исправно слал по расписанию, сообщения выбрасывались. **Никогда не выполнялись:** уведомления по сделкам, пересчёт УБА, обновление whitelist relay'я, ночная чистка e2e-аккаунтов, якоря цепи T3.6, отправка кода подтверждения. Обнаружено 2026-07-27 при разборе «письмо не пришло». Исправлено `include=_TASK_MODULES` в `app/worker.py`; `tests/test_worker.py` проверяет, что всё из `beat_schedule` и всё, что диспатчится из кода, зарегистрировано, и что ни один модуль из `app/tasks/` не забыт | `backend/app/worker.py`, `backend/tests/test_worker.py` |

<a id="L088"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **КРИТИЧНО — отсечённые nginx'ем запросы возвращали `200` с HTML.** `error_page 502 503 504 = /_error.html` стоял на уровне `server`, а знак `=` перезаписывает статус на статус отданной страницы, то есть на **200**. Под правило попадал и `limit_req` (по умолчанию отвечает 503), и любой 502/504 при перезапуске бэкенда. Итог: клиент получал `200` с HTML вместо ошибки и читал это как успех. Обнаружено 2026-07-29 — отсечённый по лимиту passkey-логин вернул 200, фронт сохранил `undefined` как токен, следующий запрос ответил 401 без всякой связи с причиной. Существовало с T_SEC.1. Исправлено: `limit_req_status 429` (семантически верно и выводит rate-limit из-под 5xx-правила) + `error_page` перенесён внутрь `location /` — дружелюбная страница нужна человеку, API-клиенту нужен настоящий статус | `nginx/default.conf` |

<a id="L089"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Гоча прокси-заголовков**: `uvicorn` без `--proxy-headers` не знает про HTTPS перед собой и строит редиректы как `http://…`; браузер на https-странице блокирует их как mixed content. Отказ бесшумный — в логе 307 и ничего после. Ломает **любой** редирект, включая автоматический по слэшу от FastAPI | `docker-compose.dev.yml` (команда uvicorn) |

<a id="L090"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Гоча slowapi + PEP 563**: `from __future__ import annotations` вместе с декоратором `@limiter.limit` ломает разбор тела запроса — обёртка slowapi оставляет аннотации строками, FastAPI не распознаёт Pydantic-модель и трактует параметр как query-скаляр, отдавая `422 {"loc": ["query","body"]}` на любой запрос. По отдельности безвредно: `keypair.py` с future-import без rate-limit работает, `auth.py` с rate-limit без future-import тоже. Правило: **в модуле с `@limiter.limit` future-import не ставить** (T3.13, `nostr_auth.py`) | `backend/app/api/{nostr_auth,auth,keypair}.py` |

<a id="L091"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Шум финализаторов Redis — точечно подавлен в conftest.** `redis.asyncio` держит `StreamWriter`, привязанный к своему loop'у; когда GC добирается до `Connection`, `__del__` зовёт `loop.call_soon()` на закрытом loop'е → `RuntimeError`. Из `__del__` исключение выбросить некуда, CPython отдаёт его в `sys.unraisablehook`, тот печатает трейсбек в **stderr** и продолжает. Всплывает там, где loop'ы живут **внутри** теста: hypothesis в `test_contract_fuzz` (~15 примеров, каждый в своём loop'е) и `asyncio.run` в Celery-пути `test_deal_chain`; teardown-фикстура закрывает клиента лишь того loop'а, что жив в конце тест-функции. Момент печати определяет GC, поэтому трейсбек садится рядом со случайным тестом. Решено `_silence_dead_loop_finalizers` — хук на **одно** исключение (`RuntimeError` + «Event loop is closed» + redis в источнике), всё прочее пропускается. Раньше стоял `filterwarnings("ignore")` в двух модулях: он глушил весь класс не разбираясь и не убирал stderr. Прода не касается — там один долгоживущий loop | `backend/tests/conftest.py` |

<a id="L092"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Гоча asyncio-Redis**: клиент `redis.asyncio` привязывается к event loop, в котором создан. Кеш на уровне модуля ломается под pytest-asyncio — он делает новый loop на каждый тест, со второго идёт `Event loop is closed`, а брошенные соединения падают в собственном `__del__` уже после смерти loop'а (`PytestUnraisableExceptionWarning`). Общий клиент вынесен в `core/redis_client.py` с кешем **по loop'у** и `aclose_current()`; conftest закрывает его в teardown. Отдельный урок: у `token_blacklist` эта же болезнь пряталась за fail-soft — под тестами отзыв токенов молча не работал, и тесты про logout проходили, не проверяя механизм | `backend/app/core/redis_client.py`, `backend/app/core/{challenge,token_blacklist}.py`, `backend/tests/conftest.py` |

<a id="L093"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **КРИТИЧНО — `SyncSessionLocal` не изолирован тестами.** `app/core/database.py` строит его из `settings.DATABASE_URL`, то есть из **боевой** БД. Override `get_db` в conftest подменяет только async-сессию, поэтому любой Celery-таск, вызванный из теста напрямую, работал по проду. 2026-07-26 `test_cleanup_e2e_users_task_deletes_stale` вызвал `cleanup_e2e_users()` без подмены → удалено 22 реальных аккаунта с каскадом по сделкам, сообщениям и рёбрам доверия (36 → 14 юзеров). Исправлено autouse-фикстурой `sync_sessions`, которая перевешивает `SyncSessionLocal` **в каждом импортирующем модуле** (символ импортирован по значению, патч самого `core.database` не помогает). При появлении нового таск-модуля — добавлять его в кортеж фикстуры | `backend/tests/conftest.py` (`sync_sessions`), `backend/app/core/database.py`, `backend/app/tasks/*` |

<a id="L094"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **⚠️ Гипотеза, НЕ подтверждённая**: пересборка `celery-beat` перезапускает планировщик и `cleanup_e2e_users` может отработать сразу. Появилась 2026-07-26 как объяснение падения 36 → 14 юзеров на проде — **объяснение оказалось неверным**, настоящей причиной был неизолированный `SyncSessionLocal` (строка выше). Сама механика beat'а никем не проверялась; прежде чем на неё опираться, проверить, а не цитировать | `backend/app/tasks/cleanup.py` |

<a id="L095"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| FastAPI app + /health + CORS + роутеры + lifespan (User Zero promote) | `backend/app/main.py` |

<a id="L096"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Настройки (pydantic-settings) | `backend/app/core/config.py` |

<a id="L097"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| JWT (HS256, 30д), bcrypt | `backend/app/core/security.py` |

<a id="L098"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| get_current_user dependency + is_superuser helper | `backend/app/api/deps.py` |

<a id="L099"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Auth API: register/login/me + normalize/case-insensitive/trim (T1.15) | `backend/app/api/auth.py` |

<a id="L100"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Invite + Connection API + /me/invites (T1.14) | `backend/app/api/social.py` |

<a id="L101"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Trips API (POST/GET + фильтры) — `can_carry` check (T1.24) | `backend/app/api/trips.py` |

<a id="L102"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Deals API (match/accept/event/confirm) + DealEvent append-only + inquiry.deal_id linking (T1.22) | `backend/app/api/deals.py` |

<a id="L103"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| DealVault API (чат + upload) — MAX 10MB, streaming SHA-256, MIME whitelist (T1.19) | `backend/app/api/dealvault.py` |

<a id="L104"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Inquiry API (TripInquiry + InquiryMessage) | `backend/app/api/inquiries.py` |

<a id="L105"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Admin API (Dispute + Vault access + Users) — все через require_perm() | `backend/app/api/admin.py` |

<a id="L106"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| RBAC: Permission enum + Role + perms_of() + require_perm() FastAPI dep (T1.24 pt.1) | `backend/app/core/permissions.py` |

<a id="L107"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Waitlist API + Telegram admin notify (T1.18) | `backend/app/api/waitlist.py` |

<a id="L108"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Waitlist: подтверждение заявителю + письмо владельцу, разовая рассылка по долгу (T_UX.8) | `backend/app/tasks/notifications.py` (`send_waitlist_emails`, `send_pending_waitlist_confirmations`, `_send_waitlist_pair`), `backend/app/models/waitlist.py` (`confirmation_sent_at`), `backend/alembic/versions/0042_waitlist_confirmation_sent.py`, `backend/app/api/waitlist.py`. Отметка ставится только по `True` из `send_email` — см. строку ниже |

<a id="L109"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Авторизация чтения waitlist через роль, а не общий секрет (T_UX.8 pt.2) | `backend/app/core/permissions.py` (`Permission.WAITLIST_READ`, superuser-секция), `backend/app/api/waitlist.py`. `ADMIN_API_TOKEN` и `require_admin_token` удалены — это было единственное место, где авторизация шла статическим заголовком мимо `get_current_user`/`require_perm` |

<a id="L110"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Шаблоны писем: один layout, шесть локалей, восемь писем (T_UX.9) | `backend/app/emails/layout.html`, `backend/app/emails/locales/{en,ru,ua,pl,fr,es}.json`, `backend/app/core/email_templates.py` (`render` — единственная точка входа), `backend/app/tasks/notifications.py` (`_send`, `_notify_user`, `_catalogue_for`). Текстовая часть строится из каталога, а не вырезается из HTML. `en` — и fallback, и эталон: недостающий ключ разрешается в него, а не в исключение |

<a id="L111"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Почтовая консоль в админке, два контура (T_UX.9 pt.2) | `backend/app/core/email.py` (`Circuit`, `live()`, `preview()`), `backend/app/api/admin.py` (`/admin/email/{status,templates,test}`), `backend/app/core/permissions.py` (`EMAIL_MANAGE`), `frontend/src/pages/AdminEmailPage.tsx`, `frontend/src/api/admin.ts`. **Контуры — раздельные наборы переменных, а не флаг:** `PREVIEW_SMTP_*` против `SMTP_*`. Тестовая отправка прошита на `preview()`, параметра до боевого нет. Отрисовка шаблонов не читает ни один контур — страница работает при сломанной почте. Пароль не отдаётся наружу ни в каком виде |

<a id="L112"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Граница ошибок рендера (T_UX.11) | `frontend/src/components/ErrorBoundary.tsx` (+ `RouteErrorBoundary` по умолчанию), подключён в `frontend/src/App.tsx` **внутри router'а и снаружи `Suspense`** — упавший `lazy()`-чанк отклоняет приостановленный промис, и видит это только граница выше. Различает две причины: устаревший чанк после деплоя (свой текст, одна автоперезагрузка под замком `sessionStorage`) и настоящую ошибку (текст ошибки показан для отчёта). Ключ — `location.pathname`, иначе граница залипает и вся сессия выглядит сломанной. Тесты: `frontend/src/test/ErrorBoundary.test.tsx` |

<a id="L113"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Предпросмотр писем без отправки (T_UX.9) | `backend/app/cli/email_preview.py` — рендер в `/tmp/vimana-email-preview/*.html` + `index.html`; `mailpit` в `docker-compose.dev.yml` под профилем `mail` (SMTP 1025, веб 8025, оба на loopback). Браузер показывает идеальный случай, Mailpit — то, что реально ушло по SMTP: заголовки, обе MIME-части, кодировки |

<a id="L114"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Язык получателя (T_UX.9) | `backend/app/models/user.py` (`locale`), `backend/app/models/waitlist.py` (`locale`, nullable), `backend/alembic/versions/0043_locale_columns.py`, `backend/app/schemas/user.py`, `frontend/src/pages/{RegisterPage,LandingPage}.tsx` шлют `i18n.language`. Дефолт `en`, не `ru`; существующие аккаунты не бэкфилились — язык по имени не угадывают |

<a id="L115"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Сканирование загрузок общим clamd (T3.8 pt.2) | `backend/app/core/file_validation.py` (`scan_for_malware`, `_clamav_target`), `CLAMAV_HOST` → `172.31.20.59:3310`. Сканер **общий с Mailu**: база сигнатур ~2 ГБ уже в памяти почтового хоста, второй копии на `t3.small` места нет. Включено и проверено вживую 2026-08-09 (EICAR → `infected`, обычный текст → `clean`). Протокол clamd без TLS и без аутентификации, поэтому путь приватный: проброс только на приватный IP + правило SG от группы Vimana + общий VPC. Проброс — в `docker-compose.override.yml` у Mailu, **обязан пережить апгрейд Mailu**, иначе сканирование прекратится молча |

<a id="L116"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Elastic IP у Vimana (инфраструктура) | Публичный адрес инстанса **закреплён** — на него указывает домен `vimana.dealvault.club`, и он не меняется при stop/start. Уточнено владельцем 2026-08-09; более ранняя запись в памяти агента об «IP меняется при рестарте» устарела. Практическое следствие: правила по адресу (security group, allowlist) на этот инстанс держатся, а приватный адрес `172.31.19.152` в дефолтном VPC стабилен тем более. Почтовый сервер Mailu — **в том же регионе и VPC**, что открывает приватный путь к его `clamd` без выхода в интернет (`T3.8 pt.2`) |

<a id="L117"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Почему письмо о смене пароля существует (T_SEC.5 pt.3, 2026-08-10) | Владелец спросил, как пароль вообще может смениться чужими руками, если перебор закрыт лимитами. Перебор и правда закрыт — но смена почти никогда с него не начинается. Реальные пути: **повторное использование пароля** (одна верная попытка, лимит на неё не срабатывает), фишинг, скомпрометированный ящик, незалоченное устройство. Письмо решает исход ровно в одном из них — **атакующий знает пароль, но не владеет ящиком**: он меняет пароль и запирает владельца, письмо уходит на адрес, которого у него нет, и владелец возвращает аккаунт через «Забыли пароль?». Это же и самый вероятный сценарий из перечисленных. Обратная сторона названа честно: при скомпрометированном ящике письмо будет удалено и пользы не принесёт. Отсюда формулировка — не тревога, а средство: где нажать и почему оно сработает. **Пробел, который остаётся:** после T3.28 вход возможен кодом на почту без пароля вовсе, и такой вход не порождает никакого письма. Сигналом для этого случая было бы «вход с нового устройства» — его нет |

<a id="L118"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Абстракция канала доставки (T3.26/T3.27) | `backend/app/core/channels.py` (`available_for`, `deliver`, `proves`, `enabled`), `backend/app/api/auth.py` (`/contact/channels`, `/contact/request-code`, `/contact/confirm`), `backend/app/tasks/notifications.send_channel_code`. **Канал доказывает то, куда доставляет, а не то, что ввёл пользователь:** `telegram` свидетельствует о чате и никогда о номере, поэтому для телефона не предлагается; `telegram_gateway` доставляет через Telegram, но свидетельствует о номере — отображение живёт в `proves()` |

<a id="L119"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Контакты и коды как таблицы (T3.25) | `backend/app/models/contact.py`, `backend/app/core/contacts.py`, `backend/app/core/contact_verification.py`, `backend/alembic/versions/0045_user_contacts.py`. Уникальность — **частичным** индексом только по подтверждённым строкам: полный `UNIQUE` позволил бы неподтверждённой заявкой на чужой номер запереть настоящего владельца навсегда. `users.email`/`phone` остаются денормализованным основным контактом. Телефон нормализуется в E.164 при записи в профиль (422 на непарсимое) |

<a id="L120"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| `send_email` сообщает, ушло ли письмо (T_UX.8) | `backend/app/core/email.py`. Возвращает `bool`: `False` при незаданном SMTP или пустом получателе, `True` только после `sendmail`. Заведено потому, что молчаливый выход неотличим от успеха на стороне вызывающего — именно так `✅ готово` держалось у Telegram и WhatsApp. Кто пишет в БД «отправлено», обязан читать результат |

<a id="L121"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Categories API (search + auto-create on match, T1.17) | `backend/app/api/categories.py` |

<a id="L122"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Airports API (search + nearest + cascade country→city→airport, T1.10/T1.16) | `backend/app/api/airports.py`, `backend/app/core/airports.py` |

<a id="L123"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Telegram bot webhook + linking через /start {token} (T1.7) | `backend/app/api/telegram.py`, `backend/app/core/telegram.py` |

<a id="L124"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| R2/S3 клиент + health check (T1.20) | `backend/app/core/storage.py` |

<a id="L125"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| AES-256-GCM at-rest шифрование (T1.21) — property-facade `text` на модели | `backend/app/core/crypto.py`, `backend/app/models/deal.py` |

<a id="L126"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Cursor pagination utils Page[T] (T1.19) | `backend/app/core/pagination.py` |

<a id="L127"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| slowapi rate-limit + X-Forwarded-For key (T1.19) | `backend/app/core/rate_limit.py` |

<a id="L128"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Global exception handler + X-Request-ID middleware + jsonable_encoder fix (T1.19) | `backend/app/main.py`, `backend/app/core/logging_setup.py` |

<a id="L129"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Email + WhatsApp (Twilio) notifications | `backend/app/core/email.py`, `backend/app/core/whatsapp.py` |

<a id="L130"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Celery worker + beat (notifications, dispute checks) | `backend/app/worker.py`, `backend/app/tasks/notifications.py` |

<a id="L131"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Superuser (User Zero) startup promotion — idempotent | `backend/app/core/superuser.py` |

<a id="L132"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Boarding pass PDF (WeasyPrint) | *(планируется, слот под T6.1)* |

<a id="L133"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Async SQLAlchemy engine + Base + get_db() | `backend/app/core/database.py` |

<a id="L134"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Alembic async migrations (0001–0010) | `backend/alembic/env.py`, `backend/alembic/versions/` |

<a id="L135"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Все доменные модели (13 таблиц) | `backend/app/models/` |

<a id="L136"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Изолированная тестовая БД `vimana_test` + идемпотентные seed-фикстуры | `backend/tests/conftest.py` |

<a id="L137"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| 140 backend-тестов (auth, trips, deals, dealvault, dealvault_attachments, arbiter, dual_role, permissions, hardening_block3/4/5, encryption, inquiry, notifications, race_conditions, social, telegram, waitlist, categories, airports) | `backend/tests/` |

<a id="L138"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Frontend SPA — React 18 + Vite + TypeScript + Tailwind + i18n 6 языков | `frontend/src/` |

<a id="L139"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Frontend RBAC: hasPerm() + Permission enum mirror | `frontend/src/lib/permissions.ts` |

<a id="L140"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| ModeSwitcher в Navbar (T1.24) + разный визуал Dashboard | `frontend/src/components/ModeSwitcher.tsx`, `pages/DashboardPage.tsx` |

<a id="L141"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| InquiryPanel — right-side drawer/panel с encrypted-at-rest badge | `frontend/src/components/InquiryPanel.tsx` |

<a id="L142"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| ImageLightbox — full-screen preview с Esc/click-outside close | `frontend/src/components/ImageLightbox.tsx` |

<a id="L143"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Admin pages (`/admin/disputes`, `/admin/users`, `/admin/deals/:id/vault`) | `frontend/src/pages/Admin*.tsx` |

<a id="L144"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Landing + Waitlist public route (T1.18) | `frontend/src/pages/LandingPage.tsx` |

<a id="L145"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Frontend smoke-тесты (7 кейсов через vitest) | `frontend/src/test/`, `frontend/src/**/*.test.tsx` |

<a id="L146"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Docker compose dev с nginx dynamic DNS resolver + SSL termination | `docker-compose.dev.yml`, `nginx/default.conf` |

<a id="L147"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Тестовые прогоны: повседневный vs полный (2026-07-25)** — повседневный: `pytest -m "not fuzz"` (~2.5 мин, без schemathesis-фаззера) для итераций во время разработки; **полный: `pytest` без фильтра — блокирующе обязателен перед закрытием любой задачи** (маркер зарегистрирован в pytest.ini). Ускорение прогона 490→312 с: (1) `BCRYPT_ROUNDS=4` — env ставится ТОЛЬКО в pytest-процессе через conftest, прод-хеширование остаётся 12 раундов, сид-юзеры перехешируются автоматически (bcrypt хранит cost в хеше); (2) `ALTER DATABASE vimana_test SET synchronous_commit = off` — per-database, основная БД не затронута; `fsync=off` сознательно НЕ используется (кластерная настройка, риск коррупции основной БД) | `backend/{pytest.ini,tests/conftest.py,app/core/security.py}` |

<a id="L148"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Деплой frontend после T_SEC.2 (D-STATIC-FRONTEND)** — в работающем контейнере НЕТ npm/node (multi-stage: `node:22-alpine` собирает → `nginx:1.27-alpine` раздаёт `dist/`), source volume-mount убран. `docker compose exec frontend npm run build` → «npm: executable file not found»; `restart frontend` НЕ подхватывает изменения исходников. Единственный способ выкатить фронт (включая один только `version.ts`): `docker compose -f docker-compose.dev.yml up -d --build frontend` | `frontend/Dockerfile`, `docker-compose.dev.yml` |

<a id="L149"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Nginx custom 502/503/504 page с auto-refresh + healthcheck-based startup (2026-07-14) | `nginx/_error.html`, `nginx/default.conf`, `docker-compose.dev.yml` |

<a id="L150"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Vite build vendor chunk splitting (react/i18n/phone) — main bundle < 500 kB | `frontend/vite.config.ts`, `frontend/package.json` |

<a id="L151"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Receiving Address helper (T1.26) + share-address message prefix `📍 SHARED ADDRESS` | `backend/app/core/address.py` |

<a id="L152"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| GeoNames city autocomplete (T1.26) — reuses `cities15000.txt` из T1.16 | `backend/app/core/cities.py`, `backend/app/api/cities.py` |

<a id="L153"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Nostr keypair core (T2.2) — coincurve secp256k1, Schnorr sign/verify, AES-256-GCM wrap для nsec | `backend/app/core/keypair.py` |

<a id="L154"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Signing helper (T2.2 pt.2) — NIP-01 event format (kind 4801 vault_message / 4802 deal_event), event_id per NIP-01, ±5 min clock-skew guard. Vault-message strict для self-custody (422 без sig); deal-event lenient (unsigned OK). Legacy pt.1 helpers сохранены | `backend/app/core/signing.py` |

<a id="L155"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Keypair endpoints (T2.2) — /me/keypair/{status,export,claim,import} | `backend/app/api/keypair.py` |

<a id="L156"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| NIP-07 signing (T2.2 pt.2) — `signVaultMessageViaNip07(dealId, text, isSystem)` через `window.nostr.signEvent()`; api/dealvault.ts авто-подписывает если self-custody | `frontend/src/{lib/nostr,api/dealvault}.ts` |

<a id="L157"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Claim self-custody UI (T2.2 pt.2) — кнопка + модалка с warn (амбер), доступна если `has_encrypted_nsec && nip07` | `frontend/src/components/KeypairSection.tsx` |

<a id="L158"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Nostr event schema fields (T2.2 pt.2) — `nostr_event_id VARCHAR(64)`, `nostr_created_at BIGINT`, `nostr_pubkey VARCHAR(64)` на deal_vault_messages + deal_events (nullable для backward-compat pt.1 записей) | миграция `0015_nostr_event_format` |

<a id="L159"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Threshold e2e schema (T2.3) — `is_e2e BOOLEAN DEFAULT false`, `wrapped_shares JSONB`, `read_packages JSONB` на deal_vault_messages | миграция `0016_threshold_encryption` |

<a id="L160"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Threshold NIP-44 v2 core (T2.3, переведён в `T_KEYS.1` 2026-08-02) — `nip44_encrypt`/`nip44_decrypt`: HKDF поверх x-координаты ECDH, ChaCha20, HMAC-SHA256 над `nonce||ct`, паддинг по расписанию спецификации. Прежний `nip04_*` (AES-256-CBC PKCS7 **без MAC**) удалён целиком — не помечен deprecated, а снят: неаутентифицированный конверт в хранилище, построенном на обнаружимости изменений, был неправильным примитивом. Миграция единовременная, ветки совместимости нет: замер на проде 2026-08-02 показал ноль E2E-сообщений и ноль контейнеров, то есть ноль существующих конвертов; `E2EPayload` валидатор blob'а | `backend/app/core/threshold.py` |

<a id="L161"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Threshold endpoints (T2.3) — `/threshold/arbiter-info`, `/threshold/dealvault/messages/{id}/reveal-my-share`, `/threshold/disputes/{deal_id}/arbiter-reveal` + `arbiter_share_revealed` audit-event | `backend/app/api/threshold.py` |

<a id="L162"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Threshold client crypto (T2.3) — `encryptE2E` (SSS split → NIP-44 wrap через `window.nostr.nip44` → AES-GCM), `decryptE2E` (own read_package → session_key → AES-GCM decrypt), `decryptFromShares` (dispute recovery) через `@noble/{ciphers,curves,hashes}` + `shamir-secret-sharing` + `window.nostr.nip04.*` | `frontend/src/lib/threshold.ts` |

<a id="L163"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Threshold client API + UI (T2.3) — `api/threshold.ts`, encrypt-on-send в `api/dealvault.ts` (self-custody + NIP-07 + parties known), inline decrypt в DealVaultPage с "🔒 расшифровываю…" fallback | `frontend/src/{api/threshold,api/dealvault,pages/DealVaultPage}.tsx` |

<a id="L164"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Nostr publish bridge core (T3.5 pt.1) — `build_event(trip, carrier, url)` kind 30402 NIP-99 + `publish_event` websockets к whitelist + own strfry, `NOSTR_PUBLISH_ENABLED` toggle | `backend/app/core/nostr_publish.py` |

<a id="L165"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Nostr publish Celery + endpoints (T3.5 pt.1) — `publish_trip_to_nostr` task при POST /trips, `GET /trips/{id}/nostr-event` on-demand регенерация | `backend/app/tasks/nostr_publish.py`, `backend/app/api/trips.py` |

<a id="L166"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| strfry контейнер (T3.5 pt.1) — `docker-compose --profile nostr up nostr-relay`, LMDB volume, minimal config; НЕ запускается по умолчанию | `docker-compose.dev.yml`, `nostr/strfry.conf` |

<a id="L167"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Nostr NIP-07 self-custody publish (T3.5 pt.2) — `POST /api/nostr/publish-signed` принимает event JSON, recompute event_id + verify Schnorr sig + verify pubkey совпадает с current_user.nostr_pubkey + verify carrier == trip.carrier_id | `backend/app/api/nostr.py::publish_signed_event` |

<a id="L168"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Nostr WoT-gate (T3.5 pt.2) — strfry writePolicy plugin читает `/data/allowed_pubkeys.txt` (mtime hot-reload), Celery hourly task `refresh_allowed_pubkeys` генерирует файл из активных TrustEdge + arbiter/superuser role. Non-Vimana pubkey → reject | `nostr/write_policy.py`, `backend/app/tasks/nostr_whitelist.py` |

<a id="L169"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Nostr metrics + republish (T3.5 pt.2) — single-row `publish_metrics` таблица (success/error counter + last_attempt_at), `bump_publish_metric` на любом publish, `GET /nostr/metrics` публичный, `POST /nostr/republish/{trip_id}` под `NOSTR_REPUBLISH` permission (superuser) | `backend/app/{core/metrics,models/metrics,api/nostr}.py`, миграция `0019_publish_metrics` |

<a id="L170"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| УБА core (T3.1) — формула F_norm × Q_norm × V_norm × D_factor × V_verify_norm × 1000, level slugs, `compute_components/compute_uba/level_of/recompute_and_persist` (sync session для Celery) | `backend/app/core/uba.py` |

<a id="L171"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| УБА Celery beat (T3.1) — `recompute_all_uba` каждый час для активных за 90 дней carrier-ов; queue=notifications | `backend/app/tasks/uba.py`, `backend/app/worker.py` |

<a id="L172"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| УБА endpoints (T3.1) — `GET /api/me/uba` + `GET /api/users/{id}/uba` → `{uba, level, components}` | `backend/app/api/uba.py` |

<a id="L173"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| УБА UI (T3.1) — `UBASection` в профиле: score, level chip (navy/cyan/amber градация), 4 component tile'а | `frontend/src/{api/uba,components/UBASection,pages/ProfilePage}.tsx` |

<a id="L174"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Playwright smoke suite (T_TEST.3) — `frontend/e2e/` отдельный npm-пакет, `baseURL` prod, 6 spec'ов: single-context (golden/verification/recipient), multi-context pt.2 (invite-flow/auth-rehydrate/admin-guard), headed режим для Mac, trace всегда | `frontend/e2e/{package.json,playwright.config.ts,helpers.ts,specs/*}` |

<a id="L175"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| E2E user cleanup (T_TEST.3) — Celery beat `cleanup_e2e_users` раз в 24ч по convention `@e2e.vimana.local`, каскадный delete через 12 таблиц | `backend/app/tasks/cleanup.py`, beat schedule в `worker.py` |

<a id="L176"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Admin users viewer (T_TEST.3) — `email_contains` filter, `DELETE /admin/users/{id}` async cascade, «test» chip, bulk-friendly UI | `backend/app/api/admin.py`, `frontend/src/{api/admin,pages/AdminUsersPage}.ts(x)` |

<a id="L177"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Receiving addresses (T_UX.4 A) — `receiving_addresses` таблица + partial-unique on default per user, backfill из legacy `User.receiving_*` через 0023 (contract-фаза с удалением старых колонок — future) | миграция `0023_receiving_addresses`, `backend/app/{models/address,api/addresses,core/address}.py`, `backend/tests/test_addresses.py` |

<a id="L178"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| User avatars (T_UX.4 B) — `users.avatar_key VARCHAR(255)`, presigned R2 URL per-request, jpeg/png/webp, max 3 MB, тот же R2 бакет что для DealVault attachments | миграция `0024_user_avatar`, `backend/app/{api/avatar,core/avatar_url}.py`, `backend/tests/test_avatar.py` |

<a id="L179"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Chat address picker (T_UX.4 C) — `share-address` в DealVault + Inquiry принимает `address_id`, backend fallback на default → legacy | `backend/app/api/{dealvault,inquiries}.py`, `frontend/src/components/ShareAddressModal.tsx` |

<a id="L180"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Tamper-evident deal chain (T3.6) — `deal_events` расширены `seq/entry_hash/prev_hash` (NOT NULL) + `UNIQUE(deal_id, seq)`. Only `append_deal_event` создаёт entry (advisory-lock per deal + hash preimage с fixed field order, scope-binding deal_id, presence-bytes, canonical_json raises на non-serializable). Прямой `db.add(DealEvent)` падает на flush | миграция `0025_deal_event_chain`, `backend/app/{core/deal_chain,models/deal,api/{admin,deals,threshold}}.py`, `backend/tests/test_deal_chain.py` (39 тестов) |

<a id="L181"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Chain anchors to Nostr (T3.6) — `deal_chain_anchors(deal_id, seq, entry_hash, nostr_event_id, relays JSON)`. Hourly Celery task публикует head'ы в третьесторонние relay'и signed отдельным `CHAIN_ANCHOR_NSEC`. Row только при ≥1 relay accepted → auto-retry на failed publish. `NOSTR_FRIENDLY_RELAYS` (не own strfry) — evidential weight | `backend/app/{core/chain_anchor,tasks/chain_anchor,models/deal,worker}.py` |

<a id="L182"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Vault content chain (T3.7) — `message_added`/`file_added`/`sealed`/`identity_ref` event types; `content_hash_of(ciphertext, nonce)` = sha256 хранимых байтов (E2E верифицируется без расшифровки; ротация MESSAGE_ENCRYPTION_KEY — только envelope); сообщение/файл и его chain-событие коммитятся одной транзакцией; `verify_content()` сверяет content_hash/file_hash цепи с хранимыми строками (ловит delete/swap при интактной цепи); `GET /deals/{id}/chain` расширен sealed_at + coverage + content_mismatches. ВАЖНО: `deal.sealed_at` читать до verify-вызовов (`expire_all` → MissingGreenlet) | миграция `0026_vault_completeness`, `backend/app/{core/deal_chain,api/{dealvault,deals,admin},models/deal}.py`, `backend/tests/test_vault_completeness.py` (9 тестов) |

<a id="L183"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Seal / unseal (T3.7, D-SEAL-SEMANTICS) — `Deal.sealed_at` при confirm; guard `_ALLOWED_WHEN_SEALED = {dispute_opened, arbiter_opened}` в `append_deal_event` под advisory lock; `open_dispute` снимает seal, `resolve_dispute(closes_deal)` ставит повторно; `SealedError` → HTTP 409 на messages/attachments/events/share-address | `backend/app/{core/deal_chain,api/{admin,deals,dealvault}}.py` |

<a id="L184"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Identity ↔ Deal пересечение (T3.9, D-DVLT-PROTOCOL) — `submit-document` в сделке атомарно создаёт: `IdentityContainer` (шифр ключом владельца) + badge + **копию документа в сделке** (system-message + Attachment `identity_doc`, plaintext в R2, виден участникам) + 3 события цепи; `identity_ref.doc_hash` == `Attachment.file_hash` == `IdentityContainer.doc_hash` (тройное совпадение, проверяется `verify_content`). Kind `identity_doc` не загружается через generic-endpoint (415). Self-upload вне сделки — без цепи | миграция `0027_identity_doc_kind`, `backend/app/{api/verification,core/deal_chain,models/deal}.py`, `backend/tests/test_identity_ref.py` (6 тестов) |

<a id="L185"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Контент-валидация загрузок (T3.8) — whitelist-сигнатуры (jpeg/png/webp/heic+brands/pdf), `validate_upload(data, declared_mime)` (декод изображений Pillow `verify()`+`load()`, полиглоты умирают) и `validate_document(data)` (MIME сниффится из байтов, для verification-документов). Валидация ДО записи в R2/`IdentityContainer`; отказ → 422 + warning (без байтов). 4 поверхности: DealVault attachments, avatar, verification submit-document + self-upload. Pillow==10.4.0 (weasyprint<11). Поймал битый PNG_1X1 в тестах (CRC IDAT) — фикстура теперь генерится программно | `backend/app/core/file_validation.py`, `backend/app/api/{dealvault,avatar,verification}.py`, `backend/tests/test_file_validation.py` |

<a id="L186"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Статические лендинги DealVault (T3.10) ⛔ **сняты из выдачи 2026-08-02**, владелец выбрал средний вариант (новый лендинг `T_UX.7`); файлы перенесены в `marketing/…-deployed.html` вместе с правками тона. Как это работало — `frontend/public/dealvault-v5-corporate.html` + `dealvault-v5-rebel.html` отдаются как обычные файлы: Vite копирует `public/` в `dist/` (`publicDir` по умолчанию), внутренний nginx фронта резолвит их через `try_files $uri` до SPA-fallback'а. Роут в `App.tsx` и правка `nginx/default.conf` не нужны. Страницы вне i18n (только RU) и вне SPA-навигации — осознанно, решение владельца «оставить в html-виде». Ранее здесь стояли v3b/v4b (сняты 2026-07-26, исходники остаются в `marketing/`) | `frontend/public/dealvault-v5-corporate.html`, `frontend/public/dealvault-v5-rebel.html` |

<a id="L187"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| CTA на статических страницах (T3.10) ⛔ **снят вместе со страницами 2026-08-02** — переключаются **все** элементы с `data-auth-label` разом (на v5 их три: шапка, герой, финальная кнопка — иначе залогиненный видел бы разные подписи сверху и снизу). Гость: «Получить инвайт»; финальная кнопка → `/register`, верхние скроллят к `#cta`. Живая сессия: «Зайти в аккаунт» → `/dashboard`. **Auth-куки в проекте нет** — JWT в `localStorage['token']`, уходит `Authorization: Bearer`; скрипт читает куки (`token`/`access_token`/`vimana_token`) первыми на случай будущей cookie-сессии, затем localStorage; JWT `exp` проверяется, протухший токен сессией не считается. **Файл обязан быть внешним**: прод-CSP `script-src 'self'` (T_SEC.2) без `'unsafe-inline'` молча блокирует любой инлайновый `<script>` на этих страницах | `frontend/public/vimana-cta.js` |

<a id="L188"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| УБА chip на карточке рейса (T3.2) — TripOut carrier_uba+carrier_uba_level (batch-lookup), `<UBAChip>` компонент с scoped-цветом | `backend/app/{api/trips,schemas/marketplace}.py`, `frontend/src/components/UBAChip.tsx` |

<a id="L189"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| OperatorAccessGrant (T3.2) — модель + auto-create на open_dispute + explicit grant/revoke endpoints + gate на arbiter vault read (нужен ≥1 active grant) | `backend/app/{models/deal,api/admin}.py`, миграция `0017_operator_access_grants` |

<a id="L190"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Verification container encryption (T2.1) — AES-256-GCM key = owner's nsec[:32], custodial-only | `backend/app/core/verification.py` |

<a id="L191"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Verification endpoints (T2.1) — create/respond/submit/escalate/self-upload/public listing/revoke | `backend/app/api/verification.py` |

<a id="L192"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Polite-decline контракт (T2.1 pt.3) — `GET /deals/{id}/verification-requests` = единственный источник для sender-баннера; `VerificationRequestOut.status`/`.target_role` типизированы enum'ами → OpenAPI объявляет значения, TS-юнионы подкреплены контрактом | `backend/app/schemas/verification.py`, `backend/tests/test_verification.py`, `frontend/src/{api/verification.ts,pages/DealPage.tsx,components/VerificationDeclineBanner.tsx}` |

<a id="L193"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Verification frontend components — VerificationSection (profile), VerificationBadgeChip, RequestModal, RespondModal | `frontend/src/components/Verification*.tsx` |

<a id="L194"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Trust Graph core (T2.4) — BFS до глубины 6, sybil-guard, symmetric add_dealt_with/add_invited, refresh_trust_counts | `backend/app/core/trust.py` |

<a id="L195"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Trust Graph endpoints (T2.4) — /me/trust-circle, /users/{id}/trust-metrics | `backend/app/api/trust.py` |

<a id="L196"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Trust Graph auto-populate — dealt_with на confirm_deal, invited на accept_invite | `backend/app/api/{deals,social}.py` |

<a id="L197"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| Trust Circles UI (T2.4) — depth selector 1–6, counter tiles, hop list | `frontend/src/{api/trust,components/TrustCirclesSection}.tsx` |

<a id="L198"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| KeypairSection frontend — status/export/import UI + NIP-07 detection | `frontend/src/components/KeypairSection.tsx`, `frontend/src/lib/nostr.ts` |

<a id="L199"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| AddressForm / AddressCard — profile form + chat card render | `frontend/src/components/Address{Form,Card}.tsx` |

<a id="L200"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Тип карточки — колонка, а не префикс в тексте (T3.34, 2026-08-16).** T1.26 писал `📍 SHARED ADDRESS` в начало сообщения, и фронт узнавал карточку, разбирая строку обратно. Для одной карточки это работало; каталог §6.9.4 содержит тридцать семь, и парсер строк на них не масштабируется. Отсюда три решения, каждое против очевидной альтернативы. **`card_kind` — строка, а не DB-enum:** каталог растёт каждой задачей от T3.35 до T3.39, и enum превращал бы новый тип карточки в миграцию; валидация живёт в `core/cards.py`, где добавить член ничего не стоит. **Конверт не шифруется:** сервер обязан читать тип и состояние, иначе не может ни проверить переход, ни построить напоминание, ни показать «ждём подтверждения» — чувствительное остаётся в зашифрованном `text`. **Бэкфилл обязателен, и он и есть смысл миграции `0050`:** без него все ранее расшаренные адреса отрисовались бы сырым текстом в момент переключения фронта, причём в старых сделках, которые никто не переоткрывает, чтобы это заметить. Текст при этом не тронут — адрес по-прежнему в нём, переехало только распознавание | `backend/app/models/deal.py`, `backend/app/core/cards.py`, `backend/app/api/dealvault.py`, `backend/alembic/versions/0050_vault_card_typing.py`, `frontend/src/components/AddressCard.tsx` |

<a id="L201"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Роль подтверждающего проверяется на сервере, скрытая кнопка — украшение (T3.34, 2026-08-16).** `POST …/messages/{id}/ack` сверяет роль вызывающего с `requires_ack_by` и отвечает 403 чужой стороне, 409 на повторный ответ, 422 на не-карточку и на тип, которого нет в каталоге. Последнее — про строку, записанную версией, знавшей тип, которого эта не знает: отказ честнее догадки о том, что карточка значила. Отказ от подтверждения — самостоятельный исход (`declined`), а не отсутствие согласия: запись обязана показывать, что кто-то сказал «нет» | `backend/app/api/dealvault.py`, `backend/tests/test_vault_cards.py` |

<a id="L202"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Четыре группы карточек — одна кодовая дорога (T3.36–T3.39, 2026-08-16).** Логистика, владение грузом, расчёты и исключения могли стать четырьмя модулями, и каждый повторил бы одни и те же четыре проверки: сторона ли вызывающий, вправе ли эта роль поднять карточку, кто должен ответить, что меняет согласие. Вместо этого каждая карточка объявляет всё четыре в `CATALOGUE`, а единственный endpoint читает объявление — инварианты §6.9.5 живут в одном месте, и новый тип карточки это строка, а не файл. Фронт устроен так же: `lib/cardForms.ts` объявляет поля, форма рисуется по объявлению. Десять рукописных форм разошлись бы с десятью pydantic-моделями за релиз | `backend/app/core/cards.py`, `backend/app/api/cards.py`, `backend/app/schemas/cards.py`, `frontend/src/lib/cardForms.ts`, `frontend/src/components/{CardActions,DealCard}.tsx` |

<a id="L203"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Доказательство проверяется при подтверждении, а не при создании (T3.37, 2026-08-16).** Вложения в продукте грузятся к **уже существующему** сообщению (`POST …/messages/{id}/attachments`), поэтому требовать фото в момент создания карточки значило бы ломать существующий поток загрузки. Порядок такой: карточка создаётся, автор прикладывает фото, вторая сторона подтверждает; без фото подтверждение отдаёт 422. В UI у этого своё состояние — заявка без фото выглядит законченной автору и не может быть подтверждена никем. Проверка роли идёт **раньше** проверки вложения: сначала «тебе ли отвечать», потом «есть ли чем» | `backend/app/api/cards.py`, `backend/app/api/dealvault.py`, `frontend/src/components/DealCard.tsx` |

<a id="L204"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Передача после вылета — это другой рейс (T3.37, 2026-08-16).** `handoff.declared` после `Trip.depart_at` отклоняется с 409 и порождает `terms.reconfirm_requested`. Альтернатива — молча зафиксировать сегодняшний курс на условиях недельной давности — подменила бы согласованную сделку другой. Автопродления нет намеренно: продлевать молча значит решать за двоих | `backend/app/api/cards.py`, `backend/tests/test_cards_flow.py` |

<a id="L205"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Сьют, который запускают руками, умирает молча (T_TEST.8, 2026-08-22).** `registerUser` заполнял три поля на `/register`. T3.28 свёл вход и регистрацию к одному полю с кодом и увёл `/register` редиректом на `/login` — с 10 августа хелпер падает по таймауту, а вместе с ним **все десять браузерных спек**, не только a11y. Двенадцать дней сьют выглядел живым ровно потому, что его никто не запускал: в CI его нет, а руками он гоняется под конкретную задачу. Тот же силуэт, что у Telegram-канала без токена (T3.8) и у веток деградации из `T_TEST.11` — **механизм, который считается рабочим, потому что его никто не исполнял.** Отдельное следствие для планирования: e2e не может завести аккаунт вовсе, поскольку `POST /auth/register` снят, а код с почты тесту недоступен; `E2E_AUTO_VERIFY_EMAIL_DOMAINS` появился до T3.28 и снимает только подтверждение почты, не код входа | `frontend/e2e/helpers.ts`, `frontend/src/App.tsx`, `backend/app/api/auth.py`, `backend/app/core/config.py` |

<a id="L206"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Пустой `baseURL` делает забытый префикс не ошибкой, а неверным ответом (T_UX.14, 2026-08-16).** У axios-клиента `baseURL` пуст, поэтому путь без `/api` — валидный запрос на собственный origin: nginx отвечает `index.html`, вызывающий получает страницу HTML там, где ждал JSON, и экран умирает на `x.filter is not a function` тремя слоями дальше от пропущенных четырёх символов. Компонентные тесты этого не ловят **по устройству**: они мокают модуль API, и путь в них не исполняется — тридцать зелёных тестов при полностью нерабочем экране. Отсюда `src/test/apiPaths.test.ts`: читает исходники `src/api/*.ts` и требует префикс у каждого вызова. Тот же принцип, что у IDOR-матрицы — новый эндпоинт валит файл самим фактом неверного написания | `frontend/src/api/client.ts`, `frontend/src/test/apiPaths.test.ts` |

<a id="L207"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Тот же `tsc` проверяет тесты и собирает браузерный бандл (T_UX.14, 2026-08-16).** Страж выше сначала читал исходники через `node:fs` и `__dirname` — и уронил продакшн-сборку: Node-типов в конфигурации нет, а `npm run build` начинается с `tsc`. Тест, написанный ради предотвращения поломки, сам стал поломкой хуже той. Читать файлы в тестах — только через `import.meta.glob(..., { query: '?raw', eager: true })`: Vite подставляет содержимое на этапе сборки, Node-API не нужен | `frontend/src/test/apiPaths.test.ts`, `frontend/tsconfig.json` |

<a id="L208"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Выдача рейсов — доска, а не история (T_UX.19, 2026-08-17).** `GET /api/trips` фильтрует по `status == open`, и это правильно для доски. Но «мои опубликованные рейсы» в профиле — история: она обязана содержать снятые и улетевшие, иначе собственная запись перевозчика тихо переписывает себя. Параметр `status` добавлен с ограничением: **любой статус кроме `open` спрашивается только про себя** — снятый рейс уже не публичное объявление, и позволить посторонним их перечислять значило бы опубликовать чужие изменения планов. Та же ловушка второй раз: панель просила рейсы без статуса и молча теряла сматченные | `backend/app/api/trips.py`, `frontend/src/components/PublishedTripsSection.tsx`, `frontend/src/pages/DashboardPage.tsx` |

<a id="L209"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Отменять, а не удалять (T_UX.19, 2026-08-17).** Снятие рейса переводит его в `cancelled`, а не удаляет строку: по рейсу могла быть открыта переписка, и удаление оставило бы тред указывающим в пустоту — отменённый рейс хотя бы объясняет себя. Снять можно только `open`: сматченный это уже чужой план, и тихая отмена отменяет чужую доставку не спросив | `backend/app/api/trips.py`, `backend/tests/test_trip_cancel.py` |

<a id="L210"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Тест, верный только в момент написания, — это будущее падение (T3.34→T3.36, 2026-08-16).** `test_only_declared_kinds_are_implemented` утверждал «реализован ровно один тип» и упал, как только каталог вырос, — он не поймал ошибку, он устарел. Заменён на два утверждения, переживающие рост: реализованный тип обязан кем-то производиться (роль, парная эмиссия или отдельный endpoint), и создаваемый тип обязан быть помечен реализованным. Из той же серии — два теста `ack` на `handoff.declared`, которая обзавелась обязательным фото | `backend/tests/test_vault_cards.py` |

<a id="L211"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Согласие на условия идёт через тот же `ack`, что и любая карточка (T3.35, 2026-08-16).** Второй эндпоинт «принять договор» дал бы клиенту два жеста для одного действия и позволил бы предложению оказаться принятым без снимка. Вместо этого `ack` на `terms.proposed`/`terms.countered` вызывает `agree_from_proposal`, который создаёт `terms.agreed` — неизменяемый снимок, копирующий payload и **штампующий версию параметров платформы, действовавшую на момент согласия**. Это и есть механика «правка ставки не действует задним числом» из MASTERPLAN §4.1. Контр-предложение **вытесняет** (`supersedes`), а не правит: «что предлагали до этого» — вопрос, который задаёт арбитр | `backend/app/api/terms.py`, `backend/app/api/dealvault.py`, `backend/tests/test_terms.py` |

<a id="L212"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Нормализация условий считается на сервере, потому что иначе две точки входа разойдутся (T3.35, 2026-08-16).** Перевозчик задаёт базу полями рейса, отправитель предлагает своё в чате по рейсу — и оба предложения приводятся к одному виду: коридор уровня стран, расстояние, оплачиваемый вес `max(факт, объёмный)`, цена за кг и за км. Делитель объёмного веса берётся из `PlatformParameter` **по коридору**, так что админка управляет расчётом без деплоя. Расстояние — уже существовавший `airports.route_distance_km`, добавлен только `corridor_of`. Незнакомая пара IATA даёт `None`, а не исключение: у сидовых и вручную созданных рейсов кода может не быть, и падать посреди согласования условий — худший способ это сообщить | `backend/app/core/terms.py`, `backend/app/core/airports.py`, `backend/app/models/marketplace.py`, `backend/alembic/versions/0051_trip_terms_fields.py` |

<a id="L213"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **Ставки уехали из констант в версионируемую таблицу с областью действия (T3.40, 2026-08-16).** Пока их было три, константы были терпимы; сейчас их шестнадцать, и каждая — деньги пользователей. Правка **не редактирует строку**, а пишет новую версию с `effective_from`, поэтому «сколько стоило в тот день» отвечается из таблицы, а не из истории git. Область — `global` либо коридор: минимальный залог на ОАЭ↔США и на внутриевропейском маршруте не может быть одним числом, разрешение идёт коридор-первым. **Дефолты живут в коде (`core/params.py`), а не в сидах:** пустая таблица означает работу на числах MASTERPLAN §4.1, а не на nulls, и экран показывает `source`, чтобы оператор видел, что значение никем не выбрано — иначе заглушка читается как решение. Право `PARAMS_MANAGE` намеренно вне набора арбитра: разрешать спор и назначать ставку для всех — разные полномочия | `backend/app/models/platform_params.py`, `backend/app/core/params.py`, `backend/app/api/platform_params.py`, `backend/alembic/versions/0049_platform_parameters.py`, `frontend/src/pages/AdminParamsPage.tsx` |

<a id="L214"></a>

| Механика | Где реализовано (файлы) |
|---|---|
| **`op.add_column` не создаёт enum-тип — это делает только `create_table` (T3.34, 2026-08-16, найдено прогоном на сервере).** Миграция `0050` с `sa.Enum(...)` внутри `add_column` упала на `type "cardstate" does not exist`, тогда как `0049` прошла, потому что там таблица создаётся целиком. Правильная форма — `postgresql.ENUM(..., create_type=False)` плюс явный `.create(bind, checkfirst=True)` в начале `upgrade()`. База не пострадала: PostgreSQL откатил DDL транзакционно, версия осталась на 0049 | `backend/alembic/versions/0050_vault_card_typing.py` |
