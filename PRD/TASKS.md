# Vimana — Sacred Logistics · TASKS.md

> NAV ──────────────────────────────
> ↑ старший (читать ДО, при конфликте — он прав): IMPLEMENTATIONPLAN.md
> ↓ младший (читать ПОСЛЕ, определяется этим файлом): — лист
> Поток: PLANNING — пишется старший→младший · UPDATES — правится по минимальному подъёму
> ───────────────────────────────────

## Executable Backlog · Single Source of Execution

> ⚠️
> - Этот файл — **единственный**, который меняется в процессе работы.
> - Задачи выполняются **строго по порядку** (сверху вниз).
> - Нельзя перескакивать фазы из IMPLEMENTATIONPLAN.md.
> - Если задачи нет здесь — её не существует.
> - После выполнения — обновить статус здесь и зафиксировать в TECHSTATE.md / CHANGELOG.md (если применимо).
> - **Тесты (ОБЯЗАТЕЛЬНО после T_TEST.1) — 100% покрытие новой функциональности:** каждая задача обязана закрываться только после того, как:
>   1. **Все новые endpoints backend** покрыты тестами (positive + negative кейсы).
>   2. **Все новые ветки в существующих endpoints** покрыты (например, новое поле в PATCH — отдельный тест).
>   3. Весь backend-сьют проходит зелёным: `docker compose -f docker-compose.dev.yml exec -w /app backend pytest -v`.
>   4. Frontend-сборка не падает: `docker compose exec frontend npm run build` (если менялся frontend).
>   Проактивно предлагать недостающие тесты, не ждать напоминания. Правила изоляции — ENVIRONMENT.md §8. До завершения T_TEST.1 правило не применяется. Frontend-компоненты будут покрыты в T_TEST.2.
> - **Версия:** `frontend/src/version.ts` следует **фазе**, а не счёту задач — `0.{фаза:02}.{часть фазы}`, сейчас `0.03.7` (см. CLAUDE.md). Внутри одной фазы обычно не меняется.

---


> **Архивы закрытых задач** (для экономии контекста; ротация по PROJECT.md §6.4):
> - [TASKS_ARCHIVE_01.md](TASKS_ARCHIVE_01.md) — T0.1 … T3.5 pt.2 (Фазы 0–3.5, все ✅ MVP)
>
> **Стабы для закрытых задач с открытыми зависимостями:**
> - **T2.1** ✅ MVP (archived) — Peer Identity Verification. Child `T2.1 pt.3` ✅ MVP (decline_polite copy — см. ниже + T_UX.1 UI). Открытых child'ов нет.
> - **T2.2 pt.1** ✅ (archived) — Custodial keypair + management UI. **T2.2 pt.2** ✅ NIP-01 signing (archived).
> - **T2.3** ✅ MVP (archived) — Threshold 2-of-3 e2e для DealVault. Follow-ups в архиве (Inquiry чат, attachments chunk-encrypt, NIP-44 v2).
> - **T2.4** ✅ MVP (archived) — Trust Graph (WoT). Follow-ups: Redis-кэш BFS, UserBadge на TripCard, hourly Celery counters.
> - **T3.1** ✅ MVP (archived) — УБА (формула + Celery + endpoints + UI).
> - **T3.2** ✅ MVP (archived) — Оператор-арбитр + OperatorAccessGrant + UBA-chip. Escrow разблокировка → T5.x.
> - **T3.3** ✅ MVP (archived) — Recipient role + custodial keypair + server-mediated decrypt. Follow-up: UI kick-out, encryptE2E авто-recipients.
> - **T3.5 pt.1 + pt.2** ✅ MVP (archived) — Nostr publish bridge + strfry + NIP-42/WoT-gate + metrics + republish. Follow-up: **D-TRANSLATION** мультиязычный перевод описаний рейсов (pt.3).

### T3.6 — Tamper-evident hash chain over DealEvents + Nostr anchoring ✅ MVP

**Контекст.** До T3.6 `DealEvent` нёс `nostr_sig`/`nostr_event_id` (T2.2 pt.2) — подпись авторства одной записи. Она **не** доказывает три вещи, критичных для арбитража: (a) **completeness** — сервер мог не записать «неудобное» событие; (b) **ordering** — строки можно поменять местами; (c) **non-deletion** — удалённая строка не оставляет следа. Арбитраж опирается на DealVault как evidence layer, значит эти три бреши закрывать обязательно.

**Threat model (важно, документировано в module docstring):** цепь защищает от того, у кого есть **write** к БД (компрометированный backup, SQL-инъекция, rogue DBA, restore из подделанного дампа). Она **не** защищает от самой платформы — `seq` присваиваем мы, теоретически можем пересчитать всю цепь. Именно поэтому Vimana одновременно арбитр (`ARBITER_USER_ID` + серверный nsec) **и** записчик — а это ровно та конфигурация, где «наш log сходится, мы проверили» проигрывающая сторона обязана оспорить. Пробел закрывается **за пределами** модуля `deal_chain`: `chain_anchor` периодически публикует head'ы в **третьесторонние** relay'и подписанные отдельным platform anchor ключом.

**Модель + миграция:**

- [x] Миграция `0025_deal_event_chain`: `deal_events` расширен `seq BIGINT NOT NULL`, `entry_hash BYTEA(32) NOT NULL`, `prev_hash BYTEA(32) NULL` + `UNIQUE(deal_id, seq)`. Backfill существующих строк в порядке `(timestamp, id)` per deal. **Backfill не ретроактивно tamper-evident** — до миграции строки не хешировались; backfill нужен только чтобы NOT NULL встал и новые entries было к чему цеплять.
- [x] Новая таблица `deal_chain_anchors(id, deal_id, seq, entry_hash, nostr_event_id, nostr_pubkey, relays JSON, created_at)` + `UNIQUE(deal_id, seq)`. Одна строка на успешно опубликованный head.

**`app/core/deal_chain.py` (316 строк):**

- [x] `compute_entry_hash(deal_id, seq, timestamp, event_type, actor_id?, nostr_event_id?, payload, prev_hash)` — детерминистичный sha256 preimage: **fixed field order** (изменение → invalidate всех chain'ов; новая колонка + versioned migration, не edit). `deal_id` идёт первым (scope binding — entry из A нельзя переместить в B). Presence-байты `\x01`/`\x00` перед optional полями (иначе `actor_id=None` хешится как zero-UUID actor). `nostr_event_id` внутри preimage'а — связывает per-record signature с chain'ом (swap подписи → invalidate от entry дальше).
- [x] `canonical_json(payload)` — `sort_keys=True, separators=(',',':')`. **Raises `ChainError`** на non-serializable payload — никогда не подставляет `{}` вместо реального значения.
- [x] `append_deal_event(db, deal_id, event_type, actor, payload, nostr_sig?)` — единственный правильный путь создания `DealEvent`. Берёт transaction-scoped `pg_advisory_xact_lock` на deal → читает head → computes hash → INSERT. Advisory lock релизится и на commit и на rollback — нет explicit unlock который забудут.
- [x] `DealEvent` конструктор + `db.add()` напрямую → падает на flush (NOT NULL по `seq`/`entry_hash`). Это deliberate: unchained event = дыра в записях, IntegrityError громче silent gap.
- [x] `verify_chain(db, deal_id)` — recomputes все hash'и от genesis до head, ловит tamper.

**`app/core/chain_anchor.py` (208 строк):**

- [x] `anchor_pending_heads(db)` — обходит deals, у которых head опережает последний anchor. Один event per deal (не per entry — head покрывает всё под собой).
- [x] Публикует NIP-01 event (kind 30453 vimana-chain-anchor, tags `[["d", deal_id], ["seq", str], ["h", hex(entry_hash)], ["t", "vimana"], ["t", "chain-anchor"]]`, content = canonical_json({...}) с полями).
- [x] Signed через `CHAIN_ANCHOR_NSEC` env (64-hex). **Отдельный ключ** от `ARBITER_USER_ID` и от per-user keypairs — anchor это statement платформы о своих записях, mixing bluring кто про что attest'ит.
- [x] `DealChainAnchor` row записывается только если ≥1 relay accepted. Failed publish → нет строки → следующий tick retry того же head.
- [x] Evidential weight: `NOSTR_FRIENDLY_RELAYS` (третьесторонние), не `NOSTR_OWN_RELAY_URL` — своим strfry можно управлять, значит anchor только там ничего не доказывает. `DealChainAnchor.relays` = per-URL result map для аудита.

**`app/tasks/chain_anchor.py`**: Celery beat, hourly. `CHAIN_ANCHOR_NSEC` unset ИЛИ `NOSTR_PUBLISH_ENABLED=false` → no-op.

**Callsites переписаны на `append_deal_event`** (`api/{admin,deals,threshold}.py`) — раньше делали `db.add(DealEvent(...))` напрямую, теперь бы упало на NOT NULL. Тесты покрывают что во всех трёх code paths chain продолжается.

**Тесты (`test_deal_chain.py`, 39):** hash determinism, canonical JSON (sorted+stripped/None), naive timestamp treated as UTC, non-serializable payload raises (not silent `{}`), presence-byte (None vs zero-UUID vs empty string), genesis GENESIS для первой записи, first entry starts at seq=1 с prev=None, second links to first, two appends в одной транзакции chain'ятся корректно, chains independent per deal, verify empty chain OK, verify intact, edited payload detected, deleted middle entry detected, rewritten prev_hash detected, reordered entries detected, advisory lock защищает от concurrent append race, `append_deal_event` возвращает entry с corectным `seq`/`hash`, каждый admin/threshold/deals callsite chain'ится, anchor tick публикует head, anchor row только при ≥1 relay accepted, failed publish → следующий tick retry, `CHAIN_ANCHOR_NSEC` unset → no-op, `NOSTR_PUBLISH_ENABLED=false` → no-op.

**Acceptance:**
1. Любой INSERT в `deal_events` через `append_deal_event` получает `seq = max(prev_seq)+1`, `entry_hash = sha256(preimage)`, `prev_hash = head.entry_hash`.
2. `verify_chain(deal_id)` возвращает `True` для intact chain'а и `False` c detail'ом первой сломанной entry для tamper.
3. При `CHAIN_ANCHOR_NSEC` + `NOSTR_PUBLISH_ENABLED=true` hourly task публикует все head'ы, изменившиеся с прошлого anchor.
4. Прямое `db.add(DealEvent(...))` вне `append_deal_event` падает на flush — контракт вшит в схему, не в code review.
5. **39 backend-тестов зелёные.**

**Follow-up:**
1. **Публичный endpoint verification** — `GET /api/deals/{id}/chain-proof` для арбитра/проигравшей стороны: отдаёт последние N entries + anchor CID/event_id для independent verification.
2. **UI badge** на DealPage: «Chain verified · anchored at 2026-07-24 · 3 external relays». Клик → детальная страница с preimage'ами.
3. **Merkle-tree аггрегация** через N deals для дешёвого anchoring (сейчас 1 event per deal per tick — не масштабируется на >1000 active deals/hour). Anchor одного merkle-root вместо N head'ов.
4. **Chain для DealVault messages** аналогично DealEvent — сейчас vault-сообщения только signed, не chained.
5. **Rotation** для `CHAIN_ANCHOR_NSEC` — при компрометации ключа нужен overlap period с двумя ключами.

## 🔗 ЭТАП 3.6 — DealVault Protocol: полнота цепи + Identity-пересечение (Фаза 3.6)

> **Контекст этапа.** Концепция «DealVault — The Verifiable Vault Protocol» (v0.1): каждая сделка — переносимый, иммутабельный, криптографически проверяемый артефакт; Identity Vault и Deal Vault пересекаются по данным. T3.6 построил tamper-evident цепь, но только по статусным событиям. Этот этап делает цепь **полной** (сообщения, файлы, identity-события), вводит **запечатывание** при закрытии сделки и валидацию содержимого файлов. Публикация якорей остаётся выключенной (server-only, внутренняя связность) — расширение заложено схемой: Nostr (готово, T3.6), IPFS, OpenTimestamps. Решения зафиксированы в TECHSTATE `D-DVLT-PROTOCOL`. Отложено осознанно (follow-up этапа, не задачи): Query API (`TRUE/FALSE/ACCESS_DENIED` + proof), .dvlt-экспорт + Reader, включение публикации якорей, EXP-07 (участник-подписанный `prev`).

### T3.7 — Полнота цепи: сообщения, файлы, seal ✅ MVP

**Контекст.** Цепь T3.6 покрывает 11 статусных событий. `DealVaultMessage`/`Attachment` подписаны (T2.2 pt.2), но не chained: удаление сообщения из БД не ломает `verify_chain` → completeness не гарантирована именно для контента, ради которого vault существует. Сделка не запечатывается — append возможен после `closed`. Это follow-up 4 из T3.6, повышенный до задачи.

**Механика хеширования содержимого:** хешируются байты **как они хранятся** — `sha256(text_ciphertext || text_nonce)`. Для E2E-сообщений это ciphertext (сервер plaintext не видит, верификация не требует расшифровки). Следствие: ротация `MESSAGE_ENCRYPTION_KEY` с перешифровкой данных запрещена — только envelope-схема (перешифровка ключа, не данных). Формат preimage T3.6 **не меняется** — новые данные входят через `payload` обычных `DealEvent`.

- [x] Новые `DealEventType`: `message_added`, `file_added`, `sealed`, `identity_ref` (используется в T3.9). Миграция `0026` + зеркало в tests/conftest.
- [x] `message_added` — в той же транзакции, что INSERT сообщения: `append_deal_event(actor=автор, payload={message_id, content_hash: sha256(text_ciphertext+text_nonce), msg_event_id: nostr_event_id сообщения, is_e2e})`. Покрыты все точки создания сообщений: обычные, e2e, share-address, pinned route-note (match), arbiter system-message.
- [x] `file_added` — payload `{attachment_id, message_id, file_hash, kind, size_bytes, mime}`.
- [x] `sealed` — при `confirm` (→ closed): финальное событие, payload `{message_count, file_count}` + `Deal.sealed_at`.
- [x] Запрет append после seal: guard в `append_deal_event` под advisory lock → `SealedError` (API → 409). **Решено (D-SEAL-SEMANTICS):** спор после `closed` возможен — `dispute_opened` распечатывает vault (единственное content-исключение), закрывающий вердикт (`closes_deal`) запечатывает снова; `arbiter_opened` проходит через seal как audit-событие (аудит ≠ контент), system-message при чтении sealed vault арбитром не пишется.
- [x] Миграция: `deal_chain_anchors.backend VARCHAR(16) NOT NULL DEFAULT 'nostr'` (`nostr` | `ipfs` | `ots`). Код IPFS/OTS-бэкендов не писали — только схема.
- [x] **Отклонение:** вместо нового `/vault/verify` расширен уже существовавший `GET /api/deals/{id}/chain` (follow-up 1 из T3.6 оказался частично закрыт в T3.6): + `sealed_at`, coverage `{chained/total messages, chained/total files}`, `content_ok`, `content_mismatches` (новая `verify_content()` — сверка content_hash/file_hash цепи с хранимым контентом).
- [x] Старые сделки: backfill не делаем — цепь валидна, coverage честно показывает долю незачейненных сообщений.
- [x] Тесты: 9 новых (`test_vault_completeness.py`) + обновлён lifecycle-тест T3.6 (6 событий вместо 5). Грабля: `deal.sealed_at` читать ДО `verify_chain`/`verify_content` — они делают `expire_all()` → MissingGreenlet.

**Acceptance: ✅ все выполнены**
1. ✅ Отправка сообщения/загрузка файла создаёт chained-событие в той же транзакции; подмена `text_ciphertext` или файла детектируется через content_hash/file_hash.
2. ✅ Deal → `closed` порождает `sealed`; append после → 409 на всех поверхностях (messages, attachments, events, share-address).
3. ✅ `GET /deals/{id}/chain` отдаёт `ok`/`broken_at` + seal + coverage + content-проверку.
4. ✅ **657 backend-тестов зелёные** (2026-07-25).

### T3.8 — Валидация содержимого файлов (anti-dirt) ✅ MVP

**Контекст.** MIME берётся из заголовка клиента + whitelist (T1.19) — подделывается тривиально. Защиты от залива «грязи» (исполняемые, полиглоты, не-изображения под именем .jpg) нет.

- [x] **Отклонение:** вместо puremagic — рукописный whitelist сигнатур в `core/file_validation.py` (6 типов, ноль новых зависимостей, строже: контент обязан нести magic bytes заявленного типа — MZ/ELF/shebang/HTML отваливаются автоматически, не перечислением).
- [x] Изображения: Pillow `verify()` + полный decode (`load()`) — невалидный JPEG/PNG/WebP → 422. HEIC/HEIF — по сигнатуре `ftyp`+brand (Pillow без кодека, pillow-heif не тянем). Pillow закреплён `10.4.0` (weasyprint 63 требует <11).
- [x] PDF: проверка заголовка `%PDF-`.
- [x] Стриминговый SHA-256 и лимит 10 MB сохранены; валидация до записи в R2.
- [x] Отклонённые загрузки логируются warning'ом (метаданные, не содержимое).
- [x] **Бонус сверх плана:** та же валидация на avatar-upload и на verification-документы (`validate_document()` — заявленный MIME не доверяется вообще, тип сниффится из байтов; было: ноль проверок перед `IdentityContainer`). Пригодится в T3.9.
- [x] Тесты: 20+ в `test_file_validation.py` (юниты сигнатур/декода/сниффинга + API-wiring на 4 поверхности). **Валидатор на первом прогоне поймал битый PNG_1X1 в самих тестах** (рукодельный hex с неверным CRC IDAT жил с T1.19) — фикстура теперь генерируется программно, CRC корректны by construction.
- [x] Follow-up закрыт 2026-08-02: ClamAV. Скан идёт **первым** в `validate_upload`, до структурных проверок — заражённый файл отвергается независимо от того, является ли он вдобавок корректным JPEG. Протокол `INSTREAM` сырым сокетом, без зависимости: единственный пакет на PyPI (`clamd`) последний раз выходил в 2018, и брать неподдерживаемую зависимость ради двадцати строк в **средстве защиты** — неверный размен.
- [x] **Fail-open с очередью (решение владельца 2026-08-02).** Первая реализация была fail-closed: недоступный сканер отвергал загрузку. Владелец заменил — простой сканера должен стоить отложенной проверки, а не сломанного продукта. Отсюда `attachments.scan_status ∈ pending | clean | infected` (миграция `0040`) и почасовая задача `tasks.malware_rescan`, разбирающая очередь по 100 файлов. **`pending` покрывает и «сканер не настроен», и «сканер не ответил» намеренно:** со стороны читателя это одно и то же — никто не смотрел, — а отдельное слово для одного из них приглашало бы считать его безопаснее.
- [x] **Цена размена названа вслух и в коде, и в `.env.example`:** между загрузкой и отложенной проверкой файл доступен контрагенту непроверенным. `pending` никогда не синоним «безопасно», и ни один экран не имеет права рисовать его так.
- [x] Уведомление админам двумя способами (решение владельца): Telegram с дросселем в час (без него падение сканера при активной загрузке даст сотню сообщений, и канал замьютят — а следующее настоящее предупреждение никто не прочтёт) и постоянный счётчик `GET /api/admin/scan-queue`. Алерт — это момент, счётчик — состояние; растущая очередь важнее любого отдельного сбоя.
- [x] Найденная позже зараза: **пометить и уведомить, скачивание не трогать** (решение владельца). Решает человек. Файл из R2 **не удаляется** — он часть цепи доказательств, и `verify_content` сверяет хеш хранимых байтов: исчезнувший файл стал бы неотличим от подмены.
- [x] Экран очереди в админ-панели профиля (только суперпользователь — эндпоинт за `USERS_MANAGE`, спрашивать арбитром значило бы потратить запрос ради 403). Подпись под числами повторяется каждый раз намеренно: «ждут проверки» приглашает прочесть как «проверяются», тогда как файлы уже лежат в сейфах и доступны участникам.
- [x] **Попутно найден баг локалей:** в `ru.json` и `en.json` было **два** блока `"admin"`, и по правилам JSON первый молча терялся — вместе с `panelTitle`, `disputes`, `usersDesc` и остальными подписями админ-панели, которые из-за этого рендерились сырыми ключами. В остальных четырёх локалях блок один. Слито в один, ключи сохранены.

**Acceptance: ✅ все выполнены** — exe/скрипт под именем `.jpg` → 422 на всех поверхностях; валидные jpeg/png/webp/heic/pdf проходят; **696 backend-тестов зелёные** (2026-07-25).

### T3.9 — Identity ↔ Deal пересечение (identity_ref + копия документа в сделке) ✅ MVP

**Контекст + решение владельца (D-DVLT-PROTOCOL).** Данные identity, внесённые в сделку, живут в обоих vault'ах: канонический документ — в `IdentityContainer` владельца (переиспользуемый, уже так с T2.1), **полная копия — в сделке** (vault самодостаточен), связь — событие `identity_ref` в цепи через общий `doc_hash`.

- [x] `AttachmentKind.identity_doc` (миграция `0027` + зеркало в conftest); копия — Attachment на system-сообщении, R2, `file_hash` = sha256 plaintext-байтов. Доступ — участники сделки; канонический контейнер — только владелец. Через generic-endpoint kind не загружается (нет в MIME-whitelist → 415).
- [x] `identity_ref`: payload `{container_id, attachment_id, badge_id, doc_hash, doc_type, doc_country}` — тройное совпадение `doc_hash` (цепь == копия == контейнер) проверяется расширенным `verify_content()` (reasons: identity copy/container hash mismatch, missing).
- [x] `submit-document` (T2.1): контейнер + бейдж + копия + system-message + 3 события цепи (`message_added`/`file_added`/`identity_ref`) — одна транзакция (`_copy_document_into_vault`). `_read_upload` теперь возвращает `(bytes, detected_mime)` из T3.8-сниффинга. Sealed deal → 409. Self-upload вне сделки — без изменений (нет событий цепи).
- [x] UI: system-message «🪪 Identity document verified and added to the vault» + label «Документ личности» для kind.
- [x] Тесты: 6 в `test_identity_ref.py` (тройной hash-match, подмена копии — двойная детекция через file_added+identity_ref, удаление контейнера, self-upload без событий, generic-upload → 415).

**Acceptance: ✅ все выполнены** — IdentityContainer + Attachment-копия + `identity_ref` в цепи, `doc_hash` совпадает во всех трёх местах, подмена детектируется `GET /deals/{id}/chain`. **702 backend-теста зелёные** (2026-07-25).

### T3.10 — DealVault: маркетинговая презентация + лендинг ✅ MVP (2026-08-02)

**Контекст.** Сразу после закрытия T3.9 — публичная упаковка концепции «Verifiable Vault Protocol»: преза + секция лендинга на базе Concept v0.1 и реализованного (полная цепь, seal, identity-пересечение, якоря).

**Процесс (решение владельца, 2026-07-25):** это будет **версия 1**. Затем владелец подключит другие инструменты/скиллы, будет собрана альтернативная версия — и обе сравниваются перед публикацией. v1 не публикуется как финал без сравнения.

- [x] Презентация концепции — `marketing/dealvault-protocol-concept.html` (2026-08-02). Структура из постановки соблюдена: задача доверия → Vault ≠ база данных → три уровня фиксации (цепь/подпись/якорь) → формат `.dvlt` и читатель → **границы** → состояние и дорожная карта. **Предмет отличается от трёх колод в `marketing/`**: v1, v2a и v2b — питчи маркетплейса, здесь описан механизм. Раздел «Границы» сделан полноценной секцией, а не сноской: протокол, чьи пределы не названы, обещает больше, чем делает. Дорожная карта из постановки **устарела и исправлена по факту** — `.dvlt` и читатель сделаны (`T3.21`, `T3.24`), отложенными остались выгрузка **сделки**, Query API и якоря OpenTimestamps. Лежит в `marketing/`, не отдаётся; публикация — отдельным решением.
- [x] Страница на лендинге (DESIGNGUIDELINES + Bento) — **две версии в репо для сравнения**, обе статические: `frontend/public/dealvault-v5-corporate.html` и `dealvault-v5-rebel.html` (выбор владельца 2026-07-26; v3b и v4b из `public/` сняты, исходники остаются в `marketing/`).
- [x] Тон: «Trust is derived from cryptographic evidence» — без обещаний невыключенных фич. **Закрыто 2026-08-02**, правки внесены в обе версии; ниже отмечено, что именно изменилось и почему часть формулировок стала возможна только сейчас.

**Сделано 2026-07-26 (страницы):**
- Обе версии лежат в `frontend/public/` и попадают в `dist/` штатным копированием Vite (`publicDir` по умолчанию) → отдаются nginx фронта как обычные файлы по `/dealvault-v5-corporate.html` и `/dealvault-v5-rebel.html`. Ни React-роут, ни правка `nginx/default.conf` не требуются: `location /` уже делает `try_files $uri /index.html`, существующий файл выигрывает.
- Страницы остаются **статическим HTML** (решение владельца) — не переносились в `LandingPage.tsx` и не разбирались на компоненты. Следствие: они не проходят через i18n (только RU) и не участвуют в SPA-навигации.
- CTA на каждой странице продублирован трижды (шапка, герой, финальная секция) и переключается разом: гость — «Получить инвайт» (шапка и герой скроллят к `#cta`, финальная ведёт на `/register`); при живой сессии все три — «Зайти в аккаунт» → `/dashboard`. Логика в `frontend/public/vimana-cta.js`, селектор `[data-auth-label]`.
- **Отклонение от постановки:** задача ставилась как «проверить по кукам», но auth-куки в проекте нет — JWT живёт в `localStorage['token']` и уходит заголовком `Authorization: Bearer` (`api/auth.py`, `stores/auth.ts`, `api/client.ts`). Скрипт сначала читает куки (`token` / `access_token` / `vimana_token`) на случай будущей cookie-сессии, затем `localStorage`. Протухший `exp` сессией не считается.
- **Ограничение CSP:** прод-CSP из T_SEC.2 — `script-src 'self'` без `'unsafe-inline'`. Инлайновый `<script>` на этих страницах будет молча заблокирован, поэтому вся JS-логика вынесена в отдельный same-origin файл. При любой доработке страниц инлайн-скрипты не добавлять.

**Расхождение по тону (обнаружено 2026-07-26, требует решения владельца).** Выставленные v5-версии содержат формулировки сильнее, чем позволяет реализация. Текст не правился — страницы взяты как есть по решению владельца; фиксируем расхождение, чтобы оно не уехало в прод молча:
- «Ни у нас, ни у арбитра, ни у владельца платформы нет ни кнопки, ни функции, ни задней двери… чтобы изменить то, что уже произошло» и блок «Не хотим. Не будем. Не можем.» (раздел 04) — цепь **tamper-evident, не tamper-proof**. Строку в БД изменить можно; ломается сходимость цепи. Прямо противоречит threat model из docstring'а `core/deal_chain.py` (T3.6): «цепь защищает от rogue-DBA/дампа, НЕ от самой платформы».
- «Выгрузка сделки · Скоро» (раздел 08) — `.dvlt` + Reader **отложены осознанно** (D-DVLT-PROTOCOL п.6), сроков нет. «Скоро» здесь — обещание невыключенной фичи.
- «Арбитр видит переписку» (раздел 06) — доступ требует активного `OperatorAccessGrant` (T3.2), не наступает автоматически по факту приглашения.
- Формулировка про якоря/проверку без нас на страницах отсутствует — здесь расхождения нет.

**Решение владельца 2026-08-01: вариант (а) — правим формулировки, страницы не переделываем.** Общее правило вынесено в `DESIGNGUIDELINES §9.1` (дисциплина утверждений) и действует на весь продукт, а не только на эти страницы. Работы отложены — ниже готовые замены, чтобы их оставалось только внести:

| Раздел v5 | Текущая формулировка | На что заменить |
|---|---|---|
| 04 | «Ни у нас, ни у арбитра, ни у владельца платформы нет ни кнопки, ни функции, ни задней двери… чтобы изменить то, что уже произошло» + блок «Не хотим. Не будем. Не можем.» | «Любое изменение записи обнаруживается — цепь перестаёт сходиться. Проверить это можно независимо от нас, по состоянию на дату последнего якоря.» Блок «Не можем» снять: он утверждает невозможность там, где механизм даёт обнаружимость |
| 08 | «Выгрузка сделки · Скоро» | «Выгрузка сделки» переносится в отдельный блок «Дорожная карта», визуально отделённый от работающего. Слово «скоро» не используется — срока нет |
| 06 | «Арбитр видит переписку» | «Арбитр получает доступ к сейфу, когда сторона его открывает. Каждый такой доступ записан в цепи и виден обеим сторонам» |

Дополнительно, раз лендинг будет трогаться после `D-KEY-TIERS`: про владение ключом на страницах не должно появиться «мы не имеем доступа» — пока копия у платформы, это неправда. Допустимая формулировка одна: «пока копия ключа у нас — мы можем подписывать от вашего имени; удалите её — не сможем».

~~Варианты: (а) поправить три места в v5 перед деплоем; (б) взять решение v4b; (в) принять риск и снять критерий тона.~~ Выбран (а).

**Расхождение по тону закрыто 2026-08-02.** Внесено в обе версии:

- Раздел 04 — снято «ни кнопки, ни функции, ни задней двери», «Не хотим. Не будем. Не можем.» и «Нет вообще». Последнее убрано, хотя в постановке не значилось: оно утверждает ровно ту же невозможность одним абзацем выше, и оставить его значило бы поправить формулировку, не поправив утверждение. Заменено на обнаружимость с указанием, **чем** она обеспечена. Заодно формулировка «проверить можно независимо от нас, по состоянию на дату последнего якоря» **стала правдой только 2026-08-01** — до включения якорей (T3.20) она была бы таким же превышением, как то, что мы правим.
- Раздел 08 — «Скоро» снято со **всех трёх** карточек, а не только с «Выгрузки сделки»: правило «срока нет — слова „скоро“ нет» одинаково относится к платежам и ячейке (Фазы 4 и 5), и оставить два «Скоро» рядом с одним исправленным читалось бы как недосмотр. Блок переименован в «Дорожная карта», заголовок «Три вещи, которые появятся ближайшими релизами» → «Чего здесь пока нет».
- Раздел 06 — «арбитр видит переписку» → «получает доступ, когда сторона его открывает; каждый доступ записан в цепи и виден обеим сторонам».
- Проверено грепом: формулировок про владение ключом («мы не имеем доступа» и подобных) на страницах нет — ограничение из `D-KEY-TIERS` не нарушено и добавлять нечего.

**Выбор версии закрыт 2026-08-02: ни corporate, ни rebel.** Владелец выбрал **средний вариант** — новый клиентский лендинг из `T_UX.7 pt.2`, построенный на общих токенах. Обе v5-страницы сняты из выдачи (`frontend/public/` → `marketing/…-deployed.html`), вместе с `vimana-cta.js`, который существовал только ради них. Перенесены, а не удалены: в `public/` лежали версии **с правками тона от 2026-08-02**, а в `marketing/` — оригиналы от 25 июля; удаление стёрло бы исправленный текст. Freeze-лист на эти два адреса снят этим решением — до него они считались задеплоенными и неприкосновенными.

**Осталось для закрытия T3.10:** презентация концепции. Структура записана выше; берётся следующей задачей (решение владельца 2026-08-02).

**Acceptance:** преза согласована владельцем; лендинг-секция задеплоена; формулировки не заявляют невключённые механизмы как работающие. — *частично: страницы выставлены и отдаются, преза не сделана, критерий формулировок на v5 не выполнен.*

## 🔑 ЭТАП 3.7 — Идентичность и вход (Фаза 3.7)

> **Цель:** три способа входа поверх одной личности, где **личность и есть ключ**. Разделение уровней ответственности (решение владельца 2026-07-26):
>
> - **npub (Nostr-pubkey)** — личность. Неизменна на всём сроке жизни аккаунта. Отдельной сущности DID нет: `nostr_pubkey` и есть идентификатор, `did:`-обёртка не вводится.
> - **Passkey / Nostr-подпись / пароль** — способы **входа**. Сменяемы, множественны, к личности не приравниваются.
> - **JWT** — краткоживущая веб-сессия (уже есть, T_UX.3).
> - **YubiKey** — не отдельный механизм: WebAuthn-аутентификатор с аппаратной привязкой, используется как step-up.
>
> ```
>                   npub = личность
>                          │
>        ┌─────────────────┼─────────────────┬──────────────┐
>     iPhone            MacBook           YubiKey       nsec (NIP-07)
>     Passkey           Passkey           Passkey       подпись
>            ── способы доказать, что это ты ──
> ```
>
> Утеря устройства → отвязка его Passkey; npub и остальные способы входа не затронуты. **Утеря ключа → утеря личности** — это принято сознательно (см. решение №4).
>
> **⚠️ Пересмотр 2026-08-01 (`D-KEY-TIERS`).** Решения №4 и №6 ниже действуют не в исходном виде. Модель описывается вокруг личности, а не вокруг места хранения ключа: **четыре ступени** — (0) только аккаунт, серверный ключ · (1) Nostr-личность создана при регистрации, ключ у платформы · (2) копия скачана пользователем в **Identity Vault** (`.dvlt`, запечатан парольной фразой в браузере) · (3) копия на платформе удалена, подпись только в браузере. Вверх — независимость, вниз — удобство (загруженный `.dvlt` возвращает копию). Переход к самовладению **больше не требует нового ключа** (отмена №6); личность утрачивается только при двух потерях сразу (смягчение №4). Свою личность можно привязать сразу после регистрации. Подробности и обоснование — TECHSTATE `D-KEY-TIERS`, схема ступеней — IMPLEMENTATIONPLAN §3.7.1a.
>
> **⚠️ Пересмотр 2026-08-06 (`D-CONTACT-CHANNELS`).** Решение №1 ниже **отменено владельцем**: телефон возвращается в auth-путь как **подтверждаемый контакт**, наравне с почтой. Идентичностью он при этом не становится — личность по-прежнему npub, а телефон, как и почта, passkey и Nostr-подпись, лишь один из способов доказать, что это ты, и один из каналов доставки. Форма регистрации сводится к **одному полю** (почта или телефон) с выбором канала кода. Реализация — ЭТАП 3.8, обоснование — TECHSTATE `D-CONTACT-CHANNELS`, схема — IMPLEMENTATIONPLAN §6.8.
>
> **Решения владельца (2026-07-26):**
> 1. ~~Телефон убирается из регистрации и логина.~~ → **отменено 2026-08-06 (`D-CONTACT-CHANNELS`)**, см. выше. Колонка `User.phone` остаётся полем профиля, но перестаёт быть единственным местом хранения телефона: истина переезжает в `user_contacts` (T3.25).
> 2. Email **опционален**: аккаунт, созданный через Nostr-ключ или Passkey, живёт без email.
> 3. Подтверждение email **ничего не блокирует** (уточнено владельцем 2026-07-26 по итогам T3.11): это вопрос безопасности, а не прав. Неподтверждённый адрес не мешает ни войти, ни опубликовать рейс, ни начать сделку. Он ставит под вопрос сам канал — на этот адрес возвращается доступ к аккаунту и приходят уведомления по сделкам, и пока он не подтверждён, неизвестно, доходят ли письма до владельца аккаунта. Поднимает это UI (баннер + экран кода), а не API.
> 4. **Личность = ключ (self-sovereign identity).** Философия проекта ближе к Nostr и Bitcoin: «ключ и есть личность». Смена или потеря ключа означает **появление новой личности**, а не восстановление старой. Осознанный отказ от «неизменной личности при смене ключа» — концептуально чище и соответствует SSI, ценой того, что утрата ключа необратима.
> 5. **Инфраструктурный ключ ≠ личность.** Регистрация продолжает выдавать keypair, как сегодня (T2.2) — но это **служебный ключ сейфа**: им платформа шифрует и подписывает содержимое, он не показывается как «ваш Nostr-ключ» и никогда не публикуется наружу. Отдельной колонки не нужно: `key_self_custody = false` уже означает «это служебный ключ», `true` — «это личность пользователя».
> 6. **Личность создаётся при переходе — и всегда новым ключом.** `claim` в нынешнем виде убирается: он повышал служебный ключ до личности, а ключ, приватная часть которого всю жизнь лежала у платформы, суверенной личностью быть не может — доказать, что копия не осталась, невозможно. Переход (`establish identity`) даёт либо ключ, **сгенерированный в браузере пользователя** (сервер не видит nsec ни на миллисекунду), либо принесённый свой. Служебный ключ при этом уничтожается, сейфы перешифровываются на новый npub.
> 7. Восстановление по recovery-коду возвращает **доступ к аккаунту, но не ключ**. До self-custody у платформы есть всё — восстанавливается полностью. После: ключ жив (потеряно устройство) → восстанавливается всё; ключ утрачен → история и УБА **не переезжают**, чистый старт с новой личности, старый аккаунт остаётся мёртвым архивом.
> 8. Переход в self-custody **не блокируется**: экран последствий с обязательным чекбоксом, но платформа не мешает — уважение к суверенитету пользователя.
>
> **Два состояния аккаунта (следует из решений 4–7):**
>
> | | Служебный ключ (`key_self_custody = false`, дефолт) | Своя личность (`true`, после перехода / nostr-signup) |
> |---|---|---|
> | Чей ключ | платформы: nsec зашифрован и лежит у нас | пользователя: nsec у него, у нас его нет |
> | Что это для пользователя | ничего — механика шифрования сейфа, в UI как ключ не подаётся | его личность, публичный идентификатор |
> | Шифрование и подпись | служебным ключом, как сегодня | ключом пользователя |
> | Публикация рейсов | под `PLATFORM_PUBLISH_NSEC`, отобранные фильтром, имя перевозчика в содержимом | под ключом пользователя |
> | Потеря устройства | recovery-код → всё на месте | recovery-код → аккаунт и переписка, npub тот же |
> | Потеря **ключа** | невозможна — ключ не у пользователя | необратима: новая личность, старая переписка не читается никем |
>
> **Почему рейсы не публикуются служебным ключом.** Опубликованное событие остаётся в сторонних relay'ях навсегда. Если публиковать под служебным npub, который при переходе будет уничтожен, в сети останутся события от личности, более никому не принадлежащей — ровно та подмена, от которой уходит вся фаза.
>
> **Состояние прода на 2026-07-26** (проверено запросами): 36 аккаунтов, из них 24 со служебным ключом, 12 без; арбитр и superuser **без ключей** — из-за чего `threshold` 2-of-3 сейчас не собирается (`api/threshold.py:40` требует `arbiter.nostr_pubkey`). Подписанных записей 0, identity-контейнеров 0, E2E-сообщений 0, опубликованных рейсов 0. Легаси-случая «ключ уже стал личностью» **не существует** — все ключи ничем не связаны, поэтому особой ветки для старых аккаунтов не нужно, нужен бэкфилл (T3.12).

### T3.11 — Email-only регистрация + подтверждение кодом ✅ MVP

**Контекст.** Сейчас `POST /api/auth/register` принимает email **или** phone (`app/api/auth.py:26`), email никак не подтверждается, `User.password_hash` NOT NULL. Задача убирает phone из auth-путей и вводит подтверждение владения ящиком по 6-значному коду. SMTP уже настроен (`SMTP_HOST`/`SMTP_USER`/`SMTP_PASSWORD`/`SMTP_PORT`, ENVIRONMENT §секреты), `core/email.send_email()` работает с T1.7.

**Схема (миграция `0028_email_verification`):**
- [x] `users.password_hash` → **nullable** (аккаунты из T3.13/T3.14 живут без пароля).
- [x] `users.email_verified_at TIMESTAMPTZ NULL`, `email_verification_code_hash VARCHAR(255) NULL`, `email_verification_expires_at TIMESTAMPTZ NULL`, `email_verification_attempts SMALLINT NOT NULL DEFAULT 0`, `email_verification_sent_at TIMESTAMPTZ NULL`.
- [x] Backfill: существующим юзерам с email → `email_verified_at = created_at`. Живых пользователей не выкидываем в «неподтверждённые». На проде отработал: 13 из 13 аккаунтов с email помечены (2026-07-26).

**Backend:**
- [x] `UserCreate`: `phone` удалён, `email` обязателен + валидатор формы адреса и нормализация в нижний регистр. `UserUpdate.phone` **остаётся** — телефон правится в профиле.
- [x] `UserLogin.login` — только email; ветка по phone удалена. Дополнительно: `password_hash IS NULL` теперь явно отклоняется — раньше `verify_password(None)` упал бы, а с T3.13/T3.14 такие аккаунты появятся.
- [x] Код: 6 цифр через `secrets.randbelow(10**6)`, хранится **хешем** (`core.security.hash_password`), TTL 15 минут, максимум 5 попыток — исчерпание **сжигает код целиком**, а не просто отклоняет попытку.
- [x] `POST /api/auth/email/request-code` → 202. Отправка — **через Celery-таск** `app.tasks.notifications.send_verification_code`: `core/email.send_email()` синхронный `smtplib` и заблокировал бы event loop. Plaintext идёт аргументом таска — в БД только хеш, больше он нигде не существует.
- [x] `POST /api/auth/email/verify` `{code}` → ставит `email_verified_at`, чистит состояние кода. Повторный вызов — идемпотентный 200.
- [x] Rate-limit **на двух уровнях**: slowapi `5/hour` на `request-code` + `limit_req zone=email_verify_zone` на `/api/auth/email/` в `nginx/default.conf`. Плюс cooldown 60 с через `email_verification_sent_at` → 429.
- [x] **Гейта нет.** Подтверждение не влияет ни на один эндпоинт: ни вход, ни `POST /trips`, ни `POST /deals/match` его не проверяют. Первая редакция задачи вводила «мягкий гейт» на создание сделок и рейсов — снято владельцем 2026-07-26 (решение №3): подтверждение адреса это безопасность, а не права. Единственная поверхность — UI.
- [x] `MeOut.email_verified: bool` — производное свойство `User.email_verified`, читается через `model_validate(from_attributes=True)`.

**Frontend:**
- [x] `RegisterPage.tsx` — телефона в форме не было уже с T1.11, поле не трогали. Изменён только переход после регистрации: на `/verify-email`, если адрес не подтверждён. `RegisterPayload.phone` удалён из типа — теперь `tsc` ловит попытку отправить телефон, а не бэкенд в рантайме.
- [x] `LoginPage.tsx` — `auth.email` вместо `auth.emailOrPhone`, `type="email"`.
- [x] `pages/VerifyEmailPage.tsx` (роут `/verify-email` внутри Layout) + `components/EmailVerifyBanner.tsx` в `Layout.tsx`. Баннер скрыт для аккаунтов **без** email: адрес не заявлен — подтверждать нечего.
- [x] i18n — **все шесть локалей** (EN/RU/UA/PL/FR/ES), не только три: экран новый, fallback на EN для продукта с шестью языками не годится. Попутно во всех поправлен `errorDuplicate`, упоминавший телефон. Ключ `auth.emailOrPhone` остался неиспользуемым — на сборку не влияет.

**E2E:**
- [x] Ничего не гейтится → T_TEST.3 не затронут вовсе. Тем не менее env `E2E_AUTO_VERIFY_EMAIL_DOMAINS` (список через запятую, по умолчанию **пусто**) помечает регистрации на этих доменах подтверждёнными сразу — чтобы тестовые прогоны не плодили коды, которые никто не читает. На dev = `e2e.vimana.local` (TLD `.local` не резолвится), на prod **не задаётся**: флаг «подтверждён» должен означать, что кто-то реально открыл ящик. Непустое значение → WARNING в лог на старте.

**Тесты:** request-code (202 · cooldown 429 · без email → 422 · уже подтверждён) · verify (успех · неверный код · истёкший · исчерпание попыток сжигает код · идемпотентность) · код хранится хешем · регистрация с `phone` → 422 · логин по телефону → 401 · **неподтверждённый юзер публикует рейс → 201 и начинает сделку → не 403** · аккаунт без email работает как обычный · auto-verify домен не шлёт код · SMTP замокан (в реальный ящик не ходим).

**Отклонения от постановки:**
- Переменная названа `E2E_AUTO_VERIFY_EMAIL_DOMAINS` (список через запятую), а не `..._DOMAIN` — тестам нужны два домена сразу.
- Гейт был реализован и **снят в тот же день** по решению владельца. Тесты переписаны в обратную сторону: проверяют, что неподтверждённый пользователь публикует рейс и начинает сделку. Сделано намеренно — фича «подтвердите почту» склонна тихо отрастать обратно в проверку прав.
- i18n сделан на шесть локалей вместо трёх (см. выше).

**Acceptance: ⚠️ выполнены частично** — регистрация только email+пароль ✅; подтверждение не влияет ни на одно право, разница только в баннере ✅. **728 backend-тестов зелёные** (полный прогон с fuzz, 2026-07-26).

**Критерий «код приходит письмом» НЕ подтверждён.** Проверка вживую 2026-07-27: регистрация на реальный Gmail с прода — экран ввода кода отрисован, код сгенерирован, Celery-таск поставлен, **письмо не пришло**. Тесты доставку не проверяют и проверять не могут: SMTP в них замокан, иначе сьют слал бы реальную почту. Нужна настройка почтового сервера. До этого фича работает наполовину — пользователь видит требование подтвердить адрес и не может его выполнить, а `notify_email` (T1.7) с той же вероятностью не доходит. **Email-подтверждение не подавать как рабочее, пока доставка не проверена вживую.**

### T3.12 — Служебный ключ vs личность: establish identity ✅ MVP

**Контекст (решения владельца №5 и №6).** Механика T2.2 остаётся целиком: keypair выдаётся при регистрации, платформа хранит nsec, им шифруются контейнеры и подписываются записи. Меняется не код шифрования, а **чем этот ключ называется**. До перехода это служебный ключ сейфа, а не личность: пользователю он не показывается, наружу не публикуется, никаких обещаний суверенности с ним не связано.

Такой подход выбран вместо «ключа нет вообще» сознательно: тот вариант требовал переписать четыре работающих механизма — шифрование `IdentityContainer`, ветку безключевого участника в `dealvault.py`, поведение threshold при участнике без ключа и весь `D-RECIPIENT-ROLE` (невидимый custodial keypair recipient'а). Здесь не меняется ни один из них.

**Почему `claim` убирается.** Сегодня `POST /api/me/keypair/claim` повышает служебный ключ до личности, просто удаляя серверную копию nsec (`app/api/keypair.py:92-115`). Но приватная часть этого ключа всю жизнь лежала у платформы — доказать, что копия не сохранилась, невозможно ни технически, ни юридически. Личность, построенная на таком ключе, «суверенна» только на честном слове платформы, то есть ровно настолько, насколько вся фаза пытается этого избежать. Поэтому переход **всегда даёт новый ключ**.

**Одна механика на оба пути.** Ключ может родиться в браузере (`@noble/curves` уже в зависимостях фронта) или прийти из NIP-07-расширения — серверу это безразлично. Ему нужно единственное: доказательство, что вызывающий контролирует заявленный npub. Иначе достаточно прислать чужой известный npub, чтобы присвоить чужую личность — сегодняшний `import` именно это и позволяет, принимая голый `npub_hex` без всяких проверок (`app/api/keypair.py:135-141`). Поэтому эндпоинт один, а «сгенерировать» и «принести своё» — различие целиком фронтовое.

**API после задачи:**
- [x] `POST /api/me/identity/challenge` → одноразовый nonce (Redis `GETDEL`, TTL 300s). `core/challenge.py` — **не** fail-soft, в отличие от `token_blacklist`: там падение Redis означало «отдать ещё живой JWT», здесь означало бы «принять доказательство, которого мы не выдавали».
- [x] `POST /api/me/identity/establish` — проверяет подпись челленджа новым ключом (`core/identity_proof.py`), ставит `nostr_pubkey`, `key_self_custody = true`, **уничтожает** `nsec_encrypted`/`nsec_nonce`. Занятый npub → 409, повторный вызов → 409.
- [x] `POST /api/me/identity/declare-lost` + `users.key_lost_at`. Терминальное состояние: `require_live_identity` в `POST /trips` и `POST /deals/match`, публичный `UserOut.key_lost`. **Отклонение:** re-auth по паролю, а не step-up — беспарольные аккаунты получают 409 «нужен T3.15». Подписью ключа тут подтвердить нельзя: отсутствие ключа и есть то, что объявляют.
- [x] **Удалены** `claim`, `import`, `export`. `import` снят сразу (принимал голый npub без доказательства — присвоение чужой личности); `export`/`claim` пережили его на один шаг, потому что семь тест-модулей брали через них известный nsec — снос в одном коммите положил бы весь крипто-сьют.
- [x] `GET /me/keypair/status` — `identity_established` + `key_lost`; старые `key_self_custody`/`has_encrypted_nsec` оставлены, чтобы не ломать читателей.

**Схема (миграции `0029_identity`, `0030_container_key_envelope`):**
- [x] `users.nostr_pubkey` → **UNIQUE**. Дублей на проде нет (проверено 2026-07-26).
- [x] **NOT NULL — решено не делать (владелец, 2026-08-02).** Причина продуктовая, а не про чистоту данных: ступень 0 в `D-KEY-TIERS` — аккаунт, пользующийся платформенным ключом, — должна оставаться выразимой. Попутно вскрылось, чего стоило бы ограничение: оно делает недостижимым стартовый бэкфилл `ensure_service_keys` (он существует ровно для аккаунтов без ключа), то есть требует удалить и его, и `test_service_keys.py`, построенный на создании такого аккаунта, плюс правку ещё семи тестовых файлов. Половинчатый вариант — ограничение есть, мёртвый бэкфилл остался — хуже обоих. Пункт закрыт как «не делаем», а не как «потом».
- [x] `users.key_lost_at`, `trips.nostr_published_by_pubkey` (+ бэкфилл атрибуции уже опубликованных).
- [x] `identity_containers.key_envelope` + `key_envelope_sender_pubkey` (миграция `0030`, см. перешифровку).
- [x] **Бэкфилл служебных ключей** — `core/service_keys.py`, идемпотентно, в `lifespan` рядом с `ensure_user_zero`. Не в миграции: генерация требует `NSEC_ENCRYPTION_KEY`, а тащить рантайм-конфиг в историю схемы плохо. На проде выдано 7 + 1 ключей. **Попутно починен живой дефект:** у арбитра ключа не было вовсе, из-за чего threshold 2-of-3 не собирался.

**Публикация рейсов:**
- [x] Только под **`PLATFORM_PUBLISH_NSEC`** — отдельный ключ от `CHAIN_ANCHOR_NSEC`. Авторство события платформенное, `carrier_name` в `content`, `carrier_pubkey: null`; есть тест, что ключ перевозчика **не встречается в событии вообще**. Ключ внесён в whitelist собственного relay.
- [x] `build_event`/`build_deletion_event` (подписывали служебным ключом) **удалены**, а не оставлены рядом: событие, подписанное ключом, который потом уничтожается, живёт на чужих relay'ях приписанным ничьему pubkey.
- [x] Фильтр «интересных» — `core/publish_filter.py`, режимы `interesting`/`all`/`none`, правило «редкий коридор» считается по нашим же `trips`.
- [ ] **Правило «длинных хопов» — не реализовано, но и не заблокировано** (уточнено 2026-07-27). В первой редакции здесь стояло «`origin`/`destination` — свободный текст, нужна схема». **Это было неверно:** `AirportSelect` вызывает `onChange(a.iata)`, то есть в колонках лежат IATA-коды из справочника, а `core/airports.py` с T1.10 имеет и координаты, и haversine. Добавлен `route_distance_km(origin, destination) -> float | None`; правило остаётся дописать в `core/publish_filter.py`.
- [x] При переходе платформенные листинги ставятся в очередь на kind-5 (fire-and-forget: зависший листинг — устаревшая витрина, а не улика, и не должен ронять переход).

**Перешифровка при переходе:**
- [x] `read_packages`/`wrapped_shares` — переупаковка session-ключей на новый npub. **Байты контента не меняются** → `sha256(ciphertext||nonce)` тот же → цепь T3.6 и `verify_content` целы.
- [x] `IdentityContainer` — случайный ключ содержимого + NIP-44-конверт (изначально NIP-04, переведён `T_KEYS.1`) на новый npub (`doc_hash` по plaintext, тройное совпадение `identity_ref` переживает смену шифра).
- [x] **Смена формата конвертов (pt.2c, в постановке отсутствовала).** Пакет читался как ECDH(читатель, **автор сообщения**) — переадресовать его новому владельцу нельзя без приватного ключа автора. Решение: платформа перешифровывает от имени умирающего служебного ключа и записывает отправителя **в сам конверт** (`{ct, sender_pubkey}`, строка = legacy). Затронуты `dealvault`, `threshold`, arbiter-reveal и **клиентская расшифровка** в `DealVaultPage`.
- [x] **Отклонение: inline, а не Celery-таском.** Таск стартует только после коммита, то есть уже без служебного ключа; его падение необратимо — перечитать данные больше нечем. Inline любое исключение откатывает всё. Плюс **самопроверка перед уничтожением ключа**: ECDH симметричен, поэтому платформа вскрывает только что созданный конверт ключом отправителя, доходит до plaintext и сверяет `doc_hash`. Не сошлось → 500 и откат. При росте объёмов правильный ответ — разнести перешифровку и смену ключа на два шага (expand/migrate/contract), а не унести половину в очередь.

**Фронт и копирайт:**
- [x] `lib/identity.ts` — генерация ключа и подпись челленджа в браузере; сервер получает только npub и подпись. Каноническое событие обязано хешироваться побайтово как в `core/identity_proof.py`.
- [x] `KeypairSection` переписан под три состояния; кнопки «забрать сгенерированный» нет вовсе. Ключ показывается один раз, подтверждение заблокировано до чекбокса «сохранил и понимаю, что восстановить нельзя».
- [x] Копирайт: «ключ создаётся в вашем браузере и на наши серверы не уходит» — формулировка «отдаём ключ, который хранили» запрещена как неправда. i18n на все шесть локалей.
- [x] Термин «DID» не введён нигде: везде `npub`.

**Проверка криптографии — три уровня:**
- [x] Контракт сериализации: `frontend/src/test/identity.test.ts` (7) + `backend/tests/test_identity_proof_contract.py` (6), пришпилены к одному литералу. Разъехаться молча не могут.
- [x] Взаимная совместимость BIP-340: Playwright-спек `identity-establish.spec.ts` — `@noble/curves` подписывает в настоящем Chromium, `coincurve` принимает на сервере.
- [x] Поток в UI там же: чекбокс блокирует подтверждение, состояние переключается, аккаунт продолжает работать после перехода.

**Тесты:** `establish` без доказательства → 401 · голый npub → 422 · занятый npub → 409 · повторный → 409 · челлендж одноразовый · просроченный `created_at` → 401 · `claim`/`import`/`export` → 404 · **документ и session key вскрываются ключом, которого сервер не видел** · конверты контрагента не тронуты · несходящаяся самопроверка откатывает переход целиком · перешифровка идемпотентна · рейс до перехода публикуется платформенным ключом, ключ перевозчика в событии отсутствует · бэкфилл идемпотентен, покрывает арбитра, не трогает существующие ключи · `declare-lost` → создание рейса 403.

**Отклонения от постановки:**
- NOT NULL на `nostr_pubkey` отложен до отдельной миграции (бэкфилл в lifespan, а не в миграции — см. выше).
- Перешифровка inline, а не Celery-таском; добавлена самопроверка перед уничтожением ключа.
- Смена формата конвертов (pt.2c) в постановке отсутствовала — без неё аккаунты с e2e-перепиской не смогли бы перейти вовсе.
- Правило «длинных хопов» не реализовано: нужна схема для origin/destination.
- `declare-lost` для беспарольных аккаунтов ждёт step-up из T3.15.
- `export`/`claim` сняты на шаг позже `import` — иначе снос положил бы крипто-сьют.

**Найдено и починено попутно (не входило в задачу):** у арбитра не было keypair, threshold 2-of-3 не собирался · тесты писали и удаляли в **боевой** БД (`SyncSessionLocal` не изолирован) — за один прогон снесло 22 e2e-аккаунта · каскад `cleanup_e2e_users` отстал от схемы (`route_notes.created_by`) и никогда не удалял `Order` · `token_blacklist` под тестами молча не работал.

**Acceptance: ✅ все выполнены** — служебный ключ нигде не подаётся как личность и не публикуется наружу; переход всегда даёт новый ключ с доказательством владения; сейфы переезжают без нарушения цепи и с доказанной читаемостью; `claim`/`import`/`export` не существуют; у всех аккаунтов есть ключ и threshold собирается. **773 backend-теста, 20 vitest, 9 Playwright — зелёные** (полный прогон с fuzz, 2026-07-27).

### T3.13 — Вход и регистрация по Nostr-ключу ✅ MVP

**Контекст.** Механика доказательства владения уже была построена в T3.12 (`core/challenge.py` + `core/identity_proof.py`) и здесь переиспользована целиком — добавились только новые purpose и путь регистрации без пароля.

- [x] `POST /api/auth/nostr/challenge` `{pubkey_hex}` → nonce (Redis `GETDEL`, TTL 300s). Выдаётся **на любой pubkey без проверки**: отказ по неизвестному превратил бы эндпоинт в оракул «у каких ключей есть аккаунты», а челлендж без приватного ключа бесполезен.
- [x] `POST /api/auth/nostr/verify` — гасит челлендж, проверяет подпись, отдаёт JWT.
- [x] Неизвестный pubkey → **404 `nostr_pubkey_unknown`**, не 401: клиенту нужно отличать «не зарегистрирован, предложи регистрацию» от «подпись не сошлась». Аккаунт молча не создаётся — валидная подпись доказывает владение ключом, а не намерение зарегистрироваться.
- [x] `POST /api/auth/nostr/signup` — `password_hash=NULL`, `nsec_encrypted=NULL`, `key_self_custody=True`. **Возвращает токен**, а не только строку: пароля нет, без токена аккаунт был бы недостижим сразу после создания. Email опционален и, если задан, запускает подтверждение из T3.11.
- [x] Утраченная личность (`key_lost_at`, T3.12) войти не может → 403.
- [x] Rate-limit `10/minute` на оба эндпоинта: slowapi + зона `nostr_auth_zone` в nginx.
- [x] Frontend: `NostrAuthButton` на обеих страницах, подпись делает NIP-07-расширение. Расширения нет → объяснение, что это такое и что Vimana ключ не видит, а не пустая ошибка. i18n ×6.

**Отклонение от постановки — три purpose вместо URL-привязки.** Строгий NIP-98 биндит доказательство к абсолютному URL, что тащит origin из деплой-конфига в проверку подписи и ломается, как только dev и prod различаются. Вместо этого purpose (`establish` / `login` / `signup`) лежит **внутри подписанного события**: подпись, собранная для одного потока, бесполезна в другом. Тесты проверяют обе стороны — proof логина не создаёт аккаунт, proof establish не пускает войти.

**Тесты (15, `tests/test_nostr_auth.py`):** вход по ключу · чужая подпись → 401 · повторное использование челленджа → 401 · просроченный `created_at` → 401 · неизвестный pubkey → 404 · signup создаёт self-custody-аккаунт без пароля и email · токен из signup работает сразу · занятый ключ → 409 · signup с email заводит код подтверждения · утраченная личность → 403 · парольный вход не затронут.

**Не покрыто автотестами:** Playwright-спек на вход по ключу. Нужно настоящее NIP-07-расширение, а по T_TEST.3 это только persistent context с инъекцией — отдельная работа, не довесок.

**Acceptance: ✅ все выполнены** — владелец ключа входит без пароля и без email; перехваченный подписанный запрос повторно не проходит; аккаунт без пароля полностью работоспособен. **799 backend-тестов зелёные** (полный прогон с fuzz, 2026-07-27). Прирост против 773 — 15 новых юнит-тестов плюс ~11 schemathesis-кейсов, сгенерированных на три новых эндпоинта: контракт по ним отработал, ради чего полный прогон и был блокирующим.

### T3.14 — Passkeys (WebAuthn): несколько устройств на одну личность ✅ MVP

- [ ] Зависимости: backend `webauthn==2.5.2` (py_webauthn), frontend `@simplewebauthn/browser@^13`.
- [ ] Env (ENVIRONMENT §секреты + `.env.example`): `WEBAUTHN_RP_ID` (prod `vimana.dealvault.club` — см. `nginx/default.conf:29`; dev `localhost`), `WEBAUTHN_RP_NAME=Vimana`, `WEBAUTHN_ORIGIN`. RP ID **обязан** совпадать с доменом — иначе браузер отклоняет ceremony молча, без внятной ошибки.
- [ ] Миграция **`0031_webauthn_credentials`** (номер `0030` занят `container_key_envelope` из T3.12 pt.2b): `webauthn_credentials(id UUID PK, user_id FK→users ON DELETE CASCADE, credential_id BYTEA UNIQUE NOT NULL, public_key BYTEA NOT NULL, sign_count BIGINT NOT NULL DEFAULT 0, transports JSONB, aaguid VARCHAR(36), device_name VARCHAR(100), backed_up BOOL NOT NULL DEFAULT false, uv_capable BOOL, created_at, last_used_at)` + index по `user_id`.
- [ ] Ceremony-эндпоинты (challenge в Redis, TTL 300s, одноразовый):
  - `POST /api/auth/passkey/register/options` (авторизован) → `residentKey="required"`, `userVerification="preferred"`, `user.id` = байты UUID, `excludeCredentials` = уже привязанные.
  - `POST /api/auth/passkey/register/verify` → сохраняет credential; `device_name` — из ввода юзера, дефолт по User-Agent.
  - `POST /api/auth/passkey/login/options` (без авторизации) → **пустой** `allowCredentials` — usernameless-вход через discoverable credentials.
  - `POST /api/auth/passkey/login/verify` → юзер по `credential_id`, проверка подписи и `sign_count` → JWT.
  - `POST /api/auth/passkey/signup/options` + `/verify` — регистрация с нуля: `display_name` обязателен, email опционален; создаёт юзера с `password_hash=NULL` и **служебным** Nostr-keypair (как `register`). ⚠️ Формулировка «платформа держит nsec до `claim`» устарела: `claim` удалён в T3.12 — служебный ключ **не повышается** до личности, пользователь получает свою через `POST /me/identity/establish`, и это всегда **новый** ключ.
  - `DELETE /api/auth/passkey/{id}` — отвязать устройство.
- [x] **Sign-count:** регресс отклоняется только если `stored > 0`. Синхронизируемые passkey'и (iCloud, Google) копируются между устройствами by design и **всегда отдают 0** — строгая проверка отвергала бы самый распространённый passkey на втором входе.
- [x] **Guard последнего аутентификатора:** `remaining_ways_in` считает двери — пароль, свой Nostr-ключ, каждый passkey. Email **не считается** (он подтверждает адрес, а не пускает внутрь), утраченный ключ **не считается** (подписать уже не может). Удаление последней → 409.
- [x] YubiKey распознаётся по `backed_up=false` + `transports ∩ {usb, nfc}`; в списке три вида — аппаратный / синхронизируемый / только это устройство.
- [x] Frontend: `PasskeyAuthButton` на входе и регистрации, `PasskeySection` в профиле, i18n ×6. **Conditional mediation (autofill в поле email) не сделан** — отдельная работа, к основному потоку не относится.

**Тесты:** 13 юнит (`test_webauthn_rules`) + 18 эндпоинтных (`test_passkey_endpoints`) + **2 Playwright с виртуальным аутентификатором Chrome через CDP** — единственное место, где обмен проходит целиком: привязка устройства, полная очистка сессии, вход обратно **только по passkey**. Криптография там настоящая, эмулируется лишь жест пользователя.

**Отклонения от постановки:**
- Миграция `0031`, не `0030` — номер был занят `container_key_envelope` из T3.12 pt.2b.
- Формулировка про «nsec до `claim`» в постановке устарела и была поправлена до реализации, иначе воспроизвела бы отменённое поведение.
- `cryptography` поднят `44.0.0 → 44.0.2`: `webauthn 2.5.2` требует `>=44.0.2`, pip иначе не резолвится. Патч-уровень, используемые примитивы не менялись.
- Conditional mediation не реализован.

**Найдено и починено попутно (не входило в задачу):**
- **Все ошибки разбора WebAuthn улетали как 500.** `py_webauthn` бросает семейство исключений без общего базового класса; перечисление по именам гнило молча. Заменено границей: всё по ту сторону вызова библиотеки — недоверенный ввод, любой сбой там даёт 401.
- **`uvicorn` без `--proxy-headers`** строил редиректы как `http://`, браузер на https блокировал их как mixed content. Ломало **любой** редирект в приложении, отказ бесшумный.
- **`error_page ... = /_error.html` на уровне `server` возвращал `200` вместо 429/502/503** — клиент читал отказ как успех. Существовало с T_SEC.1, касалось всех API-эндпоинтов. Плюс `limit_req_status 429` вместо 503.
- **`npm ci` в Dockerfile требует лок-файл**, а он не версионировался — сборка зависела от случайного файла на сервере. Лок закоммичен.

**Acceptance: ✅ все выполнены** — одна личность, несколько passkey'ев; вход с привязанного устройства даёт тот же аккаунт; отвязка не меняет npub и не трогает остальные способы; последний способ входа удалить нельзя. **878 backend-тестов, 20 vitest, 11 Playwright — зелёные** (полный прогон с fuzz, 2026-07-29).

### T3.15 — Step-up re-auth + управление способами входа ✅ MVP

**Контекст (уточнён 2026-07-29).** Исходная постановка ссылалась на `POST /api/keypair/export` и `claim` — **оба удалены в T3.12**, так что переводить нечего. Реальный пробел другой и уже виден в коде: `POST /me/identity/declare-lost` сверяет пароль и отвечает **409 беспарольным аккаунтам** («нужен step-up из T3.15»). То есть аккаунт, созданный через Nostr-ключ или passkey, сегодня не может объявить ключ утраченным — единственную необратимую операцию, которая ему как раз и нужна.

- [x] `app/core/step_up.py` — грант в Redis, TTL 5 минут, ключ включает `user_id` **и область операции**. Токен от одной операции не подходит к другой: иначе подтверждение отвязки устройства молча разрешало бы объявить ключ утраченным. Одноразовый (`GETDEL`) — повтор перехваченного гранта не проходит.
- [x] **Fail-closed при недоступности Redis** — в отличие от `token_blacklist` (см. `D-REVOCATION-IS-BEST-EFFORT`). Там fail-soft оправдан: обычные запросы важнее. Здесь наоборот — «Redis лежит, поэтому пропускаем подтверждение» перед необратимым действием звучит неверно.
- [x] Три пруфа, по одному на каждый способ входа: пароль · WebAuthn-assertion · Nostr-подпись. Механика челленджа переиспользуется из T3.12/T3.14, добавляется свой `purpose`/scope.
- [x] **Грант идёт заголовком `X-Step-Up-Token`, не query-параметром.** Query-строка оседает в логах nginx, истории браузера и `Referer` — тот же класс утечки, что у presigned-ссылок из аудита IDOR. Есть тест, что query-вариант **не** работает.
- [x] `GET /api/auth/step-up/options` отдаёт список доступных пруфов (`available_methods`) — диалог рисует только то, чем этот аккаунт реально может подтвердить. Предлагать пароль беспарольному аккаунту хуже, чем не предлагать ничего.
- [x] **Под step-up:** `declare-lost` ✅, отвязка passkey ✅, смена email ✅, смена/установка пароля ✅. `establish` — **нет**: он уже требует доказательства владения новым ключом, и второе подтверждение ничего не добавляет.
- [x] `POST /api/auth/email/change` + `DELETE /api/auth/email/pending` — смена адреса через `pending_email`, код уходит на новый ящик, старый работает до подтверждения. Адрес не резервируется: конфликт решается на уникальном индексе при подтверждении → 409, аккаунт остаётся при рабочем адресе. Миграция `0032`.
- [x] `PUT /api/auth/me/password` — один эндпоинт на «задать» и «сменить». Поля `current_password` нет: присутствие доказано step-up'ом, а требование старого пароля снова заперло бы беспарольные аккаунты. **Завершает остальные сессии** (`users.sessions_valid_from` + `iat` в токене, миграция `0033`); сменный токен возвращается в ответе, чтобы текущее устройство не выкинуло. Предупреждение об этом показывается **до** действия — в форме и в диалоге подтверждения.
- [x] Фронт: `StepUpDialog.tsx` (спрашивает сервер, какие пруфы есть, и рисует только их), `KeypairSection` переведён с поля пароля на диалог — **до этого он был сломан на проде**: бэкенд уже ждал `step_up_token`, а форма слала пароль. `PasskeySection` — подтверждение перед отвязкой. i18n ×6.
- [x] `SecuritySection.tsx` — почта и пароль одним блоком, отдельно от `KeypairSection` (личность) и `PasskeySection` (устройства). Разделение намеренное: потеря пароля — неудобство, потеря ключа — конец личности, и общий список намекал бы, что это одного порядка вещи. Диалог подтверждения открывается по кнопке, а не по факту валидности поля: грант одноразовый и живёт 5 минут, тратить его на нерешённое действие незачем.

**Открыто:** добавление passkey/Nostr-ключа как способа входа под `ADD_AUTH_METHOD` — скоуп заведён, эндпоинтов ещё нет.

**Тесты:** step-up паролем · passkey'ем · Nostr-подписью · истёкший токен → 401 · токен от другой операции → 403 · повторное использование → 401 · грант в query-строке не принимается · беспарольный аккаунт объявляет ключ утраченным через passkey. Снято: «экспорт nsec без step-up» — `export` удалён в T3.12, защищать нечего.

**Acceptance:** ✅ любая чувствительная операция из списка требует свежего подтверждения — каким бы способом пользователь ни вошёл; аккаунт без пароля не ограничен по сравнению с парольным.

### T_KEYS.1 — Ревизия модели ключей: средний уровень владения ✅ решение принято (`D-KEY-TIERS`)

**Итог (2026-08-01).** Ревизия закрыта решением владельца, записанным как `D-KEY-TIERS` в TECHSTATE §2; схема ступеней — IMPLEMENTATIONPLAN §3.7.1a. Коротко, чтобы читать эту задачу можно было не прыгая:

- Средний уровень **есть** и он ровно один — ступень 2: ключ и у платформы, и у пользователя. Она не схлопывается с полным самовладением, потому что на ней платформа по-прежнему может всё, и это говорится вслух.
- Полное самовладение — ступень 3: **удаление копии с платформы**, а не рождение нового ключа. Решение №6 отменено: происхождение ключа недоказуемо в обе стороны, а «копии у нас нет» проверяемо.
- NIP-49 (`ncryptsec`) — формат запечатанного ключа внутри Identity Vault; запечатывание идёт **в браузере**, парольная фраза на сервер не уходит. NIP-19 (bech32) — везде, где ключ показывается или выгружается. NIP-06 (мнемоника) — **отложена**: файл `.dvlt` решает ту же задачу переносимости и делает это лучше для не-Bitcoin-аудитории; вернуться к ней имеет смысл только если появится спрос на восстановление «по бумажке без файла».
- Импорт — файлом `.dvlt`, строкой `ncryptsec`, строкой `nsec` и через NIP-07; во всех случаях с доказательством владения.
- NIP-46 (remote signing) и NIP-05 (`имя@dealvault.club`) в эту ревизию **не входят** — обе полезны, обе ортогональны лестнице; заводятся отдельно, когда до них дойдёт очередь.
- Recovery-коды сжимаются до узкой задачи «вернуть доступ к аккаунту» — см. переписанную `T3.16`.
- `declare-lost` при живой копии на платформе больше не означает «личность мертва» — см. `T3.17`.

**Осталось от задачи:** `keys.md` (пользовательский документ) описывает прежнюю схему и станет неверным — переписывается вместе с `T3.21`.

### T_KEYS.1 (архив постановки) — что именно ревизовалось

**Контекст (решение владельца 2026-07-30).** Модель владения ключом сегодня **бинарная**: либо служебный ключ целиком у платформы, либо личный ключ целиком у пользователя без какой-либо страховки. Между ними нет ничего. Отсюда одновременно и избыточность, и пробел — и оба видны в уже написанных задачах.

**Что выглядит избыточным.** Таблица трёх исходов в T3.16 сложна ровно потому, что модель бинарна: третья строка («утрачен сам ключ → не восстановить ничего») существует только из-за отсутствия среднего уровня. Десять recovery-кодов возвращают **доступ**, но не ключ — то есть решают меньшую задачу большим механизмом. При наличии зашифрованной резервной копии ключа пользователь восстанавливает **сам ключ**, что строго больше. Recovery-коды после ревизии, вероятно, сжимаются до узкого случая — аккаунт со служебным ключом и без почты, — а не остаются универсальным механизмом.

**Чего не хватает.** После `establish` резервной копии ключа не существует вовсе: единственная страховка — «скачайте файл, второй раз не покажем». Потерял файл — потерял личность. Нет ни переносимого формата, ни возможности вывести ключ заново, ни импорта уже существующей личности иначе как через расширение NIP-07.

**Ностр-совместимость — отдельный пробел.** Ключ показывается и выгружается в **hex**, а клиенты Nostr работают с bech32 (`npub1…`, `nsec1…`, NIP-19). Личность, созданная в Vimana, сегодня не переносится в damus/amethyst/coracle копированием — при том что вся философия проекта построена на «ключ и есть личность» и на видимости из любого Nostr-клиента (Фаза 3.5).

**Что предстоит спроектировать (решения принимаются в этой задаче, не раньше):**

- [ ] **Сколько уровней владения и какие.** Рабочая гипотеза — три: служебный ключ платформы · **средний: ключ пользователя с зашифрованной копией, которую платформа хранит, но не может прочитать** · полный self-custody без копий вообще. Проверить, не схлопывается ли средний с третьим при честной формулировке.
- [ ] **NIP-06 — вывод ключа из мнемоники.** 12 слов вместо файла: бумага, повторный вывод в любом совместимом клиенте, знакомый пользователям Bitcoin паттерн. Решить, становится ли мнемоника **основным** способом создания ключа. Если да — «покажем один раз» перестаёт быть единственной страховкой, и половина T3.17 меняется.
- [ ] **NIP-49 — зашифрованный `ncryptsec`.** Ключ под парольной фразой, хранится где угодно, включая наши серверы: платформа держит блоб и не может его открыть. Это и есть кандидат на средний уровень. Проверить параметры схемы по актуальной спецификации при реализации, а не по памяти.
- [ ] **NIP-19 — bech32 везде, где ключ показывается или выгружается.** Hex оставить только внутренним представлением.
- [ ] **Импорт существующей личности** — мнемоникой и `nsec`, не только через расширение. Сейчас человек с готовым Nostr-ключом без NIP-07 в Vimana войти не может.
- [ ] **NIP-46 (remote signing)** — оценить как верхний уровень. В IMPLEMENTATIONPLAN §10 он уже назван приоритетом для multi-device; решить, входит ли он сюда или остаётся отдельно.
- [ ] **NIP-05** — идентификатор `имя@dealvault.club`. Домен есть, эндпоинт статический и дешёвый; даёт человекочитаемый адрес личности в сети. Решить, часть ли это ревизии или отдельная задача.
- [ ] **Что происходит со средним уровнем при `declare-lost`.** Если платформа хранит зашифрованную копию, «ключ утрачен» перестаёт означать то же, что сегодня, — забыта парольная фраза и потерян ключ это разные события с разными исходами. Терминальность `key_lost_at` (решение №4) требует пересмотра формулировок, но **не** отмены: платформа по-прежнему не может открыть копию.

**Криптографическая модернизация — совмещена с ревизией (решение владельца 2026-08-01).** Ревизия и так переписывает создание, хранение и выгрузку ключа; трогать криптографию сейфов вторым заходом означало бы мигрировать формат дважды. Поэтому сюда же:

- [x] **NIP-04 → NIP-44 v2 — сделано 2026-08-02.** Бэкенд: HKDF + ChaCha20 + HMAC-SHA256 + паддинг по расписанию, шесть тестов включая таблицу паддинга как контракт совместимости. Фронт: `window.nostr.nip44` вместо `nip04` — ключ по-прежнему не покидает расширение, своя реализация втащила бы его на страницу. NIP-04 удалён целиком, вместе с ним ушли `padding`/`modes` из импортов. Решение в Decision Log — `D-VAULT-NIP44`. **Ловушка при снятии:** массовая замена имён переименовала и старые функции, создав дубликаты определений под одним именем — Python берёт последнее, так что работал бы NIP-44, но рядом лежали бы мёртвые тела AES-CBC, читающиеся как реализация. Поймано пересчётом определений, а не проверкой «ноль вхождений». Прежний текст постановки: Сегодня `core/threshold.py:44-75` заворачивает `wrapped_shares`, `read_packages` и конверты `IdentityContainer` в NIP-04: AES-256-CBC + ECDH-x, **без аутентификации**. В спецификации Nostr NIP-04 помечен deprecated ровно за это; замена — NIP-44 v2 (ChaCha20 + HMAC-SHA256 + padding, скрывающее длину). Новых зависимостей не требуется: на фронте уже стоят `@noble/ciphers` и `@noble/hashes`, на бэкенде `cryptography` умеет ChaCha20Poly1305. Формат конверта уже расширяем (`{ct, sender_pubkey}` из T3.12 pt.2c) — версия схемы кладётся туда же. **Окно дешёвой миграции открыто, пока на проде ноль E2E-сообщений и ноль identity-контейнеров** (замер T3.12, 2026-07-26); после первых реальных сделок это станет перешифровкой живых данных. В архиве лежит как follow-up T2.3, до задачи не поднимался.
- [ ] **`shamir-secret-sharing@^0.0.3`** (`frontend/package.json`) — pre-1.0 и диапазонный пин для примитива, на котором держится схема 2-of-3. Решение о выборе нигде не записано: ни кто поддерживает, ни почему не своя реализация поверх `@noble`. Минимум — точный пин без `^` и запись в Decision Log; property-тесты roundtrip уже есть (`test_props_threshold.py`), но они проверяют математику, а не цепочку поставки.
- [x] **Совместимость решена 2026-08-02: миграция единовременная, ветки legacy не будет.** Замер на проде (`vimana.dealvault.club`, тот же инстанс, против которого гоняется e2e): **E2E-сообщений 0, сообщений в сейфах 3 — все открытым текстом, контейнеров личности 0.** Значит NIP-04-конвертов не существует ни одного: `wrapped_shares`/`read_packages` заполняются только при `is_e2e`, а `key_envelope` — только при наличии контейнера. Перешифровывать нечего, версия схемы в конверте нужна для будущего, а не для прошлого.
- [ ] Следствие: **слой 3 (голая строка вместо `{ct, sender_pubkey}`) снимается этой же задачей**, а не отдельно — формат меняется целиком, и держать ветку под данные, которых нет, запрещено CLAUDE.md прямо.
- [ ] **Окно закроется с первым реальным E2E-сообщением или первым контейнером.** До тех пор это замена формата; после — перешифровка живого, причём часть ключей платформе недоступна (верхняя ступень `D-KEY-TIERS`), и тогда единовременный вариант станет невозможен в принципе. Замер стоит повторить непосредственно перед началом работ, если между этой записью и ими пройдёт время.

**Legacy-слои, ожидающие именно этого решения (описаны 2026-08-01, не трогаем до `D-KEY-TIERS`).** Ревизия кода нашла пять мест, где живёт обратная совместимость — при том что CLAUDE.md её запрещает. Все пять висят на одном и том же нерешённом вопросе: как выглядят способы входа и появляется ли средний уровень владения, при котором платформа создаёт и хранит Nostr-личность, не имея возможности её прочесть. Пока ответа нет, удалять их вслепую нельзя — часть станет нужна, часть исчезнет вместе со старой моделью:

1. **`core/keypair.py:88-107` — `sign_event` / `verify_event`.** Помечены «Legacy T2.2 pt.1» (сырой `sha256(payload)` вместо NIP-01 event id). Не вызываются нигде, включая тесты, — проверено полным грепом. Кандидат на прямое удаление; ждёт задачи только чтобы не разъехаться с новой схемой подписи.
2. **`core/signing.py:202-207` — ветка «нет ключа → не подписываем».** Комментарий прямо говорит `backward-compat during migration`. После бэкфилла T3.12 (`core/service_keys.py`, ключ выдан всем, включая арбитра) ветка недостижима. Опасна не тем, что мертва, а тем, что **маскирует будущий баг**: аккаунт без ключа получит молча неподписанную запись в сейфе вместо ошибки. Решение зависит от того, останется ли вообще состояние «аккаунт без ключа» в новой модели.
3. **`core/threshold.py` — две формы конверта: голая строка и `{ct, sender_pubkey}`.** ⚠️ **Проверено 2026-08-02: снимать нельзя, прежняя запись здесь была неверна.** В постановке значилось «ветка написана под данные, которых нет» — на деле голую строку производит **фронт при каждом новом E2E-сообщении**: `encryptE2E` кладёт в `wrapped_shares` результат `nip44Encrypt` напрямую. Это не слой совместимости, а основной путь. Объектная форма возникает только когда **сервер** перешифровывает конверты при переходе пользователя на свой ключ: там отправитель — умирающий служебный ключ, а не автор сообщения, и назвать его надо явно. Для свежего конверта отправитель и есть автор, чей npub уже лежит на сообщении, поэтому значение по умолчанию осмысленно. Удаление ветки сломало бы всякое новое зашифрованное сообщение. **Остаётся как есть**, из списка легаси-долгов вычеркнут.
4. **`core/address.py` — fallback на колонки `User.receiving_*`.** ✅ **Чтение снято 2026-08-02.** Замер на проде: со старыми полями остался **один** аккаунт, и у него **есть** запись в `receiving_addresses` — то есть ветка не могла сработать ни для кого. Недостижимый код в том месте, которое решает, куда поедет посылка, стоит убирать именно потому, что он выглядит страховкой и ею не является. **Колонки пока остаются:** их удаление — contract-фаза, и она достаёт до `MeOut`, `UserUpdate` и типа `User` на фронте. Снятие чтения безопасно само по себе и превращает ту миграцию в чистое удаление, без разбора «а вдруг кто-то ещё читает». ✅ **Contract-фаза закрыта 2026-08-02:** миграция `0041` (drop шести колонок; `downgrade` возвращает их пустыми и говорит об этом — данные живут в `receiving_addresses`, обратный бэкфилл гадал бы, какой адрес был единственным), поля сняты из модели, `UserUpdate`, `MeOut`, типа `User` и `UserUpdate` на фронте, зеркала в conftest. Четыре теста удалены вместе с функциональностью, пятый (приватность адреса в админском списке) переписан против новой таблицы — свойство переживает колонки. Попутно удалён `frontend/src/components/AddressForm.tsx`: его никто не импортировал, замена `AddressFormFields` появилась в T_UX.4, а файл остался жить со своим типом на старых полях. Полный сьют 1075.
   **Состав contract-фазы (инвентаризация 2026-08-02, чтобы не выводить заново):** миграция `0041` — drop шести колонок `users.receiving_*`; `models/user.py` — те же шесть полей; `schemas/user.py` — из `MeOut` и `UserUpdate` (это ломает контракт API, не только схему); `frontend/src/api/auth.ts` — из типа `User`; `frontend/src/components/AddressForm.tsx` — **разобрать**, там те же имена полей, и надо понять, это форма для новой таблицы или остаток старой; `tests/conftest.py` — зеркало колонок в `_ensure_receiving_address_columns`; `tests/test_receiving_address.py` — **13 прямых обращений через `PATCH /auth/me`**, файл целиком проверяет снимаемую функциональность и требует решения: переписать под `receiving_addresses` или удалить. Делать одним заходом: между удалением колонки и правкой схемы репозиторий не собирается.
5. **`api/keypair.py:66-68,114` — `has_encrypted_nsec` / `key_self_custody` в `/me/keypair/status`.** Оставлены «чтобы не ломать читателей»; читатель у API один и свой. Прямо противоречит `D-KEY-IS-IDENTITY`, где `key_self_custody=false` объявлен внутренним признаком «служебный ключ», а не публичным контрактом. Что здесь останется, определяет ровно эта задача: при трёх уровнях владения булев флаг перестаёт быть достаточным ответом.

Плюс докстринги, ссылающиеся на удалённое: `core/keypair.py:11` описывает `claim`, снятый в T3.12.

**Обязательные ограничения — не пересматриваются:**

- Платформа не должна получать возможность читать то, что объявлено недоступным ей. Любой средний уровень, при котором мы фактически можем восстановить ключ, — это служебный ключ под другим названием (`D-KEY-IS-IDENTITY`).
- Формулировки в UI обязаны соответствовать механизму. Если копия зашифрована парольной фразой, которую мы не знаем, — так и писать; «мы поможем восстановить» в этом случае ложь.
- Совместимость с уже существующими личностями: у части пользователей ключ уже создан по текущей схеме. Ревизия обязана предложить им путь, а не оставить в старом состоянии молча.

**Результат задачи:** решение в Decision Log под заголовком `D-KEY-TIERS`, переписанные T3.16 и T3.17 под новую модель, обновлённый `keys.md` (пользовательский документ описывает текущую схему и станет неверным), формат конвертов сейфа (NIP-44) и судьба пяти legacy-слоёв — по каждому явное «удаляем» или «остаётся, потому что».

**Acceptance:** уровни владения перечислены с точным ответом «что платформа может и чего не может» на каждом; для каждого уровня описан путь создания, хранения, выгрузки и импорта в Nostr-совместимом формате; T3.16 и T3.17 приведены в соответствие; ни одно утверждение в UI не сильнее того, что механизм действительно даёт.

### T3.16 — Recovery-коды: запасной вход, не запасная личность ✅ MVP

**Контекст (переписан 2026-08-01 под `D-KEY-TIERS`).** Граница прежняя и стала проще: **код возвращает доступ к аккаунту, но никогда не возвращает ключ.** Раньше отсюда следовала таблица из трёх исходов — она была сложной ровно потому, что модель была бинарной. С лестницей исходов два:

| Где копия ключа | Что даёт recovery-код |
|---|---|
| Ступени 1–2: копия есть у платформы | всё. Вход вернулся, ключ никуда не девался, переписка читается как раньше |
| Ступень 3: копии у платформы нет | вход в аккаунт. Ключ возвращает **файл Identity Vault и парольная фраза**, а не код — и это надо сказать на том же экране, где код показывают |

Личность утрачена только если нет **ни** копии на платформе, **ни** файла с фразой — два события, а не одно.

**pt.1 — backend ✅ (2026-08-01)**

- [x] Миграция `0035_recovery_codes` (не `0031` — номер занят `webauthn_credentials`): `recovery_codes(id, user_id FK ON DELETE CASCADE, code_hash, used_at, created_at)` + индексы по `user_id` и по `(user_id, code_hash)` — второй и есть путь, по которому ходит `consume`. `used_at` вместо удаления строки: «код был потрачен, и когда» — вопрос, который задают после неожиданного входа, а удалённая строка на него не отвечает.
- [x] **Отклонение: SHA-256 вместо bcrypt.** Постановка ссылалась на `hash_password`. Код — 12 символов из алфавита в 31 знак, выданных `secrets`: ~59 бит, словаря против него нет, и медленный KDF защищает не от того. Зато он ломает эндпоинт: bcrypt заставил бы проверять попытку **против каждого** неиспользованного кода аккаунта по очереди — десять намеренных задержек на догадку, то есть рычаг DoS, направленный на себя. Быстрый дайджест ищется равенством, одним индексным запросом. Ровно та же логика, по которой API-ключи хранят быстрым хешем, а пароли — нет; обоснование записано в докстринге `hash_recovery_code`.
- [x] Генерация: 10 кодов по 12 символов, алфавит без `0/O/1/l/I`, группировка по 4. Ввод нормализуется — регистр и дефисы это оформление, а не секрет: человек, переписывающий код с бумаги после потери телефона, ошибётся именно в них.
- [x] **Отклонение: один эндпоинт вместо двух.** `POST /api/auth/recovery/codes` и выдаёт первый набор, и заменяет прежний — для пользователя это одно действие, а отдельный `regenerate` добавил бы состояние, которое надо объяснять. Под step-up в обоих случаях: коды, выпущенные из угнанной сессии, пережили бы смену пароля, которая эту сессию и закрывала.
- [x] `POST /api/auth/recovery/consume` `{identifier, code}` — **identifier это email или npub**, что у аккаунта есть, то и принимаем: требовать «правильный вид» идентификатора в худший момент — плохая сделка. Отвечает scoped-токеном (`scope: recovery`, TTL 15 мин) **плюс step-up-грантами** на `change_password` и `add_auth_method`: потраченный одноразовый код и есть то доказательство, которое просит step-up, а у аккаунтов, которым этот путь нужен, другого доказательства обычно не осталось.
- [x] **Scoped-токен — не сессия, и это устроено запретом, а не разрешением.** `get_current_user` отвергает **любой** токен с claim'ом `scope` (403), поэтому новый скоуп не может по забывчивости унаследовать доступ ко всем существующим эндпоинтам. Две двери открываются явным `get_recovery_or_current_user`: `PUT /me/password` и регистрация passkey. Отвязка passkey под него **не** попала — потеря телефона должна позволять добавить ключ, а не тихо снять чужой.
- [x] Rate-limit: slowapi `5/hour` на `consume` + зона `recovery_zone` (5r/m, burst 3) в nginx — самая тесная в конфиге. Два уровня по обычному правилу: nginx видит адреса и не различает аккаунты, slowapi наоборот.
- [x] `MeOut.recovery_codes_remaining` — считается запросом в `/me`, а не денормализованной колонкой: вторая правда о тех же строках дороже, чем `COUNT` по десяти индексным записям.
- [x] Тесты (8, в `test_step_up.py` — там уже живут помощники step-up): десять уникальных кодов и `remaining=10` · выдача без step-up → 422 · новый набор убивает прежний · scoped-токен не пускает на `/me` (403) · код ставит пароль, вход по нему работает, повторное использование → 401 · неизвестный аккаунт и неверный код неотличимы по ответу · код принимается в нижнем регистре и с пробелами вместо дефисов · чужой код к своему аккаунту не подходит.

**pt.2 — фронт и уведомления ✅ (2026-08-01)**

- [x] Блок кодов в `SecuritySection` — там же, где почта и пароль: это способы входа, и разносить их по разным местам значило бы предлагать пользователю самому догадаться, что они одного рода. Показ один раз, «Скачать .txt» и «Скопировать», `IBM Plex Mono`, группировка по 4.
- [x] **Отклонение: отдельной страницы восстановления нет.** Восстановление живёт режимом на странице входа — там, где заблокированный человек уже стоит. Отправлять его искать отдельный адрес в худший момент — плохая идея, а маршрут `/recover` пришлось бы ещё и запоминать заранее.
- [x] **Отклонение: «баннер» стоит в самом блоке, а не глобально.** При `N ≤ 2` предупреждение показывается там, где его можно исправить одной кнопкой; глобальная плашка ходила бы за пользователем по всему продукту, ничего не решая.
- [x] Формулировка остатка привязана к ступени `D-KEY-TIERS`: пока копия ключа у платформы — «код вернёт доступ **и переписку**», иначе — только доступ. Один текст на оба состояния был бы обещанием, которого платформа не выполнит.
- [x] Письмо после использования кода (`send_recovery_code_used`) — уходит **независимо от `notify_email`**, как и код подтверждения: это не уведомление, на которое подписываются, а единственный сигнал «кто-то воспользовался кодом». В письме — остаток и что делать, если это были не вы.
- [x] i18n ×6. Счётчик через `{{n}}`, а не `{{count}}`: `count` в i18next включает подбор плюральных форм, а фраза намеренно построена «число в конце» и ни в одной локали склонения не требует.
- [x] Тесты: +1 backend (письмо уходит и несёт остаток) + 3 vitest (остаток показан, предупреждение при ≤2 и его отсутствие при 10, генерация идёт через step-up).
- [ ] Коды **не инвалидируются** при удалении копии ключа с платформы — вход они восстанавливают по-прежнему. Но текст рядом с ними обязан смениться вместе со ступенью: на 1–2 «вернёт аккаунт и всю переписку», на 3 «вернёт вход; переписку откроет ваш файл Identity Vault». Одинаковая формулировка в обоих состояниях = обещание, которого платформа не выполнит.
- [x] Письмо после использования кода — **уже было сделано, галочка отстала** (обнаружено 2026-08-02). `send_recovery_code_used` вызывается из `consume` fire-and-forget, шлётся независимо от `notify_email` (на это никто не подписывался — это единственный сигнал, по которому владелец узнаёт о чужом входе), содержит остаток кодов и покрыт тестом. In-app-уведомления в продукте нет как механизма: общего ящика сообщений не существует, а заводить его ради одного события — не та цена. Записано явно, чтобы пункт не всплыл третий раз.

**Тесты:** генерация 10 уникальных кодов · валидный код → scoped-токен · scoped-токен не проходит на обычных эндпоинтах (403) · повторное использование → 401 · чужой код → 401 · `regenerate` инвалидирует прежние · rate-limit на `consume` · аккаунт с подтверждённым email кодов при регистрации не получает · кастодиальный аккаунт после `consume` читает старую переписку · self-custody после `consume` сохраняет тот же npub.

**Acceptance:** пользователь, потерявший единственное устройство, восстанавливает доступ к тому же npub по коду и привязывает новый аутентификатор; использованный код не работает повторно; платформа не хранит коды в открытом виде; UI нигде не обещает восстановление утраченного ключа.

> **Social recovery — отложено осознанно.** Восстановление через guardians из Trust Graph (T2.4) не требует от пользователя ничего хранить, но упирается в нерешённые вопросы: выбор порога, сговор guardians, пересечение с арбитражем. Отдельно: под решением №4 social recovery всё равно **не вернёт ключ** — guardians могут подтвердить личность человека, но не воссоздать его nsec. Механика shamir-долей есть (`shamir-secret-sharing` на фронте + `app/core/threshold.py`, T2.3) — при возврате к задаче переиспользуется. Отдельная задача, не child T3.16.

### T3.17 — Необратимость: экран последствий + напоминания о восстановлении ✅ MVP

**Контекст (переписан 2026-08-01 под `D-KEY-TIERS`).** Решение №8 в силе: платформа не мешает уйти к независимости, но обязана убедиться, что цена понятна. Изменился **момент**. Раньше страшный экран стоял на создании личности; теперь личность создаётся при регистрации и ничего не стоит, скачивание файла тоже ничего не отнимает — необратим ровно один шаг, **удаление копии ключа с платформы** (переход на ступень 3). Туда и переезжает весь экран последствий. Напоминания по-прежнему привязаны к событиям, а не к таймеру: спам с первого дня научит игнорировать баннер ровно к тому моменту, когда он станет важен.

`PlatformNotice` для этого не подходит — он глобальный и админский, с поверхностями footer/trip_card/deal_page (`app/models/notices.py`). Персональное состояние выводится из полей `MeOut`, новой таблицы не требует.

**Три триггера, все событийные:**
- [ ] **Регистрация** — молчим. Ни баннера, ни письма. Риска нет.
- [ ] **Первая сделка или первый рейс** у аккаунта без email — мягкий закрываемый баннер «добавьте почту для восстановления доступа». Закрытие запоминается локально, повторно не всплывает.
- [ ] **Скачивание Identity Vault (ступень 2)** — не страшный экран, а объяснение: файл и фраза дают вторую независимую копию, платформа при этом продолжает работать как раньше. Обязательное предупреждение ровно одно, и оно **успокаивающее**: «фразу мы восстановить не можем — но пока копия ключа есть у нас, вы ничего не теряете: скачаете заново с новой фразой».
- [ ] **Удаление копии с платформы (ступень 3)** — единственный экран последствий с обязательным чекбоксом; действие не блокируется (решение №8).

  **Копирайт (смысл обязателен, формулировка может шлифоваться):**
  > **Дальше — только вы.**
  > Мы удалим свою копию вашего ключа. С этого момента подписывать и открывать сейфы ваших сделок сможете только вы — из своего файла Identity Vault или через расширение Nostr. Мы не сможем сделать это за вас, даже если очень попросите.
  >
  > Ваши сделки, история и уровень активности остаются на месте — меняется не то, чем вы владеете, а то, кто может это открыть.
  >
  > Если вы потеряете и файл, и парольную фразу — личность будет утрачена: ни мы, ни кто-либо другой её не восстановит. Коды восстановления вернут вход в аккаунт, но не ключ.

  Чекбокс — не «я согласен с условиями», а «я понимаю, что после этого платформа не сможет открыть мои сейфы». Перед подтверждением — проверка, что файл действительно скачан (ступень 2 пройдена); удалять копию у пользователя, у которого второй нет, — способ потерять личность одним кликом.
- [ ] **Где предлагается движение вверх** (не «страшный шаг», а открывающаяся возможность): пользователь хочет унести личность в другой Nostr-клиент; хочет выгрузить сейф сделки; хочет публиковать рейсы под своим именем. Предложение появляется в момент такого намерения, а не висит постоянным баннером.
- [ ] Экран прогресса перешифровки сейфов — **только для импорта чужого ключа** (там личность действительно меняется). В основном сценарии ключ прежний, перешифровывать нечего, и экрана быть не должно.

**Дополнительно:**
- [ ] Баннер «осталось N кодов восстановления» при N ≤ 2, ведёт на регенерацию.
- [ ] Письмо после удаления копии с платформы (если email есть) — фиксация факта и напоминание, где лежит файл.
- [ ] Чип «ключ утрачен» в профиле и на карточках для аккаунтов с `key_lost_at` (T3.12) — контрагент видит мёртвую личность до того, как предложит ей сделку.
- [ ] i18n EN/RU/UA. Тон — DESIGNGUIDELINES §9: предупреждение без запугивания, без `danger`-красного (он зарезервирован, см. Bento-скилл).

**Тесты:** экран последствий не пропускает удаление копии без чекбокса (frontend, vitest) · удаление недоступно, пока файл не скачан · баннер без email появляется после первой сделки и не появляется до · счётчик кодов ≤ 2 включает баннер · `key_lost` рисует чип в публичном профиле · письмо после удаления копии уходит только при наличии email (SMTP замокан).

**Сделано 2026-08-01 (закрывает задачу):**

- [x] Экран последствий и успокаивающий текст на скачивании — уже въехали с `T3.21`/`T3.22`; момент необратимости стоит там, где она наступает.
- [x] **Баннер «добавьте почту» — в существующем `EmailVerifyBanner`, а не отдельным компонентом.** Всё, что продукт говорит про почтовый канал, теперь в одном месте: либо «адрес заявлен, но не подтверждён» (T3.11), либо «адреса нет вовсе, и уже есть сделка» (T3.17). Условие «есть что терять» проверяется одним запросом `deals?limit=1` и только для аккаунта без почты. Закрывается и остаётся закрытым — это совет, а не блокировка.
- [x] Письмо после удаления копии платформы (`send_platform_copy_deleted`) — уходит независимо от `notify_email`, как и код подтверждения: это запись о необратимом изменении, а не подписка. Если это были не вы, письмо и есть способ узнать; и оно говорит, что делать — вернуть копию из своего Identity Vault, а не «сожалеем».
- [x] **Чип «ключ утрачен» на карточке рейса.** `TripOut.carrier_key_lost` — контрагент видит мёртвую личность **до** того, как предложит ей сделку, а не после. Серый, не красный: это факт о том, что аккаунт может, а не предупреждение о человеке.
- [x] Ссылки баннеров ведут на `/profile/keys` (хвост `T_UX.6` закрыт).
- [x] Тест: рейс перевозчика с `key_lost_at` помечен на листинге; флаг снимается в `finally`, потому что сид-перевозчик общий для всего сьюта.
- [x] i18n ×6.

**Не делаем в этой задаче:** экран прогресса перешифровки. Он нужен только при импорте **чужого** ключа, идёт inline в транзакции запроса (T3.12 pt.2b) и на текущих объёмах занимает доли секунды — экран прогресса ради него был бы интерфейсом ради интерфейса. Вернуться, когда появятся аккаунты с сотнями сообщений.

**Acceptance:** ✅ пользователь не может остаться единственным держателем ключа, не увидев полного списка последствий; скачивание файла при этом не подаётся как опасное действие; аккаунт без email получает напоминание ровно тогда, когда ему есть что терять; мёртвая личность видна контрагенту.

---

### T3.21 — Identity Vault: формат `.dvlt` v1 и скачивание (ступень 2) ✅ MVP

**pt.1 — backend ✅ (2026-08-01)**

- [x] `POST /api/me/identity/release-key` под step-up — отдаёт `nsec_hex` + `npub_hex` **один раз в браузер**, чтобы тот запечатал файл. T3.12 удалил эндпоинт такой же формы и был прав: тогда ключ был служебным ключом платформы, и называть его «вашим» было бы неправдой. `D-KEY-TIERS` изменил не хранение, а то, чем ключ является — от регистрации это личность аккаунта, и теперь ложью было бы отказать её владельцу.
- [x] **Парольная фраза здесь не появляется вовсе.** Запечатывание идёт в браузере (NIP-49): фраза, которую сервер видел, не может защитить файл, который сервер хранит. Это и есть вся разница между ступенью 2 и второй копией нашей же кастодии.
- [x] Миграция `0036`: `users.identity_file_released_at`. **Отклонение от постановки:** вместо «таблицы копий ключа» — одна отметка времени. Ступень складывается из двух фактов: держит ли копию платформа (`nsec_encrypted IS NULL` — уже в схеме) и держит ли пользователь. Второе знать нельзя, можно только засвидетельствовать: мы знаем, что отдали ключ, а не что файл сохранён. Таблица «копий» описывала бы состояние, которого у нас нет.
- [x] `GET /me/keypair/status` → `key_copies ∈ platform_only | both | user_only` — одно слово вместо трёх булевых, выводится, а не хранится.
- [x] Ступень 3 отвечает `409` с внятным текстом вместо пустого 200: «копии у нас нет, ключ только в вашем Identity Vault».
- [x] Тесты: +3 (владелец забирает копию и наша остаётся · без step-up отказ · после удаления нашей копии — 409).

**pt.2 — формат файла и фронт ✅ (2026-08-01)**

- [x] **NIP-49 в браузере** (`lib/identity.ts`): scrypt (N=2¹⁶, r=8, p=1) + XChaCha20-Poly1305, парольная фраза нормализуется NFKC — иначе та же фраза с другой раскладки или из другого приложения дала бы другой ключ. Строка `ncryptsec1…` — bech32 с поднятым лимитом: стандартные 90 символов кодировщик молча не пропускает, а в NIP ограничения длины нет.
- [x] **Байт key-security = `0x00`** («ключ заведомо обращался небезопасно»). У NIP-49 есть отдельное поле ровно под наш случай, и честный ответ здесь ничего не стоит: ключ выдан платформой при регистрации и его приватная половина жила у нас. Ключу, рождённому в браузере, полагается `0x01` — когда такой путь начнёт давать файл, он так и скажет.
- [x] Формат `.dvlt`: `{v, type: "identity", npub (bech32), ncryptsec, created_at, label?}`. Открытого `nsec` в файле нет — проверяется тестом на содержимое.
- [x] Новая зависимость `@scure/base` — bech32 для `ncryptsec`/`npub`. Того же автора, что весь noble-стек, минимальная и аудированная; она же понадобится NIP-19 в остальных местах.
- [x] UI в `KeypairSection`: строка ступени (`копия только у нас` / `у вас и у нас` / `только у вас`), поле парольной фразы, скачивание под step-up. Текст рядом с фразой **успокаивающий, а не пугающий**: пока копия ключа у платформы, забытая фраза стоит одного повторного скачивания, а не личности.
- [x] i18n ×6.
- [x] Тесты (3 vitest): запечатал → открыл той же фразой · неверная фраза **падает**, а не отдаёт правдоподобный мусор (аутентификация XChaCha20-Poly1305 — на этом держится «мы не сможем помочь восстановить») · файл переносим и не содержит открытого ключа.

**Осталось (в `T3.22`/`T3.24`):** удаление копии с платформы и ридер, открывающий `.dvlt` без нас.

**Контекст.** Первая половина лестницы `D-KEY-TIERS`: у пользователя появляется своя копия ключа, а у платформы ничего не отнимается. Файл — не служебный дамп, а самостоятельный артефакт: он должен открываться без нас и через год.

- [ ] **Формат.** JSON, поля: `v` (версия формата), `type: "identity"`, `npub` (bech32, открыто), `ncryptsec` (NIP-49, ключ под парольной фразой), `created_at`, `label`, параметры KDF, `sig` — подпись содержимого самим ключом (самопроверка на подмену). **Открытого `nsec` в файле не бывает ни при каких условиях.** Расширение `.dvlt` — тот же формат, что у экспорта сделки (`D-DVLT-PROTOCOL`), различаются `type`; читатель один.
- [ ] **Запечатывание — в браузере.** Сервер отдаёт ключ во вкладку один раз (на ступени 1–2 он у него и так есть), фраза вводится там же, scrypt и шифрование выполняет клиент (`@noble/ciphers`, `@noble/hashes` уже в зависимостях). **Парольная фраза не уходит на сервер и нигде не хранится** — на этом держится всё утверждение о переносимости, и любой эндпоинт, принимающий фразу, ломает его.
- [ ] Скачивание требует step-up (T3.15) — операция выдаёт ключ, это чувствительно по определению.
- [ ] **Опционально: хранить файл и у нас.** Пользователь может оставить запечатанный файл на платформе как резервную копию — открыть его мы не можем, это просто блоб. Отдельный флаг, отдельная кнопка удаления.
- [ ] Статус личности в профиле одной строкой: «копия только у нас» / «копия у вас и у нас» / «копия только у вас».
- [ ] Схема: таблица копий ключа вместо булева `key_self_custody` (`kind ∈ platform | user_file`, наличие, `created_at`, `deleted_at`). Удаление копии — запись в истории, а не молчаливый `UPDATE ... NULL`.
- [ ] i18n ×6.

**Acceptance:** скачанный файл открывается сторонним инструментом по `ncryptsec`; строку из него принимает damus/amethyst; парольная фраза не встречается ни в одном запросе к серверу (проверяется тестом на клиентском коде); статус в профиле соответствует фактическому набору копий.

### T3.22 — Независимость: удаление копии с платформы (ступень 3) ✅ MVP

**pt.1 — backend ✅ (2026-08-01)**

- [x] `DELETE /api/me/identity/platform-copy` под step-up (скоуп `declare_lost` — операция того же порядка необратимости). Ключ, npub и сделки не меняются; заканчивается только **наша способность** действовать, и заканчивается она в схеме, а не в обещании.
- [x] Предусловие: `identity_file_released_at` не пуст, иначе `409`. Без него кнопка была бы измельчителем личности глубиной в один клик — и ровно для тех, кто ещё не понял, что такое ключ. Суверенитет пользователь выбирает сам, но входить в него вслепую — не выбор.
- [x] Повторный вызов — идемпотентный `200`: запрошенное состояние уже наступило.
- [x] Тесты: +2 (без скачанного файла → 409 · после скачивания копия исчезает, npub тот же, второй вызов не ошибка).

**pt.2 — фронт: привести лестницу в порядок ✅ (2026-08-01)**

- [x] Скачивание Identity Vault — основное действие ступени 1; удаление копии — отдельная кнопка, появляющаяся **только на ступени 2** (`copies === 'both'`), с экраном последствий, обязательным чекбоксом и step-up. Бэкенд отказывает тем же условием (409), так что защита стоит с обеих сторон, а не только в разметке.
- [x] Путь «создать другой ключ» **не удалён, а понижен**: это единственная e2e-проверка того, что `@noble/curves` и `coincurve` договариваются по BIP-340 (`identity-establish.spec.ts`), и удалять её ради перестановки кнопок было бы дорого. Он стал тихой ссылкой внизу и переименован в «Использовать другой ключ» — по сути это импорт принесённой личности (T3.23), а не переход к владению.
- [x] Копирайт переписан ×6. Было: «Обратного пути нет. Создаётся новый ключ, а тот, что храним мы, уничтожается» — язык отменённого решения №6. Стало: «Это заменит вашу личность… Если вы хотите владеть своим нынешним ключом — скачайте Identity Vault и удалите нашу копию: так личность останется прежней». Экран последствий переехал туда, где необратимость действительно наступает.
- [x] Текст ступени 3 говорит ровно о том, что меняется: «Ключ, npub и ваши сделки остаются такими же. Заканчивается наша возможность». Ни одного утверждения сильнее механизма (DESIGNGUIDELINES §9.1).

**Осталось:** `.txt` с открытым `nsec` для вновь созданного ключа — заменяется на запечатанный `.dvlt` вместе с локальным запечатыванием в `T3.24`.

> **Срочно (обнаружено на живом аккаунте 2026-08-01).** В `KeypairSection` до сих пор стоит **старый путь T3.12** — главная кнопка «Создать ключ в этом браузере» мгновенно уводит со ступени 1 на ступень 3: генерирует **новый** ключ, уничтожает копию платформы и отдаёт `.txt` с открытым `nsec`. Копирайт там же говорит языком отменённого решения №6 («обратного пути нет», «тот, что храним мы, уничтожается»). Владелец прошёл по единственной заметной кнопке и получил именно это. Пока путь на месте, каждый следующий пользователь повторит.

- [ ] Главным действием ступени 1 становится **скачать Identity Vault** (T3.21), а не «создать ключ».
- [ ] Удаление копии — отдельная кнопка ступени 2 с экраном последствий (`T3.17`), недоступная, пока файл не скачан.
- [ ] «Использовать моё Nostr-расширение» переезжает в импорт (`T3.23`): это принесённая личность, а не переход.
- [ ] Копирайт про «создаётся новый ключ» снимается везде — под `D-KEY-TIERS` переход не требует нового ключа.
- [ ] `.txt` с открытым `nsec` (`keyBackupText`) заменяется на `.dvlt`; для уже выданных — локальное запечатывание существующего ключа (в `T3.24`, серверу для этого ничего не нужно).

**Контекст.** Вторая половина лестницы и единственный необратимый шаг. После него платформа физически не может подписать от имени пользователя — и это должно быть правдой на уровне кода, а не политики.

- [ ] `DELETE /api/me/identity/platform-copy` под step-up. Проверка предусловия: копия у пользователя есть (T3.21 пройден), иначе 409 с внятным объяснением.
- [ ] После удаления сервер **не может** подписать: все серверные пути подписи для этого пользователя отвечают 409 и уводят на клиентскую подпись. Тест обязателен именно на это — что не осталось ни одного эндпоинта, который «ещё как-то умеет».
- [ ] Подпись и расшифровка переезжают в браузер: NIP-07 либо ключ, распечатанный из файла на время сессии (в памяти вкладки, не в `localStorage`).
- [ ] Возврат вниз: загрузка `.dvlt` + фраза восстанавливает копию на платформе. Тот же эндпоинт, что импорт (T3.23), с проверкой, что npub совпадает с текущим.
- [ ] Экран последствий и копирайт — `T3.17`.

**Acceptance:** после удаления ни один эндпоинт не подписывает от имени пользователя; загрузка файла возвращает прежнее поведение; npub не меняется ни в одну сторону.

### T3.23 — Привязка своей личности: импорт (быстрый путь) ✅ MVP

**pt.1 — возврат копии на платформу ✅ (2026-08-01)**

- [x] `POST /api/me/identity/restore-platform-copy` под step-up: пользователь отдаёт `nsec` из своего Identity Vault, платформа снова хранит копию. Ступень 3 — **выбор, а не приговор**: кто-то сочтёт полное самовладение неудобным (второе устройство, общий доступ, просто передумал) и вернётся на ступень 2. Ключ, npub и история при этом не двигаются вовсе.
- [x] **Ключ обязан быть своим.** Сервер выводит npub из присланного `nsec` и сравнивает с текущим; чужой → 409. Иначе дверь с надписью «вернуть копию» тихо принимала бы чужой ключ, а смена личности — это другой путь, с перешифровкой сейфов и своим доказательством.
- [x] Повтор при живой копии — идемпотентный 200.
- [x] Тесты: +3 (возврат ставит `both` и не меняет npub · чужой ключ → 409 · повтор — no-op).

**pt.2 — смена личности разрешена ✅ (решение владельца 2026-08-01)**

- [x] `establish` больше не отвечает 409 установленной личности: аккаунт может заменить ключ. Цена названа на экране до вызова — всё подписанное прежде остаётся подписанным ключом, которого у аккаунта больше не будет; сделки, история и УБА остаются.
- [x] **Одно жёсткое ограничение — арифметика, а не политика.** Сейфы зашифрованы **текущим** ключом, и перешифровать их можно только им. На ступени 3 копии у нас нет, поэтому смена отвечает 409 с указанием, что делать: сначала вернуть копию (`restore-platform-copy`). Половинчатый переход, оставляющий сделки нечитаемыми, был бы хуже отказа.
- [x] `identity_file_released_at` сбрасывается при смене: скачанный файл принадлежит **прежнему** ключу, и оставленная отметка заставила бы профиль утверждать, что копия нового ключа у пользователя есть. Ровно та тихая неправда, ради которой лестница и вводилась.
- [x] Тот же npub → 409 («это уже ключ этого аккаунта»): смена, которая ничего не меняет, — не смена.
- [x] Фронт: экран последствий теперь показывается и установленной личности, с **обязательным чекбоксом**; без него обе кнопки (создать в браузере / взять из NIP-07) заблокированы.
- [x] Тесты: +2 (смена работает, пока копия у нас, и сбрасывает отметку о файле · на ступени 3 отказ с инструкцией, при этом челлендж выдаётся — он бесполезен без подписи).

**pt.3 — след смены ключа ✅ (2026-08-01)**

- [x] Миграция `0037`: `users.previous_nostr_pubkey` + `identity_changed_at`. Под `D-KEY-TIERS` **любой** `establish` меняет npub — включая первый, потому что служебный ключ был личностью аккаунта с регистрации. Значит подписанное раньше остаётся подписанным ключом, которого у аккаунта уже нет: валидно, проверяемо и привязано к идентификатору, который больше не отвечает.
- [x] В профиле — строка «Ключ этой личности был изменён \<дата\>» и прежний ключ рядом. Без даты это было бы утверждение сильнее факта: сегодняшний ключ не покрывает вчерашние записи (`D-EVIDENCE-DECAYS`).
- [x] Ключи показываются как `3233…4457` — четыре и четыре. Шестьдесят четыре hex-символа никто не читает и глазами не сверяет; четырёх с каждого конца хватает, чтобы узнать знакомый ключ, а полное значение — в одну кнопку.
- [x] Хранится только публичная половина прежнего ключа: приватная нам никогда не принадлежала.
- [x] Тест: смена оставляет датированный след, до неё поля пусты.
- [x] **Поправлен `test_challenge_refused_once_established`** — он закреплял отменённое правило (челлендж запрещён установленной личности). Гейт на челлендже и не был защитой: nonce без подписи ничего не даёт, а правила живут там, где происходит смена.

**pt.4 — принести свой ключ файлом или строкой ✅ (2026-08-01)**

- [x] Третий путь в экране смены: файл `.dvlt`, строка `ncryptsec1…`, строка `nsec1…` или голый hex. Все они сходятся туда же, куда и ключ, созданный в браузере: пара живёт во вкладке и подписывает челлендж сервера. Сервер маршруты не различает и не должен — он проверяет единственное, что имеет значение: что вызывающий владеет заявленным ключом.
- [x] Парольная фраза используется **только здесь**, на файле или зашифрованной строке, и никуда не отправляется.
- [x] Поле фразы показывается всегда, а не появляется по мере ввода: форма, угадывающая, что вы собираетесь вставить, читается как недоверие.
- [x] Одно сообщение на все ошибки: неверная фраза, не тот файл и опечатка в ключе для человека перед экраном неразличимы, и гадать вслух, что именно не так, значит гадать.
- [x] i18n ×6.

**Осталось за задачей:** публичный показ смены ключа контрагенту — в `T3.18` (страница личности).

**Контекст.** Аккаунт создан, но пользователь пришёл со своим ключом. Четыре входа, все с доказательством владения (`core/identity_proof.py`, механика T3.12).

- [ ] `.dvlt` + фраза · строка `ncryptsec` + фраза · строка `nsec` · NIP-07-расширение.
- [ ] **Права различаются по способу.** NIP-07 — ключа мы не видим вовсе, аккаунт сразу на ступени 3. `nsec`/`ncryptsec`/файл — ключ распечатывается в браузере, и пользователь **явно** выбирает, оставлять ли копию платформе; **по умолчанию не оставлять**.
- [ ] Импорт **чужого** ключа = смена личности: сейфы перешифровываются на новый ключ (механика T3.12 pt.2b, включая самопроверку перед уничтожением старого доступа). Импорт **своего же** ключа (возврат файла) ключ не меняет и перешифровки не требует — ветки различаются сравнением npub.
- [ ] Занятый npub → 409. Голый npub без доказательства → 422 (это была дыра в удалённом `import` из T2.2).
- [ ] Rate-limit на попытки импорта: подбор фразы к чужому файлу должен стоить дорого.

**Acceptance:** личность из другого Nostr-клиента работает в Vimana без потери подписей; принесённый ключ не попадает на сервер, если пользователь этого не выбрал; повторный импорт своего файла не запускает перешифровку.

### T3.24 — Ридер `.dvlt`: открыть без платформы ✅ MVP

**pt.1 — локальное запечатывание существующего ключа ✅ (2026-08-01)**

- [x] На ступени 3 (копии у платформы нет) в профиле появляется «Запечатать ключ, который у вас уже есть»: вставить `nsec1…` или 64 hex-символа из старого бэкапа + парольная фраза → скачивается `.dvlt`. **Ни одного обращения к сети**: ключ разбирается, запечатывается и сохраняется в этой же вкладке.
- [x] Существует потому, что аккаунты, прошедшие старый путь T3.12, остались с `.txt`, где приватный ключ лежит **открытым текстом**. Это единственный способ закрыть такой файл, не трогая сервер, — платформе для этого нечего дать, у неё копии нет.
- [x] `npub` в файле выводится **из самого ключа** (`npubFromNsec`), а не берётся из аккаунта: файл обязан описывать то, что в нём лежит, иначе опечатка уедет вместе с чужим ключом.
- [x] Тесты: +2 (принимаются обе формы, которые реально бывают на руках — hex и `nsec1…`, мусор отвергается · публичная половина выводится из ключа).

**pt.2 — сам ридер ✅ (2026-08-01, решение владельца: два новых файла разрешены)**

- [x] `frontend/reader.html` + `frontend/src/reader.ts` — **вторая точка входа сборки**, а не роут SPA. Роут пришлось бы отдавать сервером, а страница существует ровно для случая, когда сервера у вас нет. Ни API-клиента, ни авторизации, ни импортов из приложения — только те же примитивы NIP-49, что и в профиле.
- [x] Без фреймворка и без инлайновых скриптов: прод-CSP `script-src 'self'` (T_SEC.2) молча заблокировал бы инлайн — те же грабли, что уже описаны в T3.10.
- [x] **Проверяет две вещи и говорит об этом на самой странице:** файл расшифровывается заданной фразой, и ключ внутри выводит тот публичный ключ, что записан рядом. Второе не формальность — контейнер с чужим npub возле вашего ключа выглядел бы совершенно нормально.
- [x] `type: "deal"` распознаётся и **честно отклоняется**: экспорт сделки отложен осознанно (`D-DVLT-PROTOCOL` п.6), и ридер, делающий вид, что откроет файл, которого никто не может создать, — ровно тот перебор, который запрещает DESIGNGUIDELINES §9.1.
- [x] Язык — английский, без i18n: то же осознанное ограничение, что у статических лендингов T3.10. Формат Nostr-стандартный, аудитория шире шести локалей.

**pt.3 — один самодостаточный файл ✅ (2026-08-01)**

- [x] `vite-plugin-singlefile` включён **только для ридера**: `npm run build` = сборка приложения, затем `vite build --mode reader`. Приложению плагин противопоказан — он инлайнит всё в один HTML и уничтожает и code splitting, и кэшируемые vendor-чанки; ридеру он нужен ровно за это же свойство.
- [x] Один конфиг с ветвлением по `mode`, а не второй файл конфигурации: разница между сборками — три строки, и держать их рядом дешевле, чем синхронизировать два файла.
- [x] `emptyOutDir: false` во второй сборке — она идёт после основной, и `dist/` уже не пуст.
- [x] Следствие: `reader.html` в `dist/` самодостаточен — сохраняется на диск и открывается без сети и без нас. Это и есть проверка обещания, а не его повторение.

**Формат `.dvlt` v2 — запечатан целиком (решение владельца 2026-08-01).** В v1 под паролем был только ключ, а `npub` и дата лежали открыто. Ключ при этом был в безопасности, но **файл называл своего владельца**: открыв его в текстовом редакторе, посторонний узнавал, чья это личность. Разница не косметическая — она превращает «файл на флешке» в «личность этого человека, на флешке». В v2 в открытом виде остаётся ровно то, что нужно, чтобы понять, **как** файл открывать: версия, тип и параметры KDF; всё остальное — внутри XChaCha20-Poly1305 под той же парольной фразой. Внутри рядом с `nsec` лежит и `ncryptsec` — чтобы строку можно было вставить в damus без второго прогона scrypt. Имя файла перестало содержать npub по той же причине. Обратной совместимости с v1 нет намеренно (CLAUDE.md): файлы выданы одному человеку и перевыпускаются за минуту.

**Гоча кодировки (найдено владельцем 2026-08-01).** Скачанный `.txt` с кодами восстановления открывался кракозябрами: `Blob` без `charset` пишет UTF-8, а Блокнот без BOM читает как cp1251. Исправлено BOM'ом и `text/plain;charset=utf-8` в обоих местах, где мы отдаём текст (коды и бэкап ключа). Файл, которому положено внушать доверие, не может выглядеть сломанным.

**Контекст.** Обещание переносимости проверяется ровно одним способом — файл открывается, когда платформы нет. Ридер должен быть статической страницей: файл + фраза → ключ и содержимое; ни аккаунта, ни сети.

- [ ] Статическая страница (тот же приём, что у лендингов T3.10: файл в `frontend/public/`, вся логика в отдельном `.js` — CSP `script-src 'self'` инлайн запрещает).
- [ ] Открывает оба типа `.dvlt`: `identity` (показывает npub и, по кнопке с предупреждением, `nsec`) и `deal` (содержимое сейфа + результат проверки цепи).
- [ ] Работает офлайн: страницу можно сохранить и открыть с диска. Никаких запросов наружу — это проверяется тестом, а не обещанием.
- [ ] Формулировки: «проверено локально, по состоянию на дату последнего якоря» — не «verified forever» (та же граница, что в `T3.19`/`T3.20`).

**Acceptance:** сохранённая на диск страница открывает файл без сети; проверка цепи сделки даёт тот же результат, что `GET /deals/{id}/chain` на живой платформе.

### T3.18 — Публичная страница личности ✅ MVP

**pt.1 — backend ✅ (2026-08-01)**

- [x] `GET /api/identities/{npub}` — **по ключу, а не по внутреннему id**: ключ и есть личность (`D-KEY-TIERS`), а id строки в публичной ссылке не значит ничего. Читается без авторизации: маркетплейс, где нельзя посмотреть, с кем имеешь дело, — это маркетплейс, где не заключают сделок.
- [x] Состав: имя, аватар, дата первой активности, УБА и уровень, `highest_verification_level`, счётчики доверия, число сделок, `key_lost` и — из `T3.23` — **дата смены ключа с прежним npub**: подписанное до смены остаётся за ключом, которого у личности больше нет, и контрагенту это стоит узнать заранее, а не удивиться.
- [x] Никогда, ни на одном уровне видимости: почта, телефон, адреса получения, что-либо из сейфа. Это не «приватные поля профиля», это другая категория данных.
- [x] Миграция `0038`: `users.public_profile ∈ full | minimal | hidden`, по умолчанию `full` — эти данные и так были публичны через метрики, и тихо спрятать всех значило бы поменять продукт под пользователями вместо того, чтобы дать им выбор.
- [x] **Видимость применяется одной функцией** `core.permissions.visible_to` / `require_visible`, вызываемой из **каждого** публичного среза: страницы, `/users/{id}/uba`, `/users/{id}/trust-metrics`. Скопированное по эндпоинтам условие ломается одинаково: следующее публичное поле добавит тот, кто про настройку не слышал, и «скрытый» аккаунт продолжит отвечать о себе по другому URL.
- [x] `hidden` → **404, а не 403**: 403 подтверждает существование аккаунта, то есть ровно то, что скрытие должно прекратить.
- [x] `get_current_user_optional` в `deps.py` — публичная страница обязана открыться и посетителю с протухшей сессией, а не встречать его ошибкой про учётные данные, которых у него не спрашивали.
- [x] Тесты: +3 (открывается по ключу без входа и не содержит приватных полей · `hidden` даёт 404 и прячет метрики, но владелец видит себя · `minimal` показывает факт существования и уровень верификации, и больше ничего).

**pt.2 — фронт ✅ (2026-08-01)**

- [x] `/i/{npub}` — маршрут **вне защищённых**: это страница, которую контрагент открывает, **прежде чем** решить иметь дело, и за логином она не отвечала бы ничьему вопросу.
- [x] 404 и «скрыто» рисуются одинаково. Различить их на фронте значило бы ответить ровно на тот вопрос, отвечать на который скрытие и отказывается.
- [x] `minimal` не рисует страницу с пустыми полями, а говорит прямо: личность показывает только факт существования и уровень проверки.
- [x] Каждое утверждение с датой: «здесь с», и — если было — когда сменился ключ, с прежним ключом рядом в коротком виде (`D-EVIDENCE-DECAYS`).
- [x] Настройка видимости — на `/profile`, а не в «Доступе и ключах»: это про то, каким тебя видят, а не про то, как ты входишь. Сохраняется сразу по клику: кнопка «Сохранить» на одно поле добавила бы только состояние, в котором радио говорит одно, а сервер другое.
- [x] `UserUpdate.public_profile` валидируется `Literal` на бэкенде — неизвестное значение в `visible_to` откатилось бы к `full`, то есть опечатка в UI молча раскрыла бы аккаунт, попросивший себя спрятать.
- [x] i18n ×6.

**Осталось за задачей:** обход графа по `peer_verified` (публичные поручительства как способ дойти до личности) — он же нужен `T3.19`, чтобы к завершённой личности приходили прогулкой по рёбрам, а не по прямой ссылке.

**Контекст.** Публичного представления пользователя в проекте нет вообще: эндпоинта `GET /api/users/{id}` не существует, есть только частные срезы (`/users/{id}/uba`, `/users/{id}/trust-metrics`, `/users/{id}/verifications`). Контрагент не может посмотреть, с кем имеет дело, одной ссылкой — и нечем поделиться.

Нужна и сама по себе, и как фундамент под T3.19: архив завершённых личностей — это та же страница с датой конца.

- [ ] `GET /api/identities/{npub}` — по **npub**, не по внутреннему `user.id`. Личность и есть ключ (решение №4), а внутренний идентификатор — деталь реализации, которой незачем быть в публичной ссылке.
- [ ] Состав: `display_name`, аватар, дата первой активности, УБА + уровень, `highest_verification_level`, счётчики trust (`verifications_issued/received_count`, `dealt_with_count`), число закрытых сделок, `key_lost_at` (если есть). Всё это **уже публично** в `UserOut` — новизна в том, что оно собрано в одном месте и адресуемо.
- [ ] **Никогда:** email, phone, `receiving_*`, содержимое сейфов.
- [ ] **Обход графа — только по `peer_verified`** (решение владельца 2026-07-27). Три вида рёбер из T2.4 — три разных ответа:
  - `peer_verified` **публично по природе**: поручительство, которого никто не видит, не имеет веса. Без прослеживаемой цепочки бейдж превращается в утверждение платформы, а не сети. Плюс семантика верна — это публичное высказывание *о человеке*.
  - `dealt_with` — **нет**. Список клиентов перевозчика и маршрутов отправителя, коммерческая тайна. Сделка — частная транзакция двоих.
  - `invited` — **нет**. Социальный граф, кто кого привёл.
  
  Счётчики (`dealt_with_count` и прочие) остаются публичными как сейчас: число — не то же самое, что имена.
- [ ] Настройки видимости в профиле: `public_profile ∈ {full, minimal, hidden}`. `minimal` — только факт существования и уровень верификации; `hidden` — 404 для всех, кроме владельца. Дефолт `full`.
- [ ] **Видимость обязана распространяться на уже существующие метрики** — `GET /users/{id}/uba` и `GET /users/{id}/trust-metrics` отдают данные любому авторизованному и о новой настройке ничего не знают. Если их не тронуть, `hidden` будет прятать страницу, но не цифры на ней: закрылся от посторонних, а УБА и счётчики доверия по-прежнему читаются прямым запросом. Настройку применять в одном месте (общая проверка), а не копировать условие по эндпоинтам — иначе следующий публичный срез снова про неё забудет.
- [ ] Тест на каждый уровень видимости **против всех** публичных срезов сразу, а не только против страницы личности.
- [ ] **Скрытая личность в графе не исчезает, а обезличивается:** узел рисуется как «личность, скрытая владельцем», без имени и без ссылки, а её поручительство **продолжает считаться** в пользу того, за кого ручались. Скрыть можно себя, но не свой вклад в чужую репутацию — он уже не только твой.
- [ ] Фронт: `/i/{npub}`, доступна без авторизации. Чип «ключ утрачен» для завершённых. Переход к завершённой личности возможен **прогулкой по поручительствам** от живых участников — архив вплетён в сеть, а не стоит отдельным зданием.
- [ ] Каждое доказательство на странице показывается **с датой** — см. `T_TRUST.1`.

**Acceptance:** любую личность можно открыть по ссылке с её npub; приватные поля не утекают ни при каком значении видимости; `hidden` действительно скрывает.

### T3.19 — Архив завершённых личностей ✅ (2026-08-01)

**Контекст (решение владельца 2026-07-27).** Завершение личности — не смерть аккаунта, а **смена жанра**: из участника сети он становится историческим документом. Способность действовать и достоверность прошлых действий у нас впервые независимы — потеря ключа отнимает первое и не трогает второе.

> **Identity retired. Evidence lives on.**

Слово **Archive**, не Cemetery: кладбище — место утраты, архив — место сохранения, и второе есть буквально DealVault. Метафора — музей авиации: под выведенным из эксплуатации бортом пишут часы налёта, и это уважение, а не позор.

**UI завершённой личности — смена режима, а не урезанный профиль:**
- [x] Модалка **один раз**, при первом входе после утраты: что произошло, что осталось, что теперь. Постоянная модалка на необратимое состояние — нытьё, её закрывают не читая. `ArchiveNotice`, момент показа определяется `archive_notice_seen_at`, а не localStorage: решение принимается один раз на аккаунт, а не один раз на браузер.
- [x] Дальше — постоянный баннер (состояние не меняется, значит видно всегда) и профиль в виде архивной карточки: период работы, публичная запись, историческая активность, даты первой и последней подписи. Карточка (`ArchiveRecordCard`) одна и та же в профиле и на публичной странице — числа совпадают по построению, а не потому, что две реализации сошлись.
- [x] Кнопки действий **прячутся**, а не отдают 403 по клику: публикация рейса на Dashboard. Приглашения **оставлены** — сервер их не запрещает, они ничего не подписывают, и спрятать рабочую кнопку значило бы соврать в другую сторону.
- [x] Никакого «удалить аккаунт»: запись остаётся, потому что она наполовину чужая.
- [x] Трёхпозиционная настройка видимости у завершённой личности **скрыта**: вопрос теперь один и задаётся в модалке. Два контрола над одним исходом — это гонка, в которой проигрывает тот, которому пользователь поверил.

**Окно 15 дней — точная механика (решение владельца 2026-07-27):**
- [x] Отсчёт от `key_lost_at`. **Бездействие → экспонат становится видимым** по истечении срока. Никаких подтверждений не требуется: дефолт — сохранение, как во всём продукте. Дата **вычисляется** (`core.permissions.archive_window_ends_at`), не хранится: колонка была бы второй истиной о дате, которая уже есть, и разошлась бы с ней при первой же правке.
- [x] Владелец может войти и выбрать **«Нет»** — тогда страница закрывается **навсегда**, и это решение не отменяется (`archive_choice = 'hide'`, повтор → 409).
- [x] **Асимметрия намеренная:** ошибочное «Нет» ведёт к приватности, ошибочное бездействие — к публичности, но с 15 днями на исправление. Необратима только безопасная сторона. `'show'` перезаписывается сколько угодно, `'hide'` — никогда.
- [x] **Ничего не удаляется ни в одном случае.** Закрывается витрина: страница, `display_name`, участие в агрегатах. Остаются цепь, подписи, события сделок — по ENVIRONMENT §8.2 это критические данные, и они наполовину принадлежат контрагенту. Слово «кремация» в UI **не использовано**.
- [x] Скрытая личность остаётся в графе обезличенным узлом, её поручительства продолжают работать (T3.18).
- [x] **Чтобы выбрать, надо войти.** Завершённый аккаунт войти может — для того `declare-lost` и сохраняет доступ. Но если утрачен и ключ, и доступ, человек выбора не увидит и сработает дефолт. Сказано вслух в модалке (`archive.modalNoAccess`).
- [x] Уведомление об окне: разовая модалка при первом входе **и письмо** (`send_archive_window_opened`, отправляется из `declare-lost`). ⚠️ **Прежняя запись здесь была неверна**: я написал «доставка почты в проекте ещё не работает», хотя она работает с 2026-07-30 (`D-MAIL-VIA-MAILU`). Причина отсрочки оказалась выдуманной, и из-за неё письмо не отправлялось два дня. Письмо существует ровно ради случая, который модалка покрыть не может: владелец потерял ключ **и** перестал заходить — для него срабатывает дефолт, и узнать об этом он должен от нас, а не из поисковой выдачи. Дата передаётся аргументом из `archive_window_ends_at`, а не считается заново: второй расчёт — это способ развести письмо и экран по разным дням.
- [x] Закрытие проверяется **в `visible_to`**, а не в эндпоинте страницы: следующий публичный срез унаследует правило, ничего не зная про архив. Как следствие `PATCH /auth/me` с `public_profile` у закрытого архива отвечает 409 — настройка, которая пишется, но ни на что не влияет, врёт.
- [x] Решение хранится **отдельной колонкой**, а не выводится из `public_profile`: «скрыт при живом ключе» — обратимая настройка, «витрина закрыта» — окончательное решение. Склеив их, мы сделали бы необратимым выбор, который человек делал как обратимый, и не спросив его.

**Агрегаты — только настоящие числа:**
- [x] Что есть сразу: сделки и закрытые сделки, рёбра доверия (уже были на странице), УБА, длина цепи, первая и последняя подписи. **Подписью считается только запись с `nostr_sig`**: запись цепи без неё — реальное событие, но не доказательство, и называть её подписью значило бы приписать строке то, чего в ней нет. Поэтому пара `signatures / chain_entries`.
- [x] Километры — через `core.airports.route_distance_km`. Килограммы — из `Trip.capacity`, **по различным рейсам**: две посылки на одном борту не удваивают борт.
- [x] **Формулировка «по прямой» — в самом имени поля** (`straight_line_km`), чтобы подпись на экране нельзя было написать иначе. То же для рекорда (`longest_hop_km`).
- [x] «Хвосты»: самый длинный хоп с маршрутом. Редчайшие коридоры и сложные цепочки — не сделаны: для них нужен обход графа по `peer_verified`, который остаётся открытым (см. ниже).
- [x] Метрик, которых нет, не изобретать. **Каждый итог показан со знаменателем**, из которого посчитан: `deals_closed/deals_total`, `routes_measured/routes_closed`, `signatures/chain_entries`. Ни процентов, ни средних, ни рейтингов — ни одного из них никто не измерял. Неизмеримое отсутствует, а не равно нулю: уверенный «0 км» утверждал бы измерение, которого не было.

**Тон и границы утверждений:**
- [x] «Verified forever» **не написано** — и закреплено тестом, а не бдительностью: `ArchiveRecordCard.test.tsx` падает на `forever`, `tamper-proof`, `guaranteed`. Правильная формулировка («проверяемо независимо по состоянию на <дата последнего якоря>») зависит от `T3.20` и появится вместе с якорями.
- [x] Поручительства завершённой личности **остаются в силе**: бейдж выдан, когда ключ был жив, подпись проверяется до сих пор. Сказано в модалке и в сноске карточки.

**Осталось открытым:**
- [ ] Письмо об окне — когда заработает доставка почты. Дата в письме обязана совпадать с датой в модалке; обе берутся из `archive_window_ends_at`, отдельной константы не заводить.
- [x] **Редкие коридоры сделаны 2026-08-02** — и здесь была моя ошибка в постановке: я слил в одну строку две разные вещи. Редкость коридора считается по таблице рейсов (сколько всего рейсов на площадке по этому маршруту), ровно как в `core/publish_filter.py`, и графа не требует вовсе. Графа требуют только длинные цепочки поручительств, они и остаются открытыми. Счётчик рейсов печатается рядом с коридором: редкость — свойство карты, а не человека, и строка не должна читаться как комплимент.
- [ ] Длинные цепочки поручительств — нужен обход графа по `peer_verified` (тот же, что нужен для рекомендаций).

**Осознанное следствие решения №4:** экспонаты в архиве **несвязанные**. Ссылки «эта личность продолжает ту» не будет — преемственности мы отказали намеренно. Династии = отмена решения №4, не фича архива.

**Acceptance:** вошедший в завершённый аккаунт понимает, что произошло и что делать, без чтения документации; страница читается как исторический документ, а не как сломанный профиль; ни одно утверждение на ней не сильнее того, что механизм действительно доказывает.

### T3.20 — Включить якоря цепи ✅ (2026-08-01, включено и проверено на живом тике)

**Контекст.** `core/chain_anchor.py` написан в T3.6 и **ни разу не работал**: публикация выключена (`NOSTR_PUBLISH_ENABLED=false`), `CHAIN_ANCHOR_NSEC` не задан. А до 2026-07-27 не работал бы в любом случае — воркер шёл с пустым реестром задач.

Без якорей цепь доказывает целостность против того, у кого есть доступ к БД, но **не против самой платформы**: `seq` присваиваем мы. С якорями head, опубликованный на relay'ях, которыми мы не владеем, фиксирует всё под собой чужим временем.

- [x] `CHAIN_ANCHOR_NSEC` заведён как переменная (был только в комментарии — код его читал, а `.env.example` о нём не упоминал, то есть задать его было неоткуда узнать). Документирован вместе с условием включения: якорение требует **трёх** значений сразу — `NOSTR_PUBLISH_ENABLED=true`, ключ и хотя бы один **сторонний** relay. Генерация — командой из ENVIRONMENT §5.
- [x] Проверено чтением: `anchor_deal_head` пишет `DealChainAnchor` только при `any(relays.values())`, при полном отказе строки нет и следующий тик берёт ту же голову — покрыто `test_anchor_not_recorded_when_every_relay_rejects` и `test_growth_past_an_anchor_is_picked_up_again`. Тик зарегистрирован в `beat_schedule` и модуль включён в `_TASK_MODULES` (без этого воркер отвечал бы `Received unregistered task` — так и было до 2026-07-27).
- [x] API отдаёт **где проверять и докуда**: `anchor_relays` (только принявшие — отказавший relay в списке отправил бы аудитора искать несуществующее событие), `anchor_third_party_relays` (без нашего strfry: голова, дошедшая только до него, не доказывает о нас ничего) и `unanchored_entries`. В `ArchiveRecord` — `last_anchor_at` и `anchored_deals`.
- [x] **Граница утверждения** реализована как данные, а не как обещание: строка «проверяемо независимо по состоянию на <дата>» рисуется **только** при непустом `last_anchor_at`, называет число сделок из общего числа и прямо говорит, что записанное после этой даты держится пока лишь на нашей проверке целостности. Отсутствие якорей → строки нет вовсе.
- [x] **Включено на сервере 2026-08-01.** Первый живой тик: `published: 1`, приняли `nos.lol`, `relay.primal.net`, `offchain.pub`, `nostr.mom` — четыре стороны, которыми мы не владеем. Ровно это и требовалось: голова цепи лежит у них с их временем.
- [x] Первый тик вскрыл три поломки подряд, и все три оказались реальными (см. TECHSTATE §3): неисполняемый `write_policy.py` (WoT-gate не фильтровал, а падал на каждом событии — на всём, не только на якорях), ключ якоря не в whitelist, и список релеев, где **ни один** не принимает запись от незнакомого ключа. Отдельным следствием: отказ по NIP-20 не логировался вовсе, из-за чего «отказал» и «не дошли» были неразличимы.
- [x] Свой strfry принимает якорь: после прогона `refresh_allowed_pubkeys` в `celery-worker` (у `backend` нет тома `nostr_whitelist` — см. TECHSTATE §3) политика отвечает `accept` на ключ якоря. Проверено скармливанием запроса плагину напрямую, без ожидания новой записи в цепи: `printf '{"type":"new","event":{"id":"test","pubkey":"<anchor>"}}' | python3 /etc/write_policy.py`.
- [x] Маршрут задачи проверен целиком: `anchor_deal_chains.delay().get()` вернул результат, то есть диспетчер → Redis → очередь `notifications` → воркер работает. Это и была единственная содержательная проверка — до 2026-07-27 воркер поднимался с пустым реестром и молча ронял все отправленные задачи. Часовой таймер beat отсчитывается от старта контейнера и проверяется наблюдением, не тестом.

**Acceptance:** head'ы публикуются на сторонние relay'и; любой может проверить состояние цепи на дату независимо от нас; формулировки в продукте не сильнее этого.

### T_TRUST.1 — Свежесть доказательств (сквозная) ✅ MVP (2026-08-02)

**Контекст (решение владельца 2026-07-27).** Все доказательства устаревают. Верификация вчера — сильная, неделю назад — сильная, месяц — нормальная, год — под вопросом, пять лет — много воды утекло. Сейчас продукт этого не отражает: бейдж либо есть, либо нет.

Это уровень корня, а не отдельного экрана: касается T2.1 (бейджи), T2.4 (Trust Graph), T3.1 (УБА) и любого места, где показывается «проверен».

- [x] **Дата у каждого доказательства.** В API добавлены `UserVerificationSummary.highest_level_at`, `IdentityOut.verified_at` / `last_vouched_at`, `TrustMetricsOut.last_vouched_at`. Счётчик без даты молчит о времени, а молчание читается как «недавно».
- [x] Никогда не показывать «проверен» без «когда» — и это **правило компилятора, а не ревью**: у `VerificationBadgeChip` проп `at` обязательный. Будь он необязательным, забыть дату было бы путём наименьшего сопротивления, и голый бейдж вернулся бы по одному месту за раз. `null` — допустимый ответ, он рисуется как «дата неизвестна»: выглядит хуже даты, и правильно, потому что это хуже.
- [x] Функция затухания в УБА. Гасит **бонус**, а не человека: `1.0 + (factor − 1.0) × freshness`. Старое доказательство делает обычным, но никогда не хуже непроверенного — иначе верификация стала бы обязательством, которого выгодно избегать.
- [x] Решена и записана шкала: `≤7 дн → 1.00`, `30 → 0.95`, `год → 0.70`, `5 лет → 0.40` (пол), между точками линейно. Единственный источник в коде — `core/freshness.py`, зеркало решения в TECHSTATE.
- [x] Заголовок в Decision Log — `D-EVIDENCE-DECAYS` (существовал с 2026-07-27, дополнен шкалой).
- [x] **Побочно вскрыт настоящий баг:** `refresh_highest_level` и счётчик активных бейджей игнорировали `expires_at`, который этот же модуль и проставляет (+365 дней при peer-верификации). Бейдж, истёкший два года назад, держал уровень на аккаунте вечно. Дата истечения, которую система пишет и не читает, хуже её отсутствия: это обещание на профиле, за которым ничего не стоит.

**Не сделано, осознанно:**
- [ ] **Взвешивание рёбер Trust Graph по свежести в BFS — не реализовано, потому что у веса нет потребителя.** `TrustEdge.weight` заполняется и не читается никем; `bfs_circles` прямо говорит «weights are ignored — we return raw circles». Ввести взвешивание значило бы сначала выдумать ранжирование кругов, которого в продукте нет, и подогнать под него свежесть. Вместо этого дата вынесена туда, где граф **показывается** (`last_vouched_at`). Возвращаться сюда — когда появится экран, который круги ранжирует.
- [ ] Уровень пересчитывается на INSERT/revoke бейджа, а не по таймеру, поэтому может оставаться просроченным до следующего такого события. Это меньшая ложь, чем прежняя, и чинится либо часовым тиком, либо вычислением уровня на чтение — решать вместе с тем, кто первым упрётся.

**Acceptance:** ни в одном месте продукта доказательство не показывается без своей даты; возраст влияет на УБА и на вес рёбер доверия; шкала зафиксирована в PRD, а не разбросана по коду.

### Открытые зависимости фазы

- ~~**Структурные origin/destination**~~ — **снято 2026-07-27, зависимости не было.** Я записал её по догадке, не проверив, что кладёт форма: `AirportSelect` отдаёт IATA-код, а `core/airports.py` с T1.10 держит координаты и haversine. Расстояние считается сегодня (`route_distance_km`), обе «заблокированные» фичи разблокированы. Урок общий: ограничение, попавшее в PRD без проверки кода, живёт как факт и тормозит работу.
- **Реальный налёт вместо прямой — сознательно не делаем.** Внешний сервис (FlightAware/AviationStack/OpenSky) даёт +3–7% точности ценой ключа, лимитов, денег и новой точки отказа, а для **будущего** рейса трека не существует вовсе — он появляется после вылета. Плюс отвечает не на наш вопрос: мы меряем маршрут доставки, а не мили самолёта. Проект уже отказывался от внешних API в пользу локального стека (T2.1, KYC). Если понадобится — отдельная задача с явной ценой.

---

## 📣 ЭТАП 3.8 — Контакты, каналы и оповещения (Фаза 3.8)

> **Цель:** довести до одного поля то, что сейчас растянуто на форму, и до одного механизма то, что сейчас растянуто на четыре канала. Пользователь вводит **почту или телефон**, выбирает, куда прислать код, и этим же жестом получает канал для оповещений по сделкам. Решения владельца 2026-08-06 зафиксированы в TECHSTATE `D-CONTACT-CHANNELS`, схема — IMPLEMENTATIONPLAN §6.8.
>
> **Что уже есть и переиспользуется, а не пишется заново** (проверено в коде 2026-08-06): Telegram-бот со связыванием через `/start {token}` (`api/telegram.py`), отправка в WhatsApp через Twilio (`core/whatsapp.py`), роутинг оповещений по трём каналам (`tasks/notifications.py:_notify_user`), тумблеры каналов в кабинете (`ProfilePage.tsx`), и — главное — обкатанная на почте машина кодов: TTL 15 мин, 5 попыток, хранение хешем, cooldown 60 с, rate-limit в два слоя (`core/email_verification.py`). Этап **обобщает** эту машину на четыре канала, а не заводит вторую.
>
> **Личность не затрагивается.** Телефон, почта, Telegram — контакты и способы входа. Личность остаётся npub, служебный keypair при регистрации выдаётся как сегодня (T3.12), лестница владения `D-KEY-TIERS` не меняется. Регистрация по телефону даёт ровно ту же ступень 1, что и регистрация по почте.
>
> **⚠️ Живой канал ровно один — почта (уточнено владельцем 2026-08-06).** На проде **не заданы** `TELEGRAM_BOT_TOKEN` / `TELEGRAM_BOT_USERNAME`, аккаунта Twilio нет, аккаунта Telegram Gateway нет. Из этого следует неприятное, но важное: **Telegram и WhatsApp из T1.7 никогда не доставили ни одного сообщения.** `send_telegram` выходит на первой строке при пустом токене (`core/telegram.py:11`), `send_whatsapp` — при пустом `TWILIO_ACCOUNT_SID` (`core/whatsapp.py:9`). Обе помечены в TECHSTATE `✅ готово (T1.7)` — это про код, не про доставку. Тот же разрыв, что был у почты до 2026-07-30, и найден он тем же способом: вопросом «а оно вообще дошло?», а не тестом.
>
> **Мёртвыми оказались и админские тревоги.** Все пять путей идут через `send_telegram`: сканер загрузок не отвечает (T3.8), отложенная проверка нашла заражённый файл (T3.8), ZAP нашёл High (T_TEST.7), новая заявка в waitlist (T1.18), уведомления по сделкам (T1.7). Самый острый — второй: по решению владельца 2026-08-02 заражённый файл **не блокируется автоматически**, «отметить и сказать человеку», то есть сообщение и есть весь механизм. Оно не уходит. Хуже того, `logger.warning` в фолбэке срабатывает только когда пуст `ADMIN_TELEGRAM_CHAT_IDS`; если chat_id заданы, а токена нет — не остаётся даже строчки в логе. **Создание бота и проверка этих путей — работа вне этого этапа и раньше него**, она чинит уже обещанное.
>
> **Каналы включаются не кодом, а флагами.** Абстракция пишется целиком и сразу; код любого канала не блокируется отсутствием аккаунта — блокируется только **включение и живая проверка**. Готовность на 2026-08-06:
>
> | Канал | Код | Включение | Что нужно снаружи |
> |---|---|---|---|
> | Email | есть | **живой** | ничего — доставка починена 2026-07-30 (Mailu + SES, DKIM/SPF/DMARC pass) |
> | Telegram deep-link | есть (T1.7) | ждёт | создать бота у @BotFather, задать `TELEGRAM_BOT_TOKEN` / `TELEGRAM_BOT_USERNAME` / `TELEGRAM_WEBHOOK_SECRET`, зарегистрировать webhook. Часы |
> | Telegram Gateway (код по номеру) | нет | ждёт | аккаунт + депозит на `gateway.telegram.org`. Дни |
> | SMS | нет | ждёт | аккаунт Twilio + A2P-регистрация: 10DLC в США (нужно юрлицо/EIN), Sender ID в ОАЭ (нужна местная лицензия). Недели |
> | WhatsApp | есть (T1.7) | ждёт | аккаунт Twilio + WhatsApp Business Account + верификация бизнеса в Meta + одобрение authentication-шаблона. Недели |
>
> Первая итерация (решение владельца 2026-08-06): **email**. Остальные каналы пишутся кодом и тестами в этом же этапе, но выходят из-под флага по мере появления аккаунтов — включение станет правкой переменной окружения, а не задачей. Порядок появления от владельца ожидается такой: Telegram-бот (часы) → Telegram Gateway (дни) → Twilio и всё, что за ним (недели).
>
> **Правило закрытия для каналов (урок T3.11, обязательное).** Ни одна задача канала не помечается ✅ MVP на замоканных тестах. Мок доказывает, что мы правильно позвали функцию, и ничего не говорит о том, дошло ли сообщение — ровно так email-подтверждение полгода числилось готовым, не доставив ни одного письма. Задача канала закрывается в два шага: **✅ код** (тесты зелёные, флаг выключен) и отдельно **✅ доставка** (сообщение получено живым получателем на проде, дата записана). Второй шаг может отставать от первого на недели — это нормально и честно, в отличие от одной галочки, которая означает то одно, то другое.
>
> **Почему SMS для нашего коридора — худший канал, а не первый.** Запуск идёт по ОАЭ ↔ США. Это ровно два самых зарегулированных A2P-рынка из возможных, и там, где SMS стоит недель ожидания и юрлица, Telegram Gateway стоит депозита и дня. Приоритет каналов выбран по достижимости аудитории и срокам, а не по привычке считать SMS каналом по умолчанию.

### T3.25 — Модель контактов: `user_contacts` + обобщение машины кодов ⬜

**Контекст.** Сегодня контакт — это набор колонок на `users`: `email`, `phone`, `telegram_chat_id`, `whatsapp_number`, плюс пять колонок состояния кода (`email_verification_*`). Пятый канал в этой схеме означает пять новых колонок и форк `email_verification.py`. Задача переносит истину в две таблицы, оставляя `users.email` / `users.phone` денормализованным «основным контактом» — их читает половина кода, и переписывать её ради чистоты схемы этот этап не будет.

**Схема (миграция `user_contacts`):**
- [ ] `user_contacts(id, user_id FK, channel, value, verified_at TIMESTAMPTZ NULL, is_login BOOL, created_at)`. `channel ∈ {email, sms, whatsapp, telegram}`.
- [ ] Частичный уникальный индекс `UNIQUE(channel, value) WHERE verified_at IS NOT NULL` — **неподтверждённый дубль не блокирует**. Иначе заявка на чужой телефон, которую никто не подтвердил, навсегда запирает настоящего владельца: типовая дыра, которую полный `UNIQUE` создаёт тихо.
- [ ] `verification_challenges(id, user_id NULL, channel, value, code_hash, expires_at, attempts, sent_at, purpose, created_at)`. `user_id` nullable: при регистрации по коду аккаунта ещё нет, а челлендж уже есть.
- [ ] Backfill: `users.email` → contact `email` с `verified_at = email_verified_at`; `users.phone` → contact `sms`, **неподтверждённый** (никто никогда его не подтверждал — записать подтверждённым значило бы соврать в единственном поле, ради которого таблица заводится); `telegram_chat_id` / `whatsapp_number` → соответствующие каналы, подтверждённые (chat_id получен через `/start`, то есть владение доказано жестом).

**Backend:**
- [ ] `core/email_verification.py` → `core/verification.py`: та же логика TTL / попыток / cooldown / сжигания кода при исчерпании, но параметризованная каналом и работающая с `verification_challenges` вместо колонок `users`. Исключения (`CooldownActive`, `CodeExpired`, `TooManyAttempts` и остальные) переезжают как есть.
- [ ] Нормализация телефона в **E.164** через библиотеку `phonenumbers` — не своей регуляркой. Номер без нормализации не является идентификатором: `+971 50 123 45 67`, `0501234567` и `971501234567` — один и тот же человек и три разные строки, а `UNIQUE` этого не знает.
- [ ] `POST /api/auth/email/request-code` и `/verify` продолжают работать на новом механизме — обратной совместимости не требуется (CLAUDE.md), но экран `/verify-email` из T3.11 ломать посреди этапа незачем.

**Тесты:** нормализация E.164 (три формы одного номера → одна строка) · невалидный номер → 422 · неподтверждённый дубль не блокирует чужое подтверждение · подтверждённый дубль отклоняется · backfill сохраняет `email_verified_at` · телефон после backfill неподтверждён · все кейсы кода из T3.11 зелёные на новом механизме.

**Acceptance:**
1. Один код-механизм обслуживает все каналы; добавление канала не трогает `core/verification.py`.
2. Существующие подтверждения почты переживают миграцию без потери `verified_at`.
3. Полный backend-сьют зелёный.

### T3.26 — Абстракция канала доставки ⬜

- [ ] `core/channels/` с единым интерфейсом `send_code(channel, value, code, locale)` и реализациями `email`, `telegram`, `telegram_gateway`, `sms`, `whatsapp`.
- [ ] Флаги `CHANNEL_EMAIL_ENABLED` / `CHANNEL_TELEGRAM_ENABLED` / `CHANNEL_TELEGRAM_GATEWAY_ENABLED` / `CHANNEL_SMS_ENABLED` / `CHANNEL_WHATSAPP_ENABLED`. По умолчанию живы только email и telegram.
- [ ] `GET /api/auth/channels?identifier=…` — какие каналы доступны **для этого идентификатора**: почта → только email; телефон → sms / whatsapp / telegram_gateway, из них включённые. Выключенный канал в UI не предлагается вовсе, а не предлагается и падает.
- [ ] Отправка — Celery-таском, как `send_verification_code` из T3.11 и по той же причине: транспорты синхронные, в request-path они держали бы event loop.

**Тесты:** выключенный канал не попадает в ответ `/channels` · попытка запросить код выключенным каналом → 400, а не 500 · для email-идентификатора не предлагается sms · каждая реализация замокана (в реальные шлюзы сьют не ходит, как SMTP в T3.11).

### T3.27 — Telegram как канал кода (deep-link) ⬜

**Предусловие вне задачи:** бот создан у @BotFather, `TELEGRAM_BOT_TOKEN` / `TELEGRAM_BOT_USERNAME` / `TELEGRAM_WEBHOOK_SECRET` заданы на проде, webhook зарегистрирован. Код T1.7 написан и ждёт этого с самого T1.7 — то есть механика `/start {token}` не проверена вживую ни разу. Первое, что делает эта задача, — **проверяет, что связывание вообще работает**, и только потом строит поверх него канал кода.

- [ ] Поверх существующего `/start {token}`: `POST /auth/otp/request` с `channel=telegram` возвращает ссылку `t.me/<bot>?start=<nonce>`, бот присылает код в ответ на `/start`.
- [ ] Nonce живёт в `verification_challenges`, не в `users.telegram_link_token` — токен связывания и челлендж кода это разные сущности с разным TTL.
- [ ] Тем же жестом заполняется `telegram_chat_id` и заводится контакт канала `telegram`: **вход и подписка на оповещения происходят одним действием**, второго экрана «а теперь подключите Telegram» не появляется.
- [ ] Webhook уже защищён `X-Telegram-Bot-Api-Secret-Token` (`api/telegram.py:53`) — не трогаем.

**Тесты:** `/start` с валидным nonce → код отправлен, chat_id записан, контакт создан · истёкший nonce → код не отправлен · повторный `/start` тем же nonce не выдаёт второй код · сообщения бота замоканы.

**Acceptance (два шага, по правилу закрытия каналов):** ✅ код — тесты зелёные, флаг выключен. ✅ доставка — код получен живым Telegram-аккаунтом на проде **и** попутно проверено, что пять админских путей (`notify_admins_scanner_down`, `notify_admins_infected_file`, `notify_admins_zap_findings`, waitlist, уведомления по сделкам) наконец доходят. Дата живой проверки записывается здесь.

### T3.28 — Форма из одного поля: вход и регистрация одним путём ⬜

**Решение владельца 2026-08-06: код-first, без пароля.** Пароль в регистрации отсутствует; `password_hash` уже nullable с T3.11, аккаунты без пароля живут с T3.13/T3.14 — модель к этому готова, менять её не нужно.

**Backend:**
- [ ] `POST /api/auth/otp/request {identifier, channel}` → **202 всегда**, независимо от того, существует аккаунт или нет. Разный ответ на «есть» и «нет» — это оракул перебора аккаунтов, а форма входа публична по определению.
- [ ] `POST /api/auth/otp/verify {identifier, code}` → аккаунта нет → создаётся (служебный keypair выдаётся как обычно, T3.12), есть → вход. Возвращает JWT + флаг `is_new`.
- [ ] `POST /api/auth/register` и `UserCreate` удаляются вместе с парольной веткой регистрации. Вход по паролю **остаётся** для существующих аккаунтов (`/auth/login` не трогаем) — ломать вход живым пользователям ради чистоты нового пути нельзя.
- [ ] `display_name` спрашивается **после** верификации, отдельным шагом онбординга: до подтверждения контакта аккаунта нет, и спрашивать имя не у кого.

**Frontend:**
- [ ] `LoginPage` и `RegisterPage` сливаются в один путь: одно поле → определение типа идентификатора на лету → карточки доступных каналов → экран кода → экран имени для новых. Кнопки Passkey и Nostr остаются рядом без изменений.
- [ ] i18n — **все шесть локалей** (EN/RU/UA/PL/FR/ES), по правилу T3.11: экран новый, fallback на EN для продукта с шестью языками не годится.

**Тесты:** запрос кода на несуществующий идентификатор → 202 и ничего не создано · verify новым идентификатором → аккаунт создан, `is_new=true` · verify существующим → вход, `is_new=false` · вход по паролю у старого аккаунта продолжает работать · неверный код → 400 и счётчик попыток · 5 попыток сжигают код · телефон и почта дают разные наборы каналов.

### T3.29 — Антифрод каналов ⬜

**Контекст.** Публичный «отправить код» на платный канал — это классическая схема выкачивания денег (SMS pumping): атакующий гонит коды на премиум-номера подконтрольного оператора и делит выручку. Счёт приходит нам. Задача обязательна **до** включения любого платного канала и потому идёт раньше T3.30/T3.31.

- [ ] Лимиты: на идентификатор, на IP, на страну (в дополнение к существующим двум слоям — slowapi + nginx-зона, см. `feedback_two_layer_rate_limit`).
- [ ] Allowlist стран для платных каналов; страны вне списка получают только бесплатные.
- [ ] Turnstile/капча — только на подозрительных запросах, не на всех: капча на каждом входе стоит конверсии, а ловит то же самое.
- [ ] Метрика стоимости по каналам в существующий `core/metrics.py` — аномалия должна быть видна цифрой, а не счётом от провайдера в конце месяца.

**Тесты:** превышение лимита на номер → 429 · превышение на IP → 429 · страна вне allowlist не получает платный канал · бесплатные каналы при этом работают.

### T3.30 — SMS-гейт (Twilio Verify), выключен флагом ⬜

- [ ] Реализация канала `sms` через **Twilio Verify** — он закрывает генерацию, доставку, повтор и антифрод одним API, и Twilio уже в зависимостях ради WhatsApp. Наша машина кодов при этом остаётся источником истины по попыткам: два счётчика на одну попытку — гарантированное расхождение.
- [ ] `CHANNEL_SMS_ENABLED=false` до завершения A2P-регистрации. Код и тесты закрываются в этой задаче, включение — отдельным решением владельца.
- [ ] В ENVIRONMENT записать, что именно блокирует включение (10DLC / Sender ID), чтобы через два месяца это не выяснялось заново.

**Тесты:** канал выключен → не предлагается и не отправляет · при включённом флаге Twilio-клиент замокан · ошибка провайдера не роняет запрос (202 всё равно).

**Acceptance (два шага):** ✅ код — тесты зелёные, флаг выключен. ✅ доставка — SMS получена на реальный номер в стране из allowlist, после A2P-регистрации. Второй шаг ожидаемо отстаёт на недели.

### T3.31 — WhatsApp как канал кода, выключен флагом ⬜

- [ ] Отправка authentication-шаблоном: бизнес-инициированное сообщение вне 24-часового окна иначе невозможно. Sandbox Twilio для прода не годится — там получатель обязан сам написать join-код.
- [ ] `CHANNEL_WHATSAPP_ENABLED=false` до верификации бизнеса в Meta и одобрения шаблона.
- [ ] То же ограничение действует и на **оповещения** в WhatsApp, которые уже работают с T1.7: вне окна — только шаблон. Проверить и записать, что именно сейчас уходит, — сегодня это `send_whatsapp` со свободным текстом (`core/whatsapp.py`), и она молча глотает исключение.

**Тесты:** канал выключен → не предлагается · шаблонная отправка замокана · свободный текст вне окна не отправляется.

**Acceptance (два шага):** ✅ код — тесты зелёные, флаг выключен. ✅ доставка — код получен в WhatsApp на реальный номер после одобрения шаблона; тем же шагом проверяется, доходят ли **оповещения** T1.7, которые до сих пор не уходили ни разу.

### T3.32 — Матрица оповещений в личном кабинете ⬜

**Контекст.** Сейчас канал включается на **все** события разом — три булевых поля (`notify_email` / `notify_telegram` / `notify_whatsapp`). Получать в Telegram только «груз доставлен», а остальное почтой, нельзя.

- [ ] Классы событий: *сделка* (согласована / принята / в пути / доставлена / подтверждена / закрыта), *сроки* (дедлайн через 24 ч), *сообщения в сейфе*, *верификация и доверие*, *споры и арбитраж*, *безопасность*.
- [ ] Хранение — JSONB `users.notification_prefs` + словарь дефолтов в коде; неизвестный ключ читается как дефолт. Отдельная таблица не нужна: выборка всегда по одному пользователю.
- [ ] **Класс «безопасность» неотключаем.** Правило уже действует де-факто — `send_verification_code`, `send_recovery_code_used`, `send_platform_copy_deleted` и `send_archive_window_opened` намеренно не смотрят на `notify_email` — но нигде не записано. Записать явно: письмо «в аккаунт вошли по коду восстановления» это не подписка, а единственный сигнал, по которому владелец узнаёт о чужом входе.
- [ ] UI: матрица событие × канал + привязка/отвязка каналов в одном месте. Три старых тумблера мигрируют в дефолты, а не пропадают.
- [ ] `_notify_user` в `tasks/notifications.py` читает матрицу вместо трёх булевых.

**Тесты:** событие уходит только в выбранные каналы · выключенный класс не доставляется · класс «безопасность» доставляется при всех выключенных тумблерах · неизвестный ключ в JSONB читается дефолтом · миграция старых булевых в дефолты.

### T3.33 — Локализация оповещений 🟨 в основном закрыт досрочно в T_UX.9 (2026-08-08)

**Контекст.** Тексты оповещений захардкожены по-русски (`_STATUS_LABELS` и все `send_*` в `tasks/notifications.py`), при том что UI живёт на шести локалях, а поля языка у `User` нет вовсе. То есть мы аккуратно доставляем в четыре канала текст, который часть аудитории не прочтёт.

- [x] `users.locale` (дефолт `en`), заполняется при регистрации из языка интерфейса — **сделано в T_UX.9**, миграция `0043`. Правка в профиле — остаётся здесь: `UserUpdate.locale` принимается API, селектора в UI нет.
- [x] Шаблоны оповещений на шесть локалей — **сделано в T_UX.9**: один layout, шесть JSON-каталогов, восемь писем. `send_code(…, locale)` из T3.26 берёт язык оттуда же.
- [x] Неизвестная локаль → `en`, а не падение и не русский — **сделано в T_UX.9**, покрыто тестами.

**Что осталось на эту задачу:** селектор языка писем в профиле (или автоматический PATCH при переключении языка интерфейса) и подключение каналов T3.26 к тому же рендереру. Само по себе это уже не «локализация оповещений», а её хвост.

**Тесты:** оповещение приходит на языке пользователя · неизвестная локаль → en · все шесть локалей имеют все ключи (тест на полноту словаря, иначе дыры находит пользователь).

**Follow-up этапа (сознательно вне):** тихие часы и дайджест вместо мгновенной отправки · push в браузер (Web Push) как пятый канал · отписка одним кликом из письма (List-Unsubscribe) · перенос `users.email`/`users.phone` целиком в `user_contacts` с переписыванием всех читателей.

---

## 💳 ЭТАП 4 — Карточные платежи + Regulatory KYC (Фаза 4)

### T4.1 — Классический regulatory KYC (person-level)

**Контекст:** до Фазы 4 через платформу не идут деньги — P2P-верификации (T2.1) достаточно для доверия между участниками. Перед вводом карточных платежей (T4.2) регулятор требует **формальный KYC** на пользователя (не на направление): kyc-провайдер интегрируется, `KycRecord` привязывается к аккаунту. **Corridor-периметр НЕ является частью T4.1** — платформа не блокирует направления (см. `T_UX.2 Route notes` + `D-COMPLIANCE-STANCE`); person-level санкционный чек (OFAC/EU SDN) идёт как часть стандартного KYC-провайдера.

- [ ] Выбор провайдера — фиксируется в TECHSTATE Decision Log. Варианты:
  - **Sumsub** — популярный на СНГ/EU, ~€1.5/verification, поддержка 220+ стран.
  - **Onfido** — UK/EU/US, ~$1.5, лидер по SDK-качеству.
  - **Jumio** — enterprise, дороже, максимум коридоров.
- [ ] `KycRecord(id, user_id, provider, external_id, status ∈ {pending, verified, rejected, expired}, verified_at, expires_at, level)` — уровень зависит от strict/enhanced.
- [ ] `ComplianceAck(id, user_id, doc_version, category, acknowledged_at)` — версионируемое подтверждение запрещёнки и ответственности.
- [ ] Webhook от провайдера → обновляет `KycRecord.status` → триггерит evaluation прав.
- [ ] Frontend: onboarding-модалка при первой попытке карточной оплаты; SDK провайдера в iframe/webview.
- [ ] Permissions (расширение RBAC): `PLATFORM_PAYMENT_INITIATE` — требует `KycRecord.status = verified`.
- [ ] Backend-тесты: user без KYC не может создать `Payment`; webhook меняет статус; person-level sanctions match от провайдера → `KycRecord.status=rejected`.

**Acceptance:** пользователь проходит formal KYC перед первой карточной оплатой; ComplianceAck обязателен на каждую версию условий; person-level санкционный match блокирует KYC (не corridor).

### T4.2 — Платежи на платформе (карта)
- [ ] `Payment` (card), комиссия платформы.
- [ ] Интеграция карточного процессинга.
- [ ] Все транзакции пишут событие в `DealEvent`.
- [ ] **Зависит от T4.1** — пользователь без verified KYC не может инициировать платёж.
**Acceptance:** карточная оплата проходит на платформе, событие фиксируется, берётся комиссия.

---

## 🔐 ЭТАП 5 — Крипто-эскроу (Фаза 5)

### T5.1 — Эскроу BTC (HodlHodl-схема) и залог
- [ ] `Escrow`, `Collateral`.
- [ ] 2-of-3 multisig (плательщик / перевозчик / платформа-арбитр); релиз 2 подписи.
- [ ] Ключ арбитра у платформы; подпись только при споре.
- [ ] Некастодиальный кошелёк для возвратов.
- [ ] Фи за безопасную сделку (эскроу) — основной поток монетизации.
**Acceptance:** для дорогого груза залог блокируется и разблокируется по 2-of-3; платформа не держит средств; берётся эскроу-фи.

### T5.2 — USDT-эскроу
- [ ] Аналог BTC-эскроу для USDT (не-кастодиальная 2-of-3 / арбитр-подпись).
**Acceptance:** USDT-сделка обеспечивается без кастодиальности.

---

## ✨ ЭТАП 6 — Премиум + IPFS-портативность (Фаза 6)

### T6.1 — Премиум (хранение документов + учёт)
- [ ] `PremiumSubscription`, хранилище документов, отчёты/экспорт.
**Acceptance:** подписчик хранит документы и получает учётные отчёты.

### T6.2 — DealVault → IPFS
- [ ] SHA-256 → multihash → CID для каждого `Attachment`; пин в IPFS; запись `ipfs_cid`.
- [ ] `DealVaultMessage` сериализуется как signed Nostr event JSON; пин в IPFS; CID сохраняется.
- [ ] Экспорт полного DealVault сделки как IPFS DAG; CID корня = верификационный хеш.
**Acceptance:** каждое вложение имеет CID; CID верифицируется по SHA-256; полный DealVault экспортируется и проверяется без платформы.

### T6.3 — Полная Nostr + IPFS портативность
- [ ] Экспорт пакета аккаунта: профиль, все DealVault как Nostr-события, CID вложений.
- [ ] Аккаунт совместим с Nostr-клиентами: npub идентифицирует, события проверяемы.
- [ ] Решение по операторской/админ-панели зафиксировано в Decision Log TECHSTATE.
**Acceptance:** пакет данных экспортируется и верифицируется; аккаунт работает в Nostr-клиенте.

### T6.4 — ZK-Proof of Verification (ZK-native migration for IdentityContainer)

**Контекст:** T2.1 хранит фото документов в encrypted-контейнере (доступен владельцу и через escalation арбитру). T6.4 — это **эволюционный путь**, не переписывание: тот же API `IdentityContainer` / `VerificationBadge`, но backend хранения заменяется на ZK-native. Пользователь может доказать «у меня есть валидный verified document» **не раскрывая сам документ и не полагаясь на платформу** как посредника. Даже platform admin не видит содержимое. Настоящий ZK-SNARK: proof генерируется на клиенте, проверяется криптографически без обращения к серверу с документом. Зависит от T2.1 (есть контейнер и badge-механика), T2.2 (есть keypair как identity anchor), и T3.5 (Nostr relay для публикации merkle-root'ов). Задача исследовательская — вероятно нужен приглашённый cryptographer.

**Стратегия миграции:**
- T2.1 создаёт `IdentityContainer.storage_mode = 'encrypted_blob'` (default).
- T6.4 добавляет `storage_mode = 'zk_snark'` — новые контейнеры пользователь может создать в ZK-native режиме.
- Оба режима сосуществуют. Старые контейнеры можно мигрировать (client-side re-encrypt через новую схему), но не обязательно.
- Все VerificationBadge остаются валидными между режимами — они ссылаются на container, а не на схему хранения.

- [ ] **Circuit** (Circom/halo2): доказательство `∃ passport : owner_pubkey = derive(passport.private_hash) AND passport.expiry > NOW AND hash(passport) ∈ set_of_verified_passport_hashes`.
- [ ] Merkle tree of verified passport hashes — обновляется on-chain-style Nostr event'ами; клиент строит proof membership.
- [ ] Runtime: **snarkjs** (Circom) или **halo2-wasm** для генерации proof в браузере (5-30 сек на mobile).
- [ ] Event формат: новый Nostr NIP (уточнить номер / propose upstream) — «verified identity proof», содержит только proof + npub.
- [ ] Endpoint `POST /api/verifications/proof` — верифицирует proof и записывает соотв. факт (без пересохранения identity).
- [ ] **UX:** переключатель в профиле «Reveal encrypted identity to platform» ↔ «Prove verification via ZK-proof». Для параноидальных пользователей — путь без хранения фото у платформы вообще.
- [ ] Совместимость: старая (T2.1) и новая (T6.4) системы работают параллельно; арбитр через escalation в T2.1 работает как раньше, но пользователи ZK-режима остаются приватными.
- [ ] Возможно: приглашение cryptographer'а для audit'а circuit'а — фиксируется в TECHSTATE.

**Acceptance:** пользователь генерирует ZK-proof в браузере; платформа принимает его без раскрытия документа; badge «Verified» на профиле показывается без хранения фото на сервере.

---

## 🤖 ЭТАП 7 — Агентский доступ по сети (Фаза 7)

### T7.1 — MCP-сервер: сетевой транспорт или отказ от контейнера

**Контекст (2026-07-30).** `mcp-server` одиннадцать дней перезапускался по кругу, и разбор вскрыл расхождение между решением и кодом. `server.py` заканчивается `stdio_server()` — транспорт по stdin/stdout, при котором **клиент сам запускает сервер как дочерний процесс**. В контейнере stdin закрыт, `main()` получает EOF и выходит с кодом 0, `restart: unless-stopped` поднимает заново; `logs` пуст, потому что сбоя нет. Контейнер удалён из `docker-compose.dev.yml` — при stdio он бесполезен: снаружи до stdin процесса на EC2 нет пути, и `stdin_open` этого не меняет, потому что писать в него всё равно некому.

`D-AGENTIC-INTERFACE` при этом обещает MCP-сервер контейнером «как быстрый вход для AI-агентов». С stdio это недостижимо в принципе, а не сложно. **Решение принимается в этой задаче, код и Decision Log приводятся в соответствие с выбранным.**

**Вариант A — сетевой транспорт (streamable HTTP).** Сервер встаёт ASGI-приложением за существующий nginx. Три обязательных предусловия, каждое — следствие того, что один процесс начинает обслуживать всех:

- [ ] Rate-limit переезжает в Redis. Сейчас счётчики в памяти процесса с комментарием «single MCP process per stdio client» — при HTTP они становятся общими и обнуляются на каждом деплое.
- [ ] Ключ переезжает из **аргумента инструмента** в заголовок `Authorization`. Сегодня `api_key` объявлен в схеме tool'а, то есть секрет виден модели и оседает в контексте и логах хоста. Для локального stdio терпимо, для публичного эндпоинта — нет.
- [ ] Зона `limit_req` в nginx на `/mcp`. Все четыре инструмента только читают — это единственное, что делает вариант приемлемым по риску.
- [ ] `mcp==1.1.0` предшествует streamable HTTP; поднять версию, API там менялся.

**Вариант B — локально, без контейнера.** Оставить stdio, документировать установку (`claude mcp add vimana -- python server.py`, `VIMANA_API_URL` на прод). Цена называется вслух: агент не найдёт Vimana сам, кто-то ставит сервер руками, и «агентский интерфейс» сжимается до «мы поставляем MCP-сервер, который можно подключить».

**Взвесить перед выбором.** Рейсы уже публикуются в Nostr событиями kind 30402 — это и есть децентрализованный канал обнаружения, работающий без MCP. Если так, ценность MCP не в доступе к данным, а в удобстве для тех, кто уже пользуется Claude, — а это ровно аудитория варианта B.

**Acceptance:** решение зафиксировано в `D-AGENTIC-INTERFACE`; код соответствует ему; в `docker-compose.dev.yml` нет сервиса, который не может работать.

---

## 🛠 Cross-cutting задачи (не привязаны к фазе)

Задачи безопасности, UX-правил, наблюдаемости и интеграций — планируются параллельно фазовым.

### T_SEC.1 — Security hardening: Swagger UI и attack surface ✅ MVP

**Контекст.** Сейчас `/docs`, `/redoc`, `/openapi.json` открыты в prod без auth. Плюс отсутствуют HTTP security headers, нет deny-правил для типичных probe-путей (`.env`, `.git`, `wp-admin`).

- [x] `main.py` — env-флаг `EXPOSE_DOCS` (**fail-safe default `false`**; opt-in `true` только когда нужен Swagger для отладки). При `false`: `FastAPI(docs_url=None, redoc_url=None, openapi_url=None)`. Читается один раз при импорте. **Реализация fail-safe** учитывает single-server топологию Vimana (dev == prod) — новый деплой никогда случайно не выставит Swagger наружу.
- [x] nginx конфиг: `add_header` с флагом `always`: HSTS (`max-age=31536000; includeSubDomains`), X-Frame-Options `DENY`, X-Content-Type-Options `nosniff`, Referrer-Policy `strict-origin-when-cross-origin`, Permissions-Policy `camera=(), microphone=(), geolocation=(), interest-cohort=()`, CSP baseline (`default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; …; frame-ancestors 'none'`).
- [x] nginx: `server_tokens off` глобально.
- [x] nginx probe-deny: `/.env(.*)?`, `/.git`, `/.svn`, `.php|.php3|.php4|.php5|.phtml|.aspx|.jsp|.cgi`, `/wp-admin|/wp-login|/wp-content|/xmlrpc.php`, `/.ht*` → 404.
- [x] nginx rate-limit zone `auth_zone` (10 req/min per IP) на `/api/auth/login` + `/api/auth/register` с `burst=5 nodelay`. Двухслойная защита: nginx-guard перед app + slowapi внутри app.
- [x] `/health` остаётся публичным (docker healthcheck работает). `/docs`, `/redoc`, `/openapi.json` — 404 при `EXPOSE_DOCS=false`.
- [x] 3 backend-теста (`test_docs_exposure.py`): docs disabled → 404 на /docs+/redoc+/openapi.json, docs enabled → 200 на /docs+/openapi.json, /health доступен независимо от флага. Тесты через `importlib.reload(app.main)` с monkeypatched env — читают флаг заново.
- [x] `.env.example` — добавлен `EXPOSE_DOCS=true` с pointer на что менять для prod.

**Acceptance:** ✅ `EXPOSE_DOCS=false` в prod → 404 на docs surface; nginx возвращает security headers (проверяется `curl -I`); probe-пути (`.env`, `.git`, `.php`) → 404; auth-endpoints под rate-limit 10r/min + burst 5.

**Follow-up:** (1) `/health` можно ужесточить — вернуть 200 без тела (не выдавать версию). (2) Мониторинг rate-limit hits — счётчик отказов nginx в metrics.

### T_SEC.2 — Vite dev → static production build ✅ MVP

**Контекст.** До T_SEC.2 frontend-контейнер запускал `npm run dev` (Vite dev-server с HMR) как prod: (a) `unsafe-inline` + `unsafe-eval` в script-src CSP, (b) React StrictMode двойным mount'ом дёргал useEffect (реально ловилось на invite-flow → fix через backend idempotency), (c) source-код + node_modules в prod-контейнере, (d) без бандлинга/минификации.

**Изменения:**

- [x] `frontend/Dockerfile` — multi-stage: `node:22-alpine` builder → `nginx:1.27-alpine` server. Stage 1: `npm ci` + `npm run build` → `dist/`. Stage 2: копирует `dist` в `/usr/share/nginx/html`, кладёт `nginx.conf` для SPA `try_files $uri /index.html`.
- [x] `frontend/nginx.conf` — internal nginx контейнера: `/assets/` c 1y immutable cache, SPA fallback, `/health` возвращает `ok`.
- [x] `docker-compose.dev.yml` frontend — убран volume-mount (`./frontend:/app`), healthcheck переведён на `http://localhost/health` (:80 внутри).
- [x] `nginx/default.conf` — main-nginx proxy_pass `frontend:5173` → `frontend:80`, убраны websocket-upgrade headers (Vite HMR больше нет).
- [x] **CSP tightened**: `script-src 'self'` (было `'self' 'unsafe-inline' 'unsafe-eval'`). style-src остаётся с `unsafe-inline` — Tailwind + libs всё ещё пишут inline `<style>`. Полный nonce/hash pass — future.
- [x] `AcceptInvitePage` — убран `attemptedRef` guard: StrictMode-double-effect больше не может произойти (dev-only behavior), backend + так idempotent per user.

**Acceptance:**
1. Prod-build: `docker compose up -d --build frontend` — контейнер стартует, healthcheck зелёный за <30s.
2. `curl -sI https://vimana.dealvault.club/` — CSP header **без** `unsafe-eval`, **без** `unsafe-inline` в script-src.
3. `/assets/index-<hash>.js` — cache-control `public, immutable, max-age=31536000`.
4. Прямой navigate на `/deals/xxx` в новой вкладке — nginx SPA fallback → index.html → React Router → правильная страница.
5. Bundle size: `dist/assets/*.js` < 500KB gzip'ed (Vite tree-shaking).

**Follow-up:**
1. Nonce/hash-based CSP полный strict — генерация nonce в nginx per-request + injecting в HTML (нужен custom Vite plugin).
2. Self-hosted fonts — снять `fonts.googleapis.com` / `fonts.gstatic.com` из CSP.
3. Backfill Playwright `T_TEST.3 pt.2` через prod-build — сейчас тесты гоняются против prod, вопрос: производительность и стабильность после T_SEC.2.

### T_SEC.3 — Периметр контейнеров, `--reload` в проде, мёртвая зависимость ✅ MVP

**Контекст (ревизия кода 2026-08-01).** T_SEC.1 закрыл attack surface на уровне nginx, но сам nginx можно было обойти: `backend` публиковался как `0.0.0.0:8000` и порт **открыт в security group** (подтверждено владельцем). Прямой запрос на `:8000` минует TLS, security-заголовки, probe-deny и обе зоны `limit_req`; хуже — ключ slowapi берётся из `X-Forwarded-For` (`core/rate_limit.py:8-12`), который прямой клиент подставляет сам, то есть per-IP лимиты снимались сменой заголовка. Комментарий в compose при этом утверждал обратное («контейнер не опубликован на хосте, доверять форвард-заголовкам безопасно») — расхождение между обоснованием и конфигом.

`db` и `redis` тоже висели на `0.0.0.0` (5432/6379). В SG они закрыты, но защита держалась на настройке за пределами репозитория: Docker пишет DNAT-правила **до** ufw, поэтому хостовый файрвол их не покрывает. Redis к тому же без пароля, а в нём лежат гранты step-up, челленджи identity, blacklist токенов и **брокер Celery** — тот, кто пишет в брокер, ставит задачу воркеру, то есть выполняет код.

- [x] `db`, `redis`, `backend` → `127.0.0.1:<port>:<port>`. Наружу публикуются только nginx (80/443) и relay (7777, профиль `nostr`). Доступ к БД с ноутбука — SSH-туннель, не публикация.
- [x] Redis стартует с `--requirepass ${REDIS_PASSWORD:?...}` — синтаксис `:?` роняет `up` с named-ошибкой вместо тихого старта открытого Redis при отсутствующей переменной. Пароль healthcheck'у передаётся через `REDISCLI_AUTH`, а не аргументом `-a`: аргумент виден в списке процессов.
- [x] `.env.example` — `REDIS_PASSWORD` + `REDIS_URL` с паролем **литералом**. Подстановка `${...}` внутри `.env` не годится: `env_file` отдаёт значения контейнеру как есть, и приложение получило бы эти символы буквально.
- [x] Снят `--reload` у uvicorn. Он держит watcher и вторую копию приложения (на 2 ГБ инстансе, где ловили OOM, это не мелочь) и перезапускает воркеров при изменении любого файла в примонтированном `./backend` — `git pull` во время запроса ронял этот запрос.
- [x] `dockurr/strfry:latest` → `1.1.1`. Плавающий тег позволял пересборке молча подменить бинарь релея. `1.1.1` — то, во что `latest` разрешался на 2026-07-21 (сверено по реестру тегов).
- [x] Удалён `weasyprint==63.0`: не импортируется **нигде** в `backend/app` и `backend/tests` — «посадочный талон» не реализован. Единственным его следствием был потолок `Pillow<11`, то есть библиотека на пути **каждого** загруженного файла (`core/file_validation.py`) держалась на ветке 10.x. `Pillow` поднят `10.4.0 → 12.3.0`. Выбор PDF-движка — часть задачи, которая будет делать талон, а не груз до неё.
- [x] ENVIRONMENT §1 (движок PDF не выбран), §3 (`REDIS_PASSWORD`), §5.1 (что публикуется наружу), §9 (правка исходников требует пересоздания контейнера).

**Тестов не добавлено осознанно.** Задача не трогает ни одного эндпоинта и ни одной ветки приложения — меняются конфигурация запуска и список зависимостей. Проверяется прогоном существующего сьюта (Pillow 12 — единственное, что может его сдвинуть, и покрыто `test_file_validation.py`) плюс ручной проверкой из acceptance.

**Acceptance:**
1. `docker compose ps` — публикация портов только у nginx (80/443) и, при профиле `nostr`, relay; у `db`/`redis`/`backend` адрес `127.0.0.1`.
2. `curl -m 5 http://<publiс-ip>:8000/health` снаружи — таймаут/отказ, а не `200`.
3. `redis-cli -h 127.0.0.1 ping` без пароля → `NOAUTH`; приложение и Celery работают (сделка открывается, письмо ставится в очередь).
4. Полный backend-сьют зелёный после подъёма Pillow.
5. `docker compose config | grep image` — у relay версия, не `latest`.

**Проверено на проде 2026-08-01:** `ss -tlnp` показывает только 22/80/443 (8000/5432/6379 ушли), `redis-cli` без пароля → `NOAUTH`, внешний `curl` на 8000 — таймаут, **954 backend-теста зелёные** на `Pillow 12.3.0` за 349 с (до правок — 354 с, то есть подъём мажорной версии на время прогона не повлиял). Попутно уточнено по SG: 8000 наружу **не был открыт** — первоначальная оценка «дыра открыта» строилась на устном ответе, инструментальная проверка её не подтвердила. Периметр всё равно закрыт: docker-биндинг на `0.0.0.0` не покрывается ufw, и одно случайное правило в SG возвращало бы проблему целиком.

**Открыто (следующий заход, вариант B ревизии):** индексы БД, N+1 в `verify_content`, BFS Trust Graph, `_CITY_INDEX` в памяти, кэш boto3-клиента, блокирующий I/O в async-эндпоинтах, `ilike`/`cast` в фильтрах рейсов, slowapi на Redis, атомарный инкремент `publish_metrics`. Пять legacy-слоёв (см. `T_KEYS.1`) в вариант B **не входят** — они ждут решения по схемам входа и среднему уровню владения ключом.

### T_SEC.4 — Relay по TLS: `wss://<домен>/relay` ✅ MVP

**Контекст (ревизия 2026-08-01).** Фаза 3.5 закрыта, а её Integrity Check («наш relay виден в damus/amethyst/coracle») не выполнялся технически: strfry отдавал голый `ws://` на 7777, а Nostr-клиент, открытый на https-странице, такое соединение не устанавливает — браузер блокирует его как mixed content. То есть публиковать было куда, но подключиться из веба — некуда.

- [x] `nginx/default.conf` — `location /relay` с апгрейдом соединения, зоной `relay_zone` (30 r/m на рукопожатие; трафик внутри сокета ограничивает сам strfry) и `proxy_read_timeout 600s`: подписка может простаивать минутами, а дефолтные 60 с рвали бы соединение с нашей стороны и загоняли клиентов в цикл переподключений. Карта `$connection_upgrade` — чтобы `Connection: upgrade` не проставлялся обычным запросам и не ломал keepalive.
- [x] **Путь, а не поддомен.** Relay адресуется полным URL, поэтому `/relay` на существующем сертификате не хуже `relay.dealvault.club`, но не требует ни DNS-записи, ни второго продления. Переезд на поддомен позже — это server-блок, а не пересмотр решения.
- [x] `docker-compose.dev.yml` — `ports` у relay заменены на `expose`. Публиковать 7777 незачем: снаружи он бесполезен (см. выше), а как вторая дверь обходит и зону лимитов, и заголовок с настоящим IP. В security group 7777 можно закрывать.
- [x] `nostr/strfry.conf` — `realIpHeader = "x-forwarded-for"`. За прокси без этого все клиенты выглядят как один адрес, и собственные лимиты relay'я перестают работать. Безопасно ровно потому, что порт больше не опубликован: подделать заголовок некому, кроме nginx.
- [x] `.env.example` — добавлены `NOSTR_PUBLISH_ENABLED` (код его читал, а пример о нём молчал — переменная, о которой никто не узнает), `NOSTR_OWN_RELAY_URL`, `NOSTR_RELAY_URL`, `NOSTR_FRIENDLY_RELAYS`. ENVIRONMENT §3/§5.1 приведены в соответствие.

**Тестов нет осознанно:** задача не трогает ни строки Python — только конфигурацию nginx, strfry и пример env. Проверяется руками из acceptance.

**Acceptance:**
1. `wscat -c wss://<домен>/relay` (или любой Nostr-клиент) устанавливает соединение; `curl -H 'Accept: application/nostr+json' https://<домен>/relay` отдаёт NIP-11 с именем `Vimana`.
2. Прямое `ws://<ip>:7777` снаружи недоступно.
3. Соединение живёт дольше минуты без событий и не рвётся.
4. В логах strfry видны настоящие адреса клиентов, а не один адрес nginx.

**Осталось для Фазы 3.5 (не входит в задачу):** включить саму публикацию (`NOSTR_PUBLISH_ENABLED=true`) — это `T3.20`, вместе с `CHAIN_ANCHOR_NSEC` и якорями.

### T_UX.12 — Telegram-бот: включение и две дыры, найденные до включения ✅ код готов (2026-08-09)

**Контекст.** Механика бота написана с T1.7 и **ни разу не работала**: на проде не заданы `TELEGRAM_BOT_TOKEN`/`TELEGRAM_BOT_USERNAME`. Перед тем как давать владельцу инструкцию по @BotFather, код прочитан целиком — и в нём нашлись две вещи, каждая из которых сорвала бы включение.

**1. `setWebhook` не передавал `secret_token`.** Обработчик `telegram_webhook` сверяет заголовок `X-Telegram-Bot-Api-Secret-Token` с `TELEGRAM_WEBHOOK_SECRET` и при несовпадении отвечает 403. Но Telegram шлёт этот заголовок только если секрет был **зарегистрирован** вместе с URL — а `set_webhook` слал один `url`. Итог был бы образцовым в своей незаметности: бот «настроен», `setWebhook` вернул `{"ok":true}`, и **каждое** реальное обновление молча отклоняется. Никто никогда не привязался бы, и причину искали бы в Telegram. Исправлено; заодно `allowed_updates: ["message"]` — привязка читает только `/start`, остальные типы разбирались бы и выбрасывались впустую.

**2. `POST /set_webhook` требовал просто сессию.** Любой залогиненный аккаунт мог перенаправить вебхук на свой сервер: Telegram начал бы доставлять туда все обновления, включая `/start`-токены, которыми связываются аккаунты, а привязка на платформе просто перестала бы работать. Закрыто новым правом `Permission.TELEGRAM_MANAGE` (суперюзер). Своё имя, а не `USERS_MANAGE`: действие инфраструктурное — кто им владеет, решает, куда Telegram доставляет всё.

**Что делает бот (для записи, потому что это спрашивают):** ровно две вещи. Принимает `/start {token}` и связывает Telegram-чат с аккаунтом (токен выдаёт `GET /api/telegram/connect`), после чего служит **каналом доставки** — уведомления по сделкам и напоминания о сроках для пользователей, плюс пять админских тревог (сканер не отвечает, найден заражённый файл, ZAP нашёл High, новая заявка waitlist). Диалога у бота нет: он не отвечает на сообщения и не принимает команд, кроме `/start`.

**Тесты:** `setWebhook` несёт секрет и `allowed_updates` · без секрета в env поле не отправляется · эндпоинт отвечает 401 анониму и 403 обычному пользователю.

**Acceptance:**
1. После настройки владельцем `/start` из ссылки привязывает чат, `notify_telegram` включается.
2. Обновление без правильного секрета отклоняется, с правильным — обрабатывается.
3. Перенаправить вебхук может только суперюзер.
4. **⚠️ Живая проверка за владельцем** — бот создаётся вручную у @BotFather.

#### T_UX.12 pt.2 — Бот отвечает, а профиль узнаёт о привязке ✅ код готов (2026-08-09)

**Контекст.** Бот включён и работает: `chat_id` записан, админская тревога дошла. Но живая проверка дала две претензии, и обе — про обратную связь.

**1. На `/start` не приходило ничего.** В обработчике его и не было: привязка записывалась и возвращался `{"ok": "processed"}` самому Telegram, а человеку — тишина. Работающее действие выглядело как сломанное. Хуже того, **истёкшая ссылка была неотличима от сломанного бота**: обе давали пустоту.
- [x] Отвечают все три ветки: `linked` (привязали), `stale` (токен не найден — ссылка одноразовая и живёт недолго), `hello` (голый `/start` или любое сообщение — объяснение, что это за бот и что диалога у него нет).
- [x] Язык: у привязанного — `user.locale`, то есть выбранный на сайте. У двух остальных аккаунт ещё неизвестен, поэтому берётся `message.from.language_code`, который Telegram присылает сам, с откатом в `en`.
- [x] `core/email_templates.chat_message(kind, locale)` — **отдельно от `render`**: у письма есть тема, вёрстка и две MIME-части, у сообщения в чат — только строка. Прогнать одно через другое значило бы подписать бота футером с приглашением ответить на письмо, в чате, где ответить нельзя. Каталоги те же шесть и правило отката то же.
- [x] Отправка — Celery-таском `send_telegram_chat`: `send_telegram` синхронный `httpx`, а обработчик асинхронный, и Telegram ждал бы наш поход в Telegram.

**2. Плашка «Подключить Telegram» не исчезала после привязки.** Тумблер включался, но подпись оставалась «не подключён» и кнопка не пропадала. Причина не в UI: привязка происходит на сервере, в вебхуке, о котором вкладка не знает и ничего не переспрашивает.
- [x] Профиль перечитывает `me()` при возврате фокуса — ровно тот момент, когда ответ мог измениться. Слушатель вешается **только** пока привязка в ожидании (`notify_telegram && !telegram_chat_id`): профиль, перечитывающий себя на каждый alt-tab, это запрос на каждый взгляд.

**Тесты:** валидный токен → ответ `linked` на языке аккаунта · истёкший → `stale` на языке клиента Telegram · голый `/start` → `hello` · каталог `chat` полон во всех шести локалях · откат неизвестной и пустой локали в `en`. Плюс `send_telegram_chat` внесён в `DISPATCHED_BY_CODE`.

**Acceptance:**
1. Нажатие Start даёт видимый ответ; истёкшая ссылка объясняет, что делать.
2. После возврата из Telegram профиль сам показывает «подключён», без перезагрузки.
3. **⚠️ Живая проверка за владельцем.**

### T_UX.11 — Граница ошибок рендера ✅ MVP (2026-08-09)

**Контекст.** До этой задачи одно исключение в рендере разрушало дерево и оставляло пустую страницу: ни сообщения, ни следа, ни пути назад кроме набора URL вручную. Хуже того, симптом был **одинаков для несовместимых причин** — не долетевший `<Route>` (T3.3, два коммита детектива), обращение к полю у `null`, устаревший `lazy()`-чанк после деплоя (случай владельца 2026-08-08). Диагностика каждый раз начиналась с нуля.

**Две работы, и разделение — суть задачи:**
- [x] **Устаревший чанк — не баг.** После деплоя имена файлов с хешем меняются, вкладка, открытая раньше, просит файл, которого нет. Сломано только представление страницы о том, что грузить. Этот случай получает свой текст и **перезагружается сам, один раз** — просить человека нажать кнопку ради нашего деплоя значит поручать ему нашу бухгалтерию.
- [x] **Всё остальное — баг**, и говорит об этом прямо, оставляет навигацию рабочей и **показывает текст ошибки**. Не ради посетителя, а ради отчёта, который он пришлёт.
- [x] Замок на одну попытку важнее, чем кажется: если чанк действительно пропал (недоделанный деплой, плохой кэш-заголовок), перезагрузка на каждый отказ превращается в бесконечный цикл — отказ хуже белого экрана, который она заменяет. `sessionStorage` помнит, что попытка была; на второй раз человек получает кнопку и объяснение.
- [x] Распознавание чанка — по тексту сообщения, а не по имени: `ChunkLoadError` отдают не все движки, и Chrome, Safari и Firefox формулируют по-разному. Четыре шаблона, все покрыты тестом.
- [x] Подключено **внутри router'а** (ключуется по пути) и **снаружи `Suspense`**: провалившийся `lazy()` отклоняет приостановленный промис, и видит это только граница выше него.
- [x] Ключ — `location.pathname`. Без него граница залипает, и первая же ошибка делает сломанной всю оставшуюся сессию.
- [x] `pageReload.now()` вместо прямого `window.location.reload()` — `window.location` в jsdom надёжно не подменяется, а компонент, который нельзя проверить ровно на том поведении, что рискует зациклиться, не стоит своего цикла.
- [x] i18n — 7 ключей × 6 локалей. Тексты не извиняются и не пугают: «вы тут ни при чём, ничего не потеряно».

**Новые функции (PROJECT §6.2a):**
- `ErrorBoundary` (класс) — ловит и показывает. Вызывает: `RouteErrorBoundary`.
- `RouteErrorBoundary` — обёртка с ключом по пути, экспорт по умолчанию. Вызывает: `App`.
- `isChunkLoadError(error)` — отличает устаревший чанк от бага. Вызывают: `ErrorBoundary`, тесты.
- `pageReload.now()` — точка перезагрузки, подменяемая в тесте. Вызывают: `ErrorBoundary`, `Fallback`.
- `Fallback({error, staleChunk})` — экран отказа. Вызывает: `ErrorBoundary`.

**Тесты (`ErrorBoundary.test.tsx`, 11):** распознаются формулировки всех движков · `ChunkLoadError` по имени · обычный баг **не** принимается за устаревший деплой · дети рендерятся, когда всё цело · вместо белого экрана — сообщение с `role="alert"` · текст ошибки виден для отчёта · устаревший чанк перезагружает один раз · **второй раз не перезагружает** · обычный баг не перезагружает · у устаревшего чанка нет технических подробностей (нечего сообщать) · смена ключа сбрасывает ошибку.

**Acceptance:**
1. Исключение в рендере даёт экран с объяснением и кнопкой, а не пустую страницу.
2. Устаревший чанк после деплоя чинится сам, ровно одной перезагрузкой.
3. Переход на другой маршрут снимает ошибку.
4. Прогон vitest — **69 passed, 11 файлов** (2026-08-09), прод-сборка `tsc && vite build ×3 && prerender` прошла целиком.

**Не сделано намеренно:** отправка ошибок на сервер (Sentry и подобное) — это отдельное решение с внешней зависимостью и приватностью данных; пока ошибка живёт в консоли и на экране.

### T_UX.10 — Мелочи входа и профиля по замечаниям владельца ✅ MVP (2026-08-08)

**Контекст.** Четыре замечания после живой проверки passkey-регистрации.

**1. Белый экран после passkey-входа — не баг продукта.** Прошёл после перезагрузки страницы. Причина: страницы подключены через `lazy()`, и вкладка, открытая до деплоя, держала старый `index.html` со ссылками на чанки, которых после пересборки уже нет. `import()` получает 404, промис отклоняется, дерево не рендерится. **Молчаливым это делает отсутствие `ErrorBoundary` во всём проекте** — любое исключение в рендере даёт белый экран без следа, и симптом одинаков для десятка разных причин (так же было в T3.3 с `<Route>`). Записано в TECHSTATE §3a как открытое; починка — отдельной задачей.

**2. На `/login` и `/register` не было переключателя языка.** `LanguageSwitcher` живёт в `Navbar`, `Navbar` — в `Layout`, а `Layout` оборачивает только защищённые маршруты. Вход и регистрация вне его, то есть посетитель, попавший на страницу не на своём языке, не мог его сменить — ровно на тех двух экранах, где чужой язык стоит дороже всего. Добавлен в оба — в потоке колонки формы, по правому краю карточки над заголовком. Не фиксированной кнопкой в углу окна (первая редакция, поправлено владельцем в тот же день): такая принадлежит окну, а не форме — на широком экране уезжает от контента, на низком налезает на него.

**3. В профиле не было почты у аккаунта без почты.** Строка рисовалась через `{user?.email && …}`, то есть аккаунт, созданный passkey или Nostr-ключом — а такой по устройству живёт без почты (решение №2 Фазы 3.7) — не видел на экране ни упоминания почты, ни пути к тому месту, где её добавляют. Теперь строка есть всегда: с адресом либо со ссылкой «не указана — добавить» на `/profile/keys`. **Само поле в модалку не переносится**: добавление адреса требует step-up re-auth (T3.15), и его место в «Доступе и ключах», а не в модалке профиля — тот же принцип, по которому там уже живёт подсказка про адреса.

**4. «Адреса получения» → «Почтовые адреса»** во всех шести локалях (решение владельца). Затронуты `address.sectionTitle`, `address.empty`, `profile.address.title`, `profile.editHint`, `chat.shareAddress.notSet`. Старая формулировка описывала роль в сделке, новая — саму вещь, которую человек и ищет в профиле. Проверено грепом: старых формулировок в `frontend/src` не осталось.

**Новые ключи (6 локалей):** `profile.emailAdd`. Переименованы пять существующих.

**Acceptance:** переключатель языка доступен до входа · аккаунт без почты видит строку почты и путь к добавлению · раздел называется «Почтовые адреса» на всех шести языках · старых формулировок не осталось. Прод-сборка и vitest зелёные (2026-08-09).

### T_UX.9 — Письма: HTML-вёрстка, шесть локалей, предпросмотр без отправки ✅ MVP (2026-08-08)

**Контекст.** Все письма платформы были `MIMEText(plain)` на русском языке, при том что интерфейс говорит на шести. Посмотреть письмо до отправки было нельзя вовсе — единственный способ увидеть его глазами состоял в том, чтобы отправить его человеку. Оба пробела одного происхождения: почта была транспортом, а не частью продукта.

**Инструмент раньше вёрстки.** Первым сделан предпросмотр, потом шаблоны — иначе работа над письмом сводится к «отправить и посмотреть», а это ровно тот цикл, который приводит к письмам, впервые увиденным получателем.

- [x] **Mailpit** в `docker-compose.dev.yml` под профилем `mail` (не поднимается обычным `up`). Фальшивый SMTP на `1025`, веб-ящик на `8025`, оба на loopback — снаружи только через `ssh -L`. В dev `.env` `SMTP_HOST=mailpit`, `SMTP_PORT=1025`; прод продолжает ходить в Mailu.
- [x] **`app/cli/email_preview.py`** — рендерит каждое письмо в `<out>/<kind>.<locale>.html` + `.txt` + `index.html` со ссылками. Ноль инфраструктуры, ноль SMTP. Примерные данные намеренно неудобные (длинное имя, дата с зоной, тире): слишком аккуратные заглушки — то, из-за чего вёрстка ломается на настоящих данных уже после приёмки.

**Вёрстка:**
- [x] **Один layout** `app/emails/layout.html` на все письма: таблицы и инлайн-стили. Не потому что так красивее, а потому что Outlook рендерит через Word, Gmail выбрасывает почти весь `<head>`, и стилевой блок, переживший один клиент, отбрасывается следующим. Шесть отдельных HTML разошлись бы к третьей правке.
- [x] Блоки layout'а: preheader (скрытая строка после темы), заголовок, абзацы, **код** (моно, разрядка, тёмно-синяя плашка — табло вылетов), таблица фактов, CTA-кнопка (амбер — «посмотри сюда», не тревога), примечание, подвал. Шрифты — системные стеки: вебфонт в письме не загрузится, а если загрузится, то работает как отметка о прочтении.
- [x] `color-scheme: light dark` — тёмная тема не подавляется. Прибивать светлые цвета значит получить тёмный текст на тёмном фоне в части ящиков.
- [x] Ни одного внешнего запроса из письма (проверяется тестом): удалённая картинка это read receipt, и большинство клиентов её всё равно блокируют.

**Локализация — шесть языков, всегда:**
- [x] `app/emails/locales/{en,ru,ua,pl,fr,es}.json` — строки; **восемь писем**: код подтверждения, использованный recovery-код, удаление копии ключа, завершение личности, waitlist-подтверждение, waitlist-владельцу, статус сделки, напоминание о сроке.
- [x] `core/email_templates.render(kind, locale, **ctx)` — единственная точка входа. `en` — и запасной вариант, и эталон: отсутствующий файл локали, неизвестный тег и ключ, которого нет в переводе, разрешаются в английский, а не в исключение. Письмо не на том языке — недостаток; письмо, не ушедшее из-за пропущенного ключа, — авария.
- [x] Тег вида `ru-RU` сужается до языка.
- [x] **Текстовая часть собирается из каталога, а не вырезается из HTML.** Вырезание тегов из таблицы даёт нечто формально валидное и нечитаемое, а текстовую часть читают спам-фильтры и часть клиентов.

**Схема (миграция `0043_locale_columns`):**
- [x] `users.locale VARCHAR(5) NOT NULL DEFAULT 'en'`. Дефолт `en`, а не `ru`: он достаётся строкам старше колонки и любому пути, забывшему передать язык. Существующие аккаунты **не** бэкфилятся в русский — угадывать язык по имени это ровно та догадка, которая ошибается.
- [x] `waitlist.locale VARCHAR(5) NULL`. Nullable честно: у строки старше миграции языка нет, и NULL это говорит, а `'en'` утверждал бы, что мы спрашивали.
- [x] `UserCreate.locale` / `UserUpdate.locale` / `WaitlistCreate.locale`; фронт шлёт `i18n.language` из формы регистрации и с лендинга.

**Транспорт:**
- [x] `send_email(to, subject, body, html=None)` → `multipart/alternative`, **текстовая часть первой**: стандарт считает последнюю часть самой богатой, и клиент берёт последнюю, которую умеет. Обратный порядок — способ показать разметку в современном ящике. Текстовая часть не выбрасывается никогда.

**Тесты (`test_email_templates.py`, 20+ и параметризация 8×6):** каждый файл локали существует · **ни одна локаль не пропустила письмо** · ни одно письмо не пропустило ключ · метки статусов сделки совпадают во всех шести · каждое письмо рендерится на каждом языке · неизвестная/пустая/региональная локаль → `en` · **локали действительно различаются** (иначе все проверки выше пусты) · код попадает в обе части · текстовая часть без разметки · заголовок статуса локализован · неизвестный статус деградирует в своё имя · пустые факты не рисуют пустую строку · preheader скрыт · враждебное значение экранируется · предпросмотр пишет файлы · `sample_context` покрывает все письма · multipart собирается в правильном порядке · без HTML остаётся `text/plain` · в layout нет внешних ссылок. Плюс в `test_notifications.py`: письмо на языке аккаунта (es/pl), HTML-часть присутствует, неизвестная локаль не ломает доставку, waitlist-подтверждение на языке лендинга и английское при NULL.

**Новые функции (PROJECT §6.2a):**
- `core/email_templates.render(kind, locale, **ctx)` → `Rendered(subject, html, text)`. Вызывают: `tasks/notifications._send`, `_notify_user`, `cli/email_preview.main`.
- `core/email_templates.available_locales()` — шесть тегов. Вызывают: `cli/email_preview`, тесты.
- `core/email_templates._catalogue(locale)` — грузит и кэширует JSON. Вызывают: `render`, `tasks/notifications._catalogue_for`.
- `core/email_templates._fill(value, ctx)` — рендер `{{ }}` внутри строки каталога. Вызывает: `render`.
- `tasks/notifications._send(user_or_locale, to, kind, **ctx)` — рендер + отправка. Вызывают: четыре письма-таска и `_send_waitlist_pair`.
- `tasks/notifications._catalogue_for(user)` — строки `waitlist_admin` на языке владельца. Вызывает: `_send_waitlist_pair`.
- `cli/email_preview.sample_context(kind)` / `main(argv)`. Вызывают: CLI и тесты.

**Acceptance:**
1. Любое письмо открывается в браузере командой предпросмотра и в Mailpit — без единой реальной отправки.
2. Каждое из восьми писем существует на шести языках; пропуск ключа валит сьют.
3. Получатель с `locale='es'` получает испанское письмо; строка без локали — английское.
4. Письмо уходит `multipart/alternative` с текстовой частью первой.
5. Сьют зелёный — **1335 passed, 0 failed** (2026-08-08). Вживую проверено на рассылке waitlist: три письма ушли по боевому контуру, `send_email` вернул `True` на всех трёх.

**Не сделано намеренно:** селектор языка писем в профиле (API принимает `UserUpdate.locale`, UI не предлагает) — остаётся в `T3.33`. Локализация Telegram/WhatsApp берёт текстовую часть того же письма, отдельных строк для них нет.

#### T_UX.9 pt.2 — Почтовая консоль в админке ✅ MVP (2026-08-08)

**Контекст.** Смотреть письма можно было только из консоли: `python -m app.cli.email_preview`, забрать файлы с сервера, открыть в браузере. Владелец просил органы управления в UI и просмотр шаблонов подряд, с переключателем языка **писем**, не сайта.

**Два контура — главное решение задачи.** Настройки просмотра сделаны **отдельным набором переменных**, а не флагом на боевых: `PREVIEW_SMTP_HOST/_PORT/_USER/_PASSWORD` против `SMTP_*`. Причина прямо из этой недели: направить боевой хост в ловушку значило бы заглушить все настоящие письма, при том что `send_email` продолжал бы возвращать `True`, а `confirmation_sent_at` — проставляться. Флаг находится в одной правке от такой аварии, два объекта — нет.
- [x] `core/email.Circuit` + `live()` / `preview()`. `send_email(..., circuit=)` по умолчанию боевой; тестовая отправка **жёстко** прошита на `preview()` — параметра, которым можно дотянуться до живого ящика, не существует.
- [x] `_connect` шлёт `EHLO` и проверяет `has_extn`: ловушка говорит на голом SMTP без TLS и без AUTH, и попытка `login` туда падает на протоколе, а не на пароле. Боевой путь (465/587) при этом не изменился.

**Backend (`api/admin.py`, право `Permission.EMAIL_MANAGE`):**
- [x] `GET /admin/email/status` — на что смотрят оба контура. **Пароль не отдаётся ни в каком виде** — ни маской, ни длиной: у консоли на чтение нет для него применения, а скриншот страницы не должен быть утечкой.
- [x] `GET /admin/email/templates?locale=` — все восемь писем в одном языке, в фиксированном порядке. **Только отрисовка**: SMTP не задействован, ни один контур не читается. Страница работает при полностью сломанной почте — в этом и состоит разделение.
- [x] `POST /admin/email/test` — одно письмо в контур просмотра. Контур не настроен → 503 с причиной, а не тихий успех: `send_email`, возвращающий `False`, это ровно та форма отказа, ради прекращения которой всё и переделывалось.
- [x] `sample_context` переехал из `cli/email_preview.py` в `core/email_templates.py` — теперь у него два вызывающих, и примеры одни, а не две расходящиеся копии.

**Frontend (`pages/AdminEmailPage.tsx`, роут `/admin/email`, плитка в `AdminPanelSection`):**
- [x] Карточки двух контуров рядом: боевой янтарный, просмотр циановый, оба read-only. Ниже — тестовая отправка с явным текстом о том, куда она попадёт.
- [x] Письма идут подряд, каждое с темой и техническим именем. Переключатель «HTML / текстовая часть» — обе видны, потому что обе уходят.
- [x] **Переключатель языка писем — собственное состояние**, инициализируется языком интерфейса и обратно в него не пишется. Подпись говорит это вслух: языковой контрол наверху страницы читается как язык сайта, а он им не является.
- [x] Письма рендерятся в `<iframe sandbox="" srcDoc>`. Письмо — самостоятельный HTML-документ с таблицами и инлайн-стилями; вставленное в страницу, оно унаследовало бы Tailwind-reset и показало бы то, чего получатель никогда не увидит. Врущий предпросмотр хуже отсутствующего. Пустой `sandbox` — консоль не должна становиться способом исполнить то, что оказалось в шаблоне.
- [x] i18n — 33 ключа × 6 локалей.

**Тесты:** контуры читают разные настройки · ненастроенный контур не считается настроенным · `send_email` с явным контуром **не открывает боевое соединение** · ловушка без AUTH не логинится · консоль только для суперюзера (401/403) · статус не отдаёт пароль · шаблоны рендерятся при пустом `SMTP_HOST` **и** пустом `PREVIEW_SMTP_HOST` · неизвестная локаль деградирует как при настоящей доставке · тест без контура → 503.

**Новые функции (PROJECT §6.2a):**
- `core/email.Circuit` / `live()` / `preview()` — описание одного SMTP-назначения. Вызывают: `send_email`, `api/admin.email_status`, `email_test`.
- `core/email_templates.sample_context(kind)` — примерные значения. Вызывают: `cli/email_preview.main`, `api/admin.email_templates`, тесты.
- `api/admin._circuit_out(circuit)` — контур в DTO без пароля. Вызывает: `email_status`.
- `api/admin.email_status` / `email_templates` / `email_test` — три эндпоинта. Вызывает: `AdminEmailPage`.
- `pages/AdminEmailPage.CircuitCard` — карточка контура. Вызывает: `AdminEmailPage`.

**Acceptance:**
1. Суперюзер открывает `/admin/email` из профиля и видит восемь писем подряд на языке интерфейса.
2. Переключатель языка меняет **только письма**; интерфейс остаётся прежним.
3. Страница отрисовывает письма при полностью выключенной почте.
4. Тестовая отправка не может попасть в боевой контур ни при каких значениях формы.
5. Сьют зелёный — **1335 passed, 0 failed** (2026-08-08).

**Не сделано намеренно:** редактор шаблонов — только просмотр, как и просил владелец. Правка текстов остаётся правкой JSON-каталогов в репозитории.

### T_UX.8 — Waitlist: подтверждение заявителю и письмо владельцу ✅ MVP (2026-08-08)

**Контекст.** Заявка с лендинга писалась в таблицу `waitlist` и на этом заканчивалась. Уведомление шло единственным каналом — Telegram админам, а он мёртв (`TELEGRAM_BOT_TOKEN` не задан, TECHSTATE §1). Проверка базы 2026-08-08 показала цену: 3 записи с 6 июля, из них одна живая заявка от постороннего человека, пролежавшая ~14 часов незамеченной. Человек, оставивший почту, не получал ничего вообще — ни подтверждения, ни объяснения, что будет дальше.

**Решение владельца 2026-08-08:** письмо владельцу на `nyxter@dealvault.club` + письмо-подтверждение заявителю с объяснением, что при старте придёт приглашение или ссылка на регистрацию. Всем, кто оставил заявку раньше, письма разослать разово после деплоя, с отметкой об отправке.

**Схема (миграция `0042_waitlist_confirmation_sent`):**
- [x] `waitlist.confirmation_sent_at TIMESTAMPTZ NULL`. Существующие строки получают NULL — честное значение, им никто не писал, и ровно это ищет разовая рассылка. Индекса нет намеренно: три строки, запрос выполняется руками один раз.

**Backend:**
- [x] `core/email.send_email()` возвращает `bool` — `True` только если письмо ушло на SMTP-сервер. Раньше молчаливый `return` при незаданном SMTP был неотличим от успеха, и вызывающий, записавший бы «отправлено», записал бы неправду. Это ровно тот класс ошибки, который дал `✅ готово` у Telegram и WhatsApp. Старые вызывающие результат игнорируют, новый — нет.
- [x] `tasks/notifications.send_waitlist_emails(entry_id)` — подтверждение заявителю, следом уведомление владельцу на `USER_ZERO_EMAIL` (константа из `core/superuser.py`, а не второй захардкоженный адрес). `confirmation_sent_at` ставится **только** при `delivered is True`. Идемпотентна: повторная доставка таска не пишет человеку дважды.
- [x] Письмо владельцу уходит независимо от судьбы подтверждения и несёт его исход (`отправлено` / `НЕ отправлено — проверить SMTP`) плюс общее число заявок. Если адрес заявителя не принял письмо — это как раз то, что стоит знать.
- [x] `tasks/notifications.send_pending_waitlist_confirmations(dry_run=False)` — разовая рассылка тем, у кого `confirmation_sent_at IS NULL`. Расписания нет намеренно: задача закрывает долг, а повторяющийся джоб, пишущий незнакомым людям, не то, что оставляют тикать без присмотра. `dry_run=True` возвращает адреса, ничего не отправляя — письмо нельзя отозвать, а список достаточно мал, чтобы прочитать его глазами.
- [x] `api/waitlist.py` — отправка ушла с request-пути в Celery (`send_email` синхронный `smtplib`, инлайн он держал бы async-эндпоинт на два SMTP-раунда, пока посторонний ждёт форму). Недоступный брокер не роняет заявку: строка сохранена, `confirmation_sent_at` пуст, разовая рассылка подберёт.
- [x] Удалена мёртвая `_admin_emails()` — читала `ADMIN_EMAILS`, которую никто не задаёт, и не вызывалась ниоткуда. Оставлять функцию, выглядящую как то, что мы только что сделали иначе, — ловушка для следующего читателя.
- [x] Добавлен `logger.info` о новой заявке, когда `ADMIN_TELEGRAM_CHAT_IDS` пуст: цикл по пустому списку молчит, `send_telegram` при пустом токене молчит, и между ними заявка приходила совершенно бесшумно.

**Попутный баг (найден при правке, исправлен здесь же):**
- [x] В `tasks/notifications.py` **`logger` не был определён вовсе**, а три админских таска (`notify_admins_scanner_down`, `notify_admins_infected_file`, `notify_admins_zap_findings`) зовут его в ветке «некому сказать». Ветка поднимала `NameError` вместо записи в лог — то есть последний рубеж обороны был сломан ровно в том случае, ради которого существует.

**Тесты:** `send_email` возвращает `True` при доставке и `False` при незаданном SMTP · письма уходят и заявителю, и владельцу · подтверждение обещает приглашение на обоих языках · строка помечается при успехе · **строка не помечается, когда письмо не ушло** · повторный вызов не пишет дважды · несуществующая заявка — no-op · `dry_run` не отправляет ничего · разовая рассылка пишет только непомеченным и помечает их · второй прогон не пишет повторно · эндпоинт ставит таск в очередь · мёртвый брокер не роняет 201 · новая заявка создаётся непомеченной · админский фолбэк логирует, а не падает. Плюс `conftest._ensure_waitlist_confirmation_column` (тестовая БД переиспользуется, `create_all` колонку в существующую таблицу не добавит) и обе задачи внесены в `DISPATCHED_BY_CODE` в `test_worker.py`.

**Язык письма.** Подтверждение двуязычное, русский + английский, и это сознательно не «настоящая» локализация: лендинг говорит на шести языках, а строка waitlist не хранит ни одного — тело запроса заморожено на `{email, name, source}` (T_UX.7), и расширять его ради этого письма — решение крупнее самого письма. Два языка покрывают всех, кто подписался, и коридор запуска. Полная локализация придёт с `users.locale` в T3.33.

**Acceptance:**
1. Новая заявка → заявитель получает подтверждение, владелец получает уведомление на `nyxter@dealvault.club`.
2. `confirmation_sent_at` заполнен только у тех, кому письмо реально ушло.
3. Разовая рассылка закрывает всех троих накопившихся, повторный запуск никому не пишет второй раз.
4. Полный backend-сьют зелёный — **1335 passed, 0 failed** (2026-08-08).
5. **Разовая рассылка выполнена 2026-08-08 22:17 UTC:** `{'pending': 3, 'sent': 3, 'failed': 0}`, все три строки получили `confirmation_sent_at`. Закрыто владельцем. Что при этом установлено точно: SMTP принял три письма и отметки проставлены по факту приёма. Получение в ящике — со слов владельца; если письма окажутся в спаме, это отдельная находка и повод вернуться.

**Follow-up:** ~~`ADMIN_API_TOKEN` не задан на проде~~ → закрыто **pt.2** (ниже). Осталось: UI для списка заявок нет ни на фронте, ни в админке.

#### T_UX.8 pt.2 — `GET /api/waitlist` на ролевую модель ✅ MVP (2026-08-08)

**Контекст.** `ADMIN_API_TOKEN` защищал ровно один эндпоинт — чтение списка заявок — и был **единственным местом во всём проекте**, где авторизация шла статическим заголовком мимо ролевой модели. Весь остальной админский доступ (`api/admin.py`) работает через JWT + роль: `get_current_user`, `is_superuser`, `require_perm`. Обнаружилось это, когда переменная оказалась не задана на проде и список стал недоступен всем, включая владельца.

Проблема не в незаданной переменной, а в самом механизме: два способа доказать «я админ» вместо одного, причём второй не привязан ни к аккаунту, ни к аудиту — кто именно читал список, узнать нельзя. Владелец уже суперюзер (`USER_ZERO_EMAIL`, `core/superuser.py`), то есть отдельный секрет не давал ничего, кроме второй поверхности.

- [x] `Permission.WAITLIST_READ` в superuser-секции `core/permissions.py`. **Своё имя, а не переиспользование `USERS_MANAGE`:** в списке лежат люди, которые пользователями ещё не стали, и разрешение «управлять пользователями» тихо давало бы вторую, другую возможность.
- [x] `GET /api/waitlist` → `Depends(require_perm(Permission.WAITLIST_READ))`. `require_admin_token` удалён, вместе с ним ушли импорты `secrets` и `Header`.
- [x] `ADMIN_API_TOKEN` больше не читается нигде; строку из `.env` можно убрать (ENVIRONMENT §3 помечен).

**Тесты (переписаны три старых токенных):** аноним → 401 · обычный залогиненный → 403 (вход сам по себе не даёт доступа) · суперюзер → 200 и видит заявку · **оставшийся заголовок `X-Admin-Token` не является вторым входом** → 401.

**Acceptance:** список читается из браузера под аккаунтом владельца; авторизация в проекте одна; статического общего секрета не осталось. **Сьют зелёный — 1335 passed** (2026-08-08).

**Не сделано намеренно:** UI списка заявок. Теперь он возможен без возни с токеном (обычная сессия), но это отдельная задача.

### T_UX.7 — Один бренд вместо четырёх: токены, новый лендинг, ревизия экранов ✅ MVP (2026-08-02)

**Контекст (решение владельца 2026-08-02).** Скан фронта показал, что бренд существует в **четырёх** несовпадающих экземплярах, и `BENTO_DESIGN_SKILL.md` не описывает ни один из них:

| Поверхность | Фон | Основной | Акценты | Дисплей |
|---|---|---|---|---|
| Приложение (Tailwind) | `#F5F3EE` | `#0A1626` | cyan `#58B0D9`, amber `#FF7A2F` | Space Grotesk |
| `LandingPage.tsx` (свой CSS на 576 строк) | `#EDE8DC` | `#1C3252` | sky `#3D80B8`, gold `#A86E08`, amber `#C8621A` | Space Grotesk |
| `dealvault-v5-corporate.html` | `#E8ECF1` | `#0F1B2E` | bronze `#B08D5B`, teal `#6E8E9E` | **Source Serif 4** |
| `dealvault-v5-rebel.html` | `#0A0A0F` | — | amber `#FFB800`, coral `#FF3D5A`, mint `#5EEAD4` | Space Grotesk |

Это не вопрос вкуса: человек, пришедший с лендинга в продукт, попадает в другой продукт. Отдельно из механического аудита: у corporate засечный дисплей при бессечном тексте (первый по частоте признак ИИ-вёрстки и противоречие бренду), у rebel три акцента вместо одного, в приложении нет `404` и skip-link, в `index.html` нет `description`/OG, `min-height:100vh` вместо `100dvh`, `text-red-600` используется мимо токенов.

**Бриф (ответы владельца 2026-08-02):** новый лендинг — **клиентский** (не инвесторский), в дизайн-языке референса `weathered-river-b567.azzot88.workers.dev`: документальная плотность, почти чёрное на белом, нумерованные ворота `01–04`, посадочный талон как якорь, доказательства вместо прилагательных. Имя **Vimana**, шесть языков через i18n. Технически — **React-роут + пререндер в статику**. Движение — `framer-motion`. Продукт — **полная ревизия всех экранов**.

**Дилы:** `DESIGN_VARIANCE 6` · `MOTION_INTENSITY 3` · `VISUAL_DENSITY 6`. Плотность выше базовой намеренно: референс держится на насыщенности, а не на воздухе. Моция низкая: документ, который дёргается, перестаёт читаться как документ.

**Freeze-лист — меняется только явным решением, никогда молча:**
- [ ] Все 20 маршрутов и их слаги, включая `/i/:npub`, `/profile/keys`, `/reader.html` и оба `dealvault-v5-*.html` (они задеплоены, на них могут быть ссылки).
- [x] **Freeze-лист был неполон, и это выяснилось падением (2026-08-02).** Я каталогизировал `getByTestId` и `getByRole`, но не `getByText` — а `invite-flow` ждал литеральную русскую строку «Контакт добавлен в ваш профиль». После перевода экрана свежий контекст Playwright (без `lang` в localStorage) отрендерил английский, и спек упал. Починено идентификатором `invite-accepted` вместо текста: проверка, привязанная к формулировке одной локали, проверяет перевод, а не сценарий. **Правило на будущее: перед правкой видимых строк грепать спеки на `getByText`, а не только на testid.**
- [ ] **11 `data-testid`, на которых висит Playwright:** `identity-confirm`, `identity-generate`, `identity-npub`, `identity-saved`, `identity-start`, `identity-state`, `passkey-add`, `passkey-list`, `passkey-login`, `step-up-confirm`, плюс локализованный regex кнопки создания приглашения (`создать ссылку|create link|створити|utwórz|créer|crear`). Переименование любого — красный e2e, а не косметика.
- [ ] Имена полей форм (контракты `/auth/login`, `/auth/register`) и порядок шагов в них.
- [ ] Ключи i18n: удаление ключа ломает шесть локалей разом, а не одну.
- [ ] Вордмарк «Vimana» с амбровым акцентом на части слова.

**pt.1 — фундамент (сначала, потому что от него зависит остальное):**
- [ ] Один источник токенов: цвет, шрифты, радиусы, тени, z-index. `LP_CSS` из `LandingPage.tsx` исчезает вместе со своей палитрой.
- [ ] `danger` как именованный токен вместо сырого `text-red-600` (19 мест).
- [ ] Фокус-кольца везде, где есть интерактив (сейчас 48 фокус-стилей на 73 перехода — то есть не везде).
- [ ] Маршрут `404`, skip-link, `description` и OG-теги, `100dvh` вместо `100vh`.

**pt.1 ✅ (2026-08-02).** Токены сведены в `tailwind.config.js` (цвет, радиусы `card`/`field`, тени с примесью navy, именованный `z-index`); `danger` стал токеном вместо восьми оттенков сырого красного в 24 местах; одно фокус-кольцо на весь продукт через `:where(...):focus-visible` с нулевой специфичностью (было 48 фокус-стилей на 73 перехода); добавлены `404`, skip-link, `description`/OG, `100dvh`. `og:image` **не добавлен намеренно**: SVG не рендерится в превью ни у Facebook, ни у Telegram, ни у Slack, а растра 1200×630 нет — отсутствие картинки деградирует в текстовую карточку, битая ссылка в пустой прямоугольник.

**pt.2 ✅ (2026-08-02) — новый лендинг.** `LP_CSS` на 576 строк удалён вместе со своей палитрой; страница целиком на токенах. Структура в языке референса, жанр клиентский: герой с посадочным талоном → четыре факта о работающем сегодня → задача → ворота `01–04` → три механизма → **явный раздел «чего ещё нет»** → закрытие. Дисциплина утверждений §9.1 стала элементом вёрстки: у каждого механизма рядом напечатан его предел тем же весом. Версия в шапке читается из `APP_VERSION` (было зашито `V0.01.17` при актуальной `0.03.7`). Не перенесено из референса: счётчик участников (не наше число) и «tamper-proof» (превышение).

**Дополнение 2026-08-04 — талон восстановлен по оригиналу; ✅ принято владельцем 2026-08-06 после пересборки и осмотра.** Владелец сравнил с июньским инвесторским оффером и указал, что талон обеднел. Проверено по истории: **из приложения ничего не удаляли** — украшения жили только в отдельном HTML (`~/Downloads/Output/peerflew-offer/peerflew-investor-offer.html`, 9 июня, вне репозитория; байт в байт то, что отдаёт workers.dev). React-лендинг их не имел **никогда**: до T_UX.7 стоял бенто-вариант из `landing.html` (`DXB → JFK` строкой, без дуги и штрихкода), а pt.2 переписал его с нуля. В записи pt.2 названы только два сознательных непереноса — счётчик участников и «tamper-proof»; про детали талона решения нет **нигде**, то есть они не были перенесены молча. Это и есть ответ на «почему»: не решение, а пропуск.

Возвращено в `LandingPage.tsx`: **вырубка** (пунктирный отрыв + два круга Ø22 по краям, залиты цветом страницы — билет это то, что отрывают), **дуга полёта** (та же геометрия `M6 38 Q60 -6 114 38`, пунктир `3 4`, точки навy/амбер) с **самолётом, летящим по траектории** (`getPointAtLength` + наклон по касательной, туда-обратно; при `prefers-reduced-motion` припаркован в середине — рисунок это суть, движение это украшение), **штрихкод** внизу, коды подняты до 38px, тень `shadow-lift` вместо `shadow-card`. Компонент `FlightArc` объявлен в модуле, а не внутри `LandingPage`: внутри он пересоздавался бы на каждый ререндер и анимация перезапускалась бы при открытии модалки.

**Поле `Escrow · BTC/USDT held` возвращено по решению владельца** («они будут»). Напряжение с §9.1 названо, а не замолчано: эскроу — Фаза 5, механизма нет. Считаем допустимым потому, что талон это **образец** сделки (вымышленные имя и номер), а на той же странице ниже стоит явный раздел «чего ещё нет», и `escrow` в нём назван прямо. Если раздел когда-нибудь уедет — поле обязано уехать с ним.

- [x] Пререндер: SSR-бандл (`src/entry-ssr.tsx`) → `dist/landing.html`, nginx фронтенд-контейнера отдаёт его точным `= /`. Отдельный файл, не перезапись `index.html`: иначе разметку получил бы каждый маршрут и `/login` показывал бы пролетающий лендинг. Гидрация выбирается по `root.hasChildNodes()`, а не по флагу сборки, который мог бы разойтись с DOM. Три модуля сделаны SSR-безопасными: `i18n` и стор авторизации читали `localStorage` на уровне модуля, `Reveal` сериализовал бы секции с `opacity: 0`. Русский только — по локалям нужен content negotiation в nginx, иначе смысла нет.
- [x] Предупреждение сборки о чанке >500 кБ вылечено по существу: роуты грузятся по требованию, эагерно только лендинг/вход/регистрация. Главный чанк 653 → 364 кБ (gzip 210 → 134). Комментарий в конфиге обещал «под 500 кБ» и перестал быть правдой задолго до `framer-motion` — предупреждение читали как фон.
- [x] Предупреждение `act()` в тесте переключателя языка: `changeLanguage` резолвится после обработчика клика, ре-рендер прилетал вне `act`. Заодно тест стал проверять, что переключение **завершилось**, а не что началось.

**pt.3 ✅ (2026-08-02) — ревизия всех экранов.**

- [x] 33 цвета мимо токенов (`orange`/`green`/`gray`) сведены к `amber`/`success`/`navy`. 52 файла.
- [x] Радиусы: было четыре контейнерных значения вразнобой (`lg`/`xl`/`2xl`/`md`), стало два по роли — `card` для контейнеров, `field` для контролов. Визуально почти нейтрально: `rounded-lg` и `field` совпадают по значению, менялась роль. `min-h-screen` → `min-h-[100dvh]`.
- [x] **51 захардкоженная русская строка** в четырёх экранах (`DealVaultPage` 24, `DealPage` 15, `InvitePage` 6, `AcceptInvitePage` 6) переведена на i18n ×6. Пять локалей из шести показывали там русский. Остались только эндонимы переключателя языков — их локализовать нельзя по определению.
- [x] Кнопка создания приглашения переведена **с оглядкой на freeze-лист**: во всех шести локалях сохранены подстроки, по которым её ищет Playwright. Проверено значение за значением.
- [x] `toLocaleTimeString('ru-RU')` в чате сделки — время рендерилось в русском формате для всех шести локалей.
- [x] **Русская фраза уходила в цепь.** `addEvent(dealId, 'handoff', 'Груз передан перевозчику')` — этот `note` попадает в `payload` события и **хешируется в `entry_hash`**, то есть язык интерфейса отправителя становился частью доказательства, которое читает контрагент. Убран: `event_type` говорит ровно то же, обе стороны рендерят на своём языке. Заодно в цепи одной произвольной строкой меньше.

**Порядок наложения переписан частично, намеренно.** Модалки (13 мест) и подложка сведены к именованной шкале; выпадающие списки с `z-10`/`z-20` **не тронуты**: сейчас список внутри формы стоит выше липкой шапки, а шкала подняла бы шапку до 100 и список ушёл бы под неё. Проверить это можно только глазами, а слепая замена порядка наложения — верный способ сломать `AirportSelect`. Возвращаться сюда с открытым браузером.

**Acceptance:** ни одна поверхность не объявляет собственную палитру; переход «лендинг → продукт» не читается как смена сайта; Playwright и vitest зелёные без правки селекторов.

### T_UX.6 — Профиль и «Доступ и ключи» — разные экраны ✅ MVP

**Контекст (решение владельца 2026-08-01).** На профиле оказались рядом две несвязанные вещи: «кто я для контрагента» (аватар, УБА, доверие, адреса) и «чем я вхожу и чем владею» (почта, пароль, устройства, коды, ключ). После T3.16 и T3.21–T3.24 второй набор перестал помещаться: поле парольной фразы стояло рядом с адресом доставки.

- [x] `/profile` — витрина: аватар, имя, УБА, круги доверия, верификация, адреса, приглашения. На месте разделов доступа — одна карточка-дверь со ссылкой.
- [x] `/profile/keys` (`ProfileKeysPage`) — почта и пароль, коды восстановления, passkey-устройства, Identity Vault со ступенями и ссылка на ридер.
- [x] **Отдельный маршрут, а не вкладка.** Сюда нужно уметь ссылаться: из баннера «осталось 2 кода», из письма об использованном коде, из ридера («вернуть ключ в аккаунт»). Вкладка живёт в состоянии компонента, и ссылки на неё не существует.
- [x] Ридер открывается в новой вкладке как **статическая страница вне приложения** — это видно по поведению, и так и должно быть: он существует для случая, когда приложение недоступно. Маршрутом его делать нельзя, иначе обещание рушится.
- [x] «Редактировать профиль» осталось модалкой: имя и аватар — правка витрины, отдельного экрана не заслуживает.
- [x] **Второй такой же долг (T3.23, обнаружен 2026-08-02):** `identity-establish` ждал полные 64 hex-символа в видимом тексте `identity-npub`, а T3.23 по решению владельца сократила показ до `first4…last4` и увела полное значение в `title`. Спек проверял вёрстку, которой больше нет. Теперь проверяются оба свойства: в `title` действительно весь ключ, а на экране — сокращение. **Закономерность из обоих случаев: изменение того, что видно на экране, — это изменение контракта e2e.** Правка спека входит в задачу, которая меняет экран, а не в следующий прогон.
- [x] **Долг, обнаруженный 2026-08-02:** `identity-establish` и `passkey` продолжали ходить на `/profile` и искать там `identity-state`/`passkey-add`, которые этой задачей переехали на `/profile/keys`. Четыре красных спека прожили так до первого полного прогона e2e. Разделение экранов обязано было включать правку спеков — это часть переезда, а не отдельная работа.
- [x] i18n ×6.

**Осталось:** ссылки из баннера кодов и письма пока ведут в профиль вообще, а не на `/profile/keys` — доделывается вместе с `T3.17`.

### T_PERF.1 — Форма доступа к данным: индексы, N+1, блокирующий I/O ✅ MVP

**Контекст (ревизия кода 2026-08-01).** Продукт написан аккуратно, но ни разу не проходил ревизию на форму обращений к БД и к внешним сервисам. Наглядно: на всю схему было **семь индексов**, ни одного на запросе, который выполняется постоянно, — при том что `core/pagination.py` в докстринге обещает «O(log n) via index on (created_at, id)». Отдельные места лечили следствие вместо причины: у BFS Trust Graph записан follow-up «Redis-кэш», хотя кэш спрятал бы форму запроса, а не исправил её.

Сегодня всё это ничего не тормозит — на проде десятки строк. Задача сделана до Фазы 4 сознательно: после платежей та же работа делается на живой нагрузке и с миграциями под замком.

**pt.1 — доступ к БД ✅ (2026-08-01)**

- [x] Миграция `0034_hot_path_indexes` — 10 индексов, каждый под названный запрос: `deals(sender_id)` / `deals(carrier_id)` (`GET /api/deals` с `OR` по обеим сторонам), `trips(status, created_at, id)` (листинг маркетплейса: фильтр и сортировка одним индексом), `trips(carrier_id)`, `deal_vault_messages(deal_id, created_at, id)` (самое горячее чтение в продукте), `attachments(message_id)` (`selectinload` на каждую страницу чата), `inquiry_messages(inquiry_id, created_at, id)`, `trust_edges(to_user_id)`, `verification_requests(deal_id)`, `verification_badges(subject_id)`. Индексы продублированы в моделях (`__table_args__`) и в `tests/conftest.py` (`_ensure_hot_path_indexes`) — иначе тестовая БД расходится с прод-схемой ровно там, где различаются планы.
- [x] **Не индексируем осознанно:** `deal_events` (есть `UNIQUE(deal_id, seq)`), `deal_participants` и `trip_inquiries` (фильтруемая колонка ведёт их unique-констрейнт), `trust_edges.from_user_id` (ведёт `uq_trust_edge_pair_kind_source`; левый префикс — это индекс).
- [x] `verify_content` (`core/deal_chain.py`) — N+1 убран: ссылки собираются одним проходом и грузятся одним запросом на тип. Было по `db.get` на событие, то есть 300 обращений к БД на сделку с 300 сообщениями — на эндпоинте `GET /deals/{id}/chain`, который доступен любому участнику. Побочно: нечитаемый `message_id` в payload теперь даёт mismatch, а не 500 — функция для того и существует, чтобы сообщать о порче.
- [x] `bfs_circles` (`core/trust.py`) — один запрос на уровень (`from_user_id IN (frontier)`) вместо запроса на узел. Обход тот же, глубина по-прежнему ≤ 6. Follow-up «Redis-кэш BFS» из T2.4 после этого не нужен: кэшировать нечего, вопрос стал дешёвым.
- [x] `GET /api/trips` — точное сравнение вместо `ilike '%code%'` (в колонках лежат IATA-коды из `AirportSelect`; подстрочный поиск и индекс исключал, и работал неверно: `?origin=A` матчил всё с буквой A) и полуоткрытый UTC-день вместо `cast(depart_at, Date) = :date` (функция на колонке отменяет индекс). Запрос нормализуется в верхний регистр — набранный руками `dxb` находит `DXB`.
- [x] Тесты: 3 на семантику фильтров рейсов (точность, регистронезависимость, границы дня) + 1 на нечитаемую ссылку в цепи.
- [x] **Нормализация маршрута при записи** (найдено 2026-08-01 вторым падением того же теста). Точное сравнение требует одной канонической формы в колонке, а её никто не обеспечивал: `POST /api/trips` клал строку как есть. Тест сохранял `P-<hex>` в нижнем регистре и не находил его — но настоящая проблема шире теста: рейс, созданный прямым POST'ом как `dxb`, был бы невидим для любого поиска `DXB`. Чинится на стороне записи (`origin`/`destination` → `strip().upper()`), а не в каждом читателе. `AirportSelect` и так шлёт верхний регистр, так что для UI ничего не меняется.
- [x] **Поправлен `test_trips_pagination_next_cursor_walks_all_items`** (`test_hardening_block5.py`): он создавал пять рейсов с разными origin (`P-<tag>-0…4`) и собирал их обратно фильтром `P-<tag>`, то есть держался на подстрочном `ilike`. Теперь все пять публикуются из одного аэропорта — так листание коридора и выглядит на самом деле. Случай показательный: тест закреплял не намерение («курсор проходит все страницы»), а побочное свойство реализации, и падение на смене семантики фильтра было правильным поведением сьюта.

**pt.2 — блокирующий I/O и внешние клиенты ✅ (2026-08-01)**

- [x] `core/storage.get_s3_client` под `lru_cache` + `reset_client_cache()` для тестов, меняющих настройки на ходу (тот же приём, что `keypair.reset_key_cache`). Клиент строился на **каждый** вызов, а `get_presigned_url` зовётся по разу на вложение: страница сейфа с дюжиной фото собирала дюжину клиентов, чтобы подписать дюжину ссылок. Построение клиента — это разбор service-модели S3 в botocore, а не конструктор из трёх присваиваний.
- [x] `upload_file` (синхронный PUT в R2) и валидация содержимого (`validate_upload` / `validate_document` — сигнатура + полный декод Pillow) уехали в `run_in_threadpool` во всех трёх местах: `api/avatar.py`, `api/dealvault.py`, `api/verification.py`. До этого заливка файла держала event loop на всё время сетевой операции — один пользователь с большим фото останавливал обработку **всех** запросов на воркере. Библиотеки не менялись: `aioboto3` тянуть незачем, когда блокирующий вызов достаточно увести с петли.
- [x] `core/nostr_publish.publish_event` — `asyncio.gather` вместо последовательного обхода. Четыре релея по 5 с таймаута давали до 20 с на одно событие, и один недоступный релей задерживал все следующие за ним. Тест на параллельность — барьером (`asyncio.Event`), а не секундомером: при последовательной реализации он детерминированно виснет и падает по `wait_for`, а не «обычно проходит».
- [x] Тесты: +2 (`test_relays_are_published_to_at_once`, `test_s3_client_is_built_once_per_process`).
- [x] **Presign оставлен синхронным осознанно:** подпись ссылки — локальный HMAC на микросекунды, в поток его уводить дороже, чем считать. Дорогой была только сборка клиента, и она закэширована.

**pt.3 — память, счётчики, лимиты ✅ (2026-08-01)**

- [x] `core/airports.py` — построение индекса GeoNames перенесено внутрь `_load()`. Файл 8,4 МБ разбирался при импорте модуля в словарь, который читает **только** цикл загрузки; как глобал он оставался в памяти на весь срок жизни процесса впустую. Инстанс 2 ГБ, OOM уже случался (PROJECT §6.2b).
- [x] Там же нижний регистр предвычислен при загрузке (`iata_lower`/`city_lower`/`country_lower`/`alt_names_lower` в `Airport`): `search()` лоуеркейсил ~7 000 аэропортов и их альтернативные названия **на каждое нажатие клавиши** в автокомплите. `nearest()` — `heapq.nsmallest` вместо полной сортировки списка ради пяти элементов. `list_countries()`/`list_cities()` под `lru_cache`: таблица аэропортов в рантайме не меняется.
- [x] `core/metrics.bump_publish_metric` — `UPDATE ... SET c = c + 1` вместо read-modify-write через ORM: два одновременных publish читали `n` и оба писали `n + 1`, один инкремент исчезал. Докстринг обещал ровно ту атомарность, которой в коде не было, и называл несуществующую таблицу (`metrics_counters`). `get_publish_metrics` переведён на колоночный select — `select(PublishMetric)` мог быть отвечен из identity map, а при `expire_on_commit=False` там лежат значения **до** UPDATE (сессия новые не видела, их посчитала БД).
- [x] `core/rate_limit.py` — slowapi на Redis (`storage_uri=settings.REDIS_URL`, `swallow_errors=True`). В памяти счётчики обнулялись каждым деплоем — то есть ровно тогда, когда перебирающий `/api/auth/email/request-code` получал бюджет обратно, — и были per-process. Зоны nginx остаются, но они грубые per-IP; поэндпоинтные бюджеты (5/час на код, 10/мин на Nostr-вход) живут только здесь. `swallow_errors` — сознательная половина решения: недоступный Redis пропускает запрос, а не роняет его (`D-REVOCATION-IS-BEST-EFFORT`; step-up остаётся fail-closed).
- [x] **Синхронный клиент limits оставлен осознанно:** один блокирующий INCR по compose-сети на редких эндпоинтах — не то же самое, что заливка файла в R2, которую pt.2 уводил с петли. Асинхронный бэкенд `limits` требует новой зависимости (`coredis`); если задержка когда-нибудь станет заметна, ответ — он, а не возврат к счётчикам в памяти.
- [x] Тесты: +3 (счётчик не теряет инкременты и читается после UPDATE; альтернативные названия городов находятся после предвычисления регистра; статические агрегаты считаются один раз).

**Не входит:** пять legacy-слоёв (`T_KEYS.1`) — ждут `D-KEY-TIERS`.

**Acceptance:** каждый индекс отвечает за названный запрос, а не «на всякий случай»; `verify_content` и BFS выполняют число запросов, не зависящее от объёма данных; фильтры рейсов используют индекс и не матчат по подстроке; полный backend-сьют зелёный.

### T_UX.1 — Bento breakpoint rule + decline_polite copy ✅ MVP

**Bento двухколоночная сетка.** Правило: 2 колонки на desktop/tablet, **1 колонка на phone даже в landscape**. Не работает по Tailwind width-only breakpoint'ам (iPhone Pro Max landscape = 932px попадает в `md:` 768+).

- [x] Хук `useBentoLayout()` в `frontend/src/hooks/useBentoLayout.ts`: возвращает `'phone' | 'tablet' | 'desktop'` через `window.innerWidth/innerHeight` + `matchMedia('(any-pointer: coarse)')`. Правило: phone = `width < 768 OR (height < 500 AND coarse pointer)`, tablet = 768-1023, desktop = 1024+. Слушает `resize` + `orientationchange`.
- [x] Bento контейнер `<BentoGrid>` в `components/BentoGrid.tsx`: применяет `grid-cols-1` для phone, `grid-cols-1 md:grid-cols-2` для tablet+desktop. Опциональный `force` prop для форсирования 1 или 2 колонок.
- [x] DESIGNGUIDELINES.md §5 уже содержит правило (обновлено при T_UX.1 записи в PRD).
- [ ] **Миграция существующих Bento-мест** отложена в follow-up — ProfilePage/Dashboard/DealVaultPage сейчас используют собственные grid-классы, работают корректно на десктопе/tablet. Заменять на `<BentoGrid>` — механическая работа + testing per-page.

**decline_polite sender-copy.** При отказе перевозчика от verification (target_role=carrier, status=`declined_polite`) sender видит объяснение + CTA.

- [x] `frontend/src/components/VerificationDeclineBanner.tsx` — амбер-баннер: `verification.declinedPolite.senderCopy` + CTA-кнопка `verification.declinedPolite.requestCollateralCTA` (открывает модалку «Coming in Phase 5»).
- [x] Показывать в DealPage для sender при наличии `VerificationRequest.status = 'declined_polite'` с `target_role = 'carrier'` (проверка через `listDealRequests`).
- [x] i18n `verification.declinedPolite.*` — EN + RU + UA локализованы, остальные fallback на EN.
- [x] Заглушка CTA открывает модалку с текстом про Phase 5 escrow — реальная реализация в T5.x.
- [x] Vitest 5 тестов для `useBentoLayout` — desktop, tablet, phone-narrow, **iPhone 14 Pro Max landscape (932×430 coarse) → phone**, iPad portrait (768×1024 coarse) → tablet.

**Acceptance ✅:** `useBentoLayout` корректно классифицирует iPhone 14 Pro Max landscape как phone (что не может Tailwind width-only); `<BentoGrid>` доступен для использования; sender видит polite-decline banner на DealPage с рабочей CTA-заглушкой.

**Follow-up:** (1) миграция существующих Bento-мест на `<BentoGrid>` (ProfilePage, Dashboard, DealVaultPage) — механическая работа; (2) остальные 3 языка (pl/fr/es) локализовать полностью (сейчас fallback на EN).

### T_TEST.3 — Playwright smoke suite (наблюдаемая e2e) ✅ MVP

**Контекст.** Сейчас unit+integration через pytest (~250 тестов) + vitest (13 тестов). UI e2e MVP закрыт T_TEST.3.

**Софт — Playwright** (Microsoft). Мотивация в comparison table ниже (сохранена как обнаруженная логика — не удалять):

| Тул | За | Против | Вердикт |
|---|---|---|---|
| Playwright | Multi-browser (Chromium/Firefox/WebKit), встроенный trace viewer (record + offline replay), auto-wait без sleep, TS-native, docker образ `mcr.microsoft.com/playwright` | ~200 MB npm-deps | ✅ default |
| Puppeteer | Легче, official Chrome API | Только Chromium; менее удобное API для многошаговых спеков | микро-smoke на одну проверку |
| Cypress | GUI dashboard, time-travel debug | Замкнутый рантайм, медленнее в CI, сложнее headless | нет |

**Спеки (3 штуки):**

- [x] `golden-path.spec.ts` — carrier reg → trip publish → sender reg → matching. Full accept/confirm/close — pt.2 когда стабилизируется UI-selectors.
- [x] `verification.spec.ts` — VerificationSection присутствует на profile для fresh user. Full 3-tier flow с multi-context — pt.2.
- [x] `recipient.spec.ts` — `/join/deal/<bogus>` роут отвечает, не крашится. Full invite copy-paste — pt.2.
- [ ] `smoke-nostr.spec.ts` (после T3.5) — trip publish → `Trip.nostr_event_id` проставлен → `GET /api/trips/{id}/nostr-event` возвращает валидный NIP-01 event JSON. Проверяет T3.5 publish bridge.

**Инфра:**

- [x] `frontend/e2e/` — отдельный npm-пакет (`@playwright/test` не тянется в основной build). `playwright.config.ts` с `baseURL = SMOKE_BASE_URL ?? https://vimana.dealvault.club`, trace всегда, video/screenshot on failure, sequential (fullyParallel=false — shared prod DB, no races).
- [x] npm scripts: `install:browsers`, `headed`, `headed:single`, `trace`, `ci`, `show-trace`, `show-report`.
- [ ] **docker-compose profile `smoke` + `smoke-live` (VNC :6080)** — pt.2. Для MVP native запуск с Mac достаточен.

**Cleanup e2e users (T_TEST.3):**

- [x] Convention: все Playwright users регистрируются с email `<prefix>-<ts>-<rand>@e2e.vimana.local`. TLD `.local` не резолвится.
- [x] `backend/app/tasks/cleanup.py::cleanup_e2e_users` — Celery beat раз в 24ч, каскадный delete через 12 таблиц (Trips→Inquiries+Messages, Deals→Messages+Attachments+Events+Disputes+Grants+Participants, Orders, Connections, InviteLinks, TrustEdges).
- [x] Beat schedule `cleanup-e2e-users-daily` в `worker.py`.

**AdminUsersPage расширения (T_TEST.3):**

- [x] `GET /api/admin/users?email_contains=X` — case-insensitive ILIKE.
- [x] `DELETE /api/admin/users/{user_id}` — superuser hard-delete + async cascade (зеркалит логику Celery task). Cannot delete superuser or self.
- [x] Frontend AdminUsersPage — чекбокс «Only e2e test users», text-filter, счётчик total/test, амбер-chip «test», красная Delete-кнопка с confirm (двойной copy — короткий для test, строгий для non-test).

**Документация:**

- [x] `frontend/e2e/README.md` — три режима (headed/trace/ci), env override, cleanup convention, trace viewer link.

**Acceptance ✅ MVP:**
1. `cd frontend/e2e && npm install && npm run install:browsers && npm run headed` на Mac открывает Chromium, проходит 3 спека visibly с 500 мс замедлением.
2. Тестовые юзеры видны в `/admin/users` с амбер-chip'ом «test»; superuser может bulk-очистить или дождаться Celery.
3. `npm run trace` (headless) генерирует trace.zip → смотрится на trace.playwright.dev.

**Follow-up (pt.2 ✅ частично):**
1. `smoke-nostr.spec.ts` после T3.5.
2. [x] Multi-context спеки (2026-07-18): `invite-flow.spec.ts` (Alice→Bob invite copy-paste + connection symmetry), `auth-rehydrate.spec.ts` (hard-nav в новой page с сохранённым localStorage — регрессия на T_UX.3 pt.1), `admin-guard.spec.ts` (не-superuser → /admin/* → redirect + AdminPanelSection не в /profile). Dispute + arbiter reveal и decline_polite визуал отложены до T_UX.1 pt.2.
3. Docker profile `smoke` (headless в контейнере) + `smoke-live` (VNC :6080 для live с телефона).
4. CI-hook на prod-деплой + Telegram alert.
5. Trace артефакты на R2 30 дней.
6. 5 backend-тестов (`test_admin_users_cleanup.py`): filter, delete cascade, forbidden non-superuser, cannot delete superuser/self, Celery deletes stale ≥24ч + preserves fresh.

**pt.3 — Full E2E business flows (14 спеков, план 2026-07-25):**

Текущие 7 спеков — smoke (регистрация, рендер, guard'ы, multi-tab). pt.3 покрывает бизнес-флоу сквозняком через UI. Приёмы: multi-context (sender/carrier/arbiter в одном тесте), `setInputFiles` (upload из байтов), download-перехват + SHA-256 сверка, `page.request` (UI+API микс), `page.route` (сетевые сбои), `addInitScript` c фейковым `window.nostr` (self-custody без расширения — headless не грузит extensions).

**P0 — ядро сделки (реализовать первым пакетом, после T3.8):**
- [ ] 1. `deal-lifecycle.spec.ts` — carrier: рейс → sender: match → accept → handoff → received → confirm; статусы у обоих; после закрытия vault запечатан (ввод недоступен / «sealed»-ошибка). UI-покрытие T3.7.
- [ ] 2. `chat-two-users.spec.ts` — sender пишет → carrier видит; system-messages рендерятся.
- [ ] 3. `chat-file-upload.spec.ts` — PNG через `setInputFiles` → превью в чате, lightbox, файл виден второй стороне.
- [ ] 4. `dirty-file-rejected.spec.ts` — MZ-байты как `photo.jpg` → человекочитаемая ошибка, файла в чате нет. UI-покрытие T3.8.
- [ ] 5. `download-verify-hash.spec.ts` — скачать вложение → SHA-256 скачанного == `file_hash` из API == hash в `/chain`. Демо верифицируемости DealVault.

**P1 — доверие и споры:**
- [ ] 6. `verification-flow.spec.ts` — carrier запрашивает документы → sender грузит PNG-паспорт → badge в профиле.
- [ ] 7. `declined-polite.spec.ts` — перевозчик вежливо отклоняет → нейтральный баннер у отправителя, без штрафов.
- [ ] 8. `dispute-reseal.spec.ts` (3 контекста) — dispute после закрытия → чат распечатан → evidence-фото → arbiter claim + resolve(closes_deal) → снова sealed. UI-покрытие D-SEAL-SEMANTICS.
- [ ] 9. `share-address.spec.ts` — picker адреса → 📍-сообщение в чате.
- [ ] 10. `chain-verify.spec.ts` — после сделки `page.request GET /chain`: ok=true, coverage полный, sealed_at установлен. UI-badge — после follow-up 2 из T3.6.

**P2 — платформа:**
- [ ] 11. `mobile-viewport.spec.ts` — `devices['iPhone 14']`: Bento 1 колонка, BottomNav, тач-зоны.
- [ ] 12. `i18n-switch.spec.ts` — RU↔EN, сохранение выбора после reload.
- [ ] 13. `network-chaos.spec.ts` — `page.route` abort `/api/deals/*` → нет белого экрана, есть сообщение об ошибке.
- [ ] 14. `fake-nip07-e2e.spec.ts` — инъекция `window.nostr` → self-custody E2E сообщение шифруется/расшифровывается в браузере.

Ограничения (зафиксировано): email-флоу не проверяем (нет ящика); реальное NIP-07-расширение — только persistent context (обходим инъекцией); a11y/visual regression — не здесь (T_TEST.8/T_TEST.9).

**Acceptance pt.3:** P0-пакет (5 спеков) зелёный против prod ≤ 3 мин; P1/P2 — по мере, каждый спек самоочищается через `@e2e.vimana.local` convention.

### T_AGENT.1 — Агентский интерфейс: Nostr publish + MCP server ✅ pt.1 + pt.2 MVP

**Контекст.** Стороннние AI-агенты (Claude, GPT, Nostr-native клиенты) читают рейсы через стандартные протоколы: (A) Nostr publish (T3.5), (B) MCP server для Claude/Anthropic (T_AGENT.1).

**pt.1 — skeleton ✅ MVP**

- [x] `mcp-server/` — отдельный Python-процесс + Dockerfile. Использует `mcp` SDK v1.1.0.
- [x] Docker service `mcp-server` под `profiles: ["mcp"]` в docker-compose. Не стартует по умолчанию: `docker compose --profile mcp up -d mcp-server`.
- [x] `env VIMANA_API_URL` (default `http://backend:8000`) — MCP-server читает через тот же backend что и frontend. Никаких DB-креденшелов, никаких приватных доступов.
- [x] 2 tools:
  - `list_trips(origin?, destination?, date?, limit?)` — вызывает `GET /api/trips`, форматирует список с carrier_name + UBA + категориями.
  - `get_trip_details(trip_id)` — берёт `GET /api/trips/{id}/nostr-event` (или fallback на list search).
- [x] `mcp-server/README.md` — как подключить в Claude Desktop config.
- [x] **Nostr путь** (Nostr-slope) — уже реализован в T3.5 (agents подключаются к `wss://vimana.dealvault.club/relay`, фильтр `#t=vimana #t=trip`).

**pt.2 — auth + rate-limit + search + metrics ✅**

- [x] `MCP_API_KEY` env — если задан, каждый tool call должен содержать matching `api_key` arg → 401-эквивалент. Если пустой — dev mode, всё пропускается (backwards compat).
- [x] Rate-limit `MCP_RATE_LIMIT` (default 60) calls/минуту per key через sliding-window deque. `_anon` bucket для unauthed режима.
- [x] `search_trips(query, limit?)` — client-side substring search по origin/destination/carrier_name/categories. Полнотекстовый через Nostr subscribe backfeed — pt.3 (нужен NOSTR_PUBLISH_ENABLED + relay subscription).
- [x] `get_mcp_metrics()` tool — in-process per-tool counters + rejection reasons (auth / rate_limit). Persistence и Prometheus scrape — pt.3.
- [x] README обновлён — секции Auth, Rate limit, Metrics + pt.3 roadmap.

**pt.3 — deferred:**

- [ ] Nostr subscribe backfeed для реального full-text search.
- [ ] Метрики persistence (Postgres или Prometheus scrape endpoint).
- [ ] Per-user MCP tokens tied to Vimana accounts («my trips only»).
- [ ] SSE transport (сейчас stdio-only, ограничено локальным subprocess'ом).
- [ ] Backend-тесты через subprocess-запуск MCP + fake stdio.

**Acceptance MVP ✅:** `docker compose --profile mcp up -d mcp-server` стартует контейнер. При подключении в Claude Desktop через stdio → `list_tools` возвращает 2 tools + описания; `call_tool('list_trips', {origin: 'SVO'})` возвращает форматированный список активных рейсов.

### T2.1 pt.3 — decline_polite sender copy ✅ MVP

*(child T2.1; см. также T_UX.1 где UI компонент)*

- [x] Backend: `declined_polite` отдаётся как есть — `VerificationRequestStatus` это `str`-enum, JSON = `"declined_polite"`. `VerificationRequestOut.status` / `.target_role` типизированы enum'ами вместо голого `str`, чтобы OpenAPI объявлял допустимые значения (зеркало TS-юнионов `RequestStatus` / `TargetRole`). JSON-вывод не изменился.
- [x] `GET /deals/{id}/verification-requests` — источник баннера — покрыт 5 тестами (был без покрытия): точный предикат баннера (`status=declined_polite` + `target_role=carrier` + `resolved_at`), видимость обоими участниками, порядок newest-first, outsider → 403, несуществующая сделка → 404.
- [x] Frontend UI-компонент — сделан в T_UX.1 (`VerificationDeclineBanner.tsx`, рендер под `isSender && carrierPoliteDecline` в `DealPage.tsx`).

### T_UX.2 — Route notes + platform disclaimers ✅ MVP

**Статус:** MVP полностью закрыт 2026-07-19 (pt.1 backend → pt.2 admin CRUD + PlatformNoticeBanner → pt.3 UI slots → pt.4 direct-text поля + DealVault pinned system-msg на match). Multi-lang translations — pt.5 (когда появится curation workflow).

**Контекст.** Платформа **не блокирует направления** — позиция зафиксирована в TECHSTATE `D-COMPLIANCE-STANCE`. Пользователи имеют право сами решать. Vimana только **информирует** через две модели:

- `RouteNote` — плашка на конкретный коридор (или wildcard `*→X`).
- `PlatformNotice` — глобальные плашки платформы (не привязаны к коридору).

**Модель:**

- [x] `RouteNote(id, origin_iso, destination_iso, status ∈ {standard, attention, complex, restricted}, severity ∈ {info, warning, alert}, headline VARCHAR(500), body TEXT, active_from, active_until?, created_by)` — миграция 0021 + pt.4 миграция 0022 (заменила i18n_key на direct text). Wildcards `*` в origin/destination.
- [x] `PlatformNotice(id, key UNIQUE, severity, target_surface ∈ {footer, trip_card, deal_page, all}, headline, body, active_from, active_until?, created_by)`.
- [x] Direct text (pt.4) — superuser вбивает headline+body в admin CRUD, они рендерятся напрямую. Multi-lang — pt.5.

**Endpoints:**

- [x] `GET /api/route-notes?origin=X&destination=Y` — active + wildcard matching, sort by specificity → severity. Public.
- [x] `GET /api/platform-notices?surface=X` — active + `all` matches any surface filter. Public.
- [x] `POST/PATCH/DELETE /api/admin/route-notes` + `/api/admin/platform-notices` — superuser CRUD (pt.2).

**UI слоты (frontend):**

- [x] **TripCard** pill для не-standard коридоров, клик → раскрытие body (pt.3).
- [x] **NewTripPage** pre-flight warning modal для complex/restricted с «Понимаю — публикую» (pt.3).
- [x] **DealPage** sticky-banner для active note + platform notice (pt.3).
- [x] **DealVault** pinned system-message при создании сделки на flagged коридоре (pt.4 — `core/notice_pin.py::maybe_pin_route_note`).
- [ ] **Footer** постоянные `PlatformNotice(target_surface=footer)` — pt.5 (нужен Footer компонент, сейчас нет).

**Пограничные кейсы (документированы в TECHSTATE §D-COMPLIANCE-STANCE):**

1. Overlap — рендерим все, sorted by specificity+severity.
2. Time-critical updates — superuser edit → Redis invalidate → immediate propagation.
3. Legal safety — платформа = «показываем известную информацию, юридических советов не даём». ToS дисклеймер.
4. False positive — curation дисциплина; не пугаться помещать warning на всё.
5. False negative — global footer disclaimer покрывает («Vimana не проверяет direction'ы, carrier несёт риск сам»).
6. **Nostr export (T3.5)** — RouteNote **не выкладывается** в Nostr; это платформенная субъективная метадата.
7. Category × direction — deferred (pt.2).
8. Multi-hop trips — deferred до Фазы 5+.
9. Automatic feeds (ICAO/StateDept) — deferred (pt.3).
10. User-suggested notes — deferred, требует модерации.

**Backend-тесты:**

- [x] 7 тестов (`test_notices.py`): specific match, wildcard '*' matches any origin, expired note excluded, overlap ranks specific before wildcards, platform notices by surface, `target_surface='all'` matches any filter, both endpoints public no-auth.
- [x] admin CRUD (pt.2): superuser create + non-superuser 403 + delete removes row + key conflict 409.
- [x] pt.4: match on flagged corridor pins system-message; match on standard corridor doesn't.

**Acceptance:** superuser редактирует RouteNote/PlatformNotice через admin panel; изменения появляются в UI мгновенно (Redis invalidate); TripCard показывает pill на flagged коридорах; DealPage — banner; NewTripPage требует checkbox для complex/restricted направлений; никакое действие пользователя не блокируется по коридору. Платформа осталась «инфраструктурой, не цензором».

**Follow-up:**
1. **pt.5** — multi-lang translations (headline_by_lang JSONB + fallback на default). Требует editorial UI.
2. **pt.5** — Footer компонент + PlatformNotice(target_surface=footer) rendering.
3. (category, destination) правила для warnings типа «электроника > $500 в X → декларация».
4. auto-import из public sources (ICAO advisories, StateDept travel warnings).
5. community notes — user-suggested notes с модерацией.
6. Multi-hop — рейсы с промежуточными посадками, expansion на транзитные страны.

### T_UX.3 — Auth rehydrate on reload + inactivity logout ✅ MVP pt.1 + pt.2 + pt.3

**Контекст.** Обнаружено при написании T_TEST.3 Playwright recipient-спека:
после hard-nav (`page.goto` / открытие ссылки `/join/deal/:token` в новой
вкладке) Zustand auth store пустой, хотя `localStorage.token` есть. Результат:
залогиненный юзер получает redirect на `/login?next=...`, вынужден логиниться
повторно чтобы принять invite. Одновременно нет **inactivity logout**
(индустриальный стандарт — 15-30 мин без действий → auto-logout по security best-practices).

**pt.1 — Auth rehydrate on page load ✅**

- [x] `useAuthStore` расширен: `authState ∈ {'loading', 'authenticated', 'anonymous'}`, `lastActivityAt`, `hydrate()`, `bumpActivity()`. При init `token` из `localStorage`, `authState='loading'`.
- [x] `<AuthBootstrap>` в `App.tsx` — обёртка над `<Routes>`. `useEffect` on mount → `hydrate()` → `GET /api/auth/me` → `setState({user, authState: 'authenticated'})`. Пока `loading` — показывает пустой экран.
- [x] На 401/403 → `localStorage.removeItem('token')` + `authState='anonymous'`. На network/server ошибках — не clears token, ставит anonymous.
- [x] `JoinDealPage` и other direct-nav-роуты теперь корректно видят user после hard-nav.

**pt.2 — Inactivity logout (Option A frontend-only) ✅**

- [x] Global activity tracker в `<AuthBootstrap>` — listen `mousemove`, `keydown`, `scroll`, `touchstart` (passive). Debounced `bumpActivity()` — не чаще 1 раз/10 сек.
- [x] Idle timer `setInterval(30_000)` проверяет `Date.now() - lastActivityAt`. При `>= INACTIVITY_MS` → `logout('inactivity')` → `window.location.replace('/login?reason=inactivity')`.
- [x] Дефолт `INACTIVITY_MS = 30 * 60 * 1000` (OWASP). Env-override `VITE_INACTIVITY_MS`.
- [x] Warning modal за 2 мин до истечения: заголовок + body + кнопки «Stay signed in» / «Log out now». Клик по backdrop = «Stay».
- [x] LoginPage: `?reason=inactivity` → амбер-баннер «Signed out due to inactivity» перед формой.
- [x] JWT expiry — **Option A** (frontend-only): чистим `localStorage`, backend JWT остаётся валидным до natural expiry.
- [x] i18n `auth.inactivityWarningTitle/Body/LoggedOut/stayLoggedIn/logoutNow` в EN + RU.

**pt.3 — Multi-tab sync ✅**

- [x] `storage` event listener в `<AuthBootstrap>`: другая вкладка сделала `localStorage.removeItem('token')` → эта вкладка вызывает `logout('multi_tab')` → silent redirect на `/login` (без `?reason=` баннера — пользователь сам логаутнулся где-то ещё).
- [x] Симметрия для login: если в другой вкладке появился `token` (было anonymous) → синхронизируем `useAuthStore.setState({ token })` + `hydrate()` → эта вкладка становится authenticated.
- [x] `logout` reason расширен: `'inactivity' | 'manual' | 'multi_tab'`. multi_tab делает silent redirect (без URL param'ов).
- [x] Playwright regression `frontend/e2e/specs/multi-tab-logout.spec.ts` — открывает 2 page в одном browser context, register в tab A, hard-nav /profile в tab B, `evaluate(localStorage.removeItem('token'))` в tab A → tab B редиректит на /login без `reason=`.

**Backend поддержка (pt.4a — Option B) ✅**

- [x] `create_access_token` теперь кладёт `jti` (random UUID) в JWT payload.
- [x] `decode_access_token` возвращает **весь payload dict** (было: только sub).
- [x] `app/core/token_blacklist.py` — async Redis client (`redis>=5.0`), `blacklist_jti(jti, ttl)` + `is_blacklisted(jti)`. Ключ `auth:blacklist:<jti>` с TTL = remaining JWT lifetime → авто-очистка после natural expiry. Fail-soft на Redis-outage (лог + вернуть False).
- [x] `POST /api/auth/logout` — 204. Декодирует токен из Bearer, кладёт jti в blacklist. Идемпотентно: invalid/expired → тоже 204.
- [x] `get_current_user` проверяет blacklist до resolve юзера → 401 «Token revoked» на revoked jti.
- [x] Frontend `stores/auth.ts::logout` дёргает `POST /api/auth/logout` fire-and-forget перед `localStorage.removeItem`. `multi_tab` reason НЕ дёргает (оригинальная вкладка уже сделала запрос).
- [x] `backend/tests/test_logout_blacklist.py` — 5 тестов: logout → /me 401, garbage token → 204, double logout → 204/204, other user's token unaffected (jti scoping), re-login после logout → новый working token.

**Тесты:**

- [ ] Backend: `/auth/logout` blacklist'ит токен → следующий запрос с ним → 401 (backend Option B — pt.4).
- [ ] Frontend (vitest): mocked `localStorage.token` + mocked `/auth/me` → store rehydrates → user set.
- [x] Playwright (T_TEST.3 pt.2 + pt.3): `multi-tab-logout.spec.ts` — 2 page в одном context, logout в одном → второй редиректит.

**Acceptance:**
1. User логинится, закрывает вкладку, открывает по прямой ссылке `/join/deal/:token` в новой вкладке → рендерит invite-flow (не redirect на login).
2. User неактивен 30 мин → за 2 мин появляется warning → если игнорирует, автоматически logout + landing на `/login?reason=inactivity` с info-banner.
3. User logout в одной вкладке → другая вкладка тоже logout'ится (pt.3).

**Follow-up (pt.4):**
- Refresh token flow (сейчас single JWT; для «keep me logged in for 30 days» нужен long-lived refresh token + short access token).
- Device-list в профиле (superuser может revoke конкретное устройство).
- Suspicious-activity detection (impossible-travel = login из RU + LA за 10 мин → force logout всех сессий).

### T_UX.4 — Multiple addresses + Edit profile modal + avatars + landing on logo ✅ MVP

**Контекст.** Профиль-UX стал плоским: один инлайн-адрес получения, никакой Edit-кнопки, всё редактируется по месту, логотип «Vimana» вёл на dashboard, marketing-landing простаивал. Reworked за 4 инкремента.

**A — Backend addresses (2026-07-20)**

- [x] Модель `ReceivingAddress(id, user_id, label, country_iso, city, city_geoname_id, street, postal_code, note, is_default, created_at)` + partial-unique index `(user_id) WHERE is_default IS TRUE`.
- [x] Миграция `0023` — expand: создаёт таблицу, бэкфилит одну «Default»-строку каждому юзеру с заполненным legacy `receiving_country_iso`. Старые `User.receiving_*` **остаются** (contract-миграция на удаление колонок — отдельным релизом).
- [x] 5 endpoints: `GET /me/addresses`, `POST /me/addresses` (первый auto-default), `PATCH /me/addresses/{id}`, `POST /me/addresses/{id}/default` (обнуляет старый default), `DELETE /me/addresses/{id}` (при удалении default → auto-promote следующего по created_at).
- [x] `POST /deals/{id}/dealvault/messages/share-address` + `POST /inquiries/{id}/messages/share-address` теперь принимают `{"address_id": uuid?}`. Разрешение: указанный → default → legacy fallback → 422.
- [x] 10 backend-тестов (`test_addresses.py`): empty list, first auto-default, second not default, is_default clears previous, /default endpoint, update, delete promotes another, delete last, cannot touch others', country_iso normalized uppercase.

**B — Frontend profile refresh + avatars (2026-07-20)**

- [x] `<AddressesSection>` карточками с inline edit (Add/Edit/Delete/Make default).
- [x] `<AddressFormFields>` — reusable form (label + country + city autocomplete + street + postal + note).
- [x] `<EditProfileModal>` — модалка по кнопке «✎ Edit» вверху /profile: 64×64 avatar preview + Upload/Remove photo + name + phone. Адреса — отдельно в своей секции.
- [x] Two-column Bento layout на /profile: identity/UBA/Verification/Trust слева, Addresses/Contacts/Keypair/Invites/Notifications справа. Admin panel full-width под.
- [x] Аватарки: миграция `0024` (`users.avatar_key VARCHAR(255)`), `POST/DELETE /me/avatar` (multipart, jpeg/png/webp, max 3 MB, streaming SHA + size-guard). R2 бакет тот же что для DealVault attachments. `MeOut.avatar_url` = свежий presigned URL, минтится per-response через `core/avatar_url.py::me_out_with_avatar`. 5 backend-тестов (`test_avatar.py`).
- [x] ProfilePage identity-карточка показывает 48×48 avatar или fallback на инициал.

**C — Chat address picker (2026-07-20)**

- [x] `<ShareAddressModal>` (переиспользуемый) — открывается в DealVaultPage и InquiryPanel. Радио-список карточек user'ских адресов, default предвыбран. Убрана старая `confirm() + auto-default` логика — теперь всегда явный выбор.
- [x] Old handlers переписаны на модалку — сохранены только `onShare(addressId)` callback'и в parent'ах.

**D — Marketing landing on logo click (2026-07-20)**

- [x] Existing `<LandingPage>` (Bento с route example, DealVault log, boarding pass, УБА scorecard, escrow, network, corridor, Nostr, progress, missions) больше не редиректит authed-юзеров.
- [x] Navbar лого `to="/"` (было `/dashboard`) — единый home для всех.
- [x] CTA в navbar landing'а: unauthed → «Request Early Access» (waitlist модалка), authed → «Open Dashboard». i18n `landing.ctaDashboard` в EN+RU.

**Acceptance ✅ (2026-07-20 prod deploy + 7/7 Playwright smoke):**
1. Логотип «Vimana» на любой странице ведёт на `/` (landing).
2. `/profile` — 2 колонки на desktop, 1 на phone, Edit-кнопка справа от заголовка.
3. Аватар загружается через модалку, показывается в profile identity-карточке.
4. В правой колонке Addresses: Add / Edit / Delete / Make default — inline формы.
5. В чате сделки/inquiry кнопка «📍 Share address» → picker с default предвыбранным.
6. Legacy `share-address` без `address_id` продолжает работать для юзеров, ни разу не открывавших профиль после деплоя.

**Follow-up (pt.5):**
1. Celery janitor для orphaned R2 avatars после DELETE.
2. Удаление `User.receiving_*` legacy колонок отдельной миграцией (contract-фаза).
3. i18n для остальных 4 языков (pl/fr/es/ua) — сейчас EN + RU.
4. Client-side resize/crop аватарки перед загрузкой (browser Canvas API).
5. Multiple avatars / alternate identities для carrier vs sender-режимов.

### T_TEST.4 — API contract + fuzzing (schemathesis) ✅ pt.1 + pt.2 MVP

**Активация:** ✅ pt.1 закрыт 2026-07-19 (перед Фазой 4). См. `PRD/PROJECT.md §7.4`.

**pt.1 — MVP no-5xx fuzz ✅**

- [x] `schemathesis==3.39.5` в `backend/requirements.txt`.
- [x] `backend/tests/test_contract_fuzz.py` — pytest-плагин: `schemathesis.from_dict(app.openapi(), app=app, force_schema_version="30")`. FastAPI генерирует OpenAPI 3.1.0, schemathesis 3.x полноценно поддерживает только 3.0.x → патчим версию в dict + `force_schema_version="30"`. Warnings из внутренних вызовов `jsonschema.RefResolver` заглушены на уровне модуля.
- [x] `@schema.parametrize() + @settings(max_examples=15, deadline=None) → case.call()`. Один assertion: `response.status_code < 500`. Дамп body/query/headers на falure.
- [x] **Найден и починен реальный баг**: `GET /api/platform-notices?surface=X` принимал `str | None`, на `"null"` (fuzz'а) → asyncpg InvalidTextRepresentation → 500. Fix: тип `NoticeSurface | None` → FastAPI/Pydantic 422 до DB. Регрессия `test_platform_notices_invalid_surface_rejected` в `test_notices.py`.
- [x] Acceptance для pt.1: **86 test cases across ~50 endpoints × 15 examples ≈ 750 requests → 0 unhandled 5xx**. ~33 сек прогон.

**pt.2 — authed fuzz ✅**

- [x] Session-scoped `pytest_asyncio.fixture` `fuzz_user_token` — register + login regular user раз на сессию, возвращает Bearer.
- [x] Session-scoped `fuzz_superuser_token` — register + promote в superuser (raw SQL update role) + re-login.
- [x] Три parametrize'а на одной schema: `test_no_server_errors_unauthed` (pt.1) + `test_no_server_errors_authed_user` + `test_no_server_errors_authed_superuser`. `case.call(headers={Authorization: Bearer <token>})`.
- [x] `max_examples=10` для authed (vs 15 для unauthed) — балансируем время прогона (2 доп pass'а по ~30 сек каждый).
- [ ] `case.call_and_validate(checks=(status_code_conformance,))` — проверка declared status codes — deferred в pt.3 (OpenAPI auto-schema имеет gaps, много false positives).

**pt.3 — TS drift check (отложено)**

- [ ] `openapi-typescript` в `frontend/devDependencies`.
- [ ] Pre-commit hook: `npx openapi-typescript http://localhost:8000/openapi.json -o frontend/src/types/api.generated.ts` → `git diff --exit-code`. Drift → fail commit.
- [ ] Использовать generated types в `frontend/src/api/*.ts` вместо ручных interface (постепенная миграция).

**pt.4 — HTML report + CI (отложено)**

- [ ] `schemathesis run --checks all --hypothesis-max-examples=50 --html backend/reports/schemathesis.html` в отдельной CI job.
- [ ] Артефакт-загрузка отчёта.
- [ ] Bump `max_examples` до 50 в CI (сейчас 15 для скорости локально).

**Acceptance финальный:** 0 unhandled 500 на fuzz'е всех endpoint'ов (public + authed); CI ломается при drift TS-типов; HTML-отчёт после каждого прогона.

### T_TEST.5 — Property-based тесты (Hypothesis) ✅ MVP

**Активация:** ✅ pt.1 закрыт 2026-07-18 — крипта + УБА + signing. Trust BFS (DB-зависимый) — pt.2.

- [x] `hypothesis==6.122.3` в `backend/requirements.txt`.
- [x] `tests/test_props_threshold.py` — 6 property'ей на конверты (T2.3; переведены на NIP-44 в `T_KEYS.1` — заодно выяснилось, что генераторы допускали пустой plaintext, который NIP-44 отвергает по спецификации: диапазон 1..65535):
  - roundtrip для любых bytes (0–500);
  - roundtrip для UTF-8 (эмодзи, control, RTL);
  - probabilistic (два encrypt одного plaintext → разные ct — IV случаен);
  - format: `?iv=` separator присутствует;
  - wrong recipient не расшифровывает;
  - симметрия A↔B.
- [x] `tests/test_props_uba.py` — 10 property'ей на формулу T3.1:
  - `0 ≤ УБА ≤ 1000` для любых inputs;
  - монотонность по F, Q, V, D независимо;
  - `verify: None ≤ auto ≤ peer ≤ kyc`;
  - детерминизм (same input → same output);
  - `Q=0 → УБА=0` (Q gates product);
  - `level_of` возвращает только валидный slug;
  - `level_of` монотонный по score.
- [x] `tests/test_props_signing.py` — 6 property'ей на NIP-01 (T2.2 pt.2):
  - sign+verify roundtrip для любого event'а;
  - event_id детерминистичен (pure fn);
  - изменение content → verify fails;
  - wrong pubkey → verify fails;
  - изменение ts → event_id меняется;
  - изменение kind → event_id меняется.
- [ ] `tests/test_props_trust.py` (pt.2, требует DB fixture с async): BFS depth, no duplicate hops, revoked excluded.

**Acceptance:** ✅ 22 property'и × 100–200 examples каждая = ~3000+ входных данных проверено; Hypothesis не находит counterexample.

**Follow-up (pt.2):** trust BFS invariants — нужен async DB fixture, требует schema seed.

### T_TEST.6 — Load / performance (k6) 🟨 код готов, прогон за владельцем (2026-08-03)

**Активация:** перед Фазой 4 (обязательно перед платежами).

- [x] `load/` директория (не в frontend/backend). `load/scripts/*.js` — k6 сценарии, общее в `load/lib/`.
- [x] Сценарии: `register_burst.js`, `browse_trips.js`, `chat_hammer.js`, `mixed_workload.js`.
- [x] Target = **staging** (не prod) — проверка в `lib/config.js`, а не строчкой в README: прогон по проду не замечают, пока он идёт, замечают в чужой сорванной сделке. Обход `ALLOW_PROD=1` — одна переменная, набирается осознанно.
- [x] Метрики: `http_req_duration p95 < 500ms`, `http_req_failed < 1%` — в `lib/config.js`, одним объектом на все сценарии.
- [x] Docker-compose profile `load` — контейнер `k6`, монтируется **вся** `load/` в `/load`. Раздельный монтаж кусками ломал три относительных пути разом: сценарии импортируют `../lib/`, сводка читает `../baseline.json`, результаты пишутся рядом с ней.
- [x] Baseline JSON: сравнение ±10% в `lib/summary.js`, печатается рядом с каждым числом. **Файла в репозитории нет намеренно** — цифры, скопированные с чужой машины, не baseline, а слух. Первый доверенный прогон записывает свой.

**Два отклонения от постановки, записаны, а не спрятаны:**

1. **`chat_hammer` не поднимает 100 отдельных аккаунтов.** Сто регистраций + сто приглашений получателей упрутся в `auth_zone` (10 r/min на IP), и сценарий будет мерить лимитер. Настоящий предмет здесь другой — `pg_advisory_xact_lock` на сделку в `append_deal_event` (T3.6), единственная сознательная точка сериализации в продукте. Её находит конкуренция **на одной сделке**, а не толпа: сто VU по ста сделкам не тронут лок вообще. Два реальных участника пишут параллельно в один сейф — контенция сохранена, толпа убрана.
2. **429 в `register_burst` объявлен ожидаемым ответом** (`http.expectedStatuses(..., 429)`) и считается отдельным счётчиком `rate_limited`. Иначе `http_req_failed` перестаёт значить «ошибка» и начинает значить «сработала защита». Мерится при этом то, что и должно: **дёшево ли** отказывает лимитер — медленный отказ это отказ в обслуживании с лишними шагами.

**Первый прогон (2026-08-03, `browse_trips`, 100 VUs × 5 мин, по проду за неимением staging): p95 **1064 мс** · median 543 · failed **0.08 %** · 105 rps · 10 606 итераций.** Порог по ошибкам выполнен с запасом, **порог `p95 < 500` не выполнен вдвое**. Это ответ, а не поломка теста: под сотней параллельных читателей публичная выдача отвечает вдвое медленнее целевого, причём медиана вдвое ниже p95 — значит тормозит хвост, а не всё подряд. Следующий шаг — разрез по тегам (`trips:list`, `trips:page2`, `airports`, `categories`), они уже проставлены в сценарии.

**Попутно вскрылось и починено:** контейнер k6 работает под собственным uid 12345 и не мог писать в смонтированную директорию — прогон выдал все числа и потерял файл на `permission denied`. В compose добавлен `user: "${K6_USER:-1000:1000}"`; совпадение с владельцем каталога на хосте важно не само по себе, а потому что запись baseline это `cp` этого самого файла.

**Baseline записан 2026-08-06** (`load/baseline.json`, только `browse_trips`) — из того же прогона, вместе с условиями: прод по TLS, 100 VUs, 5 минут, тот же хост. Записан **сознательно ниже порога**: baseline отвечает не на «приемлемо ли», а на «не стало ли хуже, чем в прошлый раз», и ждать красивых цифр значило бы не иметь точки отсчёта до тех пор. `reqs` в файле `null` — прогон потерял сводку на правах доступа, пять чисел взяты из напечатанного, `http_reqs` среди них не было.

**Acceptance:** 100 VUs 5 мин → p95 < 500 мс, error rate < 1%. **Не выполнен по p95** (1064 мс). Задача остаётся 🟨 до разбора хвоста по тегам.

### T_TEST.7 — Security scan (OWASP ZAP baseline) + IDOR-матрица ✅ MVP (2026-08-03, прогнано)

**Активация:** после Фазы 3 (когда T_SEC.1 стабилен), обязательно перед Ф.4.

**pt.1 — ZAP baseline:**
- [x] `.zap/baseline.sh <url>` — запуск. Образ **`ghcr.io/zaproxy/zaproxy:stable`**, а не `owasp/zap2docker-stable` из постановки: это имя ретайрнуто, ZAP переехал на `ghcr.io/zaproxy`. Тег плавающий сознательно — в отличие от relay и clamav, которые крутятся **под нами** сервисами, сканер это инструмент, который наводят на URL, и устаревший сканер хуже движущегося: вся ценность в проверках, добавленных с прошлого раза.
- [x] Baseline mode (пассивный скан) безопасен на prod. Активный (`zap-full-scan.py`) из скрипта не запускается вообще — он отправляет формы и мутирует параметры, а против прода это реальные строки в реальных сделках.
- [x] Telegram alert: `notify_admins_zap_findings` рядом с `notify_admins_infected_file` — тот же путь админского сообщения, второго не заводим. Диспетчер — `app/cli/zap_report.py`, вызывается скриптом через `compose exec -T backend` (отчёт приходит на stdin). Расписания нет намеренно: скан требует человека, выбравшего цель и момент.
- [x] `.zap/rules.tsv` — известные находки, **каждая с причиной**. Правило файла записано в нём же: «шумит» — не причина, это и есть находка; молча заглушенное правило прячет настоящее срабатывание через полгода. Отдельным блоком перечислены пять правил (HSTS, CSP, X-Frame-Options, X-Content-Type-Options, Permissions-Policy), которые **нельзя** сюда добавлять: их заголовки ставит nginx с T_SEC.1, и срабатывание любого означает, что заголовок перестал отправляться.
- [x] **Решение о пороге вынесено из TSV в код.** `-I` не даёт ZAP валить прогон по warning'ам, а правило «любой High — провал, Medium считаем и печатаем» живёт в `zap_report.py`. Порог, размазанный между TSV, флагом CLI и `if` в shell, никто не может прочитать целиком.
- [x] **Нечитаемый `riskcode` трактуется как High**, а не как 0 — отчёт, который мы не смогли разобрать, не является доказательством отсутствия находок, и обратное умолчание даёт молчаливый зелёный прогон. 9 pytest на разборщик (`test_zap_report.py`).

**Первый прогон по проду (2026-08-03): `FAIL-NEW: 0 · WARN-NEW: 4 · PASS: 60 · IGNORE: 3` — High: 0, Medium: 3, Low: 4.** Acceptance выполнен. Три `IGNORE` из `rules.tsv` сработали ровно на том, подо что писались (10049 storable content, 10096 timestamps в бандле, 10109 «это SPA»). Все пять заголовков T_SEC.1 — в PASS: HSTS, CSP, X-Frame-Options, X-Content-Type-Options, Permissions-Policy отдаются.

**Три Medium, документированы (это и есть та документация, которой требует Acceptance):**

1. **CSP: Wildcard Directive** — `img-src 'self' data: blob: https:` и `connect-src 'self' wss: https:`. Вайлдкард в `img-src` **работает**: аватары отдаются presigned-ссылками R2, то есть с внешнего хоста, и подстановка конкретного домена требует значения `R2_ENDPOINT` из `.env` — сузить вслепую значит выключить аватары. `connect-src`: собственный релей живёт на `wss://<host>/relay`, то есть тот же origin, третьесторонних сокетов фронт не открывает (`SimplePool` в `frontend/src` не встречается, NIP-07 подписывает в расширении). **Кандидат на сужение** до хоста R2 и `'self'` — но с проверкой на живом релее в нескольких браузерах, а не правкой конфига наугад. Заведено как follow-up, не сделано.
2. **CSP: `style-src 'unsafe-inline'`** — Tailwind и React пишут инлайновые стили; убирается только nonce/hash-схемой на каждый ответ. `script-src` при этом **без** `unsafe-inline` (T_SEC.2), то есть то, что реально исполняется, ограничено. Осознанный остаток.
3. **Sub Resource Integrity Attribute Missing** — SRI защищает от подмены на **чужом** CDN. Бандлы отдаются со своего nginx, имена контент-хешированы (T_SEC.2), внешних скриптов нет вовсе: `script-src 'self'`. Здесь SRI охраняет нас от нас же. Не применимо.

Плюс два Low, записаны, чтобы не открывать заново: `Re-examine Cache-control Directives` (10015) и `Cross-Origin-Embedder-Policy` (90004) — второй нужен только под cross-origin isolation, которая нам не нужна (нет `SharedArrayBuffer`).

**IDOR-матрица прогнана в составе полного сьюта: 1141 passed** (было 1075 до задачи).

**pt.2 — IDOR-матрица (добавлено 2026-07-29 по итогам ручного аудита).**

**Зачем отдельно.** Ручная проверка всех 36 эндпоинтов с path-параметрами (2026-07-29) классического IDOR **не нашла**: владение проверяется везде, вложенные ресурсы скоупятся (`_get_request` сверяет `req.deal_id`, `decrypt-for-me` — `msg.deal_id`), идентификаторы — UUID v4, перебирать нечего. Но это состояние ничем не удерживается: **автоматика IDOR не ловит в принципе**. Schemathesis (T_TEST.4) проверяет «нет 500-х», для него 403 и 200 одинаково валидны. ZAP baseline пассивен и авторизацию не моделирует. То есть отсутствие IDOR держится на 41 ручной проверке 403 в 18 файлах и на внимательности следующего автора.

- [x] Два пользователя: посторонний (A) и «жертва» (B — сид-участники), не связанные ни сделкой, ни приглашением. У B полный набор: рейс, сделка, сообщение в сейфе, verification-request, спор, адрес, inquiry + бейдж, passkey, route-note и platform-notice прямыми вставками (создать их через API нельзя без сканера документов, аутентификатора и суперюзера).
- [x] Прогон таблицей: **каждый** эндпоинт с path-параметром вызывается токеном A против объекта B. Ожидание — 403 или 404, **никогда 200 и никогда 500**. Тела запросов лежат в самой таблице: без них FastAPI отвечает 422 **до** обработчика, и отсутствующая проверка прав прошла бы тест, ни разу не выполнившись.
- [x] Отдельно — вложенная подмена: свой `deal_id` + чужой `message_id` (`decrypt-for-me` и загрузка вложения), чужой `req_id`, чужой участник.
- [x] Матрица строится **из роутера**: `app.routes` → пути с `{`, сверка с таблицей ожиданий **в обе стороны**. Новый эндпоинт валит тест фактом появления; исчезнувший — тоже, иначе таблица тихо перестаёт описывать приложение.
- [x] **Три вердикта вместо одного.** `DENIED` — отказать. `PUBLIC` — читается посторонним по решению (публичная страница личности, УБА, trust-метрики, событие рейса, открытие inquiry по чужому рейсу), причина написана рядом, проверка снижается до «не 500». `CAPABILITY` — токен приглашения **и есть** авторизация; требовать здесь 403 значило бы требовать обратного от фичи, поэтому проверяется другое: неизвестный токен → ровно 404.
- [x] Роли: обычный юзер против арбитерских путей (внутри матрицы), арбитр без заявленного спора, **арбитр с отозванным `OperatorAccessGrant`** — claim → чтение 200 → отзыв согласия → чтение 403. Иначе фразу «арбитр видит переписку, только пока сторона держит доступ открытым» продукту нечем подтвердить.
- [x] Presigned-ссылки: TTL `identity_doc` **строго** меньше общего. Равенство означало бы, что различие снял чей-то рефакторинг и никто не заметил.

**Найдено по дороге и починено:** `POST /threshold/dealvault/messages/{id}/reveal-my-share` проверял **форму** сообщения раньше **участия** — посторонний с чужим `message_id` получал 400 «Message is not e2e-encrypted», то есть подтверждение, что сообщение существует, и его тип. Больше ничего не утекало, но эндпоинт описывал строки людям без прав на них. Порядок переставлен; проверка участия не требует больше данных, чем проверка формы, так что цена нулевая. Существующий `test_reveal_my_share_forbidden_for_third_party` работает на e2e-сообщении и ожидал 403 и раньше — поэтому баг и дожил до матрицы: тест проверял правильный случай.

**Acceptance:** ZAP baseline на prod = 0 High, ≤ 3 Medium (документированные); IDOR-матрица покрывает **все** эндпоинты с path-параметрами и падает при появлении непокрытого. Матрица написана на 49 маршрутов и в репозитории; **прогон за владельцем** (правило «никаких локальных pytest» — CLAUDE.md).

### T_TEST.8 — Accessibility (axe-core в Playwright) 🟨 код готов, прогон за владельцем (2026-08-03)

**Активация:** после Фазы 3, закрывает обещание DESIGNGUIDELINES §Accessibility про WCAG 2.2 AA.

- [x] `@axe-core/playwright` (+ `@types/node`, спека читает `axe-rules.json` с диска) в `frontend/e2e/package.json`, скрипт `npm run a11y`.
- [x] `specs/a11y.spec.ts` — 5 канонических страниц (`/`, `/register`, `/login`, `/dashboard`, `/profile`), последние две под сессией через тот же `registerUser`, что и смоук. Ожидание пустого списка нарушений, но падение **читаемое**: правило, impact, до трёх селекторов и ссылка на объяснение — дефолтный ассерт печатает JSON-ком, а человеку нужно «что, насколько плохо, где».
- [x] Скан после `networkidle`, а не `domcontentloaded`: половина экранов рисуется после первого XHR, и axe оценил бы пустой скелет — самая доступная страница в мире это страница, на которой ничего нет.
- [x] `axe-rules.json` — теги и исключения. **Теги фиксируют объём**: `wcag2a…wcag22aa`, ровно то, что обещано в DESIGNGUIDELINES; best-practice-правила — это совет, и падение сьюта по совету учит людей отключать сьют. Исключение сейчас одно (`region`, не уровень AA) и записано в основном затем, чтобы его не вернули как будто это требование. Заведён отдельный список `$pending` — «принято временно» и «решили, что не применимо» это разные утверждения, и выглядеть одинаково они не должны.

**Честная граница, записана в заголовке спеки:** зелёный прогон означает «машина не нашла нарушений», а не «WCAG 2.2 AA выполнен». axe покрывает примерно треть уровня AA; осмысленность порядка фокуса и полезность текста ошибки остаются за человеком.

**Первый прогон (2026-08-03): 5 из 5 страниц падают.** Найдено два класса, оба настоящие; оба закрыты в тот же день. **Повторный прогон — за владельцем** (нужна пересборка фронта: токены живут в конфиге Tailwind, статический бандл их вшивает).

- [x] **`select-name` (critical) — исправлено.** `<select>` типа документа в `VerificationSection` не имел имени вовсе: контрол, который описывает себя только текущим выбранным вариантом, для скринридера безымянный. Добавлен `aria-label` (+ соседнему полю кода страны, у которого именем работал `placeholder`), ключи `verification.docTypeLabel` / `docCountryLabel` ×6 локалей.
- [x] **Тот же дефект классом шире — исправлено превентивно.** В `AddressFormFields` шесть `<label>` стоят **соседями** своих полей, без `htmlFor`: такая подпись не называет ничего. Связаны через `useId()` — не через фиксированные id, потому что две формы адреса могут быть открыты одновременно, и тогда подписи второй указывали бы на поля первой. Форма не открыта на дефолтном `/profile`, поэтому axe её не увидел; нашлось чтением рядом стоящего кода.
- [x] **`color-contrast` — правило отключено, палитра возвращена (решение владельца 2026-08-03, после отката).** Замена шкалы была сделана и **откачена в тот же день по требованию владельца**: «лендинг стал унылым, исчезли детали». Это верно и это главный вывод задачи — сплошной токен уровня AA убирает не только низкий контраст, но и градацию, на которой держится вёрстка: где было три степени приглушения, стала одна. Тест нашёл настоящее расхождение с обещанием WCAG AA, но **решение, чем платить за это обещание, не тестовое**. Правило `color-contrast` и следующее из него `link-in-text-block` отключены в `axe-rules.json` с записанной причиной; в DESIGNGUIDELINES §6 добавлено прямое признание: по контрасту текста продукт AA не выполняет, и утверждать обратное — нарушение §9.1. Что осталось от находки: она задокументирована, а не «починена» и не спрятана.
- [x] **Структурные правки сохранены — они не про вид.** `list`/`listitem` (7 пунктов на лендинге): `<Reveal>` рендерил `<div>` **между** `<ol>` и `<li>`, из-за чего список переставал объявлять длину, а пункты — быть пунктами. У `Reveal` появился проп `as`, классы не тронуты, картинка идентична. Анимация — украшение и не должна решать, что за элемент в документе.

<details><summary>Что именно было заменено и откачено (для истории)</summary> Приглушённый текст был задан прозрачностью, и почти вся шкала лежала ниже AA на светлых подложках: `text-navy/40` → **2.57**, `/50` → **3.43**, `/20` → **1.52**, `text-cyan` как текст → **2.43** при требуемых 4.5. Прозрачностная шкала это и приглашает: каждый шаг выглядит выбором дизайнера и ни один не несёт числа, поэтому палитра уезжает под линию по одному компоненту за раз — ровно тот довод, по которому `danger` стал токеном в T_UX.7.
  - Заведены два семантических токена: **`muted` `#545C67`** (navy 70% над белым, зафиксирован **solid** — так он одинаково рендерится на белом и на ivory: 6.79 и 6.12) и **`link` `#1A7299`** (5.37 / 4.84). Первый кандидат в цвет ссылки, `#1B7BA8`, отброшен: 4.72 на белом, но **4.26 на ivory** — «проходит на той подложке, которую я проверил» это и есть чинимый баг.
  - Сквозная замена: **356** употреблений `text-navy/{20…70}` → `text-muted` в 55 файлах, **57** употреблений `text-cyan*` → `text-link` в 34. `border-navy/*` и `bg-cyan/*` не тронуты — это не текст.
  - **Тёмные поверхности имеют свою шкалу и свои две дыры**, найденные тем же счётом: `text-ivory/45` на navy = **4.16**, `text-white/40` на navy = **3.79**. Обе подняты до /70 (8.41 и 7.06). `text-white/60` (7.06) и `text-ivory/70` проходили и оставлены.
  - Иерархия уплощалась: где были /40 и /60 как два уровня приглушения, оставался один. Ровно это и оказалось неприемлемым.

</details>

**Acceptance пересмотрен:** 0 violations на 5 страницах **по правилам, которые остались включёнными**. Контраст выведен из-под теста сознательно и с записанной ценой — это не «сделали зелёным», это «решили и записали». Остаток находок первого прогона (`button-name ×3`, `label ×1`) ждёт селекторов: в сводке они потерялись, глазами по коду не опознаются, гадать не буду.

### T_TEST.9 — Visual regression (Playwright screenshots)

**Активация:** Ф.5 (когда UI не меняется каждый день).

- [ ] `frontend/e2e/specs/visual/*.spec.ts` — 10 канонических экранов.
- [ ] Первый прогон = baseline PNG'и коммитятся в repo.
- [ ] `toHaveScreenshot({ maxDiffPixelRatio: 0.005 })`.
- [ ] `npm run visual:update` — обновить baseline'ы намеренно.

**Acceptance:** 10 baseline'ов; CI ломается при diff > 0.5%.

### T_TEST.10 — Mutation testing (mutmut / stryker)

**Активация:** Ф.5+ (когда unit-покрытие уже хорошее).

- [ ] `pip install mutmut` (backend), `npm i -D @stryker-mutator/core @stryker-mutator/vitest-runner` (frontend).
- [ ] Pilot-модули: `backend/app/core/{crypto,keypair,signing,threshold,uba,trust,permissions}.py` + `frontend/src/{hooks/useBentoLayout,lib/threshold}.ts`.
- [ ] `mutmut run --paths-to-mutate=app/core/` → `mutmut html` → `mutation-report.html`.

**Acceptance:** mutation kill-rate ≥ 60% на критичных модулях; оставшиеся мутанты — задокументированные ложные позитивы или пробелы фиксятся.

### T_TEST.11 — Chaos engineering (Toxiproxy)

**Активация:** Ф.5+ (когда сервис production-grade).

- [ ] `docker-compose profile chaos` — Toxiproxy между backend ↔ db, backend ↔ redis.
- [ ] Сценарии в `chaos/`:
  - `db-timeout.sh` — Postgres 30 сек timeout → API 503 не 500.
  - `redis-drop.sh` — Redis down → rate-limit fail-open, sessions expire нормально.
  - `slow-nostr-relay.sh` — publish task не блокирует POST /trips.
- [ ] Прогон в staging (не prod). Раз в неделю ручной.
- [ ] `chaos-report.md` со списком degradations vs graceful behavior.

**Acceptance:** каждый сценарий → 503/degraded, не 500 crashed. Zero потерянных транзакций.

### EXP-07 — Tamper-evident DealVault: хеш-цепь + внешнее закрепление 🧪 экспериментальное

**Статус:** исследование, вне roadmap. **Узкая версия уже реализована и закрыта как T3.6** (серверная цепь по `deal_events`, часовой анкер). EXP-07 — целевая модель поверх неё, кода нет.

**Разбор целиком:** [BUZZ.md](BUZZ.md) — анализ внешнего проекта buzz (Nostr-релей, Apache 2.0) + вытекшая проработка доказательности DealVault.

**Чего T3.6 не закрывает.** `DealEvent.nostr_sig` доказывает авторство одной записи, но не полноту, не порядок и не факт неудаления — это T3.6 закрыл. Осталась тонкость уровнем глубже: custodial-подпись ставится нашим сервером нашим же ключом, поэтому **против платформы** она не доказывает ничего, а серверную цепь мы сами и строим. Vimana при этом одновременно арбитр и хранитель записи. Чтобы доказательство работало против нас самих, нужны две вещи, которых в T3.6 нет: `prev` **внутри подписываемого участником события** и цепь, включающая сообщения, а не только события.

- [ ] Решить 7 открытых вопросов из BUZZ.md §7 (публиковать ли 4802, непрозрачный `anchor_id`, разрешение развилки, подпись `prev` на кнопках, частота анкера, содержимое `/chain`, единая таблица цепи).
- [ ] Единая таблица `deal_chain_entries` — одна цепь на сделку, включающая и сообщения, и события (взаимный порядок = предмет спора). Заменяет колонки в `deal_events` из T3.6; переезд потребует пересчёта цепи — см. BUZZ.md §7 п.7.
- [ ] `prev` в теге NIP-01 `["prev","<hex>"]` — читаем сервером, подписан участником, контент остаётся непрозрачным. **Это и есть главное отличие от T3.6**, где `prev` проставляет сервер.
- [ ] Непрозрачный `anchor_id` вместо открытого `deal_id` в теге анкера — в T3.6 `deal_id` публикуется открыто и выдаёт объём и активность сделок.
- [ ] `GET /deals/{id}/chain` — граница заверенности (до какого звена и чьей подписью), а не одна зелёная галочка.

**Зависимости:** поверх T3.6. Требует `NOSTR_PUBLISH_ENABLED=true` (сейчас false — см. T3.5 follow-up). Не конфликтует с T2.3 (threshold) и T3.2 (grants).

**Acceptance (когда/если активируется):** правка, удаление или перестановка любой записи ломает верификацию; участник с одним своим подписанным сообщением на руках может доказать состояние всей истории под ним без доступа к нашей БД.

### Follow-up: npm audit — разбор по достижимости (обновлено 2026-08-02)

**Восемь advisory, ни одна не достижима в проде.** Разобрано поимённо, чтобы следующий `npm audit` не начинал анализ с нуля. Счётчик «7 уязвимостей (5 moderate, 1 high, 1 critical)» вводит в заблуждение: он считает пакеты в цепочке зависимостей, а не поводы для беспокойства.

**Dev-only, прода не касаются вовсе** (T_SEC.2 — прод это статический `dist/`, `vite dev` там не запускается):
- [ ] `esbuild ≤0.24.2` — GHSA-67mh-4wv8-2f99, любой сайт может слать запросы к dev-серверу и читать ответ. Риск на машине разработчика, не на сервере.
- [ ] `vite` — path traversal в `.map` оптимизированных зависимостей; обход `server.fs.deny` **только на Windows**.
- [ ] `launch-editor` — раскрытие NTLMv2-хеша через UNC-пути, **только на Windows**. Приходит транзитивно через vite. Команда на macOS/Linux.
- [ ] `vitest` — **это и есть «critical»**: при запущенном Vitest UI можно прочитать и выполнить произвольный файл. У нас нет скрипта с `--ui` (`test` и `test:run` — обычный прогон), то есть сервер не поднимается. **Не добавлять `--ui` в скрипты**, не разобравшись; если понадобится — только с привязкой к localhost.

**`react-router` ≤7.17.0 — три advisory, проверены на достижимость 2026-08-02:**
- [x] *Open redirect через обратный слэш* (CVE-2025-68470 bypass) и *open redirect ведущий к XSS* — нужен сток, куда попадает пользовательский ввод. Прогреп всех `navigate()` и `to={}`: цели либо литералы, либо элементы статических списков. Единственный внешний параметр — `returnUrl` — **не читался вообще**, и это оказалось не защитой, а сломанным сценарием (см. ниже). Теперь читается через `safeReturnUrl` с узкой проверкой, три теста фиксируют формы из advisory.
- [x] *Arbitrary Constructor Injection в `deserializeErrors()`* — путь гидрации data-router'а. `createBrowserRouter`, `RouterProvider`, `hydrationData`, `HydratedRouter` в коде отсутствуют (проверено грепом). Пререндер T_UX.7 использует `StaticRouter`, который этой функции не касается. **Прежняя запись «SSR у нас нет» устарела 2026-08-02** — SSR появился, но вывод не изменился, и важно понимать почему: дело не в отсутствии SSR, а в отсутствии data-router'а.
- [ ] Апгрейд `react-router-dom` 6 → 7 остаётся отдельной задачей: исправление лежит в 7.18+, переход мажорный, а на роутере висят 11 `data-testid` Playwright и весь protected-блок. Делать с прогоном e2e, не строчкой в консоли.

**Найдено по ходу разбора (2026-08-02), починено:**
- [x] `AcceptInvitePage` отправлял неавторизованного на `/login?returnUrl=/invite/<token>`, а `LoginPage` параметр не читал: после входа человек попадал на дашборд, приглашение молча терялось, и пригласивший не узнавал об этом никогда. Правильная реализация чтения **и есть** защита от open-redirect — валидатор отвергает схему, `//`, обратный слэш и всё, что не начинается с одного `/`.

- [ ] **Не чинить через `npm audit fix --force`.** 2026-08-01 эта команда подняла vite 5 → 8 и vitest 2 → 4, после чего `@vitejs/plugin-react` перестал резолвиться и сборка фронта на сервере упала на `npm ci`. Мажорные апгрейды инструментов сборки — отдельная задача с отдельным прогоном, а не побочный эффект правки зависимости.

---

## ✅ Критерий финиша: Альфа 1

**Всё, что до закрытия всех фаз, — бета.** Версия остаётся в серии `0.x.y` и показывает, где в дорожной карте находится сборка: `0.{фаза:02}.{часть фазы}`. Сейчас `0.03.7`.

**Альфа 1 выходит, когда закрыты все фазы** — 1, 2, 3, 3.5, 3.6, 3.7, 4, 5, 6, 7. Не раньше и не по совокупности ощущений: критерий счётный, и в этом его смысл.

> Прежний критерий («V1 = Фаза 1: отправитель и перевозчик находят друг друга, фиксируют передачу в Чёрном ящике, доходят до подтверждения получения») выполнен давно и остаётся описанием **ядра продукта**, а не рубежа релиза. Оставлять его как «финиш» значило бы считать проект законченным на Фазе 1 — при том что Nostr-relay, threshold-шифрование, passkeys и цепь событий уже работают поверх. Пересмотрено решением владельца 2026-07-30.

---

### 🔒 Правила (Reminder)
- Для начала работы: берите `T0.1`.
- Не знаете, что дальше? Берите следующую задачу со статусом `[ ]`.
- Сделали задачу? Ставьте `[x]`, опишите сделанное, синхронизируйте TECHSTATE/ENVIRONMENT/CHANGELOG.
- **`frontend/src/version.ts`** — схема `0.{фаза:02}.{часть фазы}` (Фаза 3.7 → `0.03.7`, Фаза 4 → `0.04.0`). Меняется при переходе на новую фазу или её часть, **не после каждой задачи**.
