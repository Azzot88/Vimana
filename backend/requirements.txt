# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Auth
PyJWT==2.10.1
bcrypt==4.2.0
python-multipart==0.0.12

# Background tasks
celery==5.4.0
redis==5.2.0

# Storage (R2/S3)
boto3==1.35.77

# Cryptography (T1.21 at-rest AES-GCM + Phase 2 Nostr keypair)
cryptography==44.0.0
coincurve==20.0.0

# Sync DB driver (Celery tasks)
psycopg2-binary==2.9.10

# Rate limiting
slowapi==0.1.9

# Notifications
twilio==9.4.0

# Nostr relay publish (T3.5) — WebSocket client for kind-30402 events
websockets==13.1

# Utilities
pydantic==2.10.3
pydantic-settings==2.6.1
python-dotenv==1.0.1
httpx==0.28.0
pycountry==24.6.1

# PDF generation (boarding pass)
weasyprint==63.0

# T3.8 — image decode validation for uploads (already a transitive dep of
# weasyprint; pinned explicitly because file_validation.py imports it directly)
Pillow==11.0.0

# Tests
pytest==8.3.4
pytest-asyncio==0.24.0
# T_TEST.5 — property-based tests for crypto/math (threshold, UBA, signing)
hypothesis==6.122.3
# T_TEST.4 — OpenAPI contract fuzzing (in-process ASGI, no external server)
schemathesis==3.39.5
